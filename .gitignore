# DuckDB 预编译库（tools/fetch-duckdb.sh 取；设计见 docs/architecture/dependencies/duckdb-linking.md）
/third_party/duckdb/*/
# 构建产物：根与**任意成员目录**下的 target 都不入库（cargo test 的工作目录是包根，
# 相对路径的产物会落在 crates/<成员>/target 里—— 探针踩过一次，268 MB）
target/
**/*.rs.bk
node_modules/
v1/frontend/node_modules/

# 运行时生成物（配置/数据/日志/临时/扩展）——设计见 docs/architecture/runtime/data-paths.md
# 口径：软件生成的一切都在自己的安装目录下（默认 = 可执行文件所在目录），且一律不入库
/.rds/
/rds-*.log
# 工具脚本与真机调试跑出来的日志（生成物；`docs/tmp/screenshot.png` 是截图，不属生成物）
/docs/tmp/*.log
/tools/*.log
# 手工回归跑的输出（历史上的 `targetm3-regression.log` 曾误入库；日志不入库）
/target*.log
