## 中文版 `README.zh-CN.md`

**语言：** [English](README.md) | [中文](README.zh-CN.md)

<div align="center">
  <h1>📊 RdataStation</h1>
  <p><em>Analysis begins where the query ends.</em></p>
  <p><strong>取数立本，分析明道；数不虚取，析不妄断。</strong></p>
</div>

---

> 如果你也曾在SQL查询结果和Excel之间反复横跳，你不是一个人。这是我们尝试解决这个问题的一次努力。

[![Rust](https://img.shields.io/badge/Rust-000000?logo=rust)](https://www.rust-lang.org)
[![Tauri](https://img.shields.io/badge/Tauri-2.x-FFC131?logo=tauri)](https://tauri.app)
[![Vue3](https://img.shields.io/badge/Vue-3.x-4FC08D?logo=vue.js)](https://vuejs.org)
[![DuckDB](https://img.shields.io/badge/DuckDB-built--in-FFF000)](https://duckdb.org)
[![License](https://img.shields.io/badge/License-MIT%2FApache--2.0-blue)](./LICENSE)

---

## 📖 这是什么？

RdataStation 是一个**本地优先、不上云**的数据库桌面工具。

市面上的数据库管理工具已经非常成熟。DBeaver是开源社区的瑰宝，DataGrip是SQL编辑体验的标杆，Navicat以稳定易用著称。我们对此充满敬意。

RdataStation的诞生，不是要取代这些伟大的产品，而是尝试回答一个它们因历史架构原因而尚未聚焦的问题：

**当用户执行完一条SQL，拿到几十万行结果集后，下一步该怎么办？**

导出到Excel？导入Python写pandas？贴到Jupyter里绘图？这个过程不仅割裂，而且在大数据量下会迅速崩塌。

**我们的答案：让分析发生在查询之后，在工具内部完成。**

---

## 👀 一个熟悉的场景

你写了一条SQL，查出来20万行数据。

你想按地区做个汇总。导出CSV，Excel打不开。切到Python，import pandas，加载CSV，写groupby。笔记本风扇呼呼转。

你只是想对查询结果多追问几个问题而已。

**在一台普通笔记本上对50万行数据的测试中：** Excel 加载耗时超过30秒，筛选时出现卡顿。Python + pandas 加载约需8秒，分组聚合约需3秒。RdataStation + DuckDB 返回相同的分组结果约在0.3秒内完成。_（测试环境为标准配置笔记本，实际结果可能因环境而异。）_

**在RdataStation里：** 在结果集面板上，点一下 **"DuckDB分析"** 按钮。20万行结果在本地DuckDB里变成一张临时表。输入聚合SQL，毫秒级返回结果。不需要导出，不需要Excel，不需要Python。整个追问过程，在一个工具里完成。

- 这张临时表是**会话级**的——关闭面板即自动清理
- 如果想把分析成果留下来，点**"持久化"**——数据存入项目级DuckDB
- 如果整个团队都需要，点**"提升为公共资产"**——存入应用级DuckDB，所有项目都能引用

---

## 🎯 它是什么，不是什么

RdataStation聚焦于**查询之后、可视化之前**这个分析地带。

连接、查询、管理是**底座**。二次分析、联邦查询、数据探查是**延伸**。图表、仪表盘、报告——这些交给插件生态。

我们不是在做"什么都有的数据库工具"。我们在做的是"查询结果到分析结论之间的那座桥"。

---

## 💡 设计哲学

### 🏠 本地优先，数据主权在用户手中

所有数据库连接信息、查询历史、分析缓存只存在于你的电脑上。没有遥测，没有云同步，没有"登录才能使用"。你的数据库密码、查询历史、分析成果，永远不会离开你的电脑。

### 🧩 SQL是入口，但不必是终点

RdataStation以SQL为分析入口——连接、查询、二次分析、联邦查询、数据探查，都通过SQL完成。当分析结果需要进一步加工时，插件系统可以接管：用Python绘制图表、用Extism插件做数据脱敏或格式转换。SQL负责"分析什么"，插件负责"产出什么"。两者在同一个工作台内协作。

---

## ⚙️ 如何做到：背后的架构设计

<img width="5044" height="4164" alt="image" src="https://github.com/user-attachments/assets/862bc7e1-5a97-4e27-bf8e-76761fbcc962" />

### 🧠 DuckDB 作为分析引擎

DuckDB 在 RdataStation 中不是"另一个可连接的数据源"，而是嵌入产品核心的分析引擎。

- **二次分析**：任何查询结果集，可以一键转入本地 DuckDB 临时表。`GROUP BY`、窗口函数、子查询——毫秒级完成，不需要导出到外部工具。
  - _什么时候需要它：_ 你刚跑完一条查询，不想导出、不想等 Excel 加载，就想在工具里继续追问。
- **联邦查询**：一条 SQL 关联 MySQL 的表、PostgreSQL 的视图和本地的 CSV 文件。
  - _什么时候需要它：_ 用户表在 MySQL，订单表在 PostgreSQL，对照文件在桌面上。一条 SQL 联起来。
- **数据探查**：点击结果表格中任一列，侧边面板即时展示该列的数据画像。数值列显示均值、最小值、最大值、中位数；文本列显示去重计数、空值率、频率分布 Top 10。所有计算由本地 DuckDB 在毫秒级完成，不触动远程数据库。
  - _什么时候需要它：_ 在分析之前，你想快速了解数据长什么样——有没有空值？分布如何？一键出答案。
- **三种结果集模式**：即时过滤（前端，零延迟）、SQL 过滤（拼接 WHERE 重新查询数据库，类似 DBeaver）、DuckDB 深度分析（聚合、窗口函数、联邦查询）。
- **版本快照**：公共维度表更新时，旧版本保持为快照不被覆盖。每个项目锁定到特定版本。新版本发布后，各项目自主决定何时同步。分析结果始终可复现。
  - _什么时候需要它：_ 你在1月用"产品分类 v1"表做了一份季度报告，3月有人把它更新成 v2。没有版本快照，你的报告会悄悄变化。有了快照，它保持原样。

### 🏗️ 双层架构：应用级 + 项目级

RdataStation 将数据工作组织为两层，每层都有各自的 SQLite 持久化存储和 DuckDB 分析引擎。

- **项目级（个人分析沙箱）**：每个项目拥有独立的 SQLite 和 DuckDB 文件，项目之间物理隔离。你的分析过程、中间结果、探索性查询保持独立，互不干扰。
- **应用级（公共服务层）**：存放公共连接模板、主数据、维度表和分析资产。所有项目都可以引用。

当同一份数据被多个项目反复使用——比如供应商主数据被营销分析、库存分析、采购分析三个项目同时需要——用户可以**手动将其提升**到应用级公共服务层。一次清洗，处处可用。当主数据更新时，新版本作为快照创建。每个项目独立决定何时同步新版本，或锁定旧版本。

### 🧩 插件系统（已实现）

RdataStation 提供两种扩展通道：

- **Extism 轻量插件（WASM 沙箱）**：社区开发的插件，适合 SQL 格式化、数据脱敏、代码生成、分析模板。沙箱运行，确保安全。支持 Rust、Go、Python、JavaScript 等多语言开发。
- **Sidecar 重量依赖**：独立进程管理，适合 JDBC 桥接（需要 JVM）和 Python 分析环境（需要 pandas 等）。按需启动，空闲时完全不占用资源。Sidecar 进程崩溃不影响主程序。

---

## 🚧 项目状态

🟢 **活跃开发中 — v0.5.x (Alpha)**

核心架构已稳固，所有主要模块功能可用。当前处于前后端精炼与优化阶段。

### ✅ 已完成

#### 数据库连接

- [x] **MySQL** — sqlx 原生连接，支持 SSH 隧道、SSL/TLS
- [x] **PostgreSQL** — sqlx 原生连接，支持 SSH 隧道、SSL/TLS
- [x] **SQLite** — rusqlite 原生连接，bundled 支持
- [x] **DuckDB** — 嵌入式分析引擎，原生驱动
- [x] **连接配置** — 全局 + 项目级，25 列字段全链路对齐
- [x] **认证体系** — AES-256-GCM 加密凭据，7 种认证方式（password, LDAP, Kerberos, OAuth2, OS auth, trust, SSH）
- [x] **网络隧道** — SSH（russh）、SSL/TLS（native-tls）、代理支持

#### SQL 编辑器与查询执行

- [x] **CodeMirror 6** — 语法高亮、SQL 语言支持、错误诊断
- [x] **多模式执行** — 执行选中、执行全部、执行当前语句
- [x] **DuckDB 加速** — 一键将查询结果转入本地分析
- [x] **智能执行** — 执行前行数预估
- [x] **批量执行** — 多语句独立结果集
- [x] **查询取消** — 中断正在执行的查询
- [x] **SQL 历史** — 后端持久化执行历史
- [x] **SQL 解析器** — sqlglot-rust 集成，语句解析与路由
- [x] **草稿编辑器** — 未保存文件支持、草稿持久化、崩溃恢复
- [x] **文件 CRUD** — 脏关闭拦截、外部变更检测、跨窗口冲突解决

#### 数据分析与 DuckDB

- [x] **二次分析** — 查询结果 → 本地 DuckDB 临时表
- [x] **联邦查询** — 跨库 JOIN（MySQL + PostgreSQL + CSV）
- [x] **数据探查** — 列级统计（均值、中位数、最大/最小值、空值率、频率分布）
- [x] **版本快照** — 不可变维度表版本
- [x] **全文搜索** — DuckDB FTS 集成
- [x] **导入/导出** — CSV、Parquet、JSON、Arrow
- [x] **DuckDB 扩展** — 可插拔扩展加载
- [x] **查询计划** — DuckDB EXPLAIN ANALYZE 支持

#### 项目与资产管理

- [x] **双层架构** — 应用级共享服务 + 项目级隔离
- [x] **分析资源管理器** — 文件夹层级、版本管理、标签、回收站
- [x] **项目 CRUD** — 创建、加载、保存、删除项目
- [x] **元数据浏览器** — Schema 树、懒加载子节点、数据库对象导航
- [x] **SQL 模板** — 可复用参数化查询

#### 插件系统

- [x] **Extism WASM 运行时** — 沙箱化插件执行（Rust, Go, Python, JS）
- [x] **Sidecar 适配器** — JVM 桥接（JDBC）、Python 分析环境
- [x] **插件管理器** — 安装、加载、卸载、依赖解析
- [x] **宿主函数** — 插件 ↔ 宿主 API 桥接
- [x] **权限系统** — 基于能力的插件权限

#### 缓存与性能

- [x] **查询缓存** — 带 TTL 的结果缓存
- [x] **元数据缓存** — Schema/表/列元数据缓存
- [x] **LRU 缓存** — 有界内存缓存与淘汰
- [x] **内存守卫** — 可配置内存上限
- [x] **性能监控** — 运行时指标采集
- [x] **缓存预热** — 预加载缓存

#### 日志与诊断

- [x] **结构化日志** — 可配置日志级别、滚动文件
- [x] **日志脱敏** — 自动密码/凭据遮蔽
- [x] **日志存储** — 持久化日志查询

#### 跨窗口与布局

- [x] **dockview-vue** — VSCode 风格 IDE 布局（面板、标签页、分屏）
- [x] **跨窗口支持** — 弹出编辑器面板到独立窗口、合并回来
- [x] **状态同步** — 跨窗口编辑器状态同步与冲突解决
- [x] **活动栏** — 扩展导航侧边栏
- [x] **可定制布局** — 拖拽面板重排

#### 安全

- [x] **AES-256-GCM** — 所有密码/凭据静态加密
- [x] **密码脱敏** — 日志中 URL 凭据自动遮蔽
- [x] **参数化查询** — `query_with_params()` 防 SQL 注入
- [x] **本地优先** — 零遥测、零云同步、零登录

#### 数据库迁移

- [x] **45 项迁移** 覆盖 4 个数据库（global, project_meta, connection_metadata, project_analysis）
- [x] **迁移管理器** — 版本化 Schema 演进

### 🔧 进行中 / 计划中

- [ ] **Schema 感知自动补全** — 从元数据缓存获取表/列建议（spec 已起草）
- [ ] **SQL 执行计划** — 数据库原生 EXPLAIN 可视化（spec 已起草）
- [ ] **SQL 方言转译** — MySQL ↔ PostgreSQL ↔ SQLite（spec 已起草）
- [ ] **分页查询** — 后端 LIMIT/OFFSET 原生分页（spec 已起草）
- [ ] **前端组件测试** — vitest + @vue/test-utils 测试套件（spec 已起草）
- [ ] **E2E 集成测试** — 全链路 Tauri 命令测试（spec 已起草）
- [ ] **多模式编辑器** — query/analysis/code 模式分离（spec 已起草）
- [ ] **导航栏优化** — 虚拟树性能、无障碍访问（spec 已起草）
- [ ] **属性面板** — 数据库对象详情面板（设计已起草）

### 📊 项目统计

| 类别          | 数量                                       |
| ------------- | ------------------------------------------ |
| Rust 源文件   | ~500+                                      |
| Tauri 命令    | ~30                                        |
| 前端扩展      | 8 个内置                                   |
| Vue 组件      | ~50+                                       |
| 数据库迁移    | 45                                         |
| Rust 测试文件 | 32                                         |
| Spec 文档     | 13                                         |
| 支持数据库    | 4 种（MySQL, PostgreSQL, SQLite, DuckDB）  |
| 认证方式      | 7 种                                       |
| 插件运行时    | 3 种（Extism WASM, Sidecar, Tauri 适配器） |

### ⚡ 快速开始

```bash
# 前置条件：Rust、Node.js、Tauri CLI
git clone https://github.com/ling-love-xin/RdataStation.git
cd RdataStation
npm install
cargo tauri dev
```

如果你需要一个稳定可靠的数据库工具，DBeaver、DataGrip、Navicat、TablePlus都是经过多年打磨的优秀选择。

如果你认同RdataStation的设计方向——让分析发生在查询之后、项目间隔离但共享公共资产、数据完全留在本地——欢迎关注、试用、提建议，或一起参与。

🔍 我们的不同之处
RdataStation 不是要做一个"更好的 DBeaver"或"更好的 DataGrip"。这些工具在自己的领域已经足够优秀。RdataStation 只是聚焦在了工作流中一个不同的环节。这不意味着哪种做法更正确——只是不同的起点，服务于不同的需求。

传统数据库工具 RdataStation 为什么对你重要
核心聚焦 连接数据库、编写SQL、展示结果 查询结果之后的分析过程 你不需要为了追问几个问题就切换到另一个工具
分析引擎 未内置。用户导出到Excel、Python或BI工具再做分析 DuckDB 嵌入核心 分析留在工具内。不用导出，不用等
性能 仅远程查询执行。反复分析每次都需访问远程数据库 DuckDB 本地加速。首次查询取回数据；后续所有分析本地毫秒级运行 第一次查询是唯一一次往返；后续每次追问都是本地速度
结果集工作流 展示结果。如需分析则导出 三种模式：即时过滤、SQL重新查询、DuckDB深度分析——全在一个面板内 从"随便看看"到"深度分析"不用切换工具
数据架构 扁平连接列表。无项目隔离 双层架构：项目级隔离 + 应用级共享服务 营销分析不会意外干扰库存分析
共享资产 无内置机制。手动交换文件 维度表和主数据可提升到应用级，版本化管理，跨项目复用 一次清洗，处处可用。不再需要维护三份相同的供应商表
数据主权 多数工具推动云同步、遥测或登录 本地优先。不上云。零遥测 你的数据库密码和分析成果永远不会离开你的电脑
可扩展性 要么封闭生态，要么传统插件模式 Extism WASM 沙箱用于轻量插件 + Sidecar 用于重量依赖 安全的社区扩展 + 隔离的重量运行时，一套架构兼顾
🤝 参与
欢迎任何形式的贡献，无论你的背景如何。

💬 只想聊聊想法？ 来 Discussions —— 在这个阶段，对产品方向本身的反馈也很宝贵

🐛 发现了 bug 或有功能建议？ 提一个 Issue

🔧 想贡献代码？ 看看我们的 good first issues 或阅读 设计文档 了解架构

🌏 英语不是你的母语？ 帮我们改进这份 README 的翻译——每一个修正都有价值

⭐ 喜欢这个想法？ 点一个 star，帮助更多人看到这个项目

📄 许可证
MIT / Apache-2.0 双许可
