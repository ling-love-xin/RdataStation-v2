# 分析资源管理器 前端设计方案

> **文档版本**: v1.7 | **最后更新**: 2026-05-12 | **同步**: v1.7 审计
>
> 本文档为 Phase 1 设计蓝图，描述完整目标架构。当前已实现（v1.7）组件：
>
> - ✅ `AnalyticsResourceManager.vue` — 主容器（Pinia store + 6 composable）
> - ✅ `CreateResourceModal.vue` — 创建/编辑模态框（全 naive-ui）
> - 🔜 其余组件为 Phase 4 目标（参见 `ANALYTICS_RESOURCE_PROGRESS.md`）

## 一、组件架构

```
views/workbench/
└── components/
    └── AnalyticsResourceManager/
        ├── AnalyticsResourceManager.vue          # 主容器
        ├── ResourceSearchBar.vue                # 搜索栏
        ├── ResourceTagFilter.vue                # 标签筛选栏
        ├── ResourceTree.vue                     # 资源树（文件夹 + 列表）
        │   ├── FolderItem.vue                   # 文件夹项（可折叠）
        │   ├── ResourceItem.vue                 # 资源项
        │   └── EmptyState.vue                   # 空状态
        ├── ResourceDetailPanel.vue              # 详情面板
        │   ├── TableDetail.vue                 # 表详情
        │   ├── FileDetail.vue                  # 文件详情
        │   └── ConnectionDetail.vue            # 连接详情
        ├── ResourceContextMenu.vue              # 右键菜单
        └── BottomActionBar.vue                  # 底部操作栏
```

---

## 二、主容器布局（最终版）

```
┌─────────────────────────────────────────────────────────────────┐
│  分析资源管理器                                              [×] │
├─────────────────────────────────────────────────────────────────┤
│  🔍 搜索资源名称...                                    [▼ 高级] │
├─────────────────────────────────────────────────────────────────┤
│  作用域: [全部] [🌍全局] [📂项目] [📌临时]                       │
│  类型:   [📄文件] [📊表] [🔌连接]                               │
├─────────────────────────────────────────────────────────────────┤
│  ┌─ 文件夹（用户创建，可折叠）───────────────────────────────┐  │
│  │  📁 供应链分析  [▼]                      [+ 新建] [⋮]    │  │
│  │  📁 营销分析    [▼]                                     │  │
│  └───────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────┤
│  资源列表（紧跟在文件夹下方，没有文件夹时直接显示）              │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 📄 订单数据.csv              [📂项目]         12.5 MB   │  │
│  │ 📊 供应商主数据 v3           [🌍全局]      45,678 行   │  │
│  │ 📊 库存表                     [📂项目]      12,345 行   │  │
│  │ 🔌 MySQL生产库               [🌍全局]                      │  │
│  │ 📊 temp.result_123           [📌临时]       1,234 行   │  │
│  └───────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────┤
│  [📥 导入文件]  [🧹 清理临时表]  [🔄 刷新]    [🔌 新建连接 ➚]  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 三、标签栏设计

### 3.1 标签分组（互斥）

```
┌─────────────────────────────────────────────────────────────┐
│  作用域: [全部] [🌍全局] [📂项目] [📌临时]                  │
│  类型:   [📄文件] [📊表] [🔌连接]                           │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 标签视觉设计

| 标签    | 图标 | 颜色         | 说明             |
| ------- | ---- | ------------ | ---------------- |
| 全部    | —    | 默认         | 显示所有资源     |
| 🌍 全局 | 🌍   | 蓝色 #165DFF | 应用级资产       |
| 📂 项目 | 📂   | 绿色 #00B42A | 项目级资产       |
| 📌 临时 | 📌   | 橙色 #FF7D00 | 会话级，注意清理 |
| 📄 文件 | 📄   | 灰色         | —                |
| 📊 表   | 📊   | 灰色         | —                |
| 🔌 连接 | 🔌   | 灰色         | —                |

### 3.3 标签交互

- **单选互斥**：同组内只能选一个
- **跨组叠加**：作用域 + 类型可同时选（如：全局 + 表）
- **清除**：点击"全部"或再次点击已选标签

---

## 四、资源列表设计

### 4.1 资源项视图

```
┌───────────────────────────────────────────────────────────────────┐
│ [类型图标]  资源名称                        [作用域]    [行数/大小] │
│                                                                   │
│ 示例：                                                             │
│ 📄 订单数据.csv                            [📂项目]      12.5 MB    │
│ 📊 供应商主数据 v3                        [🌍全局]      45,678 行  │
│ 📊 temp.result_123                        [📌临时]       1,234 行 │
│ 🔌 MySQL生产库                            [🌍全局]                     │
└───────────────────────────────────────────────────────────────────┘
```

### 4.2 资源项信息层级

```
第一行：类型图标 + 资源名称（主名称，可显示别名）
第二行：引用名 / 文件路径（次要信息）
右侧：作用域标签 + 统计信息（行数/大小/更新时间）
```

### 4.3 排序支持

点击列头排序：

- 名称（A-Z / Z-A）
- 作用域（全局 > 项目 > 临时）
- 大小/行数
- 更新时间

---

## 五、右键菜单

### 5.1 资源右键菜单

```
在资源上右键：
┌─────────────────────────┐
│ 📋 预览数据              │
│ 📝 重命名                │
│ 📂 移动到文件夹        ▶ │
│ 📑 复制引用 SQL          │
│ ─────────────────────── │
│ 🚀 提升为全局            │
│ 📤 导出                  │
│ ─────────────────────── │
│ 🗑️ 删除                  │
└─────────────────────────┘
```

### 5.2 文件夹右键菜单

```
在文件夹上右键：
┌─────────────────────────┐
│ 📝 重命名                │
│ 📂 新建子文件夹          │
│ 📌 排序方式            ▶ │
│ ─────────────────────── │
│ 🗑️ 删除文件夹            │
└─────────────────────────┘
```

### 5.3 多选资源右键菜单

```
选中 3 项后右键：
┌─────────────────────────┐
│ 📂 移动到文件夹        ▶ │
│ ─────────────────────── │
│ 🚀 批量提升为全局        │
│ 📤 批量导出              │
│ ─────────────────────── │
│ 🗑️ 批量删除              │
└─────────────────────────┘
```

---

## 六、键盘快捷键

| 快捷键     | 功能                |
| ---------- | ------------------- |
| `Ctrl + F` | 聚焦搜索框          |
| `Delete`   | 删除选中资源        |
| `F2`       | 重命名              |
| `Ctrl + C` | 复制引用 SQL        |
| `Enter`    | 打开详情面板        |
| `Escape`   | 取消选中 / 关闭面板 |
| `Ctrl + A` | 全选                |

---

## 七、拖拽交互

### 7.1 拖拽到 SQL 编辑器

```
资源列表中拖拽：
📊 供应商表 ────────────────▶ SQL 编辑器（插入引用片段）
                                 └── 生成: global.supplier_v3
```

### 7.2 拖拽到文件夹

```
拖拽到文件夹：
📊 库存表 ────────────────▶ 📁 供应链分析（高亮）
                           └── 弹出确认：「移动到"供应链分析"？」

同资源可属于多个文件夹（播放列表模式）
```

### 7.3 拖拽排序

- 文件夹可以拖拽排序
- 文件夹内的资源可以拖拽排序

---

## 八、空状态引导

### 8.1 无资源时的引导

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│                     📭 暂无分析资源                          │
│                                                             │
│    开始使用：                                                │
│    • 导入 CSV/Parquet/Excel 文件                            │
│    • 执行 SQL 查询，结果自动转为资源                         │
│    • 从连接中提取表到分析区                                  │
│                                                             │
│    [📥 导入文件]           [🔌 新建连接]                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 8.2 无文件夹时的引导

```
┌─────────────────────────────────────────────────────────────┐
│  📁 文件夹                                    [+ 新建]     │
│  ─────────────────────────────────────────────────────────  │
│  💡 还没有文件夹，点击"+ 新建"创建第一个文件夹               │
│      来整理你的分析资源                                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 九、搜索功能

### 9.1 基础搜索

```
🔍 搜索资源名称...（实时搜索，模糊匹配）
```

### 9.2 高级筛选（点击 ▼ 展开）

```
┌─────────────────────────────────────────┐
│ 🔍 搜索...                         [▼] │
├─────────────────────────────────────────┤
│ ▼ 高级筛选                             │
│   作用域: [全部 ▼]                     │
│   类型:   [全部 ▼]                     │
│   日期:   [不限 ▼]                     │
│   大小:   [不限 ▼]                     │
└─────────────────────────────────────────┘
```

### 9.3 搜索历史

```
输入时显示：
┌─────────────────────────────────────────┐
│ recent: 供应商                           │
│ recent: 销售数据                         │
│ recent: orders                          │
└─────────────────────────────────────────┘
```

---

## 十、批量操作

### 10.1 多选方式

- 单击选中
- `Ctrl + 单击` 多选
- `Shift + 单击` 范围选
- `Ctrl + A` 全选

### 10.2 选中后底部显示

```
┌─────────────────────────────────────────────────────────────┐
│  ✅ 已选中 3 项                      [批量移动] [批量删除]  │
└─────────────────────────────────────────────────────────────┘
```

---

## 十一、详情面板

### 11.1 面板位置

右侧滑出，点击资源后显示

### 11.2 表资源详情

```
┌─────────────────────────────────────────┐
│ 📊 供应商主数据 v3                       │
│ ─────────────────────────────────────── │
│ 引用名: global.supplier_master_v3        │
│ 类型: DuckDB 全局表                      │
│                                          │
│ 来源: 从 persist.supplier_master 提升    │
│ 当前版本: v3                            │
│                                          │
│ 统计:                                    │
│   行数: 45,678                          │
│   列数: 12                              │
│   创建时间: 2024-05-01 10:30            │
│   更新时间: 2024-05-05 15:20            │
│                                          │
│ 引用该资源的文件夹:                      │
│   📁 供应链分析                          │
│   📁 季度报告                            │
│                                          │
│ ─────────────────────────────────────── │
│ [📋 预览数据] [📑 复制引用]              │
│ [📜 版本历史] [🚀 提升为全局]            │
│ [📤 导出]      [🗑️ 删除]                │
└─────────────────────────────────────────┘
```

### 11.3 文件资源详情

```
┌─────────────────────────────────────────┐
│ 📄 订单数据.csv                          │
│ ─────────────────────────────────────── │
│ 路径: project_files/orders.csv          │
│ 类型: CSV                                │
│ 编码: UTF-8                              │
│                                          │
│ 存储位置: 项目内                          │
│                                          │
│ 统计:                                    │
│   大小: 12.5 MB                         │
│   行数: 1,234,567                       │
│   列数: 15                              │
│   创建时间: 2024-05-01                  │
│                                          │
│ ─────────────────────────────────────── │
│ [📖 打开] [📑 复制引用]                  │
│ [🚀 提升为全局] [📤 导出]                │
│ [🗑️ 删除]                               │
└─────────────────────────────────────────┘
```

---

## 十二、底部操作栏

```
┌─────────────────────────────────────────────────────────────┐
│  [📥 导入文件]  [🧹 清理临时表]  [🔄 刷新]   [🔌 新建连接 ➚] │
└─────────────────────────────────────────────────────────────┘
```

| 按钮          | 功能                                   |
| ------------- | -------------------------------------- |
| 📥 导入文件   | 打开文件选择器，导入 CSV/Parquet/Excel |
| 🧹 清理临时表 | 清理所有会话级临时表（需确认）         |
| 🔄 刷新       | 刷新资源列表和文件夹                   |
| 🔌 新建连接   | 跳转到导航树的连接管理页面             |

---

## 十三、弹窗设计

### 13.1 删除确认

```
┌─────────────────────────────────────┐
│ 确认删除 "供应商表"？                │
│                                     │
│ ☑ 同时删除 DuckDB 中的表数据         │
│ ☐ 放入回收站（30天内可恢复）        │
│                                     │
│ [取消]  [确认删除]                  │
└─────────────────────────────────────┘
```

### 13.2 删除前检查依赖

```
⚠️ 删除前检查
─────────────────────────────────────
该资源被以下资源引用：
• 📁 供应链分析/库存表（JOIN）
• 📁 季度报告/供应商CSV（引用）

强制删除将导致下游资源不可用

[取消]  [强制删除]
```

### 13.3 新建文件夹

```
┌─────────────────────────────────────┐
│ 新建文件夹                           │
│                                     │
│ 名称: [________________]            │
│ 父文件夹: [无 / 选择父文件夹 ▼]     │
│                                     │
│ [取消]  [创建]                      │
└─────────────────────────────────────┘
```

### 13.4 移动到文件夹

```
┌─────────────────────────────────────┐
│ 移动到文件夹                         │
│                                     │
│ 选择目标文件夹:                      │
│ ○ 📁 供应链分析                       │
│ ○ 📁 营销分析                         │
│ ○ 📁 新建文件夹...                    │
│                                     │
│ ☑ 同时移动到其他已有引用位置          │
│                                     │
│ [取消]  [移动]                      │
└─────────────────────────────────────┘
```

---

## 十四、回收站

### 14.1 入口

底部操作栏增加"🗑️ 回收站"按钮

### 14.2 回收站面板

```
┌─────────────────────────────────────────────────────────────┐
│  回收站                                              [×]   │
├─────────────────────────────────────────────────────────────┤
│  已删除的资源（30天内可恢复）                              │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ 📊 供应商表          2024-05-05 删除    [恢复] [彻底删除] │
│  │ 📄 旧数据.csv        2024-05-04 删除    [恢复] [彻底删除] │
│  └─────────────────────────────────────────────────────┘  │
│                                                             │
│  [🗑️ 清空回收站]                                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 十五、状态设计

### 15.1 加载状态

```
资源加载中：
┌─────────────────────────────────────┐
│ ████████████░░░░░░░░  60%          │
│ 正在加载分析资源...                  │
└─────────────────────────────────────┘

骨架屏：
┌─────────────────────────────────────┐
│ ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░   │
│ ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░   │
│ ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░   │
└─────────────────────────────────────┘
```

### 15.2 错误状态

```
┌─────────────────────────────────────┐
│ ⚠️ 加载失败                           │
│                                     │
│ 无法获取资源列表                      │
│                                     │
│ [🔄 重试]                           │
└─────────────────────────────────────┘
```

---

## 十六、性能优化

### 16.1 虚拟滚动

使用 ag-Grid 虚拟滚动，仅渲染可见行

### 16.2 懒加载

```
展开文件夹时才加载资源：
📁 大文件夹A  [▶ 点击才加载...]
📁 小文件夹B
    ├─ 📊 表1
    └─ 📊 表2
```

### 16.3 元数据缓存

行数、列数等信息首次获取后缓存，避免重复查询

---

## 十七、核心设计原则

| 原则               | 体现                             |
| ------------------ | -------------------------------- |
| **少概念，多直觉** | 标签代替层级路径                 |
| **视图与存储分离** | 文件夹是视图，不是存储位置       |
| **渐进式复杂性**   | 基础用户用标签，高级用户用文件夹 |
| **零心智负担**     | 双击插入引用片段                 |
| **数据安全**       | 回收站、删除确认、依赖检查       |
