[package]
name = "rds-editor"
version.workspace = true
edition.workspace = true
description = "SQL 编辑器：一个内核 + 三档能力（文本 / SQL / 分析）"

# 依赖方向（硬约束）：workbench → editor → engine / database / shared。
# Phase 0（地基）只有纯模型与规则表，暂无外部依赖；
# 1a 起按需加入：engine（SqlService / sql::split）、shared（错误类型 / Arrow）、gpui-kit（视图层）。
