//! 视图内部件（状态栏）
//!
//! 查找 / 替换**不在这里**：那是编辑器内核的能力（`Ctrl+F` → 内核 `Search`，
//! 由组件库的查找面板渲染），应用层不重复造一个。
//!
//! **结果区不在这里**：结果集是独立的一块（还要长出导出 / 值预览 / 过滤 / 冻结列……），
//! 视图在 `crate::view::results`（`mod.rs` 有结构与「能用组件库就用」的台账）。

pub mod status_bar;
