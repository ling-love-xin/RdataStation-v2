# SQL 编辑器模块 · 开发方案（Phase 0 / 1a / 1b / 1c）

> 状态：**1a 已完成；1b 进行中（2026-09-18）**——实现进度见 §0 表（最近在前）。
> 关联：`editor-prototype-design.md`（长什么样）、`editor-architecture.md`（为什么这样设计，§7 现状与 §12 已知问题为权威）、`editor-prototype.html`（交互稿）。
> 方案阶段的待确认项以 `editor-architecture.md` §13 为准；开工前要关的那两项（#1 Dock 标签能力 / #3 格式化选型）已在 1a 结案。

---

## 0. 进度记录（最近在前）

| 日期 | 内容 | 状态 |
| --- | --- | --- |
| 2026-09-18（1b：**B9 切片二 —— 手动补全 + 模板片段**，B9 收圆） | **补全从“打字碰运气”变成“随时叫得出来 + 有现成写法可插”**：① **`Ctrl+Space`**（新动作 `TriggerCompletion`，键位在 `crates/app` 注册；已 grep 内核与组件库，`ctrl-space` / `"space"` 都没被占用）：手动请一次候选——**候选与打字那条完全同一份**（新导出的 `view/completion.rs::items_at`：`word_start` 算替换范围 + `request_at` 判上下文 + 端口目录），只是替换范围由我们算而不是内核的 `trigger_start_offset`。`word_start` 只吃标识符字符（**不含 `.`**）：`public.ord` 上按快捷键替换的是 `ord`，留住 `public.`；光标落在多字节字符中间时不动它（宁可把替换范围放光标处）。**拒绝要说原因**（文本模式 / 只读 / 超大文件档位三档分开说，“按了没反应”是不允许的）；真一条候选都没有也直说（“元数据可能还没载好”）。② **模板片段**：「⋯ 更多 ▾ ▸ 插入模板」列出 `sql_template_store` 里的模板（今天 6 条内置；空则不摆这一项），点一条**插到光标处并选中 `{table}` 占位符**——用户接着敲表名就直接覆写它。插入走内核的 `insert`（进撤销栈、可撤回），只读文档在这里就拒（与写文本的其它动作同口径）。③ **模板从哪来**：端口 `CompletionPort::templates()`（默认空实现，不接模板的宿主照样能用补全其余部分）；工作台实现在 `attach` 时**一次性预载**（模板菜单弹出来时才读它，而渲染路径不能做 I/O，所以启动读完就不再碰库；读不到就空着）。④ 测试口径：断言读的是内核自己的 `completion_menu_state()`（`#[doc(hidden)] pub`）——**“弹层真的开了”不是我们的副本**。**验证**：editor 单测 **371 → 377**（纯函数两项：`word_start` 六例（中段 / 词首 / 空格后 / 空串 / 越界 / 中文与多字节边界）+ `placeholder_range` 三例 · 面板四项：真按键 `Ctrl+Space` → 弹层开着且查询词是 `ord`、候选里有那张表（**顺带钉住“未绑定连接不给候选”**——用例先绑一条） · 文本模式拒绝且状态栏说原因 · 插模板后文本与选中区间都对（选中的正是 `{table}`） · 只读拒绝）· workbench 单测 **103 → 104**（模板行 → 片段：空说明不摆空字符串）· `ui_contract` 7 项（无新视图文件）· app 键位注册编译通过。⬜ 余：实时内省回稳（缓存未命中时回落实时元数据）。 | `editor/src/{completion.rs,shared.rs,commands.rs,view/{completion,host,tests}.rs}` + `workbench/src/services/editor_completion.rs` + `app/src/main.rs` | 单测 377 / 104（✅）· 真按键路径（✅） |
| 2026-09-18（1b：**B1 收尾 —— 连接绑定随会话持久化**） | **“这份 SQL 发到哪”跨重启也不丢**（架构 §12 #26 的最后一小段）：① **会话字段**：`SavedSession.connection: Option<String>`（与 `mode` / `channel` 同类——它们是同一类东西：**文档属性**，重启后默默变成“跟随当前连接”会让用户以为还在往原来那个库写）。② **落库**：`editor_contexts.connection_id` 列早就在（A12 建表就有，一直写空串），现在写真值；`None` ↔ 空串两个方向都定死（空串回来是“跟随当前连接”，不是一条空 id 的绑定）。③ **恢复要校验**：`view/host.rs::restore_connection` 用宿主的连接列表快照判“还在不在”——在就写回文档；不在（连接被删 / 换了项目）就**回退到「跟随当前连接」并把原因说出来**（留一个指向不存在连接的绑定，用户要等执行时才发现发错了地方）；**没接端口就不校验也不丢**（宁可不猜，也不抹掉用户的绑定）。**不主动建连**：启动恢复不该拨号，执行时由执行器按绑定去用。**验证**：editor 单测 **368 → 371**（会话往返带绑定且 `None` 也能原样回来 · 恢复三项：连接还在列表里→绑定真的写回文档 / 不在→回退且状态栏点名那条连接与“跟随当前连接” / 没接端口→保留绑定 · 快照带绑定（未绑是 `None`））· workbench 单测 **102 → 103**（`connection_id` 列两个方向的往返）· **真机：真 SQLite 跨重启**（`editor_session_real` 新增一例：绑定 → 关文档（落会话）→ 新一份共享状态读回 → 绑定还在且写回文档）· 顺手修一处自己的遗漏：`editor_exec.rs` 的 `analysis_tests` / `export_tests` 两模块漏了 `#[cfg(test)]`（非测试构建里它们的辅助函数成了死代码，workbench 从此零警告）。 | `editor/src/{session.rs,view/host.rs,view/tests.rs}` + `workbench/src/services/editor_session.rs` + `workbench/tests/editor_session_real.rs` | 单测 371 / 103（✅）· 真 SQLite 跨重启（✅） |
| 2026-09-18（1b：**B7 切片二 —— Parquet / XLSX 导出**，B7 收圆） | **导出格式从三种变五种，后两种经 DuckDB `COPY … TO …` 落盘**：① **为什么要过 DuckDB**：Parquet 是带 schema 的列式格式、XLSX 是 OOXML 包，都不是“拼字符串”写得出来的；DuckDB 既能把行围成表（与分析同一套 `create_temp_table_internal`）又能 `COPY` 出去（手写编码器几乎必然写出别人读不了的文件）。② **引擎侧**（`execution_service::{export_rows_via_duckdb, DuckDbExportFormat}`）：临时表建成 → `COPY (SELECT * FROM <表>) TO '<路径>' (FORMAT …)` → **无论成败都 DROP**（与 `with_analysis_temp_table` 同款承诺，否则每次导出都漏一张 `tmp_q_*`）；路径按 SQL 字面量转义（单引号双写 + 统一正斜杠）。**XLSX 两个坑（真机探针抳到）**：· 连接建时就关了 `autoinstall_known_extensions`，所以要在导出路径里**显式** `LOAD excel` → （不得已）→ `INSTALL excel; LOAD excel`，失败原因里带**扩展目录**（与本地加速同一取舍，否则用户只看到一句 “Extension not found” 不知道该把文件放哪）；· **必须显式 `HEADER true`**——不传时写出来的工作簿第一行就是数据，用 `read_xlsx(…, header = true)` 读回来会把 `1` 当列名。③ **线程**：这条路可能几秒（首次装 excel 扩展要联网，实测 3–11 秒），所以**不在事件路径同步写**，走**一次性线程 + 回执队列**（`DuckDbExportRequest` / `ExportNote` / `ExecQueue::{request_duckdb_export, drain_export_notes}`，与源动作同一套），**不占执行位**；面板用 `export_pending` 让轮询泵多活一会儿，提交时就说“正在导出…（首次装 excel 扩展可能要几秒）”。④ **口径不变**：行仍按**展示文本**桥接（`"NULL"` → 真 null），列类型由引擎按整列推断——**数字列在 Parquet / XLSX 里就是数**（比 CSV / JSON 多出来的好处；代价是 `007` 这类前导零会按数字落）；筛选中导出的是**筛后的行集**且回执明示“（已筛选）”；文本三档仍在编辑侧同步编码（`encode_rows` 对这两档给空串，叫错就回绝）。⑤ **真机台账**（`crates/engine/tests/duckdb_export_probe.rs`，新）：Parquet **离线必过**（3 行 → 350 字节 / 24 ms，`read_parquet` 读回列名 / 行序 / NULL 全对）；XLSX 写得出（3756 字节）且 `read_xlsx(header = true)` 读回列名 `id`/`name` 与三行（数字是双精度——xlsx 的存储就是 double），装不上时如实打印为跳过；另有一条把 `duckdb_extensions()` 与 `extension_directory` 打出来的状态探针（扩展装在 `<extensions_dir>/v1.5.5/windows_amd64/`）。**验证**：editor 单测 **364 → 368**（模型一项：两档都标 `duckdb_backed` 且本地不编码 / 默认文件名跟扩展名走 / 菜单五项两组；面板三项：Parquet 真的交给执行器（格式 / 路径 / 列名 / 行数 / 未筛选都逐个断言）且回执落状态栏 · 筛选中导出过去的是筛后一行且回执带“（已筛选）” · 执行器不支持时如实报“导出失败：…不支持…”）· workbench 单测 **+2**（真机级：经 `EngineQueryRunner` 导出 Parquet 并读回，`"NULL"` 字面量在盘上是真 NULL；文本档调这条路被回绝且不留文件）· engine 单测 **+1**（parquet 往返）· 探针 3 项（见⑤）。⬜ 余：列类型化输出（CSV / JSON 仍是展示文本）需先有列类型。 | `engine/src/services/execution_service.rs` + `engine/src/duckdb/mod.rs` + `engine/tests/duckdb_export_probe.rs`（新） + `editor/src/{export.rs,execution.rs,shared.rs,view/host.rs,view/tests.rs}` + `workbench/src/services/{editor_exec.rs,result_service.rs}` | 单测 368 / 102 / 437（✅）· 真机 Parquet 往返（✅）· XLSX 写出 + 读回（✅，依赖扩展可用） |
| 2026-09-18（1b：**B15 收尾 —— 结果血缘摘要**） | **“这份结果是怎么来的”变成看得见的一行字**（原型 §2.4 要求结果集携带来源摘要）：① **模型侧**：`ExecTarget::lineage()`（纯函数，与 `label()` 分开——标签说的是“用户点了哪个动作”（当前语句 / 选区 / 批量），血缘说的是“这份数据由什么产生”：四个下发动作各自有名，其余全归「原查询」）· `ExecJob` / `ExecOutcome` 带上它（**结论原样带回**，不在回填时重新猜）· `ResultEntry.lineage` + `with_lineage`（失败也带：这句话是下发筛选发出去的，对排查同样有用）。② **界面侧**：结果工具栏左段多一段来源摘要，**排在连接之前**（“这份是怎么来的”比“从哪条连接来”更常被问），工具栏文案变成 `行数 1,204 │ 耗时 1.2s │ 下发筛选 │ ●P·orders`；有自定义标题的结果（执行计划 / 分析）**用标题当来源**（不会出现“执行计划”这份结果却写着“原查询”）。③ **与原型的一处偏差（写进架构 §12 #32）**：原型把来源摘要放在**标签 hover** 上，而 0.6.1 的 `Tab` 没有 tooltip（`ComponentTooltip` 只给 `StatefulInteractiveElement`，`Tab` 不是）；挂在 `suffix` 上只能覆盖徽标那一小块，所以摆在工具栏左段——**不用悬停就能看到**。**验证**：editor 单测 **363 → 364**（血缘纯函数六例：选区 / 当前语句 / 批量 / 全部都是「原查询」，筛选 / 排序 / 取段 / 分析各自有名 · 工具栏分段顺序（血缘在连接之前）与缺项不占位 · 两条旧断言跟着工具栏文案更新 · 面板：下发筛选产生的那份血缘是「下发筛选」· 分析结果血缘是「本地分析」）。⬜ 余：沉淀为 1c 分析单元（1c 已搁置）。 | `editor/src/{execution.rs,store.rs,view/host.rs,view/results/grid.rs,view/tests.rs}` | 单测 364（✅）· 血缘随结论回到结果集（✅） |
| 2026-09-18（1b：**B15 切片二 —— 自定义分析 SQL**） | **预置菜单之外，自己写一条聚合 SQL**：① 入口 = 分析下拉底部（分割线之后）「自定义分析 SQL…」（`analysis::CUSTOM_LABEL`）——预置项仍是“点一下就跑”，这一项弹输入框。② **对话框要输入，所以状态用 `TextareaState` 实体**（`dialogs::open_analysis_sql`）：与其它对话框“不引入局部状态”的取舍**方向相反但要写清原因**——那里是选择（`Rc<Cell<_>>` 改了不会重绘，所以用动作按钮表达选择），这里是输入，而输入框的状态实体自己管重绘 / 光标 / 键盘，这正是“局部状态”该有的形状。③ **预填的是能直接跑的模板**（`CUSTOM_DEFAULT_SQL`，与「计数行数」同一条——字面量只有一处，两边不会写歪），占位符说 `{table}`，说明行写“在本地 DuckDB 上跑，数据是当前结果集已抓到的 N 行（不重跑源库）”，N 与截断数取自 `request_for` 的桥接口径（截断与计数只有那一处，界面侧不另算）。④ **空的在本地就拒**（`analysis::is_runnable`）：状态栏说“分析 SQL 为空（写一条 SELECT 再运行）”，**不打扰执行器**（提交了也只会拿到一句驱动报错）。⑤ 尺寸常量 `ui::ANALYSIS_SQL_HEIGHT`（9rem）；`ui_contract` 无需新增登记（对话框在已登记的 `dialogs.rs` 里，且无裸 `px(` 与裸色值）。**验证**：editor 单测 **359 → 363**（模型一项：模板带占位符且可运行 / 空白不可运行 / 说明含行数与截断两种口径 · 面板三项：SQL 与已抓行原样到执行器且**落新结果集不抢选中**（`active_index` 仍是 0，与 B2/B10 同一落位口径）+ 标题「分析」+ 说明进状态栏 · 空白本地拒且执行器零调用 · 对话框真打开且输入框与运行按钮真渲染（真点击按架构 §12 #29 的口径不赌））。⬜ 余：分析结果**血缘摘要**（依赖 B13 通道信息）· 沉淀为 1c 分析单元（1c 已搁置）。 | `editor/src/{analysis.rs,ui.rs,view/dialogs.rs,view/host.rs,view/tests.rs}` | 单测 363（✅）· 对话框与落地入口各一组窗口用例（✅） |
| 2026-09-18（1b：**B15 切片一 —— 结果集「⚗ 分析 ▾」接通本地 DuckDB**） | **点一下「分析」：结果集在本地 DuckDB 上被聚合，结论落新结果集（原结果一行不动）**：① **口径先定死：分析不重跑源库**——桥接的是**已经抓到的行**（`columns` + `rows`，上限 `ANALYSIS_MAX_ROWS = 20_000`，超了截断并在结果里如实写“另有 N 行超出上限未参与”）。三条理由写进 `analysis.rs` 模块头：源库断了 / 慢查询不想再跑一遍照样能分析；分析的对象**就是用户眼前这份数据**（不会悄悄换成“重查一遍”的结果）；与 B7「只导已抓取的行」同一口径。代价也写明：**值按展示文本进库**（`NULL` 字面量 → 真 NULL，其余是字符串；要按原样比较就在分析 SQL 里 `CAST(... AS VARCHAR)`）。② **纯模型 `crates/editor/src/analysis.rs`**：`AnalysisRequest{sql, columns, rows, dropped_rows}` · `menu_items(entry)`（**只摆真能跑、结果可预期**的两类：计数行数 + 逐列分组计数，最多 8 列；菜单 SQL 带 `{table}` 占位符，由引擎替换）· `request_for` / `request_for_item` / `result_entry` · `notice()`（“本地 DuckDB 分析 · 基于已抓取的 N 行…”）。③ **执行面**：`ExecTarget::Analysis(AnalysisRequest)`（`sql()` / `statements()` / `label()` 都补了分支，标签“本地分析”）+ `QueryRunner::analyze`（默认实现如实说“不支持”）+ `ExecOutcome.analysis`（结论原样带回，**不在回填时重新猜**）+ `ResultEntry.analysis` 标记（分析结果**不摆「⟳ 刷新」**——刷新会重跑分析 SQL，那是另一件事）。通道闸 / 项目只读闸对 `Analysis` **放行**（它不碰源库，写语句那套判据在这里不适用）。④ **宿主实现**（`services/editor_exec.rs::analyze`）走 `ResultService::execute_duckdb_analysis(temp_table="", sql, columns, rows)`（内存 DuckDB，不需要任何服务器）；值的桥接口径：`"NULL"` → 真 null，其余按展示文本；`json_cell_text` 把结果还原成网格文本。⑤ **真机级缺陷（踩到并修）**：`duckdb_service::query_duckdb` 原来在 prepare 之后就调 `stmt.column_name(i)` 取列名，而 duckdb-rs 1.10505 的 `column_name` / `schema()` 读的是**执行结果**（`raw_statement.rs::executed()` 断言 `The statement was not executed yet`）→ 一跑就 panic。**B15 是这段死代码的第一个调用方**（此前四个驱动都走自己的取数路径）。修法按 duckdb-rs 文档推荐的形状：先 `stmt.query([])` 把语句跑起来，再从 `Rows::as_ref()` 取回那条**已执行**的语句读 `column_names()`，随后逐行取值——列名与结果一一对应，也不再依赖未执行的 schema。⑥ 顺手把 mock 临时表那条清理用例改成**按生产语义重试几拍**（清理是非阻塞的，B15 的分析用例会在同一进程里持内存库锁，一次不成就断言失败的话测的是锁竞争，与结果集那条同一口径）。**验证**：editor 单测 **353 → 359**（菜单只给有网格的结果集 · 计数 / 分组两项的 SQL 与占位符 · 列名引号转义 · 桥接行数与上限截断 + notice 文案 · 分析结果带标记且没有“下一段”）· workbench 单测 **98 → 100**（`analysis_tests` 两项：桥接的 2 行真在内存 DuckDB 上跑出 `count(*) = 2` / `count(id) = 1`（NULL 是真 NULL）· 错误 SQL 如实回原因）· engine 单测 **436**（`query_duckdb` 列名改法不回归）· `ui_contract` 7 项（无新视图文件）。⬜ 余（切片二）：自定义分析 SQL 输入（现在只有预置菜单）· 分析结果**血缘摘要**（依赖 B13 通道信息）· 沉淀为 1c 分析单元（1c 已搁置）。 | `editor/src/{analysis.rs（新）,store.rs,execution.rs,lib.rs,view/host.rs,view/results/grid.rs}` + `engine/src/services/duckdb_service.rs` + `workbench/src/services/editor_exec.rs` + `workbench/src/components/project_host.rs`（清理用例重试） | 单测 359 / 100 / 436（✅）· 桥接行真跑内存 DuckDB（✅ 含 NULL 语义） |
| 2026-09-18（1b：**B9 补全（切片一）**——打字即补，候选来自导航缓存） | **补全从“一个词”变成了真能用的交互，而且零自建**：① **先核到内核源码**（gpui-base 0.6.1 的 `input/editor/lsp/completions.rs`）：内核已有 `CompletionProvider`（LSP 形状：`completions(text, offset, trigger, …) -> Task<Result<CompletionResponse>>` + `is_completion_trigger`）+ 弹出层 + 键盘上下选 + **替换已敲那一段**——所以只提供候选，不画下拉（与 A11 查找/替换同一结论）。**两条读源码得来的硬约束**：· **只给 `label`**——`insert_text` 存在时内核把替换范围改成「光标处」（`range.end..range.end`），于是 `from ord` 选 `public.orders` 会变成 `from ordpublic.orders`（overlay.rs::insert_completion 原文）；· 触发时机完全由 `is_completion_trigger` 决定（每次文本输入后被问一次）。② **编辑器新增 `completion.rs`（纯函数，不碰 GPUI）**：`request_at(文本, 偏移)` 判定“光标处要什么”——**表位 / 列位 / `限定名.` / 通用**；判据是沿**分号**切出当前语句、从后往前找最近的子句关键字（`select id, nam` 里紧邻的是列名，但子句是 `select` → 列位——这条是**被自己的单测抓出来的**）；找不着就退 `Any`（不猜）。`candidates(目录, 请求, 上限)`：前缀命中 > 包含命中，类别（列/表/视图/函数/关键字）→ 有前缀时短的在前（`ord` 命中时 `orders` 比 `order_items` 更可能是要的）、无前缀时字典序 → 去重 → 截断；**限定名认不出来（大概率是语句里的表别名 `o.`）就给全部列**（宁可多给，也不给一个错的子集）。关键字 48 条、常用函数 40 条内建（四方言共有那批，不分方言——那是转译的活）。③ **视图侧 `view/completion.rs`**：`SqlCompletionProvider` 把候选翻成 LSP `CompletionItem`（只给 `label` + `kind` + `detail`）；按能力表 + 编辑器只读 + **大文件档位**（A13 的“>50MB 关重能力”在这里落地）装 / 摘；触发 = 刚敲下标识符字符或 `.`（退格 / 空格 / 粘贴不触发）。④ **候选从哪来（宿主端口，与“渲染路径不做 I/O”同一纪律）**：`CompletionPort::catalog(conn_id, 通道) -> Catalog` 必须**同步 + 内存**；workbench 侧 `services/editor_completion.rs` 读**导航侧 L2 缓存**（`database::cache::NavCache`：schema/表/视图/列）并**后台预载一次**（同一连接去重，未载完就返空——补全晚一拍是常态，不弹窗不打扰）；`NavCache` 新增 `all_schemas()`（带 `schema_id`，一次把目录装进内存的读入口）。**通道决定限定名怎么写**（给错候选比不给更糟）：源库 / 加速 = `schema.表`；**联邦 = `别名.schema.表`（L2 源两段 `别名.表`）**，且**只给已挂上的源**（读会话快照；挂不上的源给出来只会让人写出跑不通的 SQL）。⑤ 未接端口时只给关键字与函数——**如实，不假装有元数据**。⬜ 余（切片二）：`Ctrl+Space` 手动触发（内核有公开的 `present_completion_items`，但属另一条交互，现在**没注册也没宣传**）· 模板片段（`sql_template_store` 6 条内置）· 缓存未命中时回落实时内省。**验证**：editor 单测 **346 → 353**（上下文判定九例（含带引号限定名、中文 SQL 偏移钳制、`with x as (` 不猜）· 按最后一段匹配且短的在前 · 限定名收敛到该表列（全名 / 最后一段两种写法）· 别名给全量 · 列位不给关键字 · 去重与上限 · 模式门控（SQL 开 / 文本关、端口未接目录为空））· database 单测 **41**（`all_schemas` 不回归）· `ui_contract` 两份清单已登记 `editor/view/completion.rs`（新增一个视图文件就必须登记，否则契约静默失效）。 | `editor/src/{completion.rs（新）,view/completion.rs（新）,view/mod.rs,view/host.rs,shared.rs,lib.rs}` + `database/src/cache.rs`（`all_schemas`）+ `workbench/src/services/editor_completion.rs（新）` + `workbench/src/{view.rs,services/mod.rs}` + `workbench/tests/ui_contract.rs` | 单测 353 / 41 · 目录组装与预载（待工作台套件重现） |
| 2026-09-18（1b：**项目只读对执行的拦截**——B12 遗留项补回） | **旧 SQL 面板那道闸回来了，而且比原来准**：① 编辑器新增**项目态端口** `editor/src/project.rs`（`ProjectState { read_only }` + `ProjectPort`；**未注入 = 不拦**——与 1b 之前的行为一致，不假装锁着），宿主实现 `workbench/src/services/editor_project.rs` 只做一件事：把项目锁（`project_ui.read_only`，与资产库 / 草稿箱同一判据）**按内存快照递过来**（渲染路径每帧读，不留缓存值——写进 `Document` 会留下“解锁了但文档还写着只读”的陈旧值）。② **闸门落在提交之前**（`view/host.rs::project_write_check`，与通道闸同一处）：项目只读 + 写源库对象 → 拒，原因“项目为只读模式：写语句被拒（要改数据先解锁项目）”，**不打扰执行器**（单测断言执行器一行都没收到）；`SELECT` 照跑——项目锁不是“不能分析”，只是“不能改数据”。③ **判据只写一处**：从 `channel::statement_allowed` 里抽出 `pub fn writes_source_object(sql) -> bool`（引擎语句类型判据；认不出就放行），通道闸与项目闸共用。④ 强度归位：项目锁属架构 §13 #9 的“**强只读**”那一档 → 直接拒；“提醒后放行”那档随连接策略（B1 余项）一起做，**不谎称已有**。⑤ 状态栏那一维跟着真值：`read_only_flags()` = 文档那一维 + 项目锁（合成在渲染时，不写回文档）。**验证**：editor 单测 **343 → 346**（`writes_source_object` 九类写 / 六类读；面板两项：项目锁下写被拒且执行器一行未收、`SELECT` 照跑、`read_only_flags` 只落“连接只读”一维；**未接端口不拦**）· workbench 单测 **95 → 96**（端口读透项目锁）· `ui_contract` 7 项（无新视图文件）。 | `editor/src/{project.rs（新）,shared.rs,channel.rs,view/host.rs,view/tests.rs,lib.rs}` + `workbench/src/services/editor_project.rs（新）` + `workbench/src/{view.rs,services/mod.rs}` | 单测 346 / 96（✅）· `ui_contract` 7（✅）· 真机无需重跑（纯编辑器侧闸门） |
| 2026-09-18（1b：B13 切片三后半 —— **联邦档真的接通**） | **「执行位置：联邦」从“占位”变成真能跑跨源查询**（细节见 `docs/architecture/federation/federation-dev-plan.md` §0）：① 引擎侧 `federation/session.rs` 加**进程内会话缓存**（按“会话主人”（本文档的连接）缓存 + 源清单指纹命中；换主源不重建会话，本地临时对象不丢；源清单变了才重建）；② 工作台 `services/editor_exec.rs` 新增 `federated_plan`（源清单组装：已连接 + 开「本地加速」+ 驱动可挂；别名 `sanitize_alias`+`unique_alias`；主源 = 本文档连接）与三个执行方法（`run_on_federation` / `fetch_next_on_federation` / `run_rewritten_on_federation`——与另两档同一套分段 / 改写形状）；结果区小字说清“挂了哪些源 + 跨源写 `别名.schema.表`”，失败时点名源；③ 门控真值（`editor_channels.rs`）：联邦档 = **≥ 2 个**已连接且可挂的源，不可用时把“还差什么”写出来；④ 历史带**参与源**（`history_store.sources` + `history::sources_text`：`MYSQL·联邦 · 源 mysql_src, pg_warehouse`）；⑤ **凭据路径纠正**（真机实测）：DuckDB 1.5.5 的 mysql / postgres 扫描器**不认 Secret**，挂载改用含凭据的**运行时连接串**（`DriverConnectionConfig.url_override`），引擎侧新增 `accel::scrub_credentials` 把离开引擎的文本里的口令抹掉（探针 `federation_credentials_probe` 钉住）；顺手修 `SET secret_directory` 与 `{system}/secrets` 对齐、MySQL Secret 注册参数名（`USER` 而非 `USERNAME`）。**验证**：engine 单测 **419 → 425** · editor **333 → 334** · workbench **212** 项全绿（含新增源清单组装三例 + 门控两例）· 真机：`federation_probe`（MySQL 354 表 + SQLite 26 表跨源）· `duckdb_accel_probe` 3 项 · `federation_credentials_probe`（新）。⬜ 余：源清单浮层（`源清单 ▾`）·「用作联邦源」标记存储 · 扫描量可见。 | `engine/src/duckdb/federation/session.rs` + `engine/src/duckdb/{accel,manager}.rs` + `engine/src/migration/global_init.rs` + `connection/src/secret.rs` + `workbench/src/services/{editor_exec,editor_channels}.rs` + `engine/src/persistence/history_store.rs` + `editor/src/history.rs` + `engine/tests/federation_credentials_probe.rs`（新） | 单测 425 / 334 / 212（✅）· 真机跨源 + 凭据脱敏（✅） |
| 2026-09-18（1b：B10 切片二 / 三 —— 方言转译 + 执行计划，**B10 收圆**） | **两件事共用一条“脚本级改写”骨架**：① **引擎新增 `sql/script.rs`**——切分（P0.4）→ 逐条改写 → **原位回填**，区间外的注释 / 空行 / 没写完的半句一个字节不动；格式化（切片一）也改成走它，两处不再各写一份。② **方言转译（切片二）**：`sql/transpiler.rs` 新增 `TranspileReport` + `transpile_with_report`——**先切分再逐条转译**，因为 sqlglot 的整篇 `transpile` 对脚本会**静默丢弃**第二条及以后的语句（实测 `"SELECT 1; SELECT 2;"` → `Ok("SELECT 1")`，架构 §12 #19），而整篇 `parse_statements` 又会在“有一句没写完”时全篇失败；单条用 `transpile_with_comments`——**实测发现**不带注释的实现会把语句开头的**前导注释吃掉**（`-- 头注释\nSELECT 1` → `SELECT 1`），静默吃注释比“不转译”严重得多。③ **编辑器新增 `translate.rs`**：`TARGETS`（PostgreSQL / MySQL / SQLite / DuckDB / SQL Server / Oracle / Snowflake / BigQuery / Redshift / 标准 SQL）· `targets_for(source)`（**排除源方言自己**——那是格式化的活）· `plan`（选区优先 / 光标映射，与格式化同构）；`format::dialect_of_known`（**认不出返回 `None`**——转译方向错不得，`dialect_of` 的“回 Ansi”只给格式化用）。④ **界面（原型 §2.2）**：工具栏新增**文档级**「⋯ 更多 ▾」（`debug_selector("editor-more")`，只在 SQL 模式），子菜单「转译为」列目标；源方言未知（未绑定连接 / 驱动认不出）时仍列目标，点下去给一句可读拒绝。⑤ **执行计划（切片三）**：引擎新增 `sql/explain.rs`——`explain_sql(dialect, sql)` 按方言生成前缀（MySQL / PG / DuckDB = `EXPLAIN`，**SQLite = `EXPLAIN QUERY PLAN`**——裸 EXPLAIN 给的是 VM 指令；SQL Server（SHOWPLAN 会话开关）与 Oracle（`EXPLAIN PLAN FOR` + 查视图）**如实返回 `None`**，不硬拼一条跑不通的语句）。⑥ **执行计划按通道**：方言取**当前通道**的——源库档用源库的，**加速 / 联邦档用 DuckDB 的**（那儿才是真要跑的引擎）；结果落**新结果集**并贴「执行计划」标题（`ResultEntry.title` + 面板侧 `pending_labels` **按位**队列——回填是一条语句一个结论，“这份是计划”不能拿布尔量猜），保留用户正在看的那份。**验证**：engine 单测 **393 → 404**（骨架 4 项 + 转译 6 项：**脚本不丢语句** · 单条接口确实会截断（把已知缺陷钉住）· 反引号换双引号 · 解析不了的逐字保留 · 注释与空行存活 · 同方言稳定）→ **408**（执行计划 4 项：各方言前缀 · 多步动作如实回绝 · 尾分号裁掉 · 空输入）· **408 → 410**（后者：可包装性判据 + 元信息语句不包装——**真机抓到的缺陷**）· editor 单测 **316 → 328**（`translate` 六项：目标表无重复且四个内置驱动都能找到目标 · 选区只翻选区 · 整篇光标留在原语句 · 脚本不丢语句 · 解析不了计数 · 源方言不在这里猜；面板五项：`⋯ 更多` 只在 SQL 模式 · 绑定 MySQL → 翻成 PG 且文本与服务层同步 · 未绑定拒绝 · 只读拒绝 · 文本模式拒绝）→ **333**（执行计划五项：带前缀且落新结果集贴标题 · SQLite 用 QUERY PLAN · 未绑定拒绝 · 文本模式拒绝 · 自定义标题覆盖“结果 N”）· `ui_contract` 7 项（无新视图文件）· `cargo check -p rds-app --all-targets` 零告警 · 真机（随 `editor_exec_real`）：**六个内置驱动全绿**（MySQL / PG 的服务器在本轮中途恢复）——执行计划按方言各就各位（MySQL / PG / DuckDB `EXPLAIN`、SQLite `EXPLAIN QUERY PLAN`）且都回**有列的网格**；**真机当场抓到一个真缺陷并修掉**：`EXPLAIN` 被分段抓取的 `window_sql` 包进子查询（`SELECT * FROM (\nEXPLAIN …\n) AS rds_segment LIMIT 1000 OFFSET 0`）在 MySQL 上报 1064 → 新增 `driver::utils::wrappable_in_subquery`（**元信息 / 会话控制语句不包装**），由 `execute_first_segment` 已有的“原路执行”分支接住（这也是“第一段也走窗口”那条设计里漏掉的一类语句）。 | `engine/src/sql/{script,explain}.rs`（新）+ `{formatter,transpiler,engine,mod}.rs` + `engine/src/driver/utils.rs` + `engine/src/services/sql_service.rs` + `editor/src/{translate.rs（新）,store.rs,format.rs,view/{host,tests}.rs,view/results/sets.rs}` + `workbench/tests/editor_exec_real.rs` | 单测 333 / 410（✅）· 脚本一条不丢（✅ 两面回归）· 真机六驱动执行计划 + 元信息不包装（✅） |
| 2026-09-18（1b：B10 切片一 —— 格式化落地） | **「⇤ 格式化」从原型上的一个词变成真动作，且只改该改的地方**：① **引擎侧给出「改了什么」的事实**（`engine/src/sql/formatter.rs`）——新增 `format_with_report(sql, dialect) -> FormatReport{text, formatted, kept_verbatim}`：先用 `sql/split.rs` 拿到**每条语句的字节区间**，逐条格式化后**回填原位**——区间之外的注释 / 空行 / 还没写完的那半句**一个字节不动**；语句间空白规整为 `;\n\n`（只在“空白 + 分号”时规整，含注释就不动）。旧 `format(sql)` 保留（`sql_parser_service` 在用）。② **编辑器侧算“该改哪儿”**（`crates/editor/src/format.rs`，新）——`dialect_of(db_type)`（mysql / postgres / sqlite / duckdb / mssql / oracle → `SqlDialect`，认不出回 `Ansi`，**不猜**）· `plan(text, selection, dialect)`：**有选区只格式化选区**；整篇时光标按“第几条语句内的第几个字节”映射回去（`map_offset`），并给 `changes()`——没改动时区分“本来就是格式化好的”与“N 条解析不了，原样保留”两种回执（两种感受完全不同）。方言**只从连接来**：`ConnectionOption.db_type` 由宿主填（同一个连接两种引擎时不能猜）。③ **界面**：工具栏执行级之后加**文档级**「格式化」（`debug_selector("editor-format")`，**只在 SQL 模式出现**），`Ctrl+Shift+F` 真注册（`crates/app`）；写回走内核 `replace_all`（**保留撤销历史**，`Ctrl+Z` 能撤回），落完恢复选区 / 光标——格式化不该让用户丢掉位置。只读拒绝、文本模式拒绝（“文本模式不解析 SQL”）各有可读原因。**验证**：engine 单测 **+9**（多语句一条不丢 · 前导注释不丢 · 空白规整但含注释的原样 · 报告口径：能格式化的条数 + 原样保留的条数）· editor 单测 **304 → 316**（`plan` 六项：方言来自驱动 · 选区只改选区 · 整篇格式化且光标留在原语句 · 解析不了的语句原样保留并计数 · 已格式化就不动 · 越界钳制；面板五项：`Ctrl+Shift+F` 真按键 → 文本被排 + 服务层拿到新文本 + 状态栏报“已格式化 N 条语句” · 半句原样保留并如实报 · 选区外一个字节不动 · 只读拒绝 · 文本模式拒绝）· `ui_contract` 7 项 · `cargo check --workspace --all-targets` / `-p rds-app --all-targets` 零告警 · 真机：**SQLite / DuckDB 全绿**，MySQL / PG 因 `192.168.3.138` 连接超时（`os error 10060`；本机 192.168.3.29 在同网段）未跑——**环境阻塞，非代码**。⬜ 余：切片二**方言转译**（硬约束 §12 #19：`transpile` 对脚本**静默丢第二条** → 必须先切分再逐条转译，并先写“脚本不得丢语句”的回归；入口是「⋯ 更多 ▾ → 方言转译…」）· 切片三**执行计划**（权威来源是源库 EXPLAIN；加速档用 DuckDB 的 EXPLAIN）。 | `engine/src/sql/{formatter,engine,mod}.rs` + `editor/src/{format.rs（新）,commands.rs,connection.rs,view/{host,tests}.rs}` + `workbench/src/services/editor_connections.rs` + `app/src/main.rs` | 单测 316（✅）· 格式化真按键且可撤销（✅）· 选区外字节不动（✅）· 真机 sqlite/duckdb（✅；mysql/pg 环境不可达） |
| 2026-09-18（1b：B13 切片三前半 —— 重挂入口 + 历史通道字段） | **两件收尾**：① **「重新挂载源库（刷新表清单）」**——加速档的数据是实时的，但在 `ATTACH` 时定型的**表清单**是静态的（源库后来新建的表在旧会话里根本不存在，真机验过）；现在「执行位置 ▾」菜单在本地跑的那两档上多一个维护项（纯函数 `channel::refresh_item_label` 决定摆不摆——源库档没有“挂载”这回事，不摆）。它走**旁路线程 + 回执**（与事务动作同一套：`ExecQueue::request_source_refresh` / `SourceNote`），**不占执行位**（不点“执行中”灯）；成功失败都回一条可读的话（“已重新挂载源库（新表可见了）” / “重新挂载失败：…”）。② **历史的「执行通道」成为正规字段**：`HistoryRecord` / `SqlHistoryEntry` / `SqlHistoryRecord` 都带上 `channel`（手写 JSON 读写同步；**老记录没有这一项 → `None`，不编一个**），`SqlExecuteOptions.channel` 由执行器填（`source` / `accelerated`）——“数据是什么库的”（`db_type`）与“语句在哪儿跑的”（通道）从此是两个维度；历史面板的来源徐章变成 `MYSQL·加速`（源库档不缀，默认档缀上去只是噪音）。**验证**：engine 单测 **389 → 390**（通道落盘与读回 + 老格式兼容）· editor 单测 **301 → 304**（旁路线程回执与失败留痕 · 面板：源库档直说不该打扰执行器 / 加速档回执回到状态栏 / 菜单项的纯函数）· workbench 真机：**加速档端到端加上“新表 → 旧会话不可见 → 重新挂载 → 可见”**（真 MySQL，✅）· `ui_contract` 7 项 · `cargo check -p rds-workbench --all-targets` 零告警 · clippy 零新增。⬜ 余：联邦外部源注册入口 · 执行计划按通道（B10）· 加速会话与分析会话（1c）的临时表共享。 | `editor/{execution,shared,channel,history}.rs` + `view/{host,tests}.rs` + `services/editor_exec.rs` + `engine/persistence/history_store.rs` | 单测 304（✅）· 重挂真机可用（✅）· 历史带通道（✅） |
| 2026-09-18（1b：B13 切片二 —— 加速档真的接通） | **「执行位置：本地加速」不再是一个状态，而是真能在 DuckDB 上跑**：① 引擎新增 `duckdb/accel.rs`——按 `conn_id` 缓存**一条专用 DuckDB 连接**（内存库；`USE rds_src` 把默认 catalog 指到附加源），源库以 `ATTACH … (TYPE mysql/postgres/sqlite, READ_ONLY)` 挂载；一条源一条连接是因为 `USE` 与临时表都挂在连接上，而且同一文件在一条连接里不能挂两次（`Unique file handle conflict`）。② **表名解析不做 SQL 改写**：会话里 `USE` 之后不写限定名的 `SELECT * FROM orders` 直接可用（跨 schema 可显式写 `rds_src.<schema>.<table>`）——比 `qualify_columns` 改写更直接，也不引入“部分限定”的不确定性。③ **写保护落到引擎侧**：`READ_ONLY` 让 DuckDB 自己拒（`attached in read-only mode`），本地临时对象（`CREATE TEMP TABLE`）照常允许——这正是“源库只读 / 会话变量可用”的分界；编辑器那道“提交前就拒”的闸门保留（两道）。④ 执行侧：`QueryRunner` 的四个方法（`run` / `run_filtered` / `run_sorted_down` / `fetch_next`）都多一个 `channel` 参数，文档的“执行位置”**真的分流**（不分流就是装饰）；第一段仍走 `window_sql`（与源库档同一口径），下发/排序仍走同一套改写；加速档的执行也进同一条历史（`db_type = MySQL·加速`，正规字段随切片三）。⑤ 宿主门控改成真值：连接开关 → 驱动类型 → 扩展可用性；扩展**没试过不拦**（让用户能选，首次执行时在工作线程装一次，联网一次之后离线可用），**试过且失败就把原话记下**来在菜单行尾显示。⑥ **纠正切片一的一处假话**：实测数据是**实时**的（源库插一行同一 DuckDB 会话立刻可见），不是“快照”——状态栏/徽标/文案改成「源库只读」，原型 §5.7 与架构 #13 / #14 同步修正；表清单在 `ATTACH` 时定型（新表要 `refresh`）。⑦ 顺手修一个真缺陷：真机探针的 `collect_with_snapshots` 自己拼 `ResultEntry` 却漏了通道（面板侧没有这个问题）。**验证**：engine 单测 **+7**（类型映射 / 文件 URL 去 scheme / 只读 ATTACH 语句 / 会话复用与 `USE` 解析 / 写拒绝与临时对象 / 重新挂载 / 扩展状态只拦真失败）· editor 单测 **300 → 301**（文档通道真的传到执行器）· workbench 门控单测改为真值口径 · **真机探针新增 `duckdb_accel_probe`**（MySQL / PostgreSQL / SQLite 三种源：只读挂载 · 未限定名 · **实时性** · 新表需重挂 · 写拒绝 · 临时对象）· **`editor_exec_real` 新增加速档端到端**（编辑器 → 执行器 → DuckDB：未限定名查到源表且结果带加速通道 · 写源库被 DuckDB 拒 · 本地临时对象可用）· `ui_contract` 7 项 · `cargo check -p rds-workbench --all-targets` 零告警。⬜ 余（切片三）：联邦外部源注册入口 · 「重新 ATTACH」界面入口 · 历史与执行计划的正规通道字段 · 加速会话与分析会话（1c）的临时表共享。 | `engine/src/duckdb/accel.rs`（新）+ `{execution,view/tests}.rs` + `services/{editor_exec,editor_channels}.rs` + `tests/{duckdb_accel_probe,editor_exec_real}.rs` | 真机：MySQL / PG / SQLite 三种源加速档可用（✅）· 写拒绝双保险（✅）· 实时性（✅） |
| 2026-09-17（1b：B13 切片一 —— 执行通道成为一等状态） | **「执行位置」三档（源库 / 本地加速 / 联邦）从概念变成可持久化的文档属性**：① editor 新增 `channel.rs`——`ExecChannel` 三档**互斥** + 能力边界（`allows_source_writes` / `allows_transactions` / `is_snapshot`）+ 持久化短码（`code` / `from_code`，认不出回源库，不伪装记住了别的）+ `ChannelAvailability(Set)` + `ChannelsPort` 端口（宿主注入；**渲染路径可调**，所以实现必须是内存快照）+ 三个纯函数：`menu_items`（不可用项**保留形态 + 行尾给原因**、当前项打勾）、`status_text`（非源库档带“快照”）、`stale_notice`、`statement_allowed`（用引擎 `parse_and_route` 判 DML/DDL——加速 / 联邦**拒绝写源库**，允许本地临时对象）。② `Document.channel`（与 `connection` / `mode` 同类，**随会话持久化**）· `ResultEntry.channel` · `ExecOutcome.channel`（结论原样带回，不在回填时重新猜）。③ 顺手改名：工作线程那层的 `ExecChannel` → **`ExecQueue`**（它是“线程 + 结果队列”，与 B13 的“在哪儿跑”不是一回事；撞名会让两件事读起来像一件）。④ 界面：工具栏右侧新增 **「执行位置：源库 ▾」**（三档菜单；置灰项带原因；没接端口就直说“尚未接入通道能力”）· 状态栏连接段之后加**通道段**（`通道 源库` / `通道 本地加速（快照）`）· 结果集标签带**通道徽标**（`源库` / `加速` / `联邦`；非当前档缀 `·旧` 并转静色）· 切通道后结果区顶部一行“**通道已切换，旧结果来自 X**”（旧结果**保留**，不删用户的东西）。⑤ 提交前的两道闸：通道不可用不提交、加速 / 联邦遇写语句**不打扰执行器**（原因可读“本地副本只读，请切回源库”）；加速 / 联邦档不摆 TX 区（没有事务语义），切走时若源库上还有未提交事务会提醒一句。⑥ 携带：`session.rs` 的 `SavedSession.channel` + 引擎 `editor_contexts` 新增 `channel` 列（老库幂等补列，默认 `source`；认不出的码回源库）；恢复时若该档已失效（开关关了 / 引擎没起来）→ **回退源库并说清原因**（原型 §5.7）。⑦ 宿主 `services/editor_channels.rs`：门控读连接快照的 `use_duckdb_fed` + `DuckDBManager::is_initialized()`（纯读、不触发初始化），**按“谁能先改”的顺序只报第一条过不去的**（连接开关 → 版本能力 → 运行时就绪）；本切片执行侧还没接加速路由，所以两档**如实不可选**（宁可不给选，也不要选了却发到源库）。**验证**：editor 单测 **282 → 300**（能力边界 / 码往返 / 菜单门控与置灰原因 / 状态段与顶部提示文案；面板级：切通道 → 后续结果带新通道 · 旧结果标灰 + 顶部提示 · 加速档拒写且不打扰执行器 · 不可用通道切不动 · 会话存得进与失效回退）· engine 单测 **+2**（通道列往返 + 老库补列取默认）· workbench 单测 **+4**（门控三条 + 会话码往返）· `ui_contract` 7 项 · `cargo check -p rds-workbench --all-targets` 零告警 · **真机六个内置驱动探针全绿**（源库档行为未变）。⬜ 余：**切片二**（加速档真接 `ATTACH` + Secret；DuckDB 扩展走外部编译库与指定目录）· **切片三**（联邦外部源注册入口 · 历史标通道 · 执行计划按通道）。 | `crates/editor/src/channel.rs`（新）+ `{service,session,store,execution,shared}.rs` + `view/{host,results/{grid,sets},widgets/status_bar}.rs` + `services/editor_channels.rs`（新）+ `engine/{duckdb/manager,persistence/workbench_context_store}.rs` | 单测 300（✅）· 门控置灰与原因是真值（✅）· 加速 / 联邦真执行待切片二 |
| 2026-09-17（1b：B14 切片四 —— 排序下发） | **右键「按此列升序 / 降序（下发源库）」**：与筛选下发同构的一条路——① 引擎侧 `rewrite_with_order`：`SELECT * FROM (<原查询去掉顶层 LIMIT 与 ORDER BY>) AS rds_sorted ORDER BY <列> [DESC]`；**不 CAST**（让源库按它自己的列类型排，数字列自然按数值，比我们猜类型靠谱）；原 ORDER BY 要**替换**掉（用户点的那一列才是他要的顺序，不是叠一层）；LIMIT 同样去掉并回报原文（提示文案换成“排序下发要能排全部行”）。② 顺带把改写结果的类型名从 `FilterRewrite` 改成中性的 **`Rewrite`**（筛选与排序两个下发共用同一个形状：`sql` + `dropped_limit`）。③ 编辑器只发意图（`ExecTarget::SortedDown { sql, column, descending }` + `QueryRunner::run_sorted_down`，默认实现明说不支持；`column` 用的是**结果集的列名**，不是列下标）；菜单项在原来那一组里（按值筛选 · 复制此值 ── 升序下发 · 降序下发 · 冻结），钩子与「按值筛选」同款。**验证**：engine 单测 **+3**（原 ORDER BY 被替换且只剩一份 · 无 ORDER BY 时补一个 · 特殊列名要引号）· editor 单测 **281 → 282**（钩子接上 + 列名与方向原样到执行器 + 落新结果集；菜单项与分隔线逐项断言）· **真机六个内置驱动**：按 `tag` 降序下发后行序为 `gamma, beta, alpha`（源库自己排）· `ui_contract` 7 项 · `cargo check -p rds-workbench --all-targets` 零告警。⬜ 余：值预览弹层（点单元格放大看）· 值编辑（写回库要有行标识）· 血缘摘要（依赖 B13 通道信息）。 | `engine/src/sql/filter.rs` + `editor/{execution,view/results/grid,view/host}.rs` + `services/editor_exec.rs` | 真机：六驱动排序下发按列重查（✅） |
| 2026-09-17（1b：B14 切片三 —— 表格右键菜单） | **右键菜单：按值筛选 / 复制此值 / 冻结列**：① **目标单元格得自己记**——组件库只公开了 `right_clicked_row`，单元格那个（`right_clicked_cell`）是私有字段，所以在 `render_td` 给数据单元格挂一个右键处理（事件从最内层派发，表格自己的 `stop_propagation` 不影响已经收到的那一笔）：记进 delegate 的 `context_cell`；菜单构造时它为空就**什么都不摆**（不猜一个“大概是想筛这个”）。② **菜单项是纯函数**（`context_menu_items`：`按值筛选「值预览」` · `复制此值` · `冻结「列名」` / `取消冻结此列`，分隔线在冻结那项前），可逐项断言；值预览折叠空白并截断到 24 个字符（长值不该把菜单撑开）。③ 三个动作的落点各不相同：**按值筛选**走面板钩子（写进筛选框 + 立刻生效，并**掐掉在途的 300ms 防抖**——这次是定稿）；**复制此值**直接写剪贴板（`App::write_to_clipboard`）；**冻结列**是表格自己的事（原生 `Column.fixed`，改完 `state.refresh(cx)` 重建列组才看得到）——菜单项点击发生在**独立事件**里（不在表格的更新栈内），所以能安全地更新表格实体（在 `context_menu` 里当场更新会撞“更新一个正在更新的实体”）。④ 冻结跟随数据：换一份结果时清空（列都换了一批，旧列号没有意义）。**验证**：editor 单测 **276 → 281**（菜单三项与分隔线 · 值预览折叠与截断 · 冻结切换与换数据清空；面板级：**钩子真的接上了**且按值筛选生效（网格收敛 + 工具栏明示）· 冻结落成 `Column.fixed = Some(Left)`（原生能力，不手搓））· `ui_contract` 7 项 · `cargo check -p rds-workbench --all-targets` 零告警。⬜ 余：右键「排序下发 SQL」（排序的下发那档，与筛选下发同构）· 值预览弹层（点单元格放大看）· 值编辑（写回库要有行标识，属后期）。 | `view/results/grid.rs` + `view/host.rs` | 单测 281（✅） |
| 2026-09-17（1b：B14 切片二 —— 下发源库） | **筛选能下到源库了：把词翻成 WHERE 重查，结果落新结果集**：① **改写放引擎**（`sql/filter.rs`）——生成 `SELECT * FROM (<原查询去掉顶层 LIMIT>) AS rds_filtered WHERE (CAST(列 AS TEXT) LIKE '%词%' ESCAPE '!') OR … ORDER BY <原查询的顶层 ORDER BY>`：包一层是因为筛选命中的是**结果集的输出列**；**去掉 LIMIT**（留着就只能在被截断的几行里找，那叫不下发），并把去掉的原文回给界面（场景 34 的“将去掉 LIMIT”提示）；**ORDER BY 提到外层**（子查询里的 ORDER BY 在外层不保证顺序）；`ESCAPE '!'` 让 `%` `_` 能被当普通字符搜（转义符不用反斜杠是为了避开 MySQL 字符串里反斜杠的语义）。顶层子句定位走 **sqlglot 的 tokenizer**：字符串 / 注释 / 子查询里的 `limit` 与 `order by` 都不算子句（都有测试盯着）。② **方言差异在宿主侧**：编辑器只发意图（`ExecTarget::Filtered { sql, filter, columns }` + `QueryRunner::run_filtered`，默认实现实话实说“不支持下发筛选”），workbench 读连接的 `db_type` 决定 CAST 目标（**MySQL 用 `CHAR`**——它没有 `TEXT` 这个 CAST 目标；其余库 `TEXT`）。③ 界面：结果工具栏加 `▢ 下发源库` 开关（组件库 `Switch`）；开着时应用筛选（含 300ms 防抖到点）就重查一次，**结果落新结果集、原结果保留**；执行器带回来的说明（“已去掉原查询的 LIMIT 10…”）进状态栏。④ 顺手补的一个体验缺口：**落在新结果集上的失败不抢选中**（B2 语义），所以状态栏原来什么都不说——现在补一句“新结果集执行失败：原因”（标签上的红点照旧）。**验证**：engine 单测 **+8**（包一层 WHERE · ORDER BY 提到外层且子查询里不再留一份 · LIMIT 去掉并报原文 · 字符串与注释里的 limit / order by 不算子句 · 子查询里的 LIMIT 要留着 · `%` `_` `!` 转义与特殊列名引号 · 单引号双写 · 空输入回绝）· editor 单测 **273 → 276**（开关打开 + 已有筛选词 → 立刻下发一次且原料正确（原 SQL / 词 / 列名）· 结果集 1 → 2 且原结果保留 · 提示进状态栏；开关关着时 **不碰源库**；执行器不支持时留原因）· **真机六个内置驱动**：下发后只回命中行（`mm` 只命中 gamma），**带 LIMIT 的原查询下发后仍能筛到第 3 行**（LIMIT 真被去掉）· `ui_contract` 7 项 · `cargo check -p rds-workbench --all-targets` 零告警。⬜ 余：右键「排序下发 SQL」与「按值筛选」· 冻结列的列级开关 · 下发的 LIMIT 提示目前只在状态栏（结果集自身的“来源说明”随通道/血缘那一步）· 下发结果不分段（超过驱动上限会标“已截断”）。 | `engine/src/sql/filter.rs`（新） + `editor/{execution,view/host,view/tests}.rs` + `services/editor_exec.rs` | 真机：六驱动下发命中正确 + LIMIT 被去掉（✅）· 本地/下发两档语义（✅） |
| 2026-09-17（1b：B14 切片一 —— 本地筛选与本地排序） | **结果集能筛能排了，两样都只动视图层（不重查）**：① **一个映射管两件事**——delegate 里新增 `view_rows`（视图行序，元素是数据行下标）：筛选与排序都只改它，**数据行一行不动**（清除筛选能原样恢复，也不会把“已抓到的窗口”搞乱）；行号列显示的是**视图行号**。② **本地筛选**（原型 §5.5）——结果工具栏加 `⌕ 筛选` 框：任一单元格子串命中（大小写不敏感），**300ms 防抖**（版本号 + 定时任务，连打只让最后一次到点；测试里用 `advance_clock` 推虚拟时钟越过窗口），有词时给一个 ✕ 清除；工具栏明示 `已筛选 N / M 行`（统计跟随筛选，`ResultToolbar.filtered`）。③ **本地排序**——数据列给 `Column.sort(ColumnSort::Default)`（组件库对 `sort: None` 的列**不响应点击**，它自己维护 `Default → Descending → Ascending` 的循环并把新值回调给 `perform_sort`），delegate 只负责记住并重算视图行序；`column()` 会把当前态回给表头，所以 refresh 后排序指示器不丢。比较规则：**两边都像数字就按数值比**（`1, 2, 10` 不该排成 `1, 10, 2`——展示文本是字符串，但用户看的是数），否则字典序，缺列排最后，同值按数据原序（稳定）。④ **导出跟随筛选**（原型 §5.5 的“导出的是当前筛选后的行集，并明示”）——`export::encode_rows` 收行集参数，`encode` 只是它传 `entry.rows` 的特例；面板在筛选中就导 `visible_rows()`，回执写 `已导出 N 行（已筛选）`。⑤ 换结果集时**排序不继承**（列都可能不是同一批）、**筛选词留着**（用户还在找同样的东西）。**验证**：editor 单测 **265 → 273**（筛选只改视图 · 视图行号跟随 · 清筛恢复原顺序 · 大小写不敏感 · 数值与字典序与缺列与取消排序 · 筛选叠排序 ·换数据清排序留筛选 · 列头可排（数据列 `Some(Default)`、行号槽 `None`）；面板级：筛选后面板行数收敛且工具栏出 `已筛选 1 / 3 行` 与 ✕ · **防抖窗口内不生效、`advance_clock` 越过后生效** · **导出跟随筛选（文件里只剩命中行 + 回执带“已筛选”）**）· `ui_contract` 7 项（新常量 `RESULT_FILTER_WIDTH` 进 editor 的 `ui.rs`）· `cargo check -p rds-workbench --all-targets` 零告警。⬜ 余：**下发源库**（原型 §5.5 的后半：拼 `WHERE` 重查 → 新结果集，随 B14 后半）· 右键「按值筛选」与「排序下发 SQL」· 冻结列（原生 `Column.fixed` 已用在 `#` 槽上，列级开关随右键菜单）· 值预览与值编辑。 | `view/results/grid.rs` + `view/host.rs` + `export.rs` + `ui.rs` | 单测 273（✅）· 导出跟随筛选且明示（✅ 窗口测试） |
| 2026-09-17（1b：B5b 余项 —— 滚动到底自动加载） | **「取下一段」接上组件库的原生加载更多，滚到到底自己取**：① `ResultGridDelegate` 实现 `has_more` / `load_more`（0.6.1 的 `load_more_if_need` 在可见范围距末尾不足 `load_more_threshold`（默认 20 行）时反复调 `load_more`）——“还有没有下一段”仍以**结果的真值**为准（`ResultStore` 的 `has_more`，面板在结果视图同步时喂给 delegate），表格不猜。② **delegate 拿不到面板**：走钩子（`LoadMoreHook`，面板构造时注入）把意图交回给知道全局的那一层，与执行通道同一口径；钩子里必须 `cx.spawn_in` **挪出当前更新栈**（面板会回头把 `loading_more` 写回 delegate，同步调用就是“更新一个正在更新的实体”→ 测试与真机都会 panic）。③ **防重入两处**：delegate 在 spawn 前**乐观置位** `loading_more`（组件库会在“接近末尾”的每一帧都调进来，不拦就会连发），面板在提交成功时置真值 `fetching_more`（同步给 delegate）、**提交失败则撤回**（否则滚动到底会一直不再触发）。④ 原生 `loading()` **未启用**（它会额外画一个 loading 视图；状态栏已有「执行中 3.4s…」），「取下一段」按钮**保留**作显式入口。**验证**：editor 单测 **262 → 265**（手动调 `load_more` → 面板真去取段、同一帧连调只发一次 · 到底后 `wants_more()` 为假 · **真渲染几帧让组件库自己触发**（`asked == [(2, 1000)]` 且只一次））· `ui_contract` 7 项 · 未动引擎与执行路径（取段仍是 B5b 那条 `Segment` + `Append`）。⬜ 余：真机上的手感（滚动到底的触发时机）随 B15 的筛选一起复测。 | `view/results/grid.rs` + `view/host.rs` | 单测 265（✅）· 渲染路径自动触发（✅ 窗口测试） |
| 2026-09-17（1b：B8 —— 历史面板实装） | **右 Dock 的 `History` 从占位变成真面板，数据换到引擎的真实历史**：① **数据源换对了**——旧占位读的是 `query_history.json`（只有 20 条纯文本、无耗时无成败）；现在读引擎的 `history_store`（每条都有**真实耗时 / 成功标志 / 失败原因 / 返回与影响行数 / 绑定连接**，写侧从 B5-1 起就是真值）。旧服务标为“已被取代”（保留 API 与它的测试，不在本次删文件）。② **视图与状态在 editor crate**（与 insight / mock 同一例：右面板只转发渲染）：`history.rs` 是数据与投影（纯函数），`view/history.rs` 是面板（搜索框 + 列表 + 动作）。③ **刷新机制**：执行发生在别的线程，所以执行路径写完历史后 `history::bump()` 摇一下版本号（`AtomicU64`，O(1)），面板 1 s 一拍比版本，**只在版本变了时才读盘**——既不每帧问文件（渲染路径不 I/O），也不会“执行完看不到新记录”。④ **交互**：搜索（引擎侧做包含匹配，空关键词 = 全部）、重放（**在中央编辑器打开，不替用户执行**——历史里什么都有，包括写语句；走宿主 `request_query` + `notify_host` 唤醒 render 消费）、删除单条、清空；空态分三句话（真没历史 / 没匹配关键词 / 读取失败——不把失败说成“暂无数据”）。⑤ **可测**：数据走端口 `HistoryPort{load,remove,clear}`（默认接引擎，测试注入假端口，**不碰真实历史文件**）；元信息一行是纯函数（`时间 · 耗时 · 行数 · 来源 · 失败原因`）。⑥ **真机抓到的引擎 bug（顺手修）**：历史存储是手写 JSON（`escape_json` + 手写提取），提取字符串时把“字符数 +1”当**字节下标**用——PG 的中文错误消息（`字段 "x" 不存在`）读盘时切在汉字中间直接 panic；`sql_preview` 是同一个毛病（按字节截断中文 SQL）。两处都改成按字符/字节边界走，并补上**反转义**（读回来的字符串原来还留着 `\"`——历史面板上的 SQL 与原因会多一层反斜杠）。**验证**：editor 单测 **253 → 262**（6 项投影纯函数：预览折叠与截断 · 相对时间与日期切换（含时钟回拨）· 耗时与行数文案 · 来源 · 记录→条目 · 失败带原因；3 项面板：**列表 → 搜索 → 重放 → 删除 → 清空全链路都真的到了端口** · 未注入重放端口报“未接入”）· engine 单测 **328 → 339**（中文 SQL 与中文失败原因往返、预览按字符数）· **真机六个内置驱动**：执行 3 次（含一次写错列名）→ 历史里三条都在、成功那条带返回行数与“刚刚”、失败那条带原因 · `ui_contract` 7 项（新视图 `view/history.rs` 已登记进尺寸/颜色两份清单）· `cargo check -p rds-workbench --all-targets` 零告警。⬜ 余：多条选择与批量删除 · 引擎侧已有的筛选（库类型 / 仅成功 / 时间范围）未接到界面 · 条目的右键菜单（复制 / 在新结果标签执行）· 旧 `query_history.rs` 与其测试的退役。 | `editor/src/history.rs` + `view/history.rs`（新） · `panels/right.rs` + `view.rs` + `services/editor_exec.rs` · `services/mod.rs` | 单测 262（✅）· 真机：执行 3 次（含 1 次失败）→ 历史 3 条且字段真实（✅ 随探针）· `ui_contract` 7 项（✅） |
| 2026-09-17（1b：结果集域目录 + 表格能力核对） | **结果集从 `view/widgets/` 摊平改成独立一块（`view/results/`），并把“哪些能用组件库、哪些要自己做”核到 0.6.1 源码**：① **为何分目录**——结果集还会长（导出 / 值预览 / 值编辑 / 洞察入口 / 过滤 / 排序 / 冻结列…），各自有状态与测试，摊在 `widgets/` 里会变成第二个 `host.rs`；而它**不该另建 crate**（状态就是编辑器文档状态的一部分：`ResultStore` 按 `DocumentId` 存，拆出去只会把状态跨 crate 传两遍；架构判定：无独立状态 → 不建 crate）。② **位移**——`widgets/` 下的 `result_grid` / `result_sets` / `error_card` → `results/{grid,sets,error_card}.rs`（加 `results/mod.rs` 写清结构与台账）；`widgets/` 只留编辑器的状态栏。引用面很小（host.rs 三处 use + ui_contract 两份清单 + verify_tree 清单）。③ **能力台账（0.6.1 源码核对，结论写进 `results/mod.rs`，避免后人再手搓）**——原生就能用：排序（`Column.sort` + `TableState::sortable` + delegate `perform_sort`）、**冻结列**（`Column.fixed`）、列宽与列序（`Column.resizable` / `movable` + `col_resizable` / `col_movable`）、行/列/单元格选择、**滚动到底自动加载**（delegate `has_more` / `load_more` / `load_more_threshold`，`load_more_if_need` 自动触发）、单元格/行/表头自定义渲染（`render_td` / `render_tr` / `render_th` / `render_group_th`）、右键菜单（`context_menu`）、分组表头、取值导出（`TableState::dump` / `dump_range` / `cell_text`）、空态与加载态与滚动定位。**没有原生、要业务侧实现**的只有三件：筛选（无 filter API，在数据层或 `rows_count` / `render_td` 里过滤）、值预览与值编辑（弹层与写回库都是业务语义）、洞察入口（业务动作）。④ 两个订正：`row_header(true)` 只是**空白选择列**（`w_3`、无行号文本）——我们的 `#` 行号列保留；B5b 手搓的「取下一段」按钮**应换原生 `has_more` / `load_more`**（滚动到底自动加载，按钮留作兜底），列为下一步。**验证**：editor 单测 253 全过（纯位移，无行为改动）· `ui_contract` 两份清单与 `verify_tree.py` 已同步新路径。 | `editor/src/view/results/{mod,grid,sets,error_card}.rs` · `view/widgets/{mod,status_bar}.rs` · `view/{mod,host}.rs` | 单测 253（✅ 位移后全过）· 取下一段接原生 `load_more`（⬜ 下一步） |
| 2026-09-17（1b：B7 —— 导出·切片一） | **结果能落盘了：CSV / JSON / INSERT（三种格式 × 两档范围）**：① **纯函数在 `export.rs`**——编码、默认表名、默认文件名、**菜单项**都是可逐条断言的形状，界面只负责画。② **三条口径**：· 值按**展示文本**导（`ResultEntry` 里就是网格那些字符串）：CSV 里 `NULL` 是字面、JSON 里给 `null`（与网格斜体同一个判据）、INSERT 里 `NULL` 不加引号；· 只导**已抓到的行**（另一档“抓全量后导出”走多轮取段）；· 没网格（写语句 / 失败）不给导（回绝可读）。③ **三种编码**：CSV 按 RFC 4180（逗号 / 引号 / 换行才包裹，内部引号双写，行分隔 `\n`）；JSON 是对象数组（两格缩进，控制字符转义），值保持字符串——展示文本是 `1` 就写 `"1"`（要类型化得先有列类型，属余项）；INSERT 多行 `VALUES`，**每 200 行一条语句**（SQLite 对多行 VALUES 有编译期上限），字面量启发式：`NULL` / 数字 / `true|false` 不加引号、其余单引号并把内部单引号双写（字符串列里存着 `123` 会被写成数字——展示文本导出的固有取舍，与 DBeaver 同类）。④ **菜单两组**（`⤓ 导出 ▾`，原型 §2.4 的右段）：分组标题 `已抓取 1,000 行` + 三格式（仅已抓取）；还能取段时再多一组 `抓全量后导出（会重跑查询）` + 三格式（**抓完了就不摆**，那种入口点下去只是多余的重跑）。⑤ **抓全量**是**多轮取段**（`pending_export` 记下格式 / 路径 / 目标结果集）：每段回填推进一步，最后一段（没拿满）才编码落盘；**取段失败 / 用户切了结果集 / 提交被通道拒了一律作废**——不落半截文件（已抓到的行一行不动）；提交失败要把在途状态撒回去（否则会残留到下一次取段）。⑥ **端口注入**：`EditorShared::attach_export_path_picker`（与另存为同款；未注入时明确报“未接入”），实现落在宿主 `services/editor_files.rs`（`rfd` + 按格式过滤），用户取消 = 什么都不做。⑦ **默认名**从 SQL 里猜首个 `FROM` 表（`public.orders` → `orders`，带引号的标识符去引号；`FROM (SELECT …)` 这种子查询**放弃猜**，回落到 `result_1`）——猜错表名比中性的默认名更糟（用户会照着一个不存在的表去插）。**验证**：editor 单测 **235 → 253**（CSV 转义 / JSON 的 NULL 与转义 / INSERT 字面量与 200 行分批 / 失败与写语句编码为空串 / 表名猜测（含 schema 前缀、引号、反引号、子查询放弃）/ 默认文件名 / 菜单项随 `has_more` 长出一组；面板级：仅已抓取真写文件且回执带行数与路径 · 取消什么都不做 · 未注入端口报“未接入” · **抓全量先取完剩下两段再落盘（4 行都在）且不新开结果集** · 取段失败不落半截文件 · 没有网格被回绝）· 副本 `cargo check -p rds-workbench --all-targets` 通过（工作区被另一种会话的 `analytics_resource` 在途改动挡着）。⬜ 余：**Parquet / XLSX** 属切片二（要经 DuckDB `COPY … TO … (FORMAT …)` 与对应扩展，依赖 duckdb 扩展安装路径的结论）· 导出**当前筛选后的行集**随 B15 · 列类型化输出（JSON 数字 / 布尔）需先有列类型。 | `editor/src/export.rs`（新）+ `view/host.rs` + `widgets/result_grid.rs` · `services/editor_files.rs` | 窗口：导出真写文件且内容可断言（✅）· 抓全量后导出（✅ 假执行器）· 取消 / 未接入 / 无网格三条回绝路径（✅）· Parquet / XLSX（⬜ 切片二） |
| 2026-09-17（1b：B5b —— 分段抓取·编辑器侧） | **分段抓取从引擎一路接到界面（B5b 收尾）**：① **首段**——编辑器执行查询走 `execute_first_segment`，头一段固定 `SEGMENT_ROWS = 1000`；“拿没拿满”就是“还有没有下一段”（拿满 = 可能有），所以 ⑦ 状态行报 `共 1,000+ 行`，旁边就是**取下一段**按钮；写语句没有分段可言（走原路、历史照记）。② **取下一段**——`ExecTarget::Segment{sql, offset, limit}` + 新的 `ResultPlacement::Append`：offset 从**已抓行数**算（不重不漏靠这个），追加进**选中的那份结果**而不是新开一份（`ResultStore::append_rows`：列形状不一致就拒绍，耗时累加，`has_more` 随最后一段更新）；失败那次**不入库**、已抓到的行一行不动（否则用户好不容易抓的两万行会被一次失败清掉），只留一句提示——且提示要在结果视图同步**之后**说，不然会被冲掉。③ **通道**长 `QueryRunner::fetch_next`（默认实现实话实说“当前执行器不支持分段抓取”）。④ **真机抳到两个真问题（都是包装泄漏）**：· **位置**——PG 的结构化 position 是相对**发给服务器的包装文本**的，编辑器按原句解释→偏 16 字节（光标落到 `yz FROM rds_affect` 上）；SQLite 同源，它把位置留在**消息文本**里（`no such column: x in <包装SQL> at offset 23`）；· **消息**——错误里回显的 `(SQL: …)` 就是包装文本，界面上冒出用户从没写过的 `rds_segment LIMIT 1000 OFFSET 0`。**修法**：引擎侧 `unwrap_segment_error` 把结论还原成用户原句（位置平移，落在包装上就**不给位置**——给错位置比不给更糟；回显 SQL 换回原句），SQLite 驱动补上结构化位置（`sqlite_error`：从 `rusqlite::Error::SqlInputError` 取 `msg` + `offset`——0 基字节、相对**本次发出的 SQL**，不再把 Display 原文（含 SQL 片段）整段搬进 reason），与 PG 两条路径同一口径。**验证**：editor 单测 **228 → 235**（段目标语义 · 通道走 `fetch_next` 且落位 Append · store 追加/列形状不一致拒绍/耗时累加 · 面板“首段 2 行 + 取下一段接成 4 行”不重不漏且不新开结果集 · 执行器不支持分段时留可读原因且不动已抓到的行）· engine 单测 328（新增错误还原：**位置平移正落在写错的词上** · 落在包装上给 None · 原句带前导空白也要对得上 · 其它错误域不被改写 · 回显 SQL 换原句）· `ui_contract` 7 项 · **真机六个内置驱动**：首段 1000 行→取下一段接成 1200 行（接缝 `1/1000/1001/1200` 逐个断言）· 错误定位六驱动全是“第 1 行 第 8 列（no_such_column_xyz）” · 中断与超时消息里不再出现包装。⬜ 余：每段重跑查询的取舍（`ORDER BY` 不稳会在两段间重复/跳过行）要写进界面——随 B15 提示文案；滚动到底自动加载可换组件库 `TableDelegate::has_more` / `load_more`（现在是显式按钮，能用但多一步）；导出两种语义（仅已抓取 / 抓全量）属 B7。 | `services/sql_service.rs` + `driver/native/sqlite.rs` · `editor/src/{execution,store,shared}.rs` + `view/host.rs` + `widgets/result_grid.rs` · `services/editor_exec.rs` | 真机：六驱动首段 1000 → 取下一段 1200 不重不漏（✅）· 定位不受包装干扰（✅） |
| 2026-09-17（1b：B5b —— 分段抓取·引擎侧） | **分段抓取的取数方式落地（编辑器接线下一步）**：① **怎么取**——`window_sql(sql, limit, offset)` 把原 SQL 套成 `SELECT * FROM ( … ) AS rds_segment LIMIT n OFFSET m`：不返回行的语句（DML / DDL / 事务控制）直接拒绍（`None`）；尾分号与空白先去掉；别名是 MySQL 这类要别名的方言要求的。为什么套子查询而不是让驱动持游标：四个内置驱动的取数路径都是“一次拿全”，持游标要改四份实现，而且 sqlite / duckdb 的语句借连接的生命周期（自引用）。**代价写进文档与后续界面：每段会重跑一次查询，`ORDER BY` 不稳的查询可能在两段之间重复/跳过行**。② `SqlService::execute_segment(conn_id, sql, limit, offset, timeout_ms)` —— 每段走普通执行路径（超时 / 取消 / 事务都一样），但不记历史、不用缓存（同一句不该在历史里写十几遍）；“还有没有下一段”由调用方从“这一步拿没拿满”判断（引擎不去问 `COUNT(*)`——对重查询是另一笔开销）。**验证**：engine 单测 +2（窗口包装与尾分号/CTE、无结果集语句拒绍）· **真机六个内置驱动**：5 行表按 2 行一段取三段，拼起来 `1..5` 不重不漏、最后一段只 1 行、DML 被拒、CTE 能分段（`editor_exec_real` 新增断言）。⬜ 下一步（编辑器侧）：首段 1000 行 + 状态行 `N+` + 「取下一段」（组件库 `TableDelegate::has_more` / `load_more`），以及“分页/取下一段”入 ⑦ 那一行。 | `services/sql_service.rs`（`window_sql` + `execute_segment`） | 真机：六驱动三段不重不漏（✅）· 下一步：>10,000 行表 → 首段 1000 行 + `1000+` |
| 2026-09-17（1b：结果区对齐原型 §2.4） | **把结果区按原型的 ⑤⑥⑦ 重排**：① **竖向顺序**变成 ⑤ 结果集标签条 → ⑥ 工具栏 → 网格 → ⑦ 状态行（之前工具栏压在标签条上面，与原型相反）；测试按 debug 选择器断言四段的 y 坐标顺序。② **⑥ 工具栏分段**（原型 `行数 1,204 │ 1.2s │ orders`）：不再把行数/列数/耗时糊成一句，改成 `行数 N` │ `影响 N 行`（写语句）│ `失败` │ `耗时 1.2s` │ `连接名` 几段可选真值（`ResultToolbar::segments()` 是纯函数）；数字带千分位；耗时不到一秒直接报毫秒（`0.0s` 什么都没说）。③ **⑦ 状态行**：`共 N 行` + `已选第 N 行`（订阅 `TableEvent` 重画，点行不改结果集）+ 截断提示（按原型口径改成 `已截断至 N 行`，警告色）。④ **错误卡片**（B6 后半，原型「错误呈现」）——失败不再是一句灰字：改成组件库 `Alert::error` 的卡片 + `定位到第 N 行`（认得出位置才有）+ `复制`（驱动原文连位置一起复制走）；失败时**不画空网格**。⑤ **网格**：首列加固定的 `#` 行号槽（窄、钉左、不可拖/不可移、不进 TSV——复制走 `ResultEntry::to_tsv`），`NULL` 按原型改成 `muted_foreground` **斜体**。**验证**：editor 单测 **216 → 228**（分段/状态行/千分位/耗时/行号槽/空网格不画槽 逐条纯函数断言 · 竖向顺序 · 已选行跟随 · 定位按钮真把光标送到出错词 · 卡片复制进剪贴板）· `ui_contract` 7 项（新 widget `error_card.rs` 已登记进尺寸/颜色两份清单）· 未动引擎与执行路径，真机无需重跑。⬜ 余：⑥ 的**筛选 / 下发开关 / 分析 ▾ / 导出 ▾** 各随 B15 / B14 / B7；⑦ 的**分页 / 取下一段**随 B5b（组件库 `TableDelegate::has_more` + `load_more` 就是干这个的，比手搓分页器合适）；⑧ 状态栏的**方言 / 编码 / 换行 / 缩进**缺真实数据源（连接端口不带驱动名、文档没有属性表）——按原型“真实值，不硬编码”先不摆。 | `view/widgets/{result_grid,result_sets,error_card}.rs` + `view/host.rs` | 窗口：竖向顺序 · 分段文案 · 状态行已选 · 错误卡片与两个按钮 |
| 2026-09-17（1b：B6 —— 错误回填） | **报错从一句文本变成“能指着看”的位置**：① **位置先补到引擎**——PG 两个驱动原来把位置丢在结构化字段里：sqlx 版只剩消息（还把 PG 的**源码行号**渲染成 “at line 1501”），tokio-postgres 版更糟，`Display` 只有一句 `db error`（用户什么都看不到）。现在两条路径都走 `query_error`：**把服务器说的话原样带出来**（消息 + `DETAIL` + `HINT`），位置从 `position`（1 基**字符**，PG 协议定义）换算成**字节偏移**（新 `driver::utils::byte_offset_for_char`，越界返回 `None` 而不是钳到末尾）；`DatabaseError::Query.position` 的约定（**0 基字节偏移**，相对 `sql`）写进 `shared/error.rs`。② **编辑器侧解析**（`crates/editor/src/diagnostics.rs`，纯函数）——`site_in_document(整篇文档, 这次发的 SQL, 错误文本)` → 出错词的字节区间 + 1 基行列 + 词名。解析顺序按可信度：引擎的 `at position N` → `LINE n:` + 插入符（DuckDB；插入符对齐的是**整行错误文本**、含 `LINE 1: ` 前缀，行尾还可能跟着引擎拼的 ` (SQL: …)`）→ `at offset N`（SQLite 的 `SqlInputError`）→ 消息里引号包着的词 / `no such table: x`（MySQL 带库名前缀的先原样试、再试最后一段；PG 中文语言包的消息也走得通）。**认不出就不给位置**——定位错比不定位更糟（用户会照着找，还以为自己的 SQL 没问题）。③ **面板三件事**——诊断（内核 `diagnostics_mut()` 画在出错词上）、定位（选中出错词）、聚焦（构造时存下窗口句柄，回填发生在没有窗口的轮询里也能 `state.focus`）；状态栏提示追加 `（第 3 行 第 17 列）`；下一次成功清掉旧诊断；位置只在结论变化时重算（要拿整篇文档做匹配）。**验证**：editor 单测 **199 → 216**（14 项解析断言——10 类真实错误文本 + 越界/认不出/文档已改过三种退化 + `end()` 按字符算，3 项面板断言：能定位则画诊断并选中出错词 · 认不出则不动光标 · 成功清旧诊断；测试按“直接驱动落地入口”的口径绕开 headless 下不能在窗口更新里再更新窗口的限制，见架构 §12 #29）· engine 单测补 `byte_offset_for_char` 并修好 B4 留下的一处（无活动连接时 `get_transaction_status(None)` 该给“没有事务”这个完整答案，不是报错）· **真机六个内置驱动「写错列名」全部定位到那个词**（第 1 行 第 8 列 × 6，`editor_exec_real` 新增断言）· clippy 我的文件零新增告警。⬜ 余：错误卡片（B3 的「已中断」终态）属 B6 后半 · 结构化位置只做了 PG 两家（DuckDB / SQLite / MySQL 的真机文本里本来就带位置，没在驱动侧重复实现）。 | `driver/utils.rs` + PG 两驱动 · `editor/src/diagnostics.rs` + `view/host.rs` | 单测：≥ 8 种真实错误文本（✅ 10 类）· **真机：故意写错列名可定位（✅ 六驱动）** |
| 2026-09-17（1b：B5 —— 结果区完整化） | **结果区从“一份结果 + 一行摘要”变成可操作的完整面**：① **写语句的真实影响行数**（引擎侧 P0.6）——`mysql_native` / `sqlite` / `duckdb` / `postgres_native` 四个驱动的写语句改走各自的 `execute` 变体拿真实计数（与 sqlx 两版同一套判据）；判定收归 `driver::utils::returns_rows`（看首个关键字，`WITH … SELECT` 不再被误判成写语句；`RETURNING` 必须是独立单词，`'returning'` / `returning_log` 不算）；编辑器侧 `QueryData` / `ResultEntry` 带上 `affected_rows`（用 `with_affected_rows` 挂，不编造 0），状态行报「影响 N 行」，结果网格的空态改说“写语句没有结果集”（不把工具栏的话再说一遍）。② **结果工具栏**（原型 §2.2）——左段：摘要（`N 行 × M 列 · 耗时` / `影响 N 行` / 失败原因）+ **来源连接**（结果**记住它在哪个连接上跑出来的**，不是读当前绑定）+ **截断提示**（警告色，只说真拿到的行数）；右段：**复制**（TSV，制表符 / 换行 / 引号按 Excel 认的写法转义；没网格就不摆）与 **⟳ 刷新**（重跑**选中那份**的 SQL，原位替换、不新开结果集）。筛选 / 下发开关 / 分析 / 导出按 B15 / B14 / B7 分层——**没实现就不摆按钮**。③ **可拖拽分栏**（原型 §2.1 ④）——编辑区 + 结果区改用组件库 `v_resizable` / `resizable_panel`（不手搓拖动条）：拖到哪记到哪（同一面板内存住），下限 `RESULT_MIN_HEIGHT`（112px：工具栏 + 标签条 + 两行网格）、上限 `RESULT_MAX_HEIGHT`（768px）。④ 顺带修一处 **flaky**：中断用例先等假执行器真的跑起来再中断（提交 ≠ 驱动在跑，否则会测到“还没开始的那条被标已取消”这条另一语义）。**验证**：editor 单测 **188 → 199**（TSV 转义 / 写语句状态行 / 影响行数与连接是可选的**真值** / 复制进剪贴板且回执可读 / 刷新重跑选中那份且原位替换 / 写语句不摆复制 / 截断提示 / 分栏不让编辑区被压成 0）· `ui_contract` 7 项 · **真机六个内置驱动**：INSERT 3 行 / UPDATE 2 行 / DELETE 0 行的影响行数断言全过（含新解封的 sqlx 版 MySQL）· editor clippy 零新增告警。⬜ 余：标签的**通道徽标**（依赖 B13）与**血缘摘要**（依赖 B14/B15）· 工具栏的连接段在“未绑定（跟随当前连接）”时不显示——解析出的实际连接名要等 B13 把通道信息带回来 · 结果区高度暂不随会话持久化。 | `driver/utils.rs` + 四个 native 驱动 · `store.rs` / `result_grid.rs` / `host.rs` / `ui.rs` | 窗口/真机：DML 显示影响行数（已过）· 分栏可拖拽 · 复制 / 刷新可用；⬜ 滚动流畅度与通道/血缘展示待 B13 后复测 |
| 2026-09-16（1b：B4 —— 事务） | **事务从“拼 SQL”变成真的会话状态**：① **引擎**（`ConnectionManager` + `SqlService`）——活动事务挂在连接管理器上（事务对象**持有那条物理连接**，即 P0.2c 的会话亲和结论），`begin / commit / rollback` 走**驱动**的事务接口（不再是 `db.query("BEGIN")`：MySQL 会报 1295）；`get_transaction_status` 从“恒 false 的桩”改成**读真值**；**语句在事务里就走事务对象**（`query_in_transaction`）；自动提交关闭时（`use_transaction`）本次执行先自动开一个（**DDL 除外**——MySQL 会隐式提交，那样“事务已开启”就骗人了）。事务用**真实连接 id**（未绑定时解析成活动连接）：拿 `active` 这种虚拟键去开事务会得到 `Connection 'active' not found`，真机上踩到过。② **编辑器**——端口长出 `RunOptions`（自动提交关 → 进事务）/ `transaction(action)` / `transaction_snapshot`；事务状态**跟着每次执行结论一起回来**（界面不必再单独问一次，问就要等）；状态栏 TX 区 = `TX 未开启` / `TX 已开启 3.4s` + 常显的**自动提交**开关 + 事务开启时的**提交 / 回滚**（原型 §2.5），只在执行器真支持事务时出现。③ **真机抓到的两个真问题**：· PostgreSQL 上 `COMMIT` **永远等不到响应**——sqlx 的连接驱动任务被挂在一个只在 `block_on` 期间被驱动的**单线程 runtime** 上，而事务提交走的是旁路调用（另一个 runtime）；改成**一个常驻多线程 runtime**（2 个工作线程）后三个库全通。**验证**：editor 单测 **181 → 188**（事务动作到执行器 + 新状态回执 · 提交关掉事务 · 忙着时回绝 · 执行选项到执行器且快照随结论回来 · 自动提交关 → 下一次执行进事务 · 状态栏动作开合 TX 区）· 提交树 `check --workspace --all-targets` 零告警 · **真机 postgres / sqlite / duckdb：回滚作废 / 提交生效**（探针新增 B4 断言；mysql 仍被依赖侧 `mysql-rsa` feature 阻塞）。⬜ 余：事务内的语句**暂时不可取消 / 无超时**（驱动 `Transaction` trait 没有取消入口）· 别处提交/回滚后面板不会自动刷新（下次执行或动作会纠正）· 自动提交开关的持久化随 B1 余项。 | ✅ 已完成 |
| 2026-09-16（1b：B3 —— 中断与超时） | **执行不再只能干等**：① **端口长出中断能力**——`QueryRunner::cancel(connection) -> Result<bool, String>`，三档语义都要能给人看（`Ok(true)` 送到了 / `Ok(false)` 没有可中断的查询 / `Err` 中断失败）；默认实现回“当前执行器不支持中断”，**不假装成功**。② **通道**——起跑记号（正在跑的作业绑定哪个连接、本轮是否已请求中断）在**提交线程上同步写**：用户可能刚按执行就按中断，那时工作线程还没拿到 job，按作业去取连接会取到空值 → 取消发给了错误的连接（这条由单测钉住）；中断本身在**一次性线程**上做（端口允许阻塞：有的驱动要发 `KILL QUERY` 这类往返），UI 线程不等；中断尝试的回执（失败 / 没在跑）走一条队列回主线程，**不吞**。③ **批量语义**（架构 §3.4）——中断后**剩余语句不再发给驱动**，各自回一条“已取消”结果集。④ **执行中的 UI**（原型 §5.1 / §4.6）——状态栏出现 `■ 中断` 与耗时累加（`执行中 3.4s…`；节拍复用既有的 60 ms 轮询泵 `cx.notify()`，不另起定时器）。⑤ **超时**——按**连接的** `query_timeout` 交给引擎（`SqlExecuteOptions::timeout_ms`）：到点由引擎自己取消并报 `Query timed out after Nms`；连接没配就是**没有超时**（不造默认值）。**验证**：editor 单测 **171 → 181**（提交瞬间按中断仍打到作业连接 · 中断只送一次给执行器 · 批量剩余标“已取消”且未发给驱动 · 没在跑时回绝并给原因 · 端口默认实现说实话 · 面板中断路径与耗时清零 · 耗时文案是纯函数）· **真机 sqlite / duckdb：超时报可读错误、慢查询中断快速返回**（探针新增两条断言）· `ui_contract` 7 项。⬜ 余：中断按钮的“已中断”终态（现在如实显示驱动回的错误文案）属 B6 错误卡片。 | ✅ 已完成 |
| 2026-09-16（1b：B2 —— 批量执行与多结果集） | **执行族五项齐了，结果区从“一份结果”变成“一份结果集列表”**：① **落位成为显式维度**——新增 `ResultPlacement{Replace, NewSet}`：普通执行替换当前选中的结果集，「在新结果标签中执行」追加一份且**原结果集保持选中**（原型 §4.4：别动用户在看的那份，新的放旁边）。② **批量（逐条独立）**——`ExecTarget::Batch(Vec<String>)` + `batch_target()`（复用 `engine::sql::split` 的词法级切分：语句**前面**的注释跟着它走，纯注释段不算可执行内容）+ `ExecMenuKind::ALL` 五项（当前语句 / 选区 / 全部 / **批量**（不足两句置灰）/ **新结果标签**）；一条 job 可带多条语句：`ExecChannel` 顺序跑完、**逐条回填**（失败不中断），忙标记覆盖**整批**（语句之间不是空闲，否则第二次提交能插进来变成隐藏的并行执行）。③ **多结果集**——`ResultStore` 从“每文档一份”改成**每文档一个列表 + 选中项**（`sets` / `active` / `select` / `set_count`），上限 `MAX_RESULT_SETS = 5` 淘汰**最旧的未选中**项（正在看的那份不会被自己的下一个结果挤掉）；新增 `view/widgets/result_sets.rs`（组件库 `TabBar::segmented` 标签条：`结果 N` + 失败项 `danger` 点；两份以上才画，一份结果不需要切换器），`result_grid::render` 收一个可选标签条元素。④ **结果区收敛到唯一读点** `EditorHostPanel::sync_result_view`（网格 + 摘要 + 标签条 + 状态栏原因一次读齐，避免“网格还是上一份、摘要已换”的错位）；面板用 `pending`（已提交未回填的**语句数**）而不是布尔量表达“执行中”（批量回填多条）。**验证**：editor 单测 **159 → 171**（批量落三个结果集且中间失败带标记 · 新结果集不抢选中 · store 的替换/追加/选中/上限淘汰 · 落位与菜单可用性是纯函数）· `ui_contract` 7 项（新 widget 已登记进尺寸/颜色两份清单）· `cargo check --workspace --all-targets` 零告警 · **真机** sqlite / duckdb：单句 + 批量三句 → 三个结果集（`editor_exec_real`，批量断言也补进了探针）。⬜ 余：标签上的**通道徽标 + 血缘摘要**与结果工具栏（筛选/分析/导出/刷新/复制）属 B5；分段抓取属 B5b。 | ✅ 已完成 |
| 2026-09-16（1b：B11 + B12 —— 编辑器成为唯一的 SQL 编辑器） | **旧 SQL 框退场，导航与 M1 桥改走编辑器契约**：① **请求入口**（B11）——`Shared` 新增 `QueryRequest{conn_id, sql, run}` + 私有字段与方法对（`request_query` / `take_query_request`，与 S3b 的 `open_file_request` 同口径：菜单 / 拖拽 / 后台回填都拿不到 `Window`）· 宿主新增 `WorkbenchView::open_query_document`（复用条件很窄：未命名 + 内容空 + **同一绑定**，否则新建；带 SQL 时用 `Shared::append_sql` **追加不覆盖**；`run = true` → `EditorHostPanel::run_all`，**M4 遗留的“查看数据不自动执行”在此关闭**）· 导航四处改调（在 SQL 编辑器中打开 / 查看数据 / 生成 SQL / 拖拽落点），`SidebarEvent::{EditorSqlRequest, OpenSqlEditor}` 两个变体随之下岗；`EditorBridge::insert_sql` 删除。② **删旧 SQL 区块**（B12）——`panels/editor.rs` 里的 Textarea + 内联执行闭包（直连全局分析库文件）+ 结果表 + 历史列表 + `use_duckdb_fed` 门控（约 250 行）与其全部状态（`sql_textarea`/`query_result`/`sql_history`/`last_executed`/`sql_for`/`pending_sql`/`_sql_sub`）及 `apply_nav_drag`/`insert_sql`/`clear_sql` 全删；`mock` 标题从 SQL 区里摘出来独立成块。③ **M1 未保存草稿拦截换实现**（panels-coupling-plan §2 的 S2 遗留项）——`components/project_host.rs` 的 `ProjectEditorBridge` 不再读 `Shared` 的三个镜像字段，改读 `EditorShared`（**草稿 = 未命名且非空的编辑器文档**；`clear` = 宿主关掉未命名文档，有路径的文件不碰）；`EditorShared` 因此提前到 `WorkbenchView::new` 构造（`build_host` 要用它）。**结果**：`Shared` 字段 **27 → 23**（`editor_dirty` / `editor_sql` / `editor_clear` / `result_epoch` 四个退场），契约 4 白名单同步。**验证**：editor 单测 **158 → 159**（宿主 `run_all` 与快捷键同路 + 空文档不打扰执行器）· `ui_contract` 7 项 · `dialog_host_layer` 4 项 · `cargo check --workspace --all-targets` 零告警 · clippy 无新增告警（editor crate 零警告）。⬜ 余：旧路径的**项目只读模式拒绝执行**要在新执行入口补回（目前只有连接侧只读维度，见架构 §13 #9） | ✅ 已完成 |
| 2026-09-16（1b：B1 切片一 — 连接绑定） | **“这条 SQL 发到哪”变成文档属性**（架构 §12 #26 的前半段关闭）：① editor —— `connection.rs`（新的**端口**：`ConnectionOption` 快照 + `ConnectionsPort::{options, ensure_connected}`，`chip_for` / `status_text` 是纯函数：`●P·orders` / `○ 未绑定连接` / 认不出的 id 显示 `?` + 原 id，**不假装它还在**）· `Document.connection`（与 `mode` / `read_only` 同类）+ `OpenRequest::with_connection` + `EditorService::{set_connection, connection_for}` · **执行通道带连接**：`QueryRunner::run(connection, sql)`、`ExecRequest` / `ExecOutcome` 都带 `connection`，`EditorShared::submit` 自己把文档的绑定取出来交给通道（调用方不需要知道连接从哪来）· 工具栏右侧**连接选择器**（菜单当前项打勾；选一个先**自动建连**，建连失败**就不绑定**并留原因；首项「跟随当前连接」= 1a 口径，如实摆着）· 状态栏最左新增连接段（只在会通信的模式出现）。② workbench —— `services/editor_connections.rs`（实现端口：列表读 `Shared::connections` **内存快照**（渲染路径不能 I/O）、建连走 `nav_runtime::{is_connected, connect_entry}` 与导航同一条；建连成功后把快照里的该条改为已连接，免得状态栏刚说“已绑定”却显示空心点）· `editor_exec.rs` 把连接交给 `SqlService::execute(conn_id, …)` · 「新建查询」带**当前选中的连接**（原型 §1.2 入口语义）。**验证**：editor 单测 **148 → 158**（连接文案 5 项纯函数 + 绑定进状态栏 / 建连失败不绑定 / **绑定真的传到执行端口**（真按键 `ctrl-enter`） / 选择器按模式显隐）+ workspace check 零告警 + editor clippy 零警告。⬜ 余：绑定随会话持久化（需 engine 侧加列）· 只读锁 ⓘ（连接侧策略来源）· 执行位置三通道（B13）· 导航右键带连接（B11） | ✅ 切片一完成 |
| 2026-09-16（1b：B16 关闭口径 + 新建入口 + 工具栏执行级） | **B16 主体完成**：① **关闭口径统一**——Dock 的 **每标签 ✕ 根本不存在**（组件库只有标签栏 `⋯` 菜单里的 `Dock.Close`），而它是否摆出来由 `TabGroup::is_closable` → **当前标签**的 `closable(cx)` 决定；面板把判据改成 `!is_dirty()` 后，脏文档那里**直接没有关闭项**（既不会静默丢，也不会“点了没反应”），关闭脏文档的唯一入口是 `Ctrl+W` → 三态确认；保存后脏点消失，关闭项自然回来。② **新建入口**：Quick Open（`Ctrl+P`）命令组新增 **「新建查询 / 新建笔记 / 新建文件」** → `WorkbenchView::new_editor_document(mode)`（三档走同一条路：开文档 → 建面板；标题由服务层未命名编号保证不重名）——在此之前“关掉最后一份文档”就没有任何回到编辑器的入口。③ **工具栏执行级（执行 ▾）**：用组件库 `DropdownButton` 做**分体按钮**（主按钮 = 执行“选区优先 → 当前语句”，与 `Ctrl+Enter` 同一条路；菜单 = 当前语句 / 选区 / 全部）· 新增纯函数 `statement_target`（忽略选区）与 `ExecMenuKind` / `target_for_menu`（显式目标，不做“选区优先”推断），菜单项**只放今天真能跑的三项**（批量执行 / 新结果标签属 1b，执行计划属 B10）· 执行 § 只在 SQL 模式出现（文本模式按能力表不通信，分析模式的执行是笔记级动作，归 1c）。**验证**：editor 单测 **144 → 148**（菜单目标三项独立 + 空文档三项全 Empty · 脏文档无可关闭判据 · 工具栏按模式分层）+ `cargo check --workspace --all-targets` 零告警 | ✅ 已完成 |
| 2026-09-16（1a：A9 对话框收尾） | **A9 全部收尾**：① `view/dialogs.rs`——三个对话框（**关闭三态** / **保存失败二次确认** / **模式切换确认**），文案表在 `mode::ConfirmKind`（标题说“要发生什么”、正文说“代价”、按钮说“按下去做什么”，**不出现“确定”这种看不出后果的词**，已有穷举单测）；单元粒度不做内嵌单选（对话框重建由 Root 决定，`Rc<Cell<_>>` 改了不会重绘）→ **两个动作按钮就是选择本身**（“整篇一个单元”/“按语句拆分”）。② **关闭三态落地入口在编辑器侧**：`request_close_document`（干净直接关 / 脏先问）+ `resolve_close_choice`（保存 → 先写盘再关，未命名先进另存为；不保存 → `close_document_now`；取消 → 什么都不做）+ 写盘失败二次确认（重试 / 另存为 / 取消，**绝不把脏文档当干净关掉**）。③ **系统文件对话框**：`rfd` 只在 workbench（`services/editor_files.rs`），打开是宿主动作，另存为做成**端口注入**（`EditorShared::attach_save_path_picker`）——编辑器不依赖 `rfd`，而且“没接端口”是**可读的失败**（状态栏说“未接入系统文件对话框”），不是静默。④ **模式指示器进了工具栏**（原型 §2.2 最左控件 `[SQL] ▾` → 菜单三档，当前项打勾）：需确认的切换**未确认前模式一点不动**（禁止静默切换）；`u1` 工具栏其余部分（执行族/格式化/历史/更多/执行位置/连接）仍未实现，**因此不放那些按钮**。⑤ `Ctrl+O` / `Ctrl+Shift+S` 注册（两键内核未占用；`Ctrl+O` 只被组件库绑在 `Command` context）。**验证**：editor 单测 **131 → 144**（对话框层真渲染 / 未确认前不过状态 / 确认后按所选粒度拆单元 / 不保存不改磁盘 / 未命名保存后写盘才关 / 取消另存为不关 / 未接端口留原因 / `Ctrl+Shift+S` 标题跟随）· ui_contract 5 项（扫描范围扩到 `dialogs.rs`）· 全工作区 `check --all-targets` 零告警（**顺手修了上一提交漏的两个 `NavReorder*` 导入，app 才能编过**）· ⚠️ **headless 下对话框按钮的“真点击”没做**：`debug_bounds` 的坐标与鼠标命中测试对不上（单跑能中、全套跑必不中），改成“断言层与按钮真渲染 + 直接驱动落地入口”（§12 #29） | ✅ 已完成 |
| 2026-09-15（1a：编辑器接入 workbench） | **编辑器已进中央 Dock**（A9 接线）：`crates/workbench/{Cargo.toml, src/view.rs}`——新增 `editor.workspace` 依赖、`WorkbenchView` 两个字段（`editor_service` / `editor_hosts`）、构造期初始开一份未命名 SQL 文档、`init_workspace` 用**链式** `DockLayout::tabs().panel_view(旧).panel_view(新)` 把编辑器面板接进中央 tab 组（**与旧面板并存，不替换**：旧面板退役属 B12）、新增 `open_in_editor(path, window, cx)`（走 `persist::open_file` + `DockArea::add_panel`，同文档复用不重建）。**仅改必要三处**，旧行为不变。验证：`cargo check --workspace --all-targets` 零告警（editor → workbench → app）+ editor 63 项 / workbench 52 项全绿。← **到此 app 启动后中央区就有一个可写、可高亮、带真实状态栏的编辑器标签** | ✅ 已完成 |
| 2026-09-15（1a：A13 + A15 收尾） | **A13 文件档位完成**：`limits.rs`（**判定纯函数** `tier_for_size`：<50MB 常规 / 50–200MB 大文件 / ≥200MB 超大；`tier_for_path` 是 I/O 只走打开事件路径；7 项单测含**真 200MB 稀疏文件**）· 文档 `Document.tier()`（档位是文档属性，与 mode/read_only 同类）+ `OpenRequest::with_tier`（≥200MB 同时置编辑器只读）· `persist::open_file` 按实际大小定档，**超大文件不整份读进内存**（原型 §4：“不建编辑器会话”）· 面板：**提示卡**（`warning` 色描边，说清限制而不是让用户撞上）。⬜ “关折叠”只有判据、**缺内核开关**（已记入代码与 A13 行）· **A15 契约完成**：`ui_contract` 扫描范围扩到 `crates/editor/src`（host / status_bar / result_grid / highlight 四个视图文件），**尺寸与颜色两项契约都过**；顺手把 `host.rs` 里最后一处 `.px(rems(…))` 改成 `px_2()`（局部间距走 Tailwind 尺度）并删掉对应常量。**editor 单测 121 → 131** · `ui_contract` 5 项全绿 | ✅ 已完成 |
| 2026-09-15（1a：A12 会话持久化） | **A12 完成**：**engine**——`engine_contexts` 表补 **`mode` 列**（`CREATE TABLE` 改新库 + `ensure_column` 幂等 `ALTER` 补老库）+ `load_latest_editor_context()`（启动只恢复最近一份）+ `EditorContext` 加 `mode`（唯一使用方，无破坏）· **修一个既有隐患**：`GlobalSqlitePool::acquire_sync` 原用 `Handle::current()`，在无 runtime 的线程（GPUI 主线程 / 普通 `#[test]`）**直接 panic**；现改为“在 tokio 上下文里返回可读错误 + 否则自建短命 runtime 驱动”· **editor**——`session.rs`（`SavedSession` + `SessionStore` 端口 + `MemorySessionStore`，5 项单测；**会话标识 = 路径键**，未命名文档不持久化；模式键 `text/sql/analysis` 与展示文案解耦，`EditorMode::as_key/from_key`）· 面板 `session_snapshot` / `restore_session` / `save_session_now`（**`Ctrl+S` 成功后与关文档时**落库）· **workbench**——`services/editor_session.rs`（真 store：包 `WorkbenchContextStore`）+ `WorkbenchView::restore_last_session`（`defer_in` 里读库；用**会话内容**开文档而非重读磁盘；恢复后收掉启动时那份空标签，走正常关闭路径）。**验收**：`crates/workbench/tests/editor_session_real.rs` 3 项在**真 SQLite** 上跑通（关文档→保存→重启→光标 12 与**分析模式**都回来 / 未命名不写库 / 会话 id 是路径键）· editor 115 → **121** · engine 300 → **301** | ✅ 已完成 |
| 2026-09-15（1a：A11 查找 / 替换） | **A11 完成——且结论是“零自建”**：先按“内核无 handler”的旧判断自建了一套（`find.rs` 匹配器 + `find_bar.rs` 查找栏 + 5 个 action + 4 条键位），实测后发现**判断是错的**：内核在 `Input` context 里绑了 `ctrl-f`/`ctrl-h` **并注册了 listener**（`state.rs:4195` 的 `on_action_search`/`on_action_replace`），界面由组件库 `SearchPanel` 渲染成浮层；按键被内核消费后外层根本收不到（已写入架构 §12 #24）。**自建部分已全部撤除**（含 `find.rs` / `find_bar.rs` / 相关 action、键位、测试），改为两条**验证测试**：（1）`the_kernel_find_panel_takes_over_on_ctrl_f`，（2）`ctrl_h_opens_the_kernel_replace_panel`（判据：按键后焦点被内核交给查找框，这是“面板真开了”的可观察信号）· editor 单测 113 → **115**（净增 2：撤除自建后又删掉 8 项查找自测）。**教训**：判断“内核有没有这个能力”要读 `on_action` 注册表，不能只 grep 会话字段 | ✅ 已完成（零告警） |
| 2026-09-15（1a：A14 最小执行 + 结果网格） | **A14 完成**：`execution.rs`（**执行目标解析**纯函数：选区 > 光标所在语句 > 全部，语句定位走 `engine::sql::split`；`QueryRunner` 执行端口（宿主注入）；`ExecChannel` 工作线程 + mpsc + 主线程轮询——同 `nav_jobs` 模式，**不引 tokio**）· `store.rs`（`ResultStore` 结果单权威——视图只是投影）· `view/widgets/result_grid.rs`（组件库 `DataTable`/`TableState` + `TableDelegate`，**首次使用该组件**，空态直说原因）· 面板：`Ctrl+Enter`/`Ctrl+Shift+Enter` 两个动作 + 轮询泵 + 结果区（有结果或执行中才占位）+ 状态栏「执行中…」· 文本模式**拒绝执行并说明原因**（读能力表 `execute`）· workbench：`services/editor_exec.rs`（`EngineQueryRunner`：`SqlService` + 活动连接，取数一律走 `batches`/`to_rows()`）· app 注册 `ctrl-enter` / `ctrl-shift-enter`。**editor 单测 100 → 113**（含 4 项真按键的窗口测试：光标所在语句 / 执行全部 / 失败留原因 / 文本模式拒绝）· **真机四库实测通过**（`crates/workbench/tests/editor_exec_real.rs`）：`✅ mysql 43ms · ✅ postgres 173ms · ✅ sqlite 1ms · ✅ duckdb 6ms`（各 1 行 × 1 列，列名 `n`）· ⬜ 未做：结果区高度可拖拽 / 多结果标签 / 执行历史 UI / 中断与超时（归 1b）· 连接绑定仍是“当前活动连接” | ✅ 已完成（零告警） |
| 2026-09-15（1a：A10 动作与快捷键） | **A10 部分完成（可独立完成的部分全做完）**：新增 `crates/editor/src/edit.rs`（**行注释开关纯函数**：选区→行块、空行跳过、缩进保持、选区结束在行首不吞下一行、CRLF/UTF-8 边界；13 项单测）· `crates/editor/src/commands.rs`（`SaveDocument` / `ToggleComment` / `CloseDocument`，namespace `editor`）· 面板 `key_context("editor")` + **`track_focus`**（没它快捷键落不到 `on_action`——**实测踩到**）+ 状态栏新增“动作失败原因”段（未命名需另存为 / 只读拒绝 / 有未保存改动，**不允许“按了没反应”**）· **关闭由宿主执行**：`close_document_in_dock(area, panel, window, cx)`（面板在自己的 `update` 里让 Dock 移除自己会**重入 panic**，见架构 §12 #23）· `focus_self`（宿主打开已打开文档时切到该标签，并同步服务层当前文档）· workbench：`close_active_editor` + `Ctrl+W` 的 `on_action`。app 层注册 **3 条键位**：`ctrl-s` / `ctrl-/` / `ctrl-w`（context 均为 `editor`）。**editor 单测 78 → 85**（含 5 项真按键盘的窗口测试：`ctrl-s` 真写盘、未命名真报原因、`ctrl-/` 真改文并可逆、只读真拒、宿主关闭真关文档+脏文档真被拦）· editor crate **clippy 零警告**（顺手清掉 4 条旧告警）· ⬜ 未做（**因此未注册、未宣传**）：`Ctrl+Enter`（随 A14 执行）· `Ctrl+F`/`Ctrl+Shift+F`（随 A11/B10）· `Ctrl+O` 与另存为（需系统文件对话框，属对话框批次） | ✅ 已完成（零告警） |
| 2026-09-15（1a：A9 持久化层） | **A9 持久化层完成**：`persist.rs` —— 唯一的 I/O 入口（只从事件路径调用，服务层与渲染路径保持零 I/O）；`open_file` 对已打开路径只激活（**不重读，避免冲掉未保存编辑**）；`save_document` **先写盘后清脏**（写失败不留假状态）；`save_as` 换路径保身份；`external_modified` 用 mtime 检测外部修改（含文件消失）；面板新增 `save()`。**editor 单测 55 → 63**（新增 8 项持久化测试，含真临时目录 I/O）· **测试逮到一个真 bug**：`if let Some(x) = shared.service().find…` 的只读借用活到分支体内 → 分支里 `update()` 撞 `RefCell already borrowed`（已修）· ⬜ 剩余：对话框与 workbench Dock 注册 | ✅ 已完成（零告警） |
| 2026-09-15（1a：A6 + A7） | **A6 只读两维度**：编辑内核 `.readonly()` 跟文档 `ReadOnly.editor`；**状态栏把两个维度分开写**（「只读」/「连接只读」，互不蕴含）。**A7 状态栏**：`view/widgets/status_bar.rs`（组件库 `StatusBar`），文案计算 `labels()` 为纯函数；左＝模式 + **语句数（`engine::sql::split_statements` 词法切分）** + 未保存，右＝两个只读维度 + Ln/Col + 已选字数；**语句数缓存**（仅内容变化时算一次，渲染期不扫描）；无数据源的字段（方言/编码/换行/缩进）**不显示占位**。**editor 单测 49 → 55** | ✅ 已完成（零告警） |
| 2026-09-15（1a：A5 模式切换） | **A5 逻辑完成**：`mode.rs` 新增切换矩阵（`SwitchPlan` / `ConfirmKind` / `SwitchContent` / `CellGranularity`）——**五个方向逐项对照原型 §1.3**（文本→SQL 免确认；SQL→文本/分析、分析→SQL、文本↔分析均需确认且给内容变换与提示语），另含**换会话**确认；内容变换为纯函数：`sql_to_cells`（粒度二选一，按**词法级**切分）、`cells_to_sql`（`;\n\n` + 尾分号）、`cells_from_text`/`cells_to_text`（文本层标记 `-- %%`，1c 落盘格式待定）。视图侧 `sync_mode` 按模式刷新着色与只读。**editor 单测 37 → 49**（新增 11 项矩阵测试 + 1 项窗口测试）· ⬜ 对话框待 A9 一并接 | ✅ 已完成（零告警） |
| 2026-09-15（1a：A4 高亮） | **A4 完成**：`view/highlight.rs` 实现 `DocumentRangeSemanticTokensProvider`——把 `engine::sql::highlight` 的词法区间编码成 LSP 语义 token（delta 编码、跨行按行切分、列按字符计），**颜色交给主题按 token 名解析**（keyword/type/function/string/number/comment/operator/punctuation/variable；**Identifier 不上色**）；文本模式不上色，分析模式留 1c 逐单元处理；大文件（>1MB）降级不上色。依赖新增：`engine` / `lsp-types`（0.97 随 gpui-base）/ `anyhow`（后两者已登记进根 Cargo.toml 唯一入口）。**editor 单测 28 → 37** | ✅ 已完成（工作区 check 零告警） |
| 2026-09-15（1a：A2 + A3） | **A2/A3 面板与内核视图**：新增 `crates/editor/src/{shared.rs, ui.rs}` + `view/{mod.rs, host.rs}`——`EditorHostPanel` 一个面板 = 一个标签 = 一份文档（Dock 提供标签条，面板只给 `tab_name`/`title`/脏点/`closable`）；内核用组件库 `input::{Editor, EditorState}`（零手搓）并接线 `InputEvent::Change` → `EditorService::set_content`（唯一权威）；`shared` 提供跨面板可克隆句柄（视图只读、变更走事件路径）；`ui.rs` 登记本 crate 结构尺寸（不反向依赖 workbench）；依赖只加 `gpui-kit`。**editor 单测 24 → 28**（新增 4 项 headless 窗口测试：标题跟随 / 脏点判据 / 3 文档并存 / 渲染不 panic）· **A4 方案按 API 核实修正**：组件无 SQL grammar，高亮走 `DocumentRangeSemanticTokensProvider` + 主题 token 名（见任务表） | ✅ 已完成（`cargo check --workspace --all-targets` 零告警） |
| 2026-09-15（1a 开动：A1 完成） | **A1 `EditorService`**：`crates/editor/src/service.rs`——文档集合唯一权威（打开/关闭/激活/重命名/内容/保存/模式/只读），`DocumentId` 一经生成永不变（另存为只换路径标题），去重规则“同路径重复打开=激活”，脏判据 `content != baseline`；**13 项单测**全绿（含 Windows 大小写归一的平台分支、关闭当前落到邻居、只读拒写）；`editor` 单测 11 → **24 项** | ✅ 已完成 |
| 2026-09-15（MySQL 事务修复 + 并发亲和结论） | **① MySQL `BEGIN` 修复**：`native/mysql.rs` 新增 `needs_text_protocol` / `execute_via_text_protocol`——事务控制与会话语句（`BEGIN`/`START TRANSACTION`/`COMMIT`/`ROLLBACK`/`SAVEPOINT`/`SET`/`USE`）改走 **文本协议**（`sqlx::raw_sql`），不再报 `1295`；探针实测：MySQL 事务内计数 = 1、`ROLLBACK` 生效 ✅ · **② P0.2c 并发亲和探针**（四库）：**SQLite / DuckDB 并发下仍同句柄；MySQL / PG 会另开物理连接**（另一侧报 `1146` / `relation does not exist`）→ **1b 的事务与会话必须 per-session 独占连接**（架构 §12 #2） · **③ DuckDB 扩展约定**写入依赖治理（外部预编译优先，扩展走指定目录 `INSTALL`，不为扩展重编内核） | ✅ 已完成（探针 12 项全绿） |
| 2026-09-15（P0.9 基线 + Phase 0 收官） | **P0.9 完成**：本模块**依赖增量 = 0**（未引 tree-sitter，sqlglot-rust 早已在依赖里）；`cargo build -p rds-app -j 2` 增量编译 **2m37s**，debug 二进制 **≈137 MiB**（数据入 §6）· **Phase 0 地基已全部完成**（P0.1–P0.10：仅余 P0.6 的“驱动层真实 `affected_rows`”按计划转 1b）· 验证汇总：引擎 297 项 / editor 11 项 / shared 22 项 / 台账探针 10 项 / 事务探针 8 项 全绿；`cargo check --workspace --all-targets` 零告警 | ✅ 已完成 |
| 2026-09-15（P0.2 实跑 + 三个结果保真度缺陷） | **P0.2 有结论**（真机四库）：PG / SQLite / DuckDB — **会话亲和成立**且 `ROLLBACK` 真实生效；驱动级事务（`execute_in_transaction`）四库中三库通过；**MySQL 的显式 `BEGIN` 被 prepared 协议拒绍**（1295，需改走驱动事务 API）→ 回写架构 §12 #2 / §7.3 #3 · **实跑又抓出三个真缺陷并修复**：① 各驱动只填 `batches`，而历史行数读的是恒空的 `total_rows` **字段**（已改用 `total_rows()`）；② `arrow_value_at` 漏了 Int32/UInt64/Float32 等位宽，兜底是 `format!("{:?}", array)`——**把整列 Debug 打印进每个单元格**（已修 + 3 项回归）；③ MySQL 列类型探测 `bool` 优先 → `COUNT(*)` 显示成 `true`（已按声明类型定排行）→ 架构 §12 #21 / #22 | ✅ 已完成（实跑验证：引擎 297 项 · shared 22 项 · 探针 10 项 · 事务探针 8 项全绿） |
| 2026-09-15（探针实跑：台账结论落地） | **P0.10 已实跑（10 项全绿）**，结论已回写原型 §7.4「行为级事实」：**类型标注 / 血缘 / 作用域 / 下推 / 差异** 均**实测可用**；差异粒度到 `SelectItem`/`Expr`/`OrderByItem`，**SELECT 列表 / WHERE / ORDER BY / LIMIT 改动都能检出**（原先担心的“漏条件改动”**已被实测推翻**）· **两处保留**：`qualify_columns` 只部分限定（`id` 仍裸列）、`unnest_subqueries` 改写为 `INNER JOIN + DISTINCT`（NULL 语义不等价）· **两处纠正**：① 格式化**不会丢注释**（行内/尾随注释→解析失败→原样返回，架构 §12 #3 改述）② `transpile` 对脚本是**静默截断**（`"SELECT 1; SELECT 2;"` → `Ok("SELECT 1")`，生产路径同样）→ 升级为 🔴（§12 #19）· 三条不变量已提升为断言（同句只 `Keep` / WHERE 改动有 `Update` / 换型 = `Remove+Insert`） | ✅ 已完成 |
| 2026-09-15（台账候选探针 + 探针编译错误修正） | **P0.10 探针就绪**：`crates/engine/tests/sqlglot_capabilities.rs`（9 组真实 SQL：作用域 / 血缘 / 类型标注 / 差异 / 下推 / 限定与展开 / 本地计划 / 转译单条限制 / 格式化注释保真；**报告式** + 仅弱断言）· **修正一个上轮埋下的编译错误**：`crates/engine/tests/transaction_affinity.rs` 写的是 `use engine::…`——集成测试是**独立 crate**，本包库目标名是 `rds_engine`（`engine` 只是**其它** crate 的依赖别名）；证据：`crates/{connection,mock}/tests/*` 分别用 `rds_connection::` / `rds_mock::` | ✅ 已完成（编译验证待本机执行，见 §6） |
| 2026-09-15（提交 + 台账二次核验） | **模块已提交**（`ac0d75f`，24 文件 / 5625 行：文档 5 件 + `crates/editor` 骨架 + engine SQL 原语 + 历史字段 + 事务探针；暂存时避开同工作区其它会话的在途改动，混文件 `Cargo.lock` / `sql/engine.rs` / `docs/architecture/README.md` 按 hunk 级分离）· **sqlglot 台账二次核验（读实现）**：修正 4 处（关键字是**逐词枚举变体** / `Token::position` 是**字符**下标 / `transpile` **只吃单条** / `builder` 证据行号），补 `Schema`+`MappingSchema`、`qualify_columns`、`optimize`、`plan`（仅本地计划）、`executor`（不采用）等条目（原型 §7.4） · **修正一个真缺陷**：高亮区间改为「字符偏移 → 字节偏移 + 取原文区间」（原实现按 `value` 回查，转义字符串落空、引号与注释标记取不到）→ 架构 §12 #20 | ✅ 已完成（编译验证待本机执行，见 §6） |
| 2026-09-15 | 文档阶段：模块入口 + 原型设计 + 架构（含现状盘点与四个假底座）+ 本方案 + 交互稿（`editor-prototype.html`，1554 行，24 条交互断言） | ✅ 已完成 |
| 2026-09-15（修订） | 按评审意见补充：**执行通道与执行族正交**（源库/加速/联邦）· 结果区从三模式过滤收敛为「筛选（本地/下发）+ DuckDB 分析」· **结果血缘与通道徽标** · 结果集只读边界与网格归属（架构 §3.6）· 任务表新增 B13/B14/B15/C9 与测试场景 30–36 | ✅ 已完成 |
| 2026-09-15（Phase 0 第一批） | **P0.4 语句切分**：`engine::sql::split`（词法级状态机 + 26 项表驱动测试）+ `SqlEngine::split_statements` + `sql_parser_service::split_sql` 委托重写 · **P0.5 历史字段**：`SqlHistoryEntry` + `save_sql_history(_into)`（耗时/成功/失败原因/行数真实，失败也留痕）+ 4 项单测 · **P0.7 骨架**：新建 `crates/editor`（`model` 能力表 + `mode` 判定规则表 + README）+ workspace 登记 · `QueryResult::from_batches` 不再把 `total_rows` 当作 `affected_rows` | ✅ 已完成（编译验证待本机执行，见 §6） |
| 2026-09-15（Phase 0 待办） | P0.1 Dock 标签能力验证 · P0.2 事务会话亲和验证 · P0.3 格式化选型 · P0.6 驱动层真实 `affected_rows` · P0.8 SQL 高亮注册 · P0.9 平台基线 | ⬜ 未开始 |
| 2026-09-15（P0.3 完成） | **格式化不再依赖新库**：sqlglot-rust 已自带 `generate_pretty`；`SqlEngine::format` 改为“多语句逐条生成 + `;\n\n` 拼接”，解析失败原样返回；旧测试从“非空”升级为 7 项回归 | ✅ 已完成 |
| 2026-09-15（P0.8 完成 · P0.2 探针就绪） | **P0.8 换路线并完成**：不再引 tree-sitter，改用 sqlglot tokenizer → `engine/src/sql/highlight.rs`（字节区间 + 类别，**13 项单测**）· **P0.2 探针就绪**：`crates/engine/tests/transaction_affinity.rs`（四类库、环境变量注入、临时表作亲和判据、不改用户数据） | ✅ P0.8 完成；P0.2 待真机运行 |
| 2026-09-15（评审决策 + P0.1 静态核实） | **分段抓取已确认纳入 1b**（新增 B5b + 测试场景 37–39）· 自动刷新/钉住 1b 顺带 · **P0.1 结论（静态）**：Dock **没有“关闭前否决”钩子**（`DockArea` 收到 `TabGroupEvent::ClosePanel` 后直接 `remove_panel_id`；`Panel::closable(cx)` 是唯一闸门；`with_renderer` / `remove_panel` 均为 `pub`）→ 关闭语义默认走“草稿兜底”（架构 §13 #15） | ✅ 已完成 |
| — | Phase 0 地基与技术验证 | ⬜ 未开始 |
| — | Phase 1a 内核 + 文本/SQL 编辑体验 + 最小执行 | ⬜ 未开始 |
| — | Phase 1b SQL 执行闭环（DBeaver 一档） | ⬜ 未开始 |
| — | Phase 1c 分析模式骨架（Cell/Output/Session，仅 SQL 单元） | ⬜ 未开始 |
| — | Phase 2 Python/Rust 内核 · 变量桥 · 富输出 · `.ipynb` | ⬜ 未开始 |

---

## 1. 现状结论（盘点摘要）

| 层 | 状态 | 证据 |
| --- | --- | --- |
| 编辑器视图 | ⛔ 只有一个 `Textarea`（无高亮/行号/补全），且仅在 `use_duckdb_fed` 连接下出现 | `crates/workbench/src/panels/` L6094 / L6809 / L6779 |
| 执行链路 | ⛔ 同步直连全局分析库文件，与连接无关、无取消/超时/事务/批量；且内联在 render 闭包内（违反 render 零 I/O） | 同上 L6821-6825；`services/query_runner.rs` |
| 结果区 | ⛔ 定宽 `div` 表格（无虚拟化/排序/过滤/分页） | 同上 L6921-6950 |
| 历史 / 导出 | 🟡 20 条 JSON 回填 / 仅 CSV | `services/query_history.rs`、`services/query_export.rs` |
| 多文档 / 快捷键 / Action | ⛔ 无 | `view.rs` L302、`commands.rs`、`app/src/main.rs` L80-110 |
| engine SQL 能力（解析/执行/历史/模板/上下文） | ✅ 已迁移但**多数零接线** | 架构 §7.2 |
| 元数据内省（补全数据源） | ✅ 已迁移（异步）但零消费 | `crates/database/src/metadata_service.rs` |
| 四个假底座（格式化/切句/事务/历史字段） | 🔴 必须先修 | 架构 §7.3 |

**关键缺口**：① 编辑器本体（内核 + 三模式）；② 执行管线（抽取 + 后台化）；③ 语句切分与格式化重做；④ 事务与历史真实化；⑤ 分析模式的模型（Cell/Output/Session）。

---

## 2. 阶段划分

```
Phase 0（地基，无 UI）
   ├─ 技术验证：Dock 标签能力 / 事务会话亲和 / 格式化选型
   ├─ 后端修复：语句切分 · 历史字段 · affected_rows
   ├─ 结构：crates/editor 骨架 + 依赖接线
   └─ 验证：SQL 语法高亮注册（tree-sitter-sql）
        │
        ├──► Phase 1a（内核 + 文本模式 + SQL 编辑体验 + 最小执行）→ 交付「能用的编辑器」
        │        │
        │        └──► Phase 1b（执行闭环：选区/当前语句/批量/取消/事务/结果区/历史/导出）→ 交付「合格的 SQL 客户端」
        │                 │
        │                 └──► Phase 1c（Cell/Output/Session + 内联输出 + .rdsnote）→ 交付「分析笔记骨架」
        │                          │
        │                          └──► Phase 2（Python/Rust 内核 · 变量桥 · 富输出 · 互操作）
```

**为什么要分 1a/1b/1c 而不是一次做完**：1a 结束即可自证价值（编辑器可用）；1b 与 1a 的验证手段完全不同（前者靠窗口/单测，后者靠真实端点回归）；1c 的复杂度在"会话与输出"，与"编辑体验/执行语义"耦合会互相拖慢。

---

### Phase 0 — 地基与技术验证（不含 UI）

| # | 任务 | 落点 | 验收 |
| --- | --- | --- | --- |
| P0.1 | **Dock 标签能力验证**：`Panel::{title, title_suffix, closable}` 能否承载脏点与“关闭前确认” | ✅ **静态结论已出（2026-09-15）**：脏点走 `title_suffix` 可行；**关闭无否决钩子**（`DockArea` :1257 收到 `TabGroupEvent::ClosePanel` 即 `remove_panel_id`；`closable(cx)` 只是静态许可）。可选路径：① 草稿兜底（默认）② 自绘标签条 + `with_renderer` + 确认后 `remove_panel` | 结论已写回架构 §12 #18 / §13 #15；运行期探针（脏点渲染 + 两个关闭路径）随 1a 的 A2 一并验证 |
| P0.2 | **事务会话亲和验证**：连接池下 `BEGIN` 与后续语句是否同一物理连接（临时表作判据，四类库均可跑） | ✅ **已实跑出结论（2026-09-15）**：`crates/engine/tests/transaction_affinity.rs`（含驱动级事务路径用例）—— PG / SQLite / DuckDB **亲和成立 + `ROLLBACK` 真实生效**；**MySQL 显式 `BEGIN` 被 prepared 协议拒绍（1295）**，驱动级事务可用 | 结论已回写架构 §12 #2 / §7.3 #3；1b 实现事务状态机时：**MySQL 的 begin/commit/rollback 必须改走驱动事务 API** |
| P0.3 | **格式化实现选型** | ✅ **选定（2026-09-15）**：sqlglot-rust 自带 `generate_pretty` + `parse_statements_with_comments`（**无需新依赖**） | `engine/src/sql/formatter.rs` 重写；回归从“非空”升级为 7 项（非 Debug 打印 / 可再解析 / 多语句不丢 / 失败原样返回 / 空输入 / 前导注释 / 各方言参数）；残留：行内注释丢失（**已源码核实** `gen_statement` 只 emit 前导注释） |
| P0.4 | **语句切分实现**：词法级扫描器（`'…'` `"…"` `` `…` `` `--` `/* */` `$$…$$`），返回 `Vec<SqlStatement{start,end,line}>` | **`crates/engine/src/sql/split.rs`**（✅ 已完成）+ `SqlEngine::split_statements` + `sql_parser_service::split_sql` 委托 | 表驱动单测 26 项（含各方言字面量/注释/嵌套/占位符/多字节/未闭合/行号）；`psql` 过程体 `$$` 块不被切开 |
| P0.5 | **历史字段贯通**：`save_sql_history` 增参（`elapsed_ms / success / error / rows_affected / rows_returned / db_type`）+ 失败路径也写 | ✅ `engine/src/persistence/history_store.rs`（`SqlHistoryEntry` + `save_sql_history` / `save_sql_history_into`）、`engine/src/services/sql_service.rs`（成功/失败双路径写入 + `db_type_of` 助手） | 单测：成功/失败各写一条且字段真实（耗时 > 0、成功标志正确、失败含原因）；存储可注入（`*_into`）不碰用户真实历史 |
| P0.6 | **影响行数**：`QueryResult::from_batches` 不再把 `total_rows` 当作 `affected_rows`（✅）；历史在写语句上记 `rows_affected`（已接线） | `crates/shared/src/models.rs`、`engine/src/services/sql_service.rs` | 遗留（转 1b）：**驱动层返回真实 affected_rows**（见架构 §12 #17） |
| P0.7 | **crate 骨架与依赖接线**：`crates/editor`（lib + `model` 能力表 + `mode` 判定表 + README）、workspace 依赖唯一入口登记 | ✅ `Cargo.toml`（members + 依赖别名）、`crates/editor/{Cargo.toml,README.md,src/*}` | `cargo check --workspace --all-targets -j 2` 零告警；`cargo test -p rds-editor --lib` 全绿（workbench → editor 的依赖线等到 1a 首次使用再连，避免死依赖） |
| P0.8 | **SQL 高亮注册验证** | ✅ **方案已变且已落地（2026-09-15）**：改用 **sqlglot tokenizer**（`sqlglot_rust::tokens`，带注释与行列位）→ `engine/src/sql/highlight.rs` 产出「字节区间 + 类别」，**不需要 tree-sitter、不需要联网取包** | 单测 **13 项**（关键字/类型/函数/字符串含引号/**转义字符串**/数字/占位符/注释/标点运算符/升序不重叠/**中文 SQL 字节区间**/未闭合降级/区间助手）；视图层只负责“类别 → 主题色”。行为级事实已记原型 §7.4（空白被丢弃、关键字逐词变体、`position` 是字符下标、`quote_char` 只给带引号标识符） |
| P0.9 | **平台与性能基线**：记录编译时间增量（新增依赖后 `cargo build -p rds-app`）与二进制体积变化 | ✅ **已记录（2026-09-15）** | 数据已写入本文件 §6：**依赖增量 = 0**（未引 tree-sitter）；增量编译 2m37s；debug 二进制 ≈137 MiB |
| P0.10 | **台账候选验证用例**（原型 §7.4 标 ⚪ 的项：作用域 / 血缘 / 类型标注 / 差异 / 下推 / 限定与展开 / 本地计划 / 转译单条限制 / 格式化注释保真） | ✅ **探针已就绪并已实跑**：`crates/engine/tests/sqlglot_capabilities.rs`（离线、不连库；报告式 + 三条已确认的不变量断言）；**结论已回写原型 §7.4**（可用 / 保留 / 纠正逐条列明） | 重跑：`cargo test -p rds-engine --test sqlglot_capabilities -j 2 -- --nocapture --test-threads=1`（10 项）；可用项提升为 `engine::sql::*` 公开 API 时随 1b/1c 一并补断言式单测 |

> Phase 0 结束时：后端"假底座"全部转真 + 结构就位 + 三个技术未知消除。

---

### Phase 1a — 内核 + 文本模式 + SQL 编辑体验（+ 最小执行）

**目标**：打开 `.sql` / `.txt` 就能得到"键盘优先、状态可信"的编辑体验；`Ctrl+Enter` 能把整篇 SQL 跑出结果（结果区先做基础版）。**此阶段结束即"能用的编辑器"**。

| # | 任务 | 落点 | 验收 |
| --- | --- | --- | --- |
| A1 | `EditorService`：打开/关闭/激活/重命名文档；`DocumentId` 稳定生成；文档集合状态 | ✅ **已完成**：`crates/editor/src/service.rs`（纯逻辑、零 I/O、不碰 GPUI） | 服务层单测 **13 项**全绿（含：同路径重复打开=激活且不新开标签 · 大小写/分隔符归一的平台差异 · 重命名保身份不保路径 · 关闭当前落到邻居 · 脏判据只比内容（撤销回原值即干净）· 只读拒写） |
| A2 | **多文档标签条**（依 P0.1 结论）：标签、脏点、关闭、`+` 新建、溢出处理 | ✅ **面板侧已完成**：`crates/editor/src/view/host.rs`（Dock 提供标签条，面板只给 `tab_name` / `title` / `title_suffix` 脏点 / `closable`）· ⬜ 待接：workbench 中央 Dock 的 tab 组注册（需先有“打开文件”入口，A9） | 窗口测试：3 文档并存（3 个面板、标题各自正确、只有被改的那个脏）· 标题跟随后同· 保存后脏点消失· 面板名/可关闭稳定· 渲染不 panic |
| A3 | 内核视图接入：`Editor` + `EditorState` 每文档一份 + 行号 + 只读态 | ✅ **已完成**：`view/host.rs` 用 `gpui_kit::component::input::{Editor, EditorState}`（零手搓）；`EditorState` → `EditorService` 回写由 `subscribe_in(InputEvent::Change)` 接线；只读态来自文档 `ReadOnly.editor` | 窗口测试：面板渲染一帧不 panic；只读/可编辑由 `readonly(read_only)` 控制（A6 再补三种组合的断言） |
| A4 | **SQL 高亮**：方言选择 + 主题角色映射（缺角色复用最接近标准 token） | ✅ **已完成**：`crates/editor/src/view/highlight.rs`——`SqlSemanticTokensProvider` 实现 gpui-base 的 `DocumentRangeSemanticTokensProvider`（**不走 tree-sitter**，组件库也无 SQL grammar）；`TokenClass → 主题词汇`映射表见文件头（`Parameter → variable`，**`Identifier` 不上色**）；颜色由活跃 `HighlightTheme` 按名字解析（**零裸色值**，主题切换自动重着色） | 单测 **9 项**：图例覆盖 / Identifier 不上色 / delta 编码 / 中文按**字符**计列 / 跨行区间按行切分（LSP token 必须单行）/ 仅产出可视区间 / 大文件降级 / 未闭合词法不影响编辑；真机明暗核对随 1b |
| A5 | **模式判定与切换**：规则表（§1.2）+ 切换矩阵（§1.3，含确认对话框与内容变换）；能力表驱动 chrome | ✅ **全部完成**：`mode.rs` —— 判定（记忆 > 扩展名 > 文本）+ **切换矩阵**（`plan_switch` 纯函数：确认类型 / 内容变换 / 提示语；禁止静默切换）+ 内容变换（`sql_to_cells` 走 `engine::sql::split`、`cells_to_sql`、`cells_from_text` / `cells_to_text`，文本层分隔标记 `-- %%`）+ **四种确认的文案表**（`ConfirmKind::{title, body, confirm_label, picks_granularity}`）；视图：`EditorHostPanel::sync_mode` 按模式刷新（着色 / 只读）· **工具栏模式指示器**（`[SQL] ▾` → 菜单）· `request_mode_switch` → 需确认则弹 `dialogs::open_switch_confirm`，`confirm_mode_switch` 落地（先写回服务层再重载内核，内容只有一条真值路径） | 单测 **14 项**（矩阵逐项 + 粒度二选一 + 字符串内分号不被误拆 + 单元文本往返 + 空输入 + 四种确认文案不重复/不含“确定” + 粒度选择器只在“转单元”出现）+ 窗口测试 **4 项**：需确认的切换未确认前不过状态、确认后按所选粒度拆单元、文本→SQL 免确认、确认回调能独立驱动 |
| A6 | **只读两维度**：编辑器只读（文档属性）与连接只读（策略）分别表达 | ✅ **已完成**：`model::ReadOnly` 两字段 → 编辑内核 `.readonly()` + **状态栏分别显示**「只读」/「连接只读」（互不蕴含）；文档属性来自 `EditorService`，同一窗口四种组合并存不互相影响 | 单测：四种组合逐项断言（仅编辑器只读不等于连接只读，反之亦然）+ 窗口测试：切模式/渲染不 panic |
| A7 | **编辑器状态栏**（真实值）：Ln/Col、选区字数、语句数（来自 `split.rs`）、方言、编码/换行/缩进、模式、脏 | ✅ **已完成**：`view/widgets/status_bar.rs`（用组件库 `StatusBar`，不手搓）——文案计算是**纯函数** `labels()`（可穷举断言）；左：模式短标签 + **语句数** + 未保存；右：两个只读维度 + Ln/Col + 已选字数 | 单测 6 项：语句数随内容变（1/3/9）+ 脏可见 + 文本模式不显示语句数 + 四种只读组合 + 光标/选区真实值。**方言 / 编码 / 换行 / 缩进暂无数据源（1b / A12）→ 不显示占位**（零 UI 造数据） |
| A8 | **脏状态**：输入事件 → 与 baseline 比较 → 置脏/清脏；标签脏点与状态栏同步 | ✅ **已完成**：`service.rs` 的 baseline 比较 + `view/host.rs` 的 `InputEvent::Change` 回写；标签脏点由 `title_suffix` 渲染 | 单测：编辑→脏、撤销回原值→干净、保存→干净 |
| A9 | 保存 / 另存为 / 外部修改检测 / 关闭三态确认（保存失败二次确认） | ✅ **已完成**：`persist.rs`（读 / 写 / mtime 外部修改检测 / `open_file` / `save_document` / `save_as`）· `EditorHostPanel::{save, save_as, document_path, document_mode, clear_message}` · `WorkbenchView` 的 `editor_service` + `editor_hosts` + `open_in_editor` · **关闭三态**：`request_close_document` / `resolve_close_choice`（保存 / 不保存 / 取消，写盘失败再问一次）· **另存为**：路径选择端口（`EditorShared::attach_save_path_picker`；workbench 用 `rfd` 实现）· **对话框**：`view/dialogs.rs`（关三态 / 保存失败 / 模式切换）· **系统文件对话框**：`services/editor_files.rs`（打开是宿主动作、另存为是端口）+ 键位 `ctrl-o` / `ctrl-shift-s` | 单测 8 项（持久化）+ **窗口测试 6 项**（关三态真弹框 / 不保存不写盘 / 未命名保存后写盘才关 / 取消另存为不关 / 未接端口留原因 / 另存为标题跟随）+ `cargo check --workspace --all-targets` 零告警 |

#### A9 收尾：workbench 接线配方（2026-09-15 **API 已逐条核实**，照此执行即可）

1. **`crates/workbench/Cargo.toml`**：加 `editor.workspace = true`（依赖键已在根 `Cargo.toml` 登记为 `rds-editor`）。
2. **`crates/workbench/src/view.rs`**：
   - 导入：`editor::model::{DocumentId, EditorMode}` · `editor::service::OpenRequest` · `editor::shared::EditorShared` · `editor::view::host::EditorHostPanel`；
   - `struct WorkbenchView` 加两字段：`editor_service: EditorShared`（跨面板共享的文档集合）与 `editor_hosts: Vec<Entity<EditorHostPanel>>`（已开面板，按 `DocumentId` 复用）；
   - `WorkbenchView::new`：`editor_service: EditorShared::new()`，并初始打开一份未命名 SQL 文档（`open(OpenRequest::untitled("", EditorMode::Sql))`）；
   - `init_workspace`：**先把 `EditorShared` clone 到局部变量**（否则 `cx.new(|cx| …)` 闭包里借 `self` 会冲突），再建 `EditorHostPanel`；中央 tab 组用**链式** `DockLayout::tabs().panel_view(legacy_handle, cx).panel_view(host_handle, cx)`（`panel_view(mut self, Arc<dyn PanelView>, &App) -> Self`，`gpui-base/src/dock/layout/builder.rs:112`，已核实可链式）。
   - 新增 `pub fn open_in_editor(&mut self, path: PathBuf, window: &mut Window, cx: &mut Context<Self>)`：走 `editor::persist::open_file(&self.editor_service, &path, editor::mode::resolve_mode(&path, None))`；已打开的就按 `DocumentId` 在 `editor_hosts` 里找面板复用（不新建），新开的 `cx.new(|cx| EditorHostPanel::new(service, id, window, cx))` 后用 **`area.add_panel(panel, DockPlacement::Center, None, window, cx)`**（`gpui-base/src/dock/dock_area.rs:402`，已核实）加入中央 tab 组。
3. **命令与快捷键（A10）**：`Ctrl+S` → `EditorHostPanel::save`；`Ctrl+/` → 行注释开关；`Ctrl+W` → 关闭当前文档（**宿主执行**：`WorkbenchView::close_active_editor` → `close_document_in_dock`；面板自己发起会重入，见架构 §12 #23）。按项目规矩在 `crates/app/src/main.rs` 绑定 keymap（**注册了才宣传**）：已注册 `ctrl-s` / `ctrl-/` / `ctrl-w`（context `editor`）。`Ctrl+O` 待系统文件对话框落地后再注册。
4. **对话框（A9，已完成）**：`crates/editor/src/view/dialogs.rs`（关三态 / 保存失败二次确认 / 模式切换确认）与它们的落地入口 `editor::view::host::{request_close_document, resolve_close_choice, request_save_as}`；挂 `Root::render_dialog_layer`（workbench 根视图已有）。对话框要能弹出来，窗口的根视图**必须是 `Root`**（`Root::update` 找不到会 panic）——写窗口测试时要把夹具包在 `Root::new(...)` 里并渲染对话框层（见 `view/tests.rs` 的 `dialog_harness`）。
| A10 | Actions 与快捷键：`Ctrl+S` / `Ctrl+Enter`（最小执行=执行全部）/ `Ctrl+/` / `Ctrl+F` / `Ctrl+Shift+F` | ✅ **可独立完成的部分已完成**：`commands.rs`（Action 声明）· `edit.rs`（行注释纯函数，14 项单测）· 面板 `key_context("editor")` + `track_focus` + `on_action` · 状态栏“失败原因”段 · `close_document_in_dock`（宿主执行关闭）· app 层注册 `ctrl-s` / `ctrl-/` / `ctrl-w` / `ctrl-enter` / `ctrl-shift-enter`。**5 项真按键盘的窗口测试**（写盘 / 报原因 / 注释可逆 / 只读拒绝 / 宿主关闭）。`Ctrl+F` 归 A11（内核能力，见该行）；`Ctrl+Shift+F` 待 B10 格式化 | 真机：已注册键全部生效（**禁止“只宣传未注册”**）；单测层面已用 `simulate_keystrokes` 逐键验证 |
| A11 | 查找 / 替换（优先用组件能力，缺则自建） | ✅ **已完成：用组件能力，零自建**。`Ctrl+F` → 内核 `input::Search`、`Ctrl+H` → `input::Replace`（两者都已绑在 `Input` context 且有 listener），界面由组件库 `SearchPanel` 渲染为浮层；本 crate **不注册键位、不自建查找栏**。曾按错误判断自建一套（见变更日志），已全部撤除 | 窗口测试 2 项：`Ctrl+F` / `Ctrl+H` 后焦点被内核交给查找框（“面板真开了”的可观察信号）+ 渲染不 panic。**真机**：在编辑器里按 `Ctrl+F` 能看到查找面板、`Ctrl+H` 出现替换行 |
| A12 | **工作区上下文持久化**：光标/选区/模式/连接绑定落 `workbench_context_store`（扩展表字段） | ✅ **已完成**：engine 侧 `editor_contexts` 补 `mode` 列（新库建表带、老库幂等 `ALTER`）+ `load_latest_editor_context`；editor 侧 `session.rs`（端口 + `SavedSession`）+ 面板 `session_snapshot` / `restore_session` / `save_session_now`；workbench 侧 `services/editor_session.rs`（真 store）+ `restore_last_session`（`defer_in` 读库）。**保存时机（1a）**：`Ctrl+S` 成功后与关文档时（定时节流保存属 1b）· **恢复范围（1a）**：只恢复最近更新的那一份 · **连接绑定**仍缺（待 1b，与 §12 #26 同一件事） | 集成测试 3 项（真 SQLite）：关闭→重开恢复光标与模式 · 未命名文档不写库 · 会话 id 是路径键 |
| A13 | 大文件档位：>50MB 关补全/折叠、>200MB 只读提示卡（分块加载推到 1b 后） | ✅ **已完成（“关折叠”除判据外待内核开关）**：`limits.rs`（纯函数 `tier_for_size` + I/O 层 `tier_for_path`；7 项单测含**真 200MB 稀疏文件**）· `Document.tier()` + `OpenRequest::with_tier`（≥200MB 置编辑器只读）· `persist::open_file` 按实际大小定档且**超大文件不读进内存** · 面板提示卡（`warning` 描边）。⬜ “关折叠”需要内核开关（`gpui-base` 0.6.1 未暴露）——判据 `disables_folding()` 已立，接线待内核 | 单测：边界（49.9 / 50 / 199.9 / 200 MB）· 窗口测试 3 项（超大：只读 + 内容长度 0 + 提示卡；大：可编辑 + 提示；常规：无提示卡） |
| A14 | 最小执行 + 基础结果网格（`DataTable`）：执行全部 → 结果集 → 表格 + 行数/耗时 | ✅ **已完成**：`execution.rs`（目标解析 / `QueryRunner` 端口 / `ExecChannel` 工作线程 + 轮询）· `store.rs`（`ResultStore` 结果单权威）· `view/widgets/result_grid.rs`（组件库 `DataTable` + `TableDelegate`）· 面板两个执行动作 + 结果区 + 状态栏「执行中…」· workbench `services/editor_exec.rs` 注入引擎执行器。⬜ 余：结果区分栏可拖拽 · 多结果标签 · 中断/超时（1b） | 集成测试：执行 → 结果入库 → 网格（真按键 + 假执行器，4 项）；**真机四库各一次 SELECT 已实测通过**（`editor_exec_real.rs`：mysql/postgres/sqlite/duckdb） |
| A15 | 契约与回归：`ui_contract` 扫描范围扩到 `crates/editor/src` | ✅ **已完成**：`crates/workbench/tests/ui_contract.rs` 的尺寸/颜色两份扫描表都加入 editor 的四个视图文件（`view/host.rs` · `widgets/status_bar.rs` · `widgets/result_grid.rs` · `view/highlight.rs`）；顺手清掉最后一处 `.px(rems(…))`（改为 `px_2()`）与对应常量 | `cargo test -p rds-workbench --test ui_contract -j 2` **5 项全绿** |

**1a 验收口径**：能新建/打开/编辑/保存 `.sql` 与 `.txt`；SQL 有高亮与真实状态栏；模式可判定可显式切换；`Ctrl+Enter` 能跑通一次查询并看到表格结果。

---

### Phase 1b — SQL 执行闭环（DBeaver 一档）

**目标**：把"执行"这件事做合格——目标可选、能中断、有事务、有历史、结果可操作、错误能定位；并对外开放自动执行接口（关闭 M4 遗留项）。

| # | 任务 | 落点 | 验收 |
| --- | --- | --- | --- |
| B1 | 连接绑定与切换：选择器（P/G/GP 短码 + 运行态）、自动建连（同 M4 口径）、失败可读原因 | ✅ **已完成（2026-09-16 切片一 / 2026-09-18 收尾）**：`connection.rs` 端口 + `Document.connection` + 执行通道带连接（`QueryRunner::run(conn, sql)`）+ 工具栏「连接 ▾」（菜单打勾 / 选后自动建连 / 失败不绑定并留原因 / 「跟随当前连接」项）+ 状态栏连接段 + workbench `services/editor_connections.rs`（列表取内存快照、建连走 `nav_runtime`）+ 「新建查询」带当前选中连接 + **绑定随会话持久化**（`editor_contexts.connection_id` 写真值；恢复时校验还在不在列表里，不在就回退并说原因——见 §0）。⬜ 余：只读锁 ⓘ（要连接侧写策略来源，与 A6 的连接只读维度合流；“提醒后放行”那档随它一起做） | 窗口测试：绑定→状态栏可见 · 建连失败不绑定且原因可读 · **绑定的连接真的到了执行器**（真按键路径）· 选择器只在 SQL 模式出现 · 绑定跨重启（✅ 真 SQLite） |
| B2 | **执行族**：执行当前语句（用 `split.rs` 定位）/ 选区（有选区优先）/ 全部 / 批量（逐条独立） | ✅ **已完成（2026-09-16）**：`ExecTarget::Batch` + `batch_target()`（词法级切分，逐条独立）· `ExecMenuKind::ALL` 五项（含批量与新结果标签，后者复用主按钮的“选区优先”目标）· `ResultPlacement{Replace, NewSet}` 决定结果落位 · 通道按**job**粒度执行：一条 job 多条语句顺序跑、逐条回填、失败不中断 | 单测：目标解析（光标位置 × 选区 × 空文档 × 批量切分）· 集成：批量 3 语句 → 3 结果集，含 1 失败不中断（窗口测试 + 真机 sqlite/duckdb） |
| B3 | 取消与超时：中断按钮 + `Ctrl+Break` 等价入口；超时按连接的 `timeout_ms` | ✅ **已完成（2026-09-16）**：**入口 = 状态栏 `■ 中断`**（原型 §5.1 给的就是状态栏 ■；`Ctrl+Break` 在 GPUI / 本机键盘上不可得，「等价入口」就是它）+ 耗时累加 `执行中 3.4s…`；`QueryRunner::cancel` 三态可读（默认实现明说“不支持中断”）· 中断在一次性线程上做、回执不吞 · 批量中断后剩余语句标“已取消”且**不再发给驱动** · 超时按连接的 `query_timeout` 交给引擎（到点自动取消 + 可读错误；连接没配就是没有超时） | 真机：长查询中断返回可读错误；超时返回可读错误（`editor_exec_real` 两条断言，sqlite / duckdb 已过；mysql 受驱动 RSA feature 阻塞，见依赖侧待办） |
| B4 | **事务**（依 P0.2 结论）：真实状态机 + 状态栏 TX 区（开启时长 / 提交 / 回滚 / 自动提交开关） | ✅ **已完成（2026-09-16）**：事务对象挂 `ConnectionManager`（持物理连接）· `begin/commit/rollback` 走驱动接口（修好 MySQL 1295）· 状态读真值 · 事务内语句走 `query_in_transaction` · 自动提交关 → 执行先自动开事务（DDL 除外）· 状态栏 TX 区（未开启 / 已开启 + 时长 + 自动提交开关 + 提交/回滚；只在执行器支持事务时出现）· 事务状态随**执行结论**回填（不额外问一次） | 集成：begin → insert → rollback → 数据未变；commit 生效（**真机 postgres / sqlite / duckdb 已过**）；跨语句事务状态一致（窗口单测 + 真机快照） |
| B5 | 结果区完整化：结果集标签条（上限 5 + 淘汰 + **通道徽标 + 血缘摘要**）、工具栏（行数/耗时/连接/**筛选 + 下发开关**/**分析**/导出/刷新/复制）、截断提示、`affected_rows` 展示 | ✅ **已完成（2026-09-17；另见首行“对齐原型 §2.4”）**：标签条（B2）+ **影响行数**（引擎侧六个驱动全给真值）+ 结果工具栏（分段 `行数 │ 耗时 │ 连接名` + 复制 / 刷新）+ 状态行（`共 N 行` · `已选` · 截断）+ 可拖拽分栏 + 网格（`#` 行号槽 / `NULL` 斜体）。⬜ 余：标签的**通道徽标**（B13）与**血缘摘要**（B14/B15）；⑥ 的筛选 / 下发开关 / 分析 / 导出各自随所属步骤落地（**没实现就不摆按钮**） | `view/results/sets.rs` + `results/grid.rs` + `store.rs` | 窗口/真机：滚动流畅；DML 显示影响行数（✅ 六驱动）；标签显示通道与血缘（⬜ B13 后） |
| B5b | **分段抓取**（已确认）：数据源按“已抓取窗口 + 取下一段”实现（`known_row_count() -> Option<u64>` / `window()` / `fetch_next(limit)` / `rows(offset,limit)`），固定 **1000 行/段**起步，未知总数显示 `N+`；导出区分“仅已抓取”与“抓全量” | ✅ **已完成（2026-09-17，见首行）**：引擎侧 `window_sql` + `execute_segment`（真机六驱动三段不重不漏）· 编辑器侧首段 `SEGMENT_ROWS = 1000` + ⑦ 状态行 `共 1,000+ 行` + 「取下一段」（`ExecTarget::Segment` + `ResultPlacement::Append` 接进选中那份，不新开结果集；失败不动已抓到的行）· **包装不对外可见**（错误位置与回显 SQL 还原成用户原句，SQLite 驱动补结构化位置）。⬜ 余：导出两种语义（属 B7）· ~~滚动到底自动加载~~（**已完成**：原生 `has_more` / `load_more`，2026-09-17 见首行） | `store.rs`（窗口状态）+ `view/results/grid.rs`（“取下一段”入口与 `N+` 展示）+ `execution.rs`（目标与落位）+ `services/sql_service.rs` | 集成：1200 行表 → 首段 1000 行 + 状态行 `1,000+`；取下一段追加不重复（**真机六驱动已过**）；导出两种语义行数一致（测试场景 37–39，随 B7） |
| B6 | 错误回填：错误位置解析（多家驱动格式）→ 诊断 + 定位 + 聚焦；无位置则只提示 | ✅ **已完成（2026-09-17；错误卡片另见“对齐原型 §2.4”那一行）**：`diagnostics.rs`（纯函数解析，认不出就不给位置）+ 引擎侧 PG 两条路径的结构化位置与真实消息 + 面板「诊断 / 定位 / 聚焦」三件事 + 错误卡片（`Alert::error` + 定位 + 复制）。⬜ 余：无位置时只提示已经做到；其余驱动的结构化位置（文本里已带，暂不重复做） | `execution.rs` + `view/` + `driver/utils.rs` | 单测：≥8 种真实错误文本（✅ 10 类）；真机：故意写错列名可定位（✅ 六驱动） |
| B7 | **导出**：CSV / JSON / INSERT / Parquet / XLSX（后者经 DuckDB 临时表） | ✅ **两切片全完成（2026-09-17 / 2026-09-18，见 §0）**：① 切片一 = CSV / JSON / INSERT 三格式 + 两档范围（仅已抓取 / 抓全量后导出）+ 路径选择端口（宿主 `rfd`）+ 默认名从 SQL 猜表名（抓全量是**多轮取段**，失败 / 切结果集 / 提交被拒不落半截文件）；② 切片二 = **Parquet / XLSX 经 DuckDB `COPY … TO …`**（`export_rows_via_duckdb`）——临时表用完即删、XLSX 显式 `LOAD excel`→`INSTALL excel`→`LOAD excel` 并把扩展目录写进失败原因、**必须 `HEADER true`**（不然首行数据会被当成列名）、**后台线程 + 回执**（首次装扩展要联网几秒，不堵 UI，不占执行位）、筛选跟随且明示。⬜ 余：列类型化输出（CSV / JSON 仍是展示文本；Parquet / XLSX 由引擎推断，数字已是数） | `editor/src/export.rs` / `execution.rs`（后台导出）/ `view/host.rs` + `engine/src/services/execution_service.rs` + `engine/tests/duckdb_export_probe.rs` | 单测 368 / 102 / 437（✅）；Parquet 真机往返（✅）；XLSX 写出 + 读回（✅，依赖 `excel` 扩展可装） |
| B8 | **历史面板**：右 Dock `History` 实装（列表 / 搜索 / 重放 / 删除 / 清空），字段真实 | `view/history.rs` + `engine::history_store` | 集成：执行 3 次 → 历史 3 条且耗时>0；失败也留痕 |
| B9 | **补全**：上下文判定 + 排序 + 元数据缓存 + 降级（关键字/函数）+ `Ctrl+Space` + 模板片段 | ✅ **两切片全完成（2026-09-18，见 §0）**：① 切片一 = 打字即补（`is_completion_trigger`：标识符与 `.` 触发）· 上下文判定与候选挑排是纯函数 · 候选取自**导航侧 L2 缓存**（`database::cache::NavCache`，后台预载 + 内存读；按通道给限定名，联邦只给已挂上的源）· 零自建下拉（内核 `CompletionProvider` + 弹层 + 键盘选择；**只给 `label`**，否则内核会把替换范围改成光标处）；② 切片二 = **`Ctrl+Space` 手动触发**（与打字那条**同一份候选**；`word_start` 算替换范围；文本模式 / 只读 / 超大文件三档拒绝理由分开说）· **模板片段**（「⋯ 更多 ▾ ▸ 插入模板」→ 插到光标处并选中 `{table}` 占位符；宿主启动时从 `sql_template_store` 预载）。降级：未接端口 / 未绑定连接 / 缓存未命中 → 只给关键字 + 函数（如实）。⬜ 余：缓存未命中时回落实时内省 · 候选超过 60 条时的翻页 | `crates/editor/src/completion.rs` + `view/completion.rs` + `view/host.rs` + `shared.rs` + `commands.rs` + `workbench/src/services/editor_completion.rs` + `app/src/main.rs` | 单测 377 / 104（✅）· 真按键 `Ctrl+Space` 弹层开着且候选正确（✅）· 未绑定连接不给候选（✅）· 插模板后选中 `{table}`（✅） |
| B10 | 格式化 / 转译 / 执行计划接线（用 P0.3 的格式化实现；EXPLAIN 按方言生成） | ✅ **已完成（2026-09-18；三个切片见 §0）**：① 格式化 = `format_with_report` 按语句区间**原位回填**（区间外一个字节不动）+ `editor/src/format.rs` 的 `plan` + 工具栏「格式化」与真注册的 `Ctrl+Shift+F`；② 方言转译 = 脚本骨架（`engine/src/sql/script.rs`）+ `transpile_with_report`（**先切分再逐条转译**，脚本不丢语句）+ `editor/src/translate.rs` 的 `plan` / `targets_for` + 「⋯ 更多 ▾ ▸ 转译为」；③ 执行计划 = `explain_sql`（按方言生成前缀；SQL Server / Oracle 如实不支持）+ **按通道取方言**（源库档源库的、加速 / 联邦档 DuckDB 的）+ 落新结果集并贴「执行计划」标题。方言一律**只从 `ConnectionOption.db_type` 来**（转译认不出就不猜） | `engine/src/sql/{formatter,script,transpiler,explain,engine}.rs` + `engine/src/driver/utils.rs` + `engine/src/services/sql_service.rs` + `editor/src/{format,translate}.rs` + `view/host.rs` + `app/src/main.rs` | 格式化结果可往返解析（✅ 单测）· **脚本一条不丢**（✅ 两侧回归）· 真机：格式化与执行计划在**六个内置驱动**通过（✅）；**硬约束（已实测，§12 #19）**：`transpile` 对脚本会**静默丢弃**第二条及以后——本实现先切分再逐条转译，并有“单条接口确实会截断”的对照测试盯着 |
| B11 | 对外接口：`open_sql(conn_id, sql)` / `execute_all(...)`（供 M4「在 SQL 编辑器中打开 / 查看数据」自动执行） | ✅ **已完成（2026-09-16）**：`Shared::request_query(QueryRequest{conn_id, sql, run})`（私有字段 + 方法对，与 `open_file_request` 同口径：生产端拿不到 `Window`）· 宿主 `WorkbenchView::open_query_document`（**复用条件很窄**：未命名 + 内容空 + 同一绑定；否则新建；带 SQL 时**追加**不覆盖；`run = true` 则调 `EditorHostPanel::run_all`）· 导航四处改调（在 SQL 编辑器中打开 / 查看数据 / 生成 SQL / 拖拽；前两者的 `SidebarEvent` 变体已删） | 窗口测试：`run_all` 与 `Ctrl+Shift+Enter` 同路且空文档不打扰执行器 · `ui_contract` 7 项 · `dialog_host_layer` 4 项全绿 |
| B12 | 移除遗留：删除 `EditorPanel` 的 SQL 区块与内联执行闭包；连接详情卡/导航树按架构 §3.3 迁出 | ✅ **SQL 区块已删（2026-09-16）**：旧 `panels/editor.rs` 的 Textarea + 内联执行闭包 + 结果表 + 历史列表 + `use_duckdb_fed` 门控（约 250 行）全删，连同其状态与 `apply_nav_drag`/`insert_sql`/`clear_sql`；**SQL 编辑器只剩 `crates/editor`**；M1 未保存草稿拦截改看“未命名编辑器文档”（`EditorShared`，不再经 `Shared` 镜像字段）。⬜ 余：连接详情卡 / 导航树本体按 §3.3 迁出（属 P2 视图下沉）· ~~旧路径的“项目只读模式拒绝执行”~~ **已在 2026-09-18 补回**（`editor/src/project.rs` 端口 + 提交前拒写；见本表首行） | `cargo check --workspace --all-targets` 零告警；`Shared` 字段 27 → 23（`editor_dirty`/`editor_sql`/`editor_clear`/`result_epoch` 四个退出历史） |
| B13 | **执行通道**：工具栏「执行位置」指示器（源库 / 本地加速 / 联邦）+ `channel_status(conn_id) -> Result<(), Reason>` 可用性判定 + 不可用原因 + 通道随文档持久化 + 切通道失效提示 | 🟡 **切片一 / 二 / 三前半已完成（2026-09-17 ~ 09-18，见 §0）**：切片一 = 三档状态 + 门控端口 + 随会话持久化 + 切通道旧结果标灰与提示 + 提交前拒写；切片二 = **加速档真接通**（`duckdb/accel.rs`：按源缓存专用 DuckDB 连接 · `ATTACH … READ_ONLY` · `USE` 解析表名 · 四类执行全分流 · 门控真值含扩展可用性）；切片三前半 = **「重新挂载源库」入口**（旁路线程 + 回执，不占执行位）+ **历史带通道正规字段**（`db_type` 与 `channel` 两个维度）。⬜ 余：联邦外部源注册入口（菜单项“注册新源…”）· 执行计划按通道（归 B10）· 加速会话临时表与分析会话（1c）共享 | `crates/editor/src/{channel,execution,history}.rs` + `view/host.rs` + `services/{editor_channels,editor_exec}.rs` + `engine/src/duckdb/accel.rs` + `engine/src/persistence/history_store.rs` | 集成：三通道各跑一次（🟡 源库 + 加速已通，联邦待接）· 未开加速的连接下拉置灰并给原因（✅）· 写语句在加速通道被拒（✅ 双保险）· 切通道后旧结果标灰 + 提示（✅）· 源库新表重挂后可见（✅ 真机） |
| B14 | **筛选下发源库**：筛选框 + `▢ 下发源库` 开关 → `re_execute_with_filter` → **新结果集**（lineage = 原结果 + 条件 + 通道） | ✅ **已完成（2026-09-17；四个切片见 §0）**：本地筛选与本地排序 · 下发筛选（引擎改写 + 方言差异在宿主 + 新结果集 + 去 LIMIT 提示）· 表格右键菜单（按值筛选 / 复制此值 / 冻结列用原生 `Column.fixed`）· **排序下发**（`rewrite_with_order`，右键两项）。⬜ 余：值预览弹层与值编辑 · 血缘摘要（依赖 B13 通道信息） | `execution.rs` + `engine/src/sql/filter.rs` + `view/results/grid.rs` + `services/editor_exec.rs` | 集成：本地筛选不重查（✅）· 下发（筛选 / 排序）产生新结果集且原结果保留（✅ 真机六驱动）· 含 `LIMIT` 的原查询给出提示（✅ 状态栏） |
| B15 | **DuckDB 分析入口**：结果集「分析 ▾」→ `execute_duckdb_analysis` → 新结果集（lineage = 临时表 + 分析 SQL）；支持“桥接当前可见行” | ✅ **切片一 / 二 + 血缘已完成（2026-09-18，见 §0）**：① 切片一 = `editor/src/analysis.rs`（纯模型：`AnalysisRequest` / 预置菜单 / 桥接 + 上限 20000 / 结果条目）+ 工具栏「⚗ 分析 ▾」+ `ExecTarget::Analysis` 与 `ExecOutcome.analysis` + 宿主 `editor_exec.rs::analyze`（内存 DuckDB，`{table}` 由引擎替换）+ 分析结果带标记（**不摆「⟳ 刷新」**）；② 切片二 = 「自定义分析 SQL…」对话框（`dialogs::open_analysis_sql`，`TextareaState` 实体 + 预填能跑的模板 + 说明里带“基于已抓到的 N 行”）+ 空白在本地就拒；③ **血缘摘要**：`ExecTarget::lineage()` → `ResultEntry.lineage` → 工具栏左段（排在连接之前；执行计划 / 分析用标题当来源）。**口径**：分析**不重跑源库**，桥接的是已抓到的行（`NULL` 字面量 → 真 NULL，其余按展示文本；超上限截断并如实说明）；落新结果集且**不抢用户正看的那份**（与 B2/B10 同一落位口径）。顺带修 `duckdb_service::query_duckdb` 的列名取法（duckdb-rs 1.10505 的 `column_name` / `schema()` 读的是**执行结果**，prepare 后未执行就 panic → 先 `query` 再经 `Rows::as_ref()` 读 `column_names()`）。⬜ 余：沉淀为分析单元（1c） | `editor/src/analysis.rs`（新）+ `{store,execution,ui,view/{host,dialogs,tests,results/grid}}.rs` + `engine/src/services/duckdb_service.rs` + `workbench/src/services/editor_exec.rs` | 单测 364 / 100 / 436（✅）；桥接行真跑内存 DuckDB（✅ 含 NULL 语义）；对话框与落地入口各一组窗口用例（✅） |
| B16 | **接住 Dock 标签关闭入口**（A9 尾巴）：让所有关闭路径都不静默丢改动；顺带把工具栏按原型 §2.2 分层 | ✅ **主体已完成（2026-09-16）**：`closable(cx) = !is_dirty()`——组件库的 TabGroup 没有每标签 ✕，`⋯` 菜单的 `Dock.Close` 按**当前标签**的该判据显隐，因此脏文档那里没有关闭项（静默丢与“点了没反应”两种坏结果都避开了）；关闭脏文档的唯一入口是 `Ctrl+W` → `request_close_document`（三态）· **新建入口**：Quick Open 三条命令 → `new_editor_document(mode)` · **工具栏**：模式指示器 + SQL 模式的 **执行 ▾**（`DropdownButton` 分体按钮；菜单项 = 当前语句 / 选区 / 全部，纯函数 `statement_target` / `target_for_menu`）。⬜ 余：格式化 / 历史 / ⋯更多 / 执行位置 / 连接随 B10 / B8 / B13 / B1 落地（**未实现就不放**）· 自绘标签条（能在 ✕ 上弹确认）仍属可选后续 | 窗口测试：脏文档 `closable == false` 且脏点仍在、保存后恢复可关 · 工具栏按模式分层（文本 / 分析不放执行）· 单测：菜单三项目标各自独立 |

**1b 验收口径**：达到"合格 SQL 编辑器"——执行族齐备、可中断、有事务与历史、结果区可过滤可导出、错误可定位、补全可用。

---

### Phase 1c — 分析模式骨架（Cell / Output / Session）

**目标**：`.rdsnote` 笔记可选连接、可编辑单元、可执行 SQL 单元并看到内联输出；Python/Rust 只留扩展位。

| # | 任务 | 落点 | 验收 |
| --- | --- | --- | --- |
| C1 | 模型：`NoteBook` / `Cell` / `Output` / `run_seq` / stale 判定（纯函数） | `crates/editor/src/notebook.rs` | 单测：插入/删除/移动后 id 稳定；stale 规则（改源/会话重启/依赖变化） |
| C2 | `Session` trait + `SqlSession`：连接 + DuckDB 分析命名空间 + `CREATE TEMP TABLE` 作为变量 | `session.rs` | 集成:临时表创建后，后续单元可 `SELECT * FROM t` |
| C3 | 笔记视图：笔记头（会话选择器 / 全部运行 / 重启会话 / 清空输出）+ 单元卡片（类型/执行/⋮/折叠/stale）+ 插入点 | `view/notebook_view.rs` | 窗口测试：3 单元渲染、执行态切换、插入与排序 |
| C4 | 内联输出：`Table`（预览 + 展开）/ `Text` / `Error`（含定位） | `view/widgets/output_block.rs` | 窗口/真机：三种输出可见；"展开"切到结果面板（同一结果集） |
| C5 | 键盘：`Shift+Enter` / `Ctrl+Enter` / `Alt+Enter` / `Ctrl+Shift+Enter` / `Esc` 单元焦点模型 | `commands.rs` + 视图 | 真机：全键位可用（与 `editor-notebook` context 匹配） |
| C6 | `.rdsnote` 读写：单元 + 元数据 +（可选）输出引用；与项目版本链对接留接口 | `persist.rs` | 集成：写→读往返一致；输出引用可失效并提示 |
| C7 | 模式转换：SQL → 分析（整篇单单元，含确认）/ 分析 → SQL（拼接脚本） | `mode.rs` | 窗口测试：两条转换路径 + 确认分支 |
| C8 | Markdown 单元：渲染态 / 编辑态切换 | `view/notebook_view.rs` | 真机：双击进编辑、失焦渲染 |
| C9 | **分析沉淀**：结果区的 DuckDB 分析可一键沉淀为分析单元（单元源 = 分析 SQL，输出 = 结果集，临时表 = 会话变量） | `notebook.rs` + `view/notebook_view.rs` + `execution.rs` | 窗口/真机：从结果区发起分析 → 生成单元 → 下游单元可引用该临时表 |

**1c 验收口径**：能创建 `.rdsnote`、选连接、写多个 SQL/Markdown 单元、`Shift+Enter` 依次执行并有内联输出、重启会话后全部标 stale、保存后重开内容与结构一致。

---

### Phase 2 — 后续（本次不排期）

| 主题 | 内容 | 前置 |
| --- | --- | --- |
| Python 内核 | sidecar（复用 `plugin/src/sidecar`）+ `ipykernel` + Jupyter 消息协议；单元 `PY` | 1c 的 `Session` 接口固化 |
| Rust 内核 | WASM 沙箱（wasmtime/extism，无外部工具链）或单元级 `cargo script`；需进度/取消/缓存 | 同上 |
| 变量桥 | 会话间以 **Arrow** 交换（SQL 临时表 ↔ DataFrame ↔ Rust） | Python/Rust 内核就位 |
| 富输出 | `Chart`（`component::chart`）/ `Insight`（M8）/ 输出导出 | 1c 输出注册表 |
| 互操作 | 导入/导出 `.ipynb`（nbformat 4.5） | 1c 模型稳定 |
| 其他 | 参数绑定（prepared statement）、拖拽插入限定名、`minicatalogs`、变量浏览器、使用手册 | — |

---

## 3. 测试场景清单

**Phase 0（后端）**
1. 切分：单语句 / 尾分号 / 多语句 / 空语句 / 字符串内分号 / 行注释 / 块注释 / `$$` 块 / 反引号标识符 / 未闭合引号（不 panic，报错）
2. 历史：成功记录耗时>0、失败也写并含原因、行数/影响行数正确、`db_type` 非 `unknown`
3. 执行结果结构：SELECT 无 `affected_rows`、DML 有

**Phase 1a（编辑体验）**
4. 模式判定：`.sql`→SQL、`.rdsnote`→分析、`.md`/`.txt`/未知→文本、入口语义覆盖、显式记忆优先
5. 切换：文本↔SQL 内容不变；SQL→文本 显示"结果标灰"提示；切换确认可取消
6. 脏状态：输入即脏、撤销回原值干净、保存干净、标签脏点与状态栏同步
7. 关闭：三态确认（保存/丢弃/取消）+ 保存失败二次确认
8. 外部修改：磁盘被改后保存 → 三态（重载/覆盖/取消）
9. 只读：编辑器只读不可输入；连接只读可输可执行但写语句被拦
10. 持久化：关闭重开恢复光标/选区/模式/连接绑定
11. 大文件：>50MB 关补全；>200MB 只读卡
12. 快捷键：`Ctrl+S` `Ctrl+Enter` `Ctrl+/` `Ctrl+F` `Ctrl+Shift+F` 全部生效

**Phase 1b（执行闭环）**
13. 目标解析：无选区执行当前语句（光标在中间语句）/ 有选区执行选区 / 全部 / 批量
14. 批量：3 条语句、第 2 条失败 → 3 个结果集且失败标红，其余成功
15. 取消 / 超时：长查询中断；超时给可读错误
16. 事务：begin→insert→rollback 数据不变；commit 生效；状态栏状态与时长正确
17. 结果区：10 万行滚动流畅；快速过滤生效；上限 5 淘汰最旧；截断提示
18. 导出：CSV/JSON/INSERT/Parquet/XLSX 落盘可回读（✅ 2026-09-17 切片一：CSV / JSON / INSERT 真写盘且断言内容；Parquet / XLSX 随切片二，依赖 DuckDB 扩展安装路径的结论）
19. 历史：执行 3 次（含 1 次失败）→ 面板 3 条且字段真实
20. 补全：`FROM` 后表、`t.` 后列、无连接降级为关键字+函数
21. 错误定位：故意写错列名 → 定位到行；无位置信息时不误报第 1 行

**Phase 1c（分析模式）**
22. 单元生命周期：插入/删除/移动后 id 稳定、输出不错位
23. 会话变量：单元 1 建临时表 → 单元 2 可查；重启会话后全 stale
24. stale：改单元 1 源 → 自身与下游标 stale；重跑后清除
25. 持久化：`.rdsnote` 写读往返一致；输出引用失效时给出提示
26. 转换：SQL→分析（整篇单单元）内容不丢；分析→SQL 拼接脚本

**跨阶段**
27. 主题：明暗切换下编辑区/结果区/单元卡/输出区对比度（`theme-preview.html` 与交互稿为基准）
28. 契约：`ui_contract` 扫描 `crates/editor/src` 零裸色值、零裸 `px(`
29. 无项目：工作台覆盖选择器时编辑器不额外报错

**通道与结果血缘（2026-09-15 修订新增）**
30. 通道门控：未开本地加速的连接 → 执行位置中加速项置灰且行尾有原因；联邦无源时给“注册新源…”入口（✅ 2026-09-17/18：置灰 + 行尾原因已落实，且**只报第一条过不去的条件**（连接开关 → 驱动类型 → 扩展可用性）；扩展**没试过不拦**、试过失败才拦并显示原话；“注册新源…”入口随切片三）
31. 通道切换：源库 → 加速后旧结果集标灰 + 顶部提示；分析模式下全部单元标 `stale`（✅ 2026-09-17：旧结果保留 + 标签缀 `·旧` 转静色 + 顶部一行“旧结果来自 X”；分析模式的单元 `stale` 随 1c）
32. 通道写限制：加速通道下 `INSERT` 被拒并提示切回源库（不得落本地副本）（✅ 2026-09-18：**两道闸**——编辑器在提交前就拒（判据在引擎语句类型上）、DuckDB 侧 `READ_ONLY` 再兜一次（真机三种源都验过）；本地临时对象仍放行）
33. 筛选本地 vs 下发：本地筛选不重查（无新结果集）；下发产生新结果集且原结果保留（✅ 2026-09-17 前半：本地筛选只改视图行序、数据行一行不动；⬜ 下发随 B14 后半）
34. 重查包裹语义：原查询含 `ORDER BY` 时结果仍有序；含 `LIMIT` 时给出“将去掉 LIMIT”提示（✅ 2026-09-17：`ORDER BY` 提到外层（引擎单测）；去掉 LIMIT 的原文进状态栏；真机六驱动“带 LIMIT 也筛得到第 3 行”）
35. DuckDB 分析：对结果集 COUNT / GROUP BY → 新结果集（血缘 = 临时表 + 分析 SQL），原结果保留
36. 分析沉淀（1c）：结果区分析 → 生成单元 → 下游单元引用该临时表；重启会话后 `stale`

**分段抓取（2026-09-15 新增）**
37. 首段与未知总数：>10,000 行的表 → 初始抓 1000 行、状态行显示 `1000+`；“取下一段”追加且不重复、不跳行（✅ 2026-09-17 真机六驱动：1200 行表首段 1000 → 取下一段接成 1200，接缝 `1/1000/1001/1200` 逐个断言；`ORDER BY` 不稳时的重复/跳过是窗口重跑的固有取舍，界面上要说明——随 B15）
38. 导出语义：区分“仅导出已抓取”与“抓全量后导出”——提示文案与实际落盘行数一致
39. 抓取中取消：取下一段过程中取消 → 已抓取部分仍可用于筛选/排序/导出（不丢已取数据）（✅ 2026-09-17：失败/取消不新开结果集、不动已抓到的行已落实；筛选与导出的那一半随 B15 / B7）

---

## 4. 风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| Dock 标签无法满足关闭拦截（P0.1） | 1a 需自绘标签条（+约 300 行，且与 Dock 视觉重复） | Phase 0 先验证；不成立则采用"自绘标签条 + Dock 仅作容器"并记录到架构 §12 |
| 事务会话亲和不可得（P0.2） | 事务功能只能"看起来能用" | 验证先行；不可得则引入 per-session 独占连接并评估对池化的影响 |
| 格式化选型失败（P0.3） | 1b 的"格式化"缺口 | 退路：只做轻量缩进与关键字大写（不做完整重排），并在 UI 明示能力边界 |
| SQL grammar 与主题角色不匹配（P0.8） | 高亮角色缺失或颜色异常 | 缺角色复用最接近标准 token（零裸色）；必要时补产品语义 token |
| editor 独立 crate 引入循环依赖 | 编译结构被破坏 | 依赖方向固定为 `workbench → editor → engine/database/shared`；editor 不得依赖 workbench（架构 §3.1） |
| 与 M4/M5 的接口漂移（`editor_set` / 草稿箱打开文件） | 收编时改动面扩大 | 契约先行（架构 §3.4），1b 的 B11/B12 一次性切换并在文档中更新映射 |
| 1c 的 `Session` 抽象过早固化 | 第二期 Python 内核接入时返工 | 抽象只保留"执行/中断/状态/命名空间"四件事；Python 相关字段（ZMQ/内核握手）**不进第一版接口** |
| 输出引用（DuckDB 临时表）生命周期 | `.rdsnote` 重开后输出失效 | 输出块显式区分"可展开的真实数据"与"已失效（重跑即可）"；默认不持久化全量输出 |
| 现有 `EditorPanel` 迁出牽连 M3/M4 宿主职责 | 迁移期功能回归 | 按架构 §3.3 逐项迁出，先迁编辑器部分；每步跑 `-p rds-workbench` 全测 |
| **通道可用性判定无来源**（架构 §12 #13） | UI 给不出准确的“加速不可用原因” | 1b 前定义 `channel_status`；暂无来源时按“未注册 Secret”降级提示，并在文案里说明是推断而非事实 |
| **重查包裹语义**（含 `ORDER BY` / `LIMIT`） | 重查结果与原查询不一致，用户困惑 | 定规则 + 显式提示（测试场景 34）；必要时提供“重查后保留 LIMIT”开关 |
| **ATTACH 快照新鲜度** | 用户拿旧快照做决策 | 结果集徽标 + 状态栏“快照” + “重新 ATTACH”入口（原型 §5.7） |
| 通道/血缘增加结果集数量 | 上限 5 与淘汰策略被更快触发 | 淘汰时提示来源；后续评估“按通道分组淘汰” |
| 编译时间与体积增长（新增 grammar 等依赖） | 开发体验 | P0.9 记录基线；新依赖一律进 workspace 唯一入口表并评估必要性 |

---

## 5. 实现位置映射（设计决策 → 代码）

| 设计决策 | 代码文件 |
| --- | --- |
| 文档/模式/只读/能力表 | `crates/editor/src/{model.rs, mode.rs}` |
| 唯一执行入口 + 后台任务 | `crates/editor/src/execution.rs`（模式参考 `crates/workbench/src/services/nav_jobs.rs`） |
| 语句切分（词法级） | `crates/editor/src/split.rs`（替换 `engine/src/services/sql_parser_service.rs::split_sql`） |
| 结果单权威 + 上限淘汰 | `crates/editor/src/store.rs` |
| 补全 | `crates/editor/src/completion.rs` + `crates/database/src/metadata_service.rs` |
| 会话与单元 | `crates/editor/src/{session.rs, notebook.rs}` |
| 持久化（工作区上下文 / `.rdsnote`） | `crates/editor/src/persist.rs` + `engine/src/persistence/workbench_context_store.rs` |
| 三模式视图 | `crates/editor/src/view/{host,text_mode,sql_mode,notebook_view}.rs` |
| Action 与快捷键 | `crates/editor/src/commands.rs` + `crates/app/src/main.rs` |
| 宿装配（中央区） | `crates/workbench/src/view.rs::init_workspace` |
| 历史字段 / 事务 / affected_rows | `engine/src/{persistence/history_store.rs, services/sql_service.rs}` |
| 格式化实现 | `engine/src/sql/formatter.rs` |
| SQL 高亮注册与主题角色 | `crates/editor/src/view/` + `assets/themes/{rds-theme.json, product-tokens.json}` |
| 尺寸常量 | `crates/editor/src/ui.rs` |
| 执行通道与门控 | `crates/editor/src/channel.rs` + `connection/src/secret.rs` + `engine/src/duckdb/federation.rs` |
| 筛选下发 / DuckDB 分析 | `crates/editor/src/execution.rs` + `engine/src/services/execution_service.rs` |
| 结果血缘与通道徽标 | `crates/editor/src/store.rs`（结果集元数据） |
| 网格渲染层（将来可提炼到 shared） | `crates/editor/src/view/widgets/grid/`（`GridDataSource` / `GridEditSink`，架构 §3.6） |
| 契约测试范围 | `crates/workbench/tests/ui_contract.rs` |
| 交互稿 | `docs/architecture/editor/editor-prototype.html` |

---

## 6. 验证方式

```sh
# 模块回归（editor 服务层 + 引擎 SQL 层）
cargo test -p rds-editor -p rds-engine --lib -j 2

# 契约（零裸色 / 零裸 px，扫描 editor + workbench）
cargo test -p rds-workbench --test ui_contract -j 2

# 全工作区编译守卫
cargo check --workspace --all-targets -j 2

# P0.2 / P0.2b 真机探针（事务会话亲和 + 驱动级事务；凭据只从环境变量读；共 8 例）
# PowerShell：
#   $env:RDS_TEST_MYSQL_URL="mysql://root:root@192.168.3.138:3306/mysql"
#   $env:RDS_TEST_PG_URL="postgres://postgres:postgresql@192.168.3.138:5432/postgres"
#   $env:RDS_TEST_SQLITE_PATH="<SQLite 文件的**副本**>"   # 原文件若是 Fossil 仓库（T.fossil）会被 Fossil 锁；探针也别直连用户仓库
#   $env:RDS_TEST_DUCKDB_PATH="D:\data\123"               # 这里是**文件**不是目录
cargo test -p rds-engine --test transaction_affinity -j 2 -- --nocapture --test-threads=1

# P0.10 能力探针（离线；报告式 + 三条不变量断言；共 10 例）
cargo test -p rds-engine --test sqlglot_capabilities -j 2 -- --nocapture --test-threads=1
```

**P0.2 / P0.2b / P0.2c 实跑结论（2026-09-15，四类库真实端点）**

| 库 | 顺序亲和（`BEGIN` + 临时表 + `ROLLBACK`） | 并发亲和（两条并发语句） | 驱动级事务（`execute_in_transaction`） |
| --- | --- | --- | --- |
| PostgreSQL | ✅ 成立 + 回滚生效 | ❌ 池另开物理连接（另一侧 `relation does not exist`） | ✅ 可用 |
| SQLite | ✅ 成立 + 回滚生效 | ✅ 两侧均可见 | ✅ 可用 |
| DuckDB | ✅ 成立 + 回滚生效 | ✅ 两侧均可见 | ✅ 可用 |
| MySQL | ✅ 成立 + 回滚生效（`BEGIN` 已改走文本协议） | ❌ 池另开物理连接（另一侧 `1146 Table doesn't exist`） | ✅ 可用 |

> 结论：**顺序执行可以靠池碰运气，并发不行**——MySQL/PG 的池在忙时会另开物理连接，
> 因此 1b 的事务 / 会话**必须 per-session 独占连接**（不能依赖池的顺序巧合）。
> SQLite / DuckDB 是单句柄语义，不受影响。MySQL 的显式 `BEGIN` 已改走**文本协议**
> （`sqlx::raw_sql`，`native/mysql.rs::needs_text_protocol`），不再报 `1295`。

- 每阶段结束：上列命令全绿 + §3 对应场景真机走通（`cargo run -p rds-app -j 2`）。
- 真机回归矩阵（1b 起每轮至少一遍）：MySQL / PostgreSQL / SQLite / DuckDB × 执行族（当前语句 / 选区 / 全部 / 批量）× 只读 / 可写 × 明暗主题。
- **P0.9 基线（2026-09-15）**：本模块**未新增任何依赖**（sqlglot-rust 已在依赖里，**不引 tree-sitter**）→ 依赖增量 = 0（D12 / P0.3 / P0.8 的选型目标已达成）。参考值：`cargo build -p rds-app -j 2` 增量编译（insight + workbench + app）**2m37s**；`target/debug/rds-app.exe` = **143,705,088 字节（≈137 MiB）**——这是 **DuckDB 静态链接时代**的记录（2026-09-16 改动态链接后这一项不再可比，见 `dependencies/duckdb-linking.md`）。
- 阶段完成后回填 §0 进度表，并同步 `editor-architecture.md` §12 与 `editor-prototype-design.md`（若交互有调整）。

---

## 7. 待确认（开工前的阻塞清单）

见 `editor-architecture.md` §13（10 项）。其中**必须先回答**的 5 项：

| # | 问题 | 建议 |
| --- | --- | --- |
| 1 | 多文档标签：Dock 自带 / 自绘 | Dock 自带（待 P0.1 验证） |
| 2 | SQL → 分析 转换粒度 | 整篇单单元 + 二次"按语句拆分" |
| 3 | 批量执行语义 | 逐条独立、失败不中断 |
| 4 | 分析模式首期语言 | SQL + Markdown（Python/Rust 第二期） |
| 5 | 是否独立 `crates/editor` | 是（§3.2 判定四条成立） |
| 6 | 加速 / 联邦通道遇写语句 | 直接拒绝 / 允许但仅落本地副本 | 直接拒绝 + 提示切回源库 |
| 7 | 执行通道默认值 | 总是源库 / 记忆上次 | 记忆上次（随文档绑定） |
| 8 | DuckDB 分析结果落点 | 新结果集 / 覆盖 / 直接作为分析单元 | 1b 新结果集（血缘）；1c 可沉淀为分析单元 |
| 9 | 标签关闭语义 | ① 草稿兜底（关闭即落草稿 + 最近关闭恢复）/ ② 自定义标签条后弹三态确认 | **①**（关闭入口单一、无绕过）；② 作为体验升级后置（API 已确认可行） |
