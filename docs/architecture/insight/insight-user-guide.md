# 洞察模块（M8）· 使用手册

> 状态：**规则编写部分为现行契约**（2026-09-15，与代码逐字段核对）；**界面部分已落地**（Phase 1–5，含 2026-09-18 的三个入口接线与**文件类数据源**，见 `insight-dev-plan.md` §0）
> 关联文件：`README.md`（模块入口）、`insight-prototype-design.md`（原型）、`insight-architecture.md`（设计理念与不变式）、`insight-dev-plan.md`（开发方案与进度）
>
> **给写规则的人**：直接跳到 §4。规则格式是**带 `deny_unknown_fields` 的严格 schema**——字段名或位置写错，整条规则会被拒绝加载（不会“部分生效”）。§4.6 有完整可照抄的示例。

## 1. 入口

洞察从几个地方进入，都落到同一个右 Dock 面板：

| 入口 | 位置 | 分析目标 |
| --- | --- | --- |
| 导航树表右键 → **查看统计** | 左侧数据库导航 | 该表 |
| 分析存档行右键 → **查看统计** | 左侧资产库（M6） | 存档本体（**CSV / Parquet / Excel / JSON**）或来源表（引用型存档） |
| 草稿箱文件右键 → **查看统计** | 左侧草稿箱（M5） | 该文件（**CSV / Parquet / Excel / JSON**） |
| 导航树表右键 → **结构洞察** | 同上 | 该表所在 Schema |
| 结果表列头右键 → **洞察此列** | 中央查询结果区 | 该列（⬜ 该入口尚未接线，见开发方案 §0 D59） |
| Quick Open（`Ctrl+P`）→ **打开洞察** | 命令面板 | 保持上次目标 |

「查看统计」只出现在**真能分析**的行上：存档 / 草稿箱里不是数据文件的条目（`.sql` / `.md`）不会摆这一项；分析表型存档（本体是一个 DuckDB **库文件**）也还没接。

面板打开但不选目标时显示空态引导，**不会**给出任何没有目标的数字。

## 2. 界面导览

### 2.1 面板结构（右 Dock，17.5rem）

```
┌─────────────────────────────────────┐
│ 洞察                          ⚙  ⟳ │  ⚙ 规则管理   ⟳ 重算
├─────────────────────────────────────┤
│  列 │ 表 │ 多列 │ 结构 │ 历史       │  五个 Tab
├─────────────────────────────────────┤
│  amount  [DOUBLE]          空 5.0%  │  目标头：永远显示「现在在看什么」
│                                     │
│  ▼ 基础统计                          │  折叠区
│    总行数 / 非空 / 空值 / 唯一值     │
│    按类型追加：平均值 / 中位数 / …   │
│  ▼ 数据分布                          │
│  ▶ 数据质量                          │
│  ▶ 样本数据                          │
├─────────────────────────────────────┤
│ 质量评分 72 良好                     │  评分卡
│ 完整性 / 唯一性 / 类型一致 / 分布均匀 │
└─────────────────────────────────────┘
```

### 2.2 五个 Tab 各自回答什么

| Tab | 回答的问题 | 目标 |
| --- | --- | --- |
| **列** | 这一列长什么样、能不能用 | 单列 |
| **表** | 这张表有哪几列、每列质量如何 | 表 |
| **多列** | 这几列之间有什么关系（相关性 / 交叉表 / 分组） | 多列 + 一条 multi 规则 |
| **结构** | 这个 Schema 整体健康吗（外键 / 类型一致 / 孤立表 / 冗余列） | Schema |
| **历史** | 和上次分析相比变化了什么 | 当前列的快照 |

### 2.3 怎么读这些数字

- **类型徽标**：`DOUBLE` / `VARCHAR` / `DATETIME` / `BOOLEAN` 是**真实类型名**；颜色只区分类型族（信息 / 成功 / 警告 / 主色），不单独承载信息。显示 `UNKNOWN` 表示该列全为空或类型未识别（BLOB / ARRAY）——此时**不会**出分布与类型专属统计。
- **质量等级色阶**（列质量、表质量、Schema 健康分共用一套）：
  | 分数 | 等级 | 颜色 |
  | --- | --- | --- |
  | ≥ 85 | 优 | 成功色 |
  | ≥ 70 | 良 | 主色 |
  | ≥ 50 | 一般 | 警告色 |
  | < 50 | 差 | 危险色 |
- **四维权重**：完整性 0.35 · 唯一性 0.25 · 类型一致 0.20 · 分布均匀 0.20。
- **采样口径**：**质量分与行数基于全量统计**（规则 SQL 是聚合查询）；只有样本值（前 5 行）与直方图（至少 10 行）是抽的，面板底部会写明。结论以全量为准。
- **临时表口径**：分析对象是**内存库里的临时表**。三种来路，进去之后完全同路：① 结果集表（宿主建的 `tmp_q_…`）；② 「查看统计」的**源取样**——导航树按表、存档 / 草稿箱按文件，洞察侧统一取 500 行落一个样本表（`tmp_i_` 前缀），再用同一份样本做列画像 / 表探查 / 多列 / 保存快照；③ 已有的分析临时表。洞察自己为一次分析建的样本表**用完即删**（建表即登记，分析结束就收）——所以不存在「洞察的表过期」这件事。只有**源**不在内存库里了（宿主关了结果集 / 会话结束），面板才会报「结果集已失效或已过期，请重新执行查询」。

## 3. 典型流程

### 3.1 看一列（最常用）

1. 执行查询，结果表出现。
2. 右键想看的**列头** → **洞察此列**（也可在导航树里右键表 → 查看统计 → 点列名）。
3. 面板切到「列」Tab：基础统计 → 数据分布 → 数据质量 → 样本，底部是质量评分卡。
4. 想留档：切「历史」Tab → **保存**。

### 3.2 摸一张表的底

1. 导航树右键表（或结果集）→ **查看统计**。
2. 面板「表」Tab 显示：表名 + 行数 + 列清单（序号 / 列名（有主键时带 `PK`）/ 真实类型 / 可空 / 质量）。
3. 点 **评估全表**：**逐列串行**评分（避免撞后端并发上限），进度行会一列列往前跑，分数也一列列长出来。
4. 完成后顶部给出表级质量（分数 + 等级 +「N 列需关注」），点任一列名 → 下钻到该列的画像。

两处口径需要知道（面板底部也常驻写明）：

- **质量分基于全量统计**（规则本身就是 `COUNT` / `AVG` 这类聚合查询）；面板里出现的“采样”只有样本值（前 5 行）与直方图（至少 10 行）。原型文档里写的「基于 500 行采样」是 v1 的做法，本期不采用——不去做一个自己并不执行的声明。
- **临时表没有主键约束**：`PK` 角标只在元数据真的带主键时才出现。从查询结果建的临时表一般没有，所以看不到 `PK` 是正常的，不是漏实现。

### 3.3 看两列的关系

1. 「多列」Tab。
2. 勾选列（列清单来自**当前结果集的真实列**，带序号 = 参数顺序）；选规则（如**相关系数**）。
3. **执行分析** → 结果按**数据形态**渲染（单值 → 键值行；多行 → 表格），已知字段名展示为中文标签。

> 多列规则要求勾选**至少 2 列**（具体几列看规则：行尾写着「需 N 列：Numeric · Numeric」），
> 且列的顺序要与规则参数顺序对应（如 `相关系数` 的参数是 `col1, col2`）——
> 相关性是有方向的，`col1` / `col2` 换位结果不同。
> 类型不匹配的规则会置灰（写清需要什么类型），不让你点一个必然报错的按钮。

### 3.4 看 Schema 健康

「结构」Tab：健康分 + 四个折叠区（外键候选 / 类型不一致 / 孤立表 / 冗余列）；嵌套区都渲染，空区会说明「空」的含义（检查过、没问题）。

- **下钻**：每行下面列出涉及的表，点表名切到该表的「表」Tab——取样走与导航树「查看统计」同一条通道（不改动你的数据库）。
- **导出**：健康条右上「导出 ▾」→ JSON / Markdown，弹系统保存对话框；存好后状态栏给一行回执。导出的是**你看到的这份结论**（界面与文件同源），文件名默认 `schema-<schema 名>`。

### 3.5 对比两次分析

「历史」Tab 列出该列的历次快照（最新在上）与当前标记，每行是「时间 · 短版本号 · 类型」，「当前」/「首版」/「更早的版本已清理」三种标记按行给出。点**保存**给当前列留一份快照（正文进项目 DuckDB，版本链进项目 SQLite）；保存是**成对的**：万一元数据没写成功，刚写入的正文会被**回滚**掉（不会留下看不见的半写记录）。底部是存储用量（取后端真实统计，取不到就整行不显示）。

**看变化**：点任一**更早**的一版（有 hover 效果的就是可点的），下方出现对比面板：

```
与 2026-09-12 18:47 对比                    ✕
3 项有变化
质量分    62 → 71        +9 ▲
总行数    3 → 4          +1 ▲
空值      1 → 0          -1 ▼
空值率    33.3% → 0.0%   -33.3% ▼
类型      DECIMAL(12,2) → DECIMAL(12,2)   不变 ●
```

对比方向固定为**「选中的那一版 → 最新一版」**（不给你选方向：这个 Tab 问的永远是「和上次比变了什么」）。再点一次那一行或点 ✕ 就关掉。最新那一版**不可点**——它没有更新的版本可比。

两个使用要点：

- **必须先打开项目**：快照落在项目目录下，无项目时「保存」置灰、历史 Tab 直接给引导（不会一直转圈）。
- **短版本号是认人的那座桥**：时间戳只精确到秒，同一秒存两次快照会看到两个一样的时间，靠版本号分辨哪条是哪条。

**差值怎么读**：

- 箭头与颜色只表示**方向**（▲ 增 / ▼ 减 / ◆ 变了但算不出数值 / ● 不变），**不代表好坏**——空值率上升是坏，唯一值上升不一定是好，所以颜色是中性的，结论得你自己下。
- 差值一律**按屏幕上的精度算**：所以不会出现「显示 62 → 62 却 +0.4」这种自相矛盾（高精度变化会先体现在倒数位上）。
- 算不出数字的项（`3 ~ 8` 这样的区间、`2.5（右偏）` 这样的带方向后缀、类型名）只说「已变」，**不编差值**。
- 类型变了的时候，旧版独有的行（如数值列的「平均」）会列出来、新版侧写「—」——不假装还有值。
- **存一版再比**：只有一版时会给「再存一版就能对比」的提示；对比需要两份快照。

**清理旧快照**：历史 Tab 头行的「清理」→ 确认框里写清了删什么（**30 天前**的快照）与当前存量，确认后正文与版本链**一并删**（删完不能恢复）。没东西可清时它会说「没有 30 天前的快照」——那是空操作，不是失败。保留天数固定（界面上不给你改），想要别的天数得改 `SNAPSHOT_RETENTION_DAYS`。

两个注意：

- 清理**不认版本链**：清完后若某一版的父版被删了，那一版会标「**更早的版本已清理**」（链的起段被剪掉）；它**不会**被误标成「首版」——数据没改，只是界面如实说明（K14 / D57）。分页列表里不判（父版可能就在下一页）。
- 「清理」与「保存」互斥（两者都在动同一批快照）：动作进行中两个按钮都会置灰。

⬜ 待落地：无（保留天数取值待拍板，见架构 §12 Q4）。

### 3.6 管理规则

面板头的 **⚙** 打开规则管理对话框（无项目时该按钮禁用：规则落在项目目录下）。

```text
┌─ 洞察规则 ────────────────────────────────────────────┐
│ [搜索规则名 / id / 分类…]   [＋ 新建项目规则] [⟳ 重新加载] │
├───────────────────────────────────────────────────────┤
│ ▼ 项目规则 · {项目}/.RSmeta/insight-rules/        (2)  │
│   我的订单空值检查   column · v1.0        [开]  ⧉     │
│   渠道分布对比       multi · v1.2         [开]  ⧉     │
│ ▼ 全局规则 · {系统目录}/insight-rules/            (0)  │
│   跨项目复用的规则放在这里，所有项目可见              │
│ ▼ 内置规则 · 应用内嵌                             (16) │
│   空值率检查         column · v1.0        [开]         │
│   ⚠ 用户的规则       project · v1.0  校验失败         │
│      第 3 行未知字段 `outputs`（期望 output）         │
├───────────────────────────────────────────────────────┤
│ 共 20 条 · 2 条禁用 · 1 条异常                         │
└───────────────────────────────────────────────────────┘
```

| 元素 | 行为 |
| --- | --- |
| 分组顺序 | 项目 → 全局 → 内置，即**覆盖优先级从高到低**（先看到「谁赢」） |
| 开关 | 改的是**生效的那一条**，不动规则文件：内置规则写一条项目级抑制记录，用户规则写自己那层的启停位 |
| ⧉ | 在**系统默认应用**里打开该规则文件（内置规则内嵌，没有这一项；文件已被删的行也不给） |
| 校验失败行 | 红色 + 错误原文（TOML 报错含行列）；文件被删是黄色一行 |
| `＋ 新建项目规则` | 在 `.RSmeta/insight-rules/` 建目录 + 写一份模板并打开。模板**能解析但不生效**（`applies_to` 留空），填上类型族才开始参与分析 |
| `⟳ 重新加载` | 丢弃缓存并按三层重建；目录监听之外的兜底与排障入口 |
| 搜索 / 底部统计 | 搜规则名 / id / 分类；统计数字取**全量**（搜索不会让「共 N 条」变小） |

两处行为值得知道：

- **开关是受控的**：点下去立即翻位（不等后台），落库失败时会把真值回填并把原因挂在状态行（如「启停未保存：…」）——不会静默丢掉失败，也不会让列表整页变错误。
- **全局层目录不存在时**，该分组给「创建目录并新建规则」：目录在**首次写入时**创建，不在启动时预设空目录（K7）。
- **信任门**（Q1 ③）：项目规则在**你做出决定前不加载**。首次打开这个对话框会弹一次确认框（列出多少条、叫什么、在哪个目录）；决定记在全局库，之后不再追问。不想加载时就选「不加载」，顶部横幅会留着改主意的入口——细节见 §4.7。

### 3.7 看一个数据文件（CSV / Excel / …）

1. 把文件放进**草稿箱**（或归档进**资产库**）。
2. 右键该文件 → **查看统计**（只有数据文件才摆这一项）。
3. 面板取 500 行作样本，之后的列画像 / 表探查 / 多列 / 保存快照都与库表**完全一样**（分析能力只有一套）。

- 能识别的格式：**CSV / TSV / TXT / Parquet / Excel（`.xlsx` / `.xls`）/ JSON / NDJSON**——靠 DuckDB 自己的读取器，数据不绕一道应用层（Excel 需 excel 扩展）。
- 文件不必先导进数据库；`.sql` / `.md` 这类文本没有「查看统计」（不摆一个点了报错的入口）。
- 样本出自**首次取样**：之后保存快照 / 下钻列 / 多列分析都接着用同一份，不重新抽。

## 4. 规则编写指南

这是本模块的**对外契约**。规则是声明式分析单元，放在 `.rule.toml` 文件里。

### 4.1 放在哪：三层作用域

| 层 | 目录 | 可见范围 | 可写 | 覆盖优先级 |
| --- | --- | --- | --- | --- |
| 内置 | 应用内嵌（随二进制分发，16 条） | 所有项目 | ❌ | 最低 |
| **全局** | `{系统目录}/insight-rules/` | 所有项目 | ✅ | 中 |
| **项目** | `{项目}/.RSmeta/insight-rules/` | 当前项目 | ✅ | 最高 |

`{系统目录}` = 应用数据目录下的 `RdataStation/system/`（与 `global.db` 同级）。子目录任意嵌套（内置规则按 `column/` `multi/` `table/` `quality/` 分类，用户规则不强制）。

**同名（`meta.id` 相同）整体覆盖**：上层规则完整替换下层，**不做字段级合并**。所以想改内置规则的行为，需要把整份规则抄到用户目录再改。

**在系统编辑器中打开任一内置规则**可以当作模板起点（规则管理对话框里的 ⧉）。

**禁用而非删除**：不想用某条内置规则时，用规则管理对话框的开关关掉即可（不需要写覆盖文件）。

### 4.2 字段全表

```toml
[meta]
id          = "my-rule"          # 必填，唯一标识（snake_case），用于覆盖与 API 引用
name        = "我的规则"          # 必填，展示名
category    = "column"           # 必填，column | multi | table | quality
applies_to  = ["Numeric"]        # 必填，适用列类型数组（见 4.3）
description = "..."              # 可选，默认空
version     = "1.0"              # 可选，默认 "1.0"
builtin     = false              # 可选，默认 false（用户规则写 false 或不写）

[query]
template    = """SELECT COUNT("{col}") FROM "{table}" """   # 必填，注意字段名是 template 不是 sql
parameters  = ["table", "col"]   # 可选（默认空数组），会被替换的参数名
result_type = "single"           # 可选，single（默认，单行对象）| list（多行数组）

[[output]]                       # 可选（默认空），一条规则可有多段
sql_name    = "cnt"              # 必填，SQL SELECT 里的列名/别名
json_name   = "count"            # 必填，输出 JSON 的键名
value_type  = "i64"              # 必填，见 4.4

[[quality]]                      # 可选，质量门控（见 4.5）
field       = "count"            # 必填，检查哪个输出字段（json_name）
min         = 1                  # 可选，下界
max         = 100000             # 可选，上界
severity    = "warning"          # 可选，默认 warning
message     = "计数偏低"          # 可选，默认 "Quality check for {field}"

[render]                         # 可选，渲染提示
component     = "NullCheckInfo"  # 可选，推荐组件名
display_order = 0                # 可选，展示排序
```

> **严格 schema**：所有表都拒绝未知字段。多写一个字段（哪怕拼错）都会导致**整条规则加载失败**。字段放错表也一样（例如把 `result_type` 写进 `[meta]`——内置规则里就有一条因此长期缺席，见 `insight-architecture.md` K 区）。

### 4.3 `applies_to` 取值

数组，元素为列类型名，**大小写不敏感**（匹配时用 `eq_ignore_ascii_case`）。内置规则实际使用的取值：

| 取值 | 含义 |
| --- | --- |
| `Any` | 任意列 |
| `Numeric` | 数值列 |
| `Text` / `VARCHAR` / `STRING` | 文本列 |
| `DateTime` / `TIMESTAMP` / `DATE` | 日期时间列 |
| `Boolean` / `BOOLEAN` / `BOOL` | 布尔列 |

多列规则用**多个元素表示多个参数位**（如 `applies_to = ["Numeric", "Numeric"]` 对应两个数值列）。

### 4.4 `value_type` 取值（**只能用这几个**）

| `value_type` | 读出的 Rust 类型 | 说明 |
| --- | --- | --- |
| `f64` | `f64` | 浮点，非空 |
| `f64?` | `Option<f64>` | 浮点，可空（SQL 返回 NULL 时输出 JSON `null`） |
| `i64` | `i64` | 整数，非空 |
| `i64?` | `Option<i64>` | 整数，可空 |
| `String` / `string` | `String` | 字符串，非空 |
| `String?` / `string?` | `Option<String>` | 字符串，可空 |
| `bool` | `bool` | 布尔，非空 |
| `bool?` | `Option<bool>` | 布尔，可空 |
| `usize` | 经 `i64` 中转的 `usize` | 计数类 |

> ✅ **写错 `value_type` 会在解析期直接报错**（列出不支持的取值与可用取值清单），不会再靠兜底分支按 `String` 读。历史遗留的 `"str"` 请改成 `String` / `String?`——内置规则里那 8 处已在 2026-09-17 全部改正（K12）。

### 4.5 质量门控怎么判

`[[quality]]` 的每条会对指定 `field` 取值并判界：

| 情况 | 结果 |
| --- | --- |
| 只设 `min`，实际值 < min | **不通过** |
| 只设 `min`，实际值为 `null` | **不通过**（记为 `actual=null`） |
| 只设 `max`，实际值 > max | 不通过 |
| **只设 `max`，实际值为 `null`** | **通过**（当前实现不判失败） |
| 所有 `[[quality]]` 均通过 | `QualityReport.passed = true` |

> ✅ **`field` 必须是本规则 `[[output]]` 里真实存在的 `json_name`**——解析期会校验（K11 / Q6 已定：早失败优于静默错），写错时直接报错并列出该规则已有的输出字段。
>
> 以前不校验：字段不存在时取值为 `null`，而「只设 `max`」的判定对 `null` 是**通过**——门控形同虚设（内置 `null-check` 就踩了这个）。现在 `null-check` 的 `null_rate` 已由 SQL 真算出来。
>
> 仍需自己判断的一处：值合法地是 `null`（如空表算不出空值率）时，「只设 `max`」仍然通过——那是刻意的（没有数据就不该判数据有问题），要令它在 `null` 时也失败就加一个 `min`。

### 4.6 完整可照抄的示例

**示例 A：单值结果 + 参数 + 质量门控**（照抄即是一条合法规则）

```toml
[meta]
id = "my-null-rate"
name = "我的空值率检查"
description = "统计总行数与空值率"
version = "1.0"
category = "column"
applies_to = ["Any"]
builtin = false

[query]
template = """
SELECT
    COUNT(*) AS total,
    COUNT("{col}") AS non_null,
    ROUND(CAST(COUNT(*) - COUNT("{col}") AS FLOAT) / NULLIF(COUNT(*), 0), 4) AS null_rate
FROM "{table}"
"""
parameters = ["table", "col"]
result_type = "single"

[[output]]
sql_name = "total"
json_name = "total_count"
value_type = "i64"

[[output]]
sql_name = "non_null"
json_name = "non_null_count"
value_type = "i64"

[[output]]
sql_name = "null_rate"
json_name = "null_rate"      # ← 与下一条 quality 的 field 对齐，门控才会生效
value_type = "f64?"

[[quality]]
field = "null_rate"
max = 0.1
severity = "warning"
message = "空值率超过 10%"

[render]
component = "NullCheckInfo"
display_order = 0
```

**示例 B：列表结果**（`result_type = "list"`，输出为数组）

```toml
[meta]
id = "my-top-values"
name = "我的 Top 值"
category = "column"
applies_to = ["Text", "VARCHAR"]
version = "1.0"
builtin = false

[query]
template = """
SELECT
    "{col}" AS value,
    COUNT(*) AS cnt
FROM "{table}"
WHERE "{col}" IS NOT NULL
GROUP BY "{col}"
ORDER BY cnt DESC
LIMIT 10
"""
parameters = ["table", "col"]
result_type = "list"

[[output]]
sql_name = "value"
json_name = "value"
value_type = "String"

[[output]]
sql_name = "cnt"
json_name = "count"
value_type = "i64"
```

### 4.7 参数替换与安全边界

**替换语法**：`parameters` 里声明的名字，在 `template` 中用 `{名字}` 引用。执行时把 `{name}` 替换为传入值。

- 替换采用两步法（先 `{name}` → 临时标记 `@name@`，再标记 → 实际值），避免 `{col}` 与 `{col_name}` 互相误匹配。
- **表名 / 列名建议用双引号包裹**：`"{table}"`、`"{col}"`（DuckDB 标识符规范；名字含特殊字符时必须加）。

**安全边界（请如实理解）**：

| 存在的防线 | 内容 |
| --- | --- |
| 参数值字符白名单 | 传入值只允许字母数字与 `_` `-` `.`，含其他字符即报错 |
| 输出字段校验 | `[[output]].sql_name` 必须在结果集中存在，否则报错 |
| **规则 SQL 静态门**（解析期） | 只放行**单条只读查询**：必须以 `SELECT` 或 `WITH` 开头；不得出现分号（末尾一个可以）；不得出现 `ATTACH` / `COPY` / `EXPORT` / `IMPORT` / `INSTALL` / `LOAD` / `PRAGMA` / `SET` / `CALL` / `CREATE` / `INSERT` / `UPDATE` / `DELETE` / `DROP` / `ALTER` / `TRUNCATE` / `VACUUM` / `BEGIN` / `COMMIT` 等关键字；不得调用 `read_*` / `parquet_*` / `*_scan` / `*_attach` / `*_query` / `glob` / `sniff_csv` / `query_table` 这类**会读文件 / 联网 / 附加外部库**的表函数 |
| **项目规则信任门**（默认不信任） | 项目层规则在**你做出决定前不加载、不参与分析**。首次打开「洞察规则」时弹一次确认框（列出带了多少条、叫什么、在哪个目录），决定记在**全局库**（不写在项目里——项目在被信任前就是不可信输入）。已拒绝也会记住（不反复追问），列表框顶部有横幅可随时改主意 |

> 静态门**只扫语法**：字符串字面量（`WHERE name = 'copy'`）、注释与**双引号包起来的名字**（`"copy"`）里出现上述词**不算违规**。所以列名 / 表名撞上关键字时，把它用双引号包起来就能过——这也是本文一直建议的写法。报错文案会直接告诉你：哪条规则、撞上了哪个词。

| **尚未落的防线** | 说明 |
| --- | --- |
| 完整沙箱 | ✖ 不计划；已核查「禁用扩展 / 只读连接」会连带砸掉连接 DuckDB 数据源、打开 CSV/Parquet、装扩展等自身能力（同一个内存单例） |
| 内容级信任 | 信任绑定**项目路径**，不是规则内容：项目里的规则后来被改动（例如 `git pull` 带进一条新规则）**不会**重新确认——否则你自己改规则也会被反复打扰。要更严一档需加规则集指纹（见架构 §12 Q1） |

> **现状（请如实理解）**：在你信任一个项目后，它带来的规则仍**原样交给 DuckDB 执行**——静态门挡的是误写与顺手抄来的危险语句，**不是安全边界**（DuckDB 没有官方解析沙箱，黑名单天然有漏）。所以“信任”这个动作本质上是在说：“我知道这个项目带的是什么”。

> 实务建议：只运行你理解或信任来源的规则文件；把项目规则目录纳入代码评审，与对待源码同样严格；**不要随意信任来源不明的项目**（尤其是克隆下来直接打开的）。

> ⚠️ 写规则时的**破坏性变更**（2026-09-17）：以前能写多语句 / DDL 的规则，现在会在解析期变成无效规则（规则管理里一行红字 + 明确文案）。改法：把多条语句合成一条 `SELECT`（子查询 / CTE），或把外部数据先由应用侧导入临时表。

### 4.8 内置规则一览（16 条）

| id | 分类 | applies_to | result_type | 参数 |
| --- | --- | --- | --- | --- |
| `null-check` | column | Any | single | table, col |
| `numeric-stats` | column | Numeric | single | table, col |
| `numeric-basic` | column | Numeric | single | table, col |
| `histogram` | column | Numeric | list | table, col |
| `text-length` | column | Text, VARCHAR, STRING | single | table, col |
| `text-frequency` | column | Text, VARCHAR, STRING | list | table, col |
| `datetime-range` | column | DateTime, TIMESTAMP, DATE | single | table, col |
| `datetime-monthly` | column | DateTime, TIMESTAMP, DATE | list | table, col |
| `boolean-ratio` | column | Boolean, BOOLEAN, BOOL | single | table, col |
| `pearson-correlation` | multi | Numeric, Numeric | single | table, col1, col2 |
| `cross-tab` | multi | Text, Text | list | table, col1, col2 |
| `grouped-stats` | multi | Numeric, Text | list | table, num_col, cat_col |
| `scatter-sample` | multi | Numeric, Numeric | list | table, col1, col2 |
| `table-row-count` | table | Any | single | table |
| `table-column-overview` | table | Any | list | table |
| `table-null-overview` | table | Any | list | table, col |

> 其中 `numeric-stats` / `histogram` / `text-*` / `datetime-*` / `boolean-ratio` 同时是**列画像基础统计的实现**——覆盖或禁用它们会连带影响列画像本身（`insight-architecture.md` K10）。
>
> 另一半（`null-check` / 四条 `table-*` / 四条 `multi`）**没有任何代码按 id 执行它们**——它们是规则库里的可复用条目，不是面板当前会跑的东西；`multi` 那四条可由多列 Tab 选着跑，其余的要等宿主侧入口。
>
> 2026-09-17 下线了两条**跑不通**的残留规则（需要时从 git 历史取）：`quality-score`（能力在 `quality_scorer.rs`，同一件事不留两份口径）与 `table-quality-overview`（要读一张从不存在的逐列统计物化表，静态 SQL 无解；能力在「评估全表」）。

## 5. FAQ 与排查

| 现象 | 原因 | 处理 |
| --- | --- | --- |
| 写好的规则在列表里找不到 | 字段名/位置写错（严格 schema，整条被拒）；或 `meta.id` 与内置规则重名而自己那份解析失败 → 仍是内置规则在生效 | 打开规则管理对话框看**校验失败行**的原文，按提示改；文件不在索引里说明读取都失败 |
| 规则列表里出现「文件不见了」 | 索引记录在、磁盘文件已删 | 恢复文件或从列表移除 |
| 打开规则管理时弹「项目规则：是否加载」 | 信任门（Q1 ③）：该项目带了规则文件而你还未做过决定 | 确认来源可信 → 「信任并加载」；不确定 → 「不加载」（记住这次决定，可在列表顶部横幅改主意） |
| 项目规则都在，但分析结果里没它们的份 | 该项目**未被信任**（未决定或已拒绝） | 规则管理顶部横幅点「信任并加载」 |
| 规则报「SQL 必须以 SELECT 或 WITH 开头 / 只能是一条语句 / 不允许出现「XXX」」 | 撞上了规则 SQL 静态门（解析期，见 §4.7）：写了多语句、DDL/DML，或列名 / 表名与关键字同名 | 改成单条 `SELECT` / `WITH`（子查询 / CTE）；列名 / 表名用双引号包起来 |
| 面板提示「结果集已失效或已过期，请重新执行查询」 | 源表（结果集临时表）已不在内存库里（宿主回收 / 会话结束）。洞察自身的样本表用完即删，不会过期 | 重新执行那条查询；仍然失败则看日志 |
| 面板提示「洞察分析任务过多，请稍候重试」 | 并发操作超过 4 | 稍候重试；避免同时触发「评估全表」与多个单列分析 |
| 大表分析报内存不足（Out of Memory） | 内存库有上限（默认 2GB，可在启动前用环境变量 `RDS_DUCKDB_MEMORY_LIMIT` 调大，如 `4GB`）；到顶时溢写到数据根的 `tmp/`，溢写总量上限默认 10GB（`RDS_DUCKDB_MAX_TEMP_SIZE`） | 调大内存上限后重试；或缩小分析范围（单列分析而非评估全表） |
| 分布区是空的 | 数值列行数 < 10（不生成直方图）；或列类型为 UNKNOWN | 正常行为，非故障 |
| 质量分是 0 或「无数据」 | 表为空或采样不到数据 | 检查数据源 |
| 点「评估全表」后部分列为 `—` | 那些列评估失败（如全空/类型未识别），失败会记日志并继续 | 查看日志；单列可单独重试 |
| 改了规则文件但不生效 | 目录监听尚未落地（Phase 0 / 0.6） | 用规则管理对话框的「重新加载」 |
| 改规则后列画像数字变了 | 基础统计由规则驱动（如 `numeric-stats`） | 属预期；见 §4.8 提示 |
| 存档 / 草稿箱里某些条目**没有**「查看统计」 | 只有**数据文件**（CSV / Parquet / Excel / JSON）才有这一项；`.sql` / `.md` 之类不是数据；分析表型存档（本体是 `analytics.duckdb` **库文件**）尚未接入 | 正常行为；把数据文件放进草稿箱 / 归档后再看 |
| 读 Excel（`.xlsx` / `.xls`）报扩展相关错误 | `read_excel_auto` 依赖 DuckDB 的 **excel 扩展**（CSV / Parquet / JSON 是内置读取器，不依赖扩展） | 装好 excel 扩展后重试（扩展安装位置与「本地加速」同一套设置） |
| 「查看统计」报「这个格式还不能分析」 | 该文件扩展名不在读取器名单里（名单只一处：`DuckDBEngine::file_reader_function`） | 转成 CSV / Parquet 后再看（`read_csv_auto` 能吃大多数分隔文本） |

## 6. 验收清单（USIT）

**画像与评分**
- [ ] 结果表列头右键「洞察此列」→ 面板切「列」并显示该列画像
- [ ] 数值 / 文本 / 日期 / 布尔 / 全空 五类列的画像内容正确分派
- [ ] 空值率 > 5% 时数字转警告色，质量区出现对应提示
- [ ] 质量评分卡四个维度分值与等级色阶与阈值（85/70/50/30）一致
- [ ] 导航树表右键「查看统计」→ 表探查显示列清单、`PK` 角标（元数据有主键时）、可空性、行数、底部口径提示
- [ ] 「评估全表」串行完成（能看到中间进度），各列出现分数；点列名下钻到列画像
- [ ] 草稿箱 / 分析存档里的 `.csv` / `.xlsx` / `.parquet` 文件右键「查看统计」→ 面板显示该文件的列画像与表探查（Excel 需 excel 扩展）
- [ ] 非数据文件（`.sql`）的右键菜单里**没有**「查看统计」（不摆点了报错的入口）

**多列与结构**
- [ ] 多列分析可选列（带序号，≥2）与规则，类型不匹配的规则置灰；执行后按**数据形态**渲染单值 / 表格
- [ ] 结构 Tab 显示健康分与四个折叠区；类型不一致点表名可下钻
- [ ] 下钻后切到「表」Tab 并显示该表的探查（取样走源取样通道，**不改动数据库**；限定名按驱动加引号）
- [ ] 结构 Tab「导出 ▾」→ JSON / Markdown：弹系统保存对话框，落盘后**状态栏**给回执（默认名 `schema-<schema 名>`；导出内容与界面同源）

**历史**
- [ ] 「保存」写入快照，历史列表出现新条目并标「当前」（首版标「首版」）
- [ ] 未打开项目时：保存入口置灰、切到历史 Tab 给引导而不是一直转圈
- [ ] 保存失败（如结果集已过期）只挂一行提示，**已有的历史列表照旧可见**
- [ ] 存储用量为后端真实统计（不是前端估算）
- [ ] 点更早的一版出现对比面板（最新那版不可点）；再点一次或 ✕ 关掉
- [ ] 对比的箭头/颜色只表方向，不变的那几行是灰的；算不出差的项显示「已变」而不是乱编数字
- [ ] 列表刷新（保存 / ⟳）后对比面板收起，不留下指向旧「当前」的差值
- [ ] 「清理」要先弹确认框（框里写着删多少天前的、当前有多少）；无项目 / 无快照时入口置灰
- [ ] 没东西可清时回执写「没有 30 天前的快照」，而不是报「清理成功」

**规则**
- [ ] ⚙ 打开的规则管理对话框显示三层分组，内置规则可禁用
- [ ] 禁用一条内置规则后，该规则不再出现在适用规则列表
- [ ] 故意写坏一个 TOML：对话框出现红色行并显示**错误原文**，且其他规则照常可用
- [ ] 「新建项目规则」在 `.RSmeta/insight-rules/` 建目录与模板并打开
- [ ] 新建的规则（照抄 §4.6 示例 A）能出现在列表中并被成功执行
