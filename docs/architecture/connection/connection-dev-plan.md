# 数据源连接模块 · 开发方案（Phase A/B/C）

> 状态：**Phase A/B + 运行时主链路修复 + 测试体系（单测 / 窗口测试 / 全局联动）已实现（2026-09-11）** · Phase C：C1 ✅（由 database-nav 承接）/ C2 ✅ / C3 ✅（传输层全部下沉）/ C4 暂缓 / C5 ✅（多连接暂存列表）/ C6 ✅（草稿持久化·无密码）/ C7 ✅（对话框 UX 收尾：保存并关闭 / 短码 / 脏标记 / 快捷键）/ C8 ✅（单文件拆分）/ C9 ✅（连续保存集成用例）/ C10 ✅（类型 × 驱动两层选择去噪）/ C11 ✅（布局稳定 / Header 去拥挤 / 标签·分组 / 文档体系） · 关联文件：`connection-prototype-design.md`（原型）、`connection-dialog-architecture.md`（架构与数据流）、`connection-user-guide.md`（使用指南）、`connection-dialog-prototype.html`（可交互原型）
> 测试补强与修复记录（2026-09-11）：① 服务层单测 + 全局联动（`crates/workbench/tests/data_source_lifecycle.rs`：保存/回读/更新/删除/同名拦截/作用域预检/测试连接 + 服务写入→加载器读回）；② 窗口级测试（`crates/workbench/tests/connection_dialog_ui.rs`：打开/渲染/关闭、五 Tab 逐个渲染、编辑入口、状态保留、重入不叠加）；③ Secret 持久化修正——`CREATE PERSISTENT SECRET`（默认 `CREATE SECRET` 为会话级会随连接丢失）+ `SET secret_directory` 指向应用可控目录 `{system}/secrets`（不再写用户主目录 `~/.duckdb`），名称净化统一小写（DuckDB 标识符折叠）；④ Secret 注册按 `use_duckdb_fed` 门控（未开启加速不注册，关闭时清理）；⑤ 对话框 `open` 幂等重入；⑥ 可测性注入点：`DataSourceService::with_analysis_db`、`secret_integration::*_at`；⑦ **项目库初始化修复**——DuckDB 迁移从 `MigrationManager`（rusqlite，无法打开 DuckDB 文件且二次 open 触发文件锁冲突）拆到 `migration/duckdb.rs`，复用已打开连接执行（global / project 两处共用），双作用域端到端用例已启用。
> Phase C 推进记录（2026-09-11）：⑧ C2 当前项目会话接入——对话框打开时自动预填项目根（已填值不覆盖）、删除路由携带项目路径（`workspace_loader::delete_connection(conn_id, project_path)`），P_/GP_ 连接在项目打开时可正常删除；⑨ 全局库单例可注入（`engine::migration::install_global_db_manager`）+ 单例生产路径集成测试（`crates/workbench/tests/global_service_singleton.rs`：`DataSourceService::global()` 与 `load_persisted_connections()` 单例分支、重复注入拒绝）；⑩ 遗留路径收尾——编辑区「DuckDB 分析库」表树与 SQL 执行（非数据源导航栏）改走 `workspace_loader::global_analysis_db_path()`（不再拼旧文件名 `global.duckdb`）；⑪ **连接运行态**——`ConnectionItem.connected` 不再冒充记录有效性，由连接管理器运行态填充（`workspace_loader::fill_connected`），列表/详情状态点反映真实连接与断开；⑫ **项目连接可见性**——`load_connections_for_scope(project_root)` 合并全局 + 项目侧 P_/GP_（未打开项目仅全局可见），面板删除刷新 / 对话框保存刷新 / 项目切换刷新三处调用点统一；⑬ **C1 状态更正**——introspection → 导航树已由 database-nav Phase A 承接（`database::MetadataService` 实时内省 + `navigator_service` 懒加载），无需重复建设；⑭ **C3 第一批收敛**——URL 处理族下沉 `crates/connection/src/url.rs`（`build_connection_url` 含 userinfo 百分号编码、`mask_password_in_url`、`extract_credentials_from_url`、`url_has_plaintext_password`），workbench / engine 三处重复实现统一（顺带修复 engine 版脱敏丢失用户名）。
> C3 第二批 + 组织元数据上提（2026-09-11）：⑮ **C3 第二批收敛**——URL 参数注入 / 改写 10 函数下沉 `crates/connection/src/url_params.rs`（`inject_auth_into_url` / `inject_username_password` / `inject_ssl_cert` / `inject_kerberos` / `inject_chain_ssl_params` / `rewrite_url_host_port` / `parse_host_port_from_url` / `append_ssl_params` / `append_url_params` / `matches_no_proxy`），workbench 侧删除本地实现并补 8 项单测；⑯ **分组/标签服务上提**（连接组织元数据归连接域）——`engine::persistence::ConnectionOrgStore` 统一分组（项目级多对多 + 排序）与标签（多值 + 检索 + 统计）读写，取代 `nav_store` 原标签实现（`nav_store` 仅保留导航视图状态）；`DataSourceService` 保存/更新时同步 tags 到权威检索表、删除时一致性清理标签与分组成员（避免孤儿），`nav_runtime` 标签读写改走新存储。
> C3 第三批（2026-09-11）：⑰ **协议链执行 + 隧道生命周期下沉**——`crates/connection/src/chain.rs` 承接 `TunnelRegistry`（按连接 ID 持有隧道守卫，`insert` 覆盖释放旧隧道 / `take` / `count` / `clear`，Clone 共享同一张表）与协议链执行（`apply_network_method` 单跳分发 + `process_chain` 多跳迭代 + SSH/代理隧道端口工厂），URL 改写复用 `url_params` 族；workbench `connection_service.rs` 删除本地实现（`tunnels` 字段改为 `connection::chain::TunnelRegistry`，断开走 `take` 并在连接清理后释放，全关走 `clear`），新增 4 项单测（注册表生命周期 / Direct 原样 / SSL 仅注入参数 / 纯 SSL 链无隧道）。**传输/协议层至此全部下沉，C3 收敛完成**；剩余为依赖 engine 的会话生命周期编排（连接信息 / 持久化 / 元数据缓存 / 连接池），按依赖方向保留在 workbench。
> 集成测试与缺陷修复（2026-09-11）：⑱ **隧道数据面集成测试**——新增 `crates/connection/tests/tunnel_roundtrip.rs`（4 项端到端：SOCKS5 隧道真实数据往返且守卫释放后本地端口关闭、HTTP CONNECT 隧道往返、两跳代理链嵌套转发、no_proxy 命中不建隧道）与 `crates/workbench/tests/connection_tunnel_cleanup.rs`（协议链隧道建立后数据库握手失败 → 断言不残留，`tunnel_count == 0`）；⑲ **集成测试暴露并修复真实缺陷**——`ConnectionService::connect_with_type` 在 `create_database` / `add_connection` 失败时未回收已登记的隧道守卫（本地端口 + 后台 accept 循环泄漏），现统一走 `release_tunnels`，并新增 `tunnel_count` 诊断方法（测试可断言）。
> 对话框入口修复（2026-09-11，真机 UI 测试发现）：⑳ **「新建数据源」各入口无法调起**——根因：gpui 的 `cx.notify()` 只重渲染该视图子树，而 `Root::open_dialog` 仅通知 Root；对话框层挂在 `WorkbenchView::render` 中，宿主不重渲染时层不会进入元素树（表现为点入口无反应、关闭后层残留、对话框内切 Tab / 增删协议链跳不刷新）。修复：① `Shared` 新增宿主重绘桥（`host_redraw`，由 `WorkbenchView::new` 注入 `WeakEntity`），打开 / 关闭（含 Esc / 点遮罩 / 内层管理器关闭）后显式通知宿主重渲染；② `EditorPanel::{request_new_connection, request_edit_connection}` 作为可测入口，编辑区按钮与侧边栏「编辑」统一走此处；③ `WorkbenchView` 观察编辑面板（`cx.observe`），对话框内部状态刷新（切 Tab / 增删跳 / 测试结果）级联到宿主；④ 新增 `crates/workbench/tests/dialog_host_layer.rs`（3 项：按钮入口渲染层 + 关闭移除层、编辑入口渲染层、面板 notify 级联宿主），以 `debug_bounds("dialog-layer")` 断言层真正进入元素树。
> 运行时稳定性（2026-09-11）：㉑ **Windows 主线程栈溢出修复**——应用启动后不久以 `thread 'main' has overflowed its stack`（0xc000041d）崩溃，表现为窗口短暂出现后消失、点入口无反应，并一度阻断真机 UI 验证（`target/rds-app.err.log` 可复现）。根因：Windows 主线程默认 1 MiB 栈，debug 构建下 gpui 视图树构建 / 布局 / 事件派发递归较深（与编译期 rustc 栈问题同源，参 `.cargo/config.toml` 的 `RUST_MIN_STACK`）。修复：`crates/app/src/main.rs` 把应用主循环放入 64 MiB 栈的专用线程（`run_app`），主线程仅负责 spawn + join 并将子线程 panic 转为退出码。
> 原型对齐（2026-09-11，真机 UI 反馈）：㉒ **常规 Tab 卡片式布局对齐原型 v5**——原型要求「driver 信息条 + 三张 section 卡片并排」（`sec-card`：边框 / 圆角 / 图标标题；`form-grid`：92px 标签列 + 弹性值列），原实现是三个竖直堆叠 section 且 Header 字段混在 Tab 内。改动：① Header 上提到 Tab 之上（名称 + 驱动类型 + 作用域 + 语义提示 / 备注 / URI + 项目路径，对齐原型 §2）；② 常规 Tab = info-banner（驱动摘要，文件型库 / 网络库文案分流）+ 三张并排卡片（`flex_wrap`，`min_w` 236px）：连接设置（主机 / 端口 / 数据库只读摘要，文件库展示数据库文件）、数据库认证（引用配置 + 管理入口 + 用户名 / 密码；引用时改为 reuse-note 说明）、连接安全（SSL 模式 + CA / 客户端证书 / 私钥）；③ 新增 `sec_card` / `grid_row` / `val_readonly` / `reuse_note` 辅助与 `lucide()` 图标加载器（`IconName` 仅含组件默认子集，数据库 / 锁 / 盾牌需按资产路径从 `AllAssets` 全量目录加载）；④ `parse_url_host_port_db` 提升为 `pub`（对话框只读摘要与服务层落库同源）。
> 原型对齐（续，2026-09-11）：㉓ **对话框布局与原型 v5 全面对齐**——对照 `connection-prototype-design.md` §2/§3 完成：① **左侧栏**（200px、右边框分隔）：类型搜索框（过滤驱动）+「数据库类型」分类树（关系型 mysql/postgres、文件型 sqlite/duckdb；选中项 coral 左边条 + 底色高亮，点击联动 Header 驱动下拉）；对话框宽度 600 → 980（`font_size * 61.25`，对齐原型 max-width）；② **文件型驱动隐藏「网络」Tab**（tab 定义表按 `is_file_db` 过滤，内容索引保持不变）；③ 网络 Tab 补 **「管理网络配置」入口**（NetworkConfigManager，`open_manager(1)`）；④ 高级 Tab **DuckDB 加速卡片对齐 `accel-card`**（warning 边框 / 底色 + 标题图标 + 副标题 + 开关；开启时展开缓存路径与说明，关闭时显示不可用）；⑤ 暂存列表（`saved-section`）按 C4 决策暂缓（C4 = 模板导入导出 + 暂存撤销 / 重做）。
> 原型对齐（修正，2026-09-11）：㉔ **驱动 / 数据库类型语义修正与布局修复**——① 语义修正：原实现把「驱动类型」下拉绑定到数据库类型（mysql/postgres/sqlite/duckdb），与原型及引擎契约不符。现按 `data_source_types`（侧栏分类树：relational 关系型 / file-based 文件型 / analytics 分析型 / nosql，共 10 类）与 `drivers`（Header 驱动下拉：驱动实现，如 `MySQL (sqlx)` / `MySQL (Official)` / `SQLite (rusqlite)` / `DuckDB (duckdb-rs)`）两层目录分离；点击类型联动该类型首个启用驱动；`db_type` / `driver_id` 落库均取驱动 id（与引擎 registry key / `drivers.id` 严格对齐，如 `mysql_native`），文件型判定改用驱动元数据 `drivers.is_file`；编辑回读按驱动 id 反查显示名；② 侧栏搜索框对齐原型（`Input::prefix` 放大镜图标）；③ Header 紧凑化为 4 行（名称 + 驱动类型 + 作用域 / 备注 / URI + 项目路径 / 作用域提示药丸）并加底部分隔线，修复「标题栏越界遮挡 Tab」。
> 文档补充（2026-09-11）：㉕ **暂存列表（多连接连续编辑）规格补全**——原型设计新增 §2.2（条目类型 Draft/Saved、`saved-item` 结构与选中态、五条交互规则、快照实现约束，参考 v1 v5 §8 `saved-section`），§4 关键交互与 §6 GPUI 落点同步更新；开发计划新增 **C5**（多连接暂存列表）并从 C4 拆出暂存语义（C4 仅保留模板导入导出）。
> C5 实现（2026-09-11）：㉖ **多连接暂存列表（连续编辑）落地**——① 数据模型：`ConnectionDraft` 快照（`name` / `saved_id` + Header 与各 Tab 全字段），`drafts: Rc<RefCell<Vec<ConnectionDraft>>>` + `draft_cursor` 存于 `ConnectionDialogState`（状态改为 `Rc<ConnectionDialogState>`，便于回调克隆句柄调用 staging 方法）；② 交互：侧栏「暂存列表」条目（type-dot + 名称 + ✕）点击切换（切换前 `capture_draft` 写回；已保存条目走 `load_for_edit`）、「+ 添加」追加空草稿、删除草稿（删至最后一条自动补位、已保存条目不在此删除）、关闭（取消 / Esc / 点遮罩）前 `staging_flush` 写回；③ **保存语义变更**：保存成功后不再关闭对话框，改为草稿转正式 + 自动追加空草稿（支持连续编辑多个连接），结果区提示「已加入暂存列表，可继续新建」；④ 打开对话框时合并已保存全局连接为列表条目（P_/GP_ 编辑入口仍在侧边栏）；⑤ 新增 `crates/workbench/tests/connection_staging.rs`（4 项：切换保留字段 / 删至最后自动补位 / 保存后转正式并补位 / 已保存条目不参与删除）。
> 文档补充（2026-09-11）：㉗ **连接模块设计架构与数据流文档**——新增 `docs/architecture/connection/connection-dialog-architecture.md`（定位与边界 / 分层架构与依赖方向 / 概念模型：数据库类型 vs 驱动实现、作用域 G_/P_/GP_、三类引用、Secret 加速、暂存草稿 / 状态所有权与对话框层挂载机制 / 五条数据流（打开、保存、暂存状态机、运行时连接）/ 10 项设计决策取舍 / 测试策略 / 实现位置映射），并在 `docs/architecture/README.md` 索引登记。
> 建议批全量实现（2026-09-11）：㉘ **对话框 UX 收尾 + 草稿跨会话持久化 + 结构拆分**——① **「保存并关闭」按钮**（footer；与「保存」共用 `do_save` 闭包，成功才关闭）；② 暂存条目**脏标记 `●`**（`draft_dirty` 比较当前表单快照与草稿）与**来源短码 `P/G/GP`** 徽标（`saved_scope_short` 按最长前缀优先解析 `saved_id`）；③ **键盘导航**：新增 `workbench::commands::{SaveConnection, TestConnection, DraftPrev, DraftNext}` Action，app 层绑定 `secondary-enter` / `ctrl-t` / `up` / `down` 到对话框容器 `key_context("connection-dialog")`；焦点在单行 Input 内时 Ctrl+Enter 走 Input 的 `Enter` action（`secondary=true`，gpui-base 单行分支 `cx.propagate()`）冒泡到容器兜底；④ **草稿跨会话持久化**：新增迁移 `global/020_add_connection_drafts.sql` + `engine::persistence::connection_draft_store`（`ConnectionDraftRow` **无密码列**、`list` / `replace_all`；`open_global` 在未初始化全局单例的进程拒绝打开，测试用 `set_db_path_override` 注入临时库），对话框 `staging_restore`（首次打开且仍为初始空草稿时恢复）/ `staging_persist`（添加 / 切换 / 删除 / 保存后 / 关闭时全量替换）；⑤ **拆分单文件**：`components/connection_dialog.rs`（3777 行）→ `connection_dialog/{mod,state,staging,render,managers,helpers}.rs`（公开路径不变，子模块 `use super::*`），并删除废弃的 `collect` / `hops_valid` 副本；⑥ **导航栏编辑入口**：`render_connection_row` 增加 ✎（写 `shared.open_edit` + `SidebarEvent::EditConnection`，复用既有对话框打开链路）；⑦ 新增测试 `connection_drafts_persist.rs`（跨会话恢复 + 密码不落库）、`connection_multi_save.rs`（单例临时库连续保存两条 → 暂存转正式 + 补空草稿 + 库中两条可读回）、`connection_draft_store` 内嵌单测（行序 roundtrip / 全量替换 / 表无 password 列）；⑧ 架构文档同步（§2.2 文件落点、§3.5 持久化与安全边界、§5.3 持久化时机、新增 §5.5 快捷键、§6 决策 #11-#14、§7 测试表、§8 进度对照、§9 映射）。
> 运行时修复记录（2026-09-10）：① 应用启动补齐 `initialize_global_system()`（`crates/app/src/main.rs`，全局库单例 + 常驻运行时）；② 路径统一——`workspace_loader` 复用单例与 `RdataStation/system` 目录，消除“保存写 A 库、列表读 B 库”的分裂；③ DuckDB Secret 落地到持久分析库（`analytics.duckdb`）并补齐删除联动与 URL 百分号解码；④ 服务层新增同名连接拦截（`INSERT OR REPLACE` 静默覆盖防护）与项目路径预检（消除半成品落库）；⑤ 对话框测试连接回显服务器版本；⑥ connection crate 清理占位死文件（commands / connection_view / connection_dialog / mod.rs）。
> 模块边界：**本模块只负责“新增/管理数据源连接”本身**（连接对话框、CRUD、作用域路由、测试连接、DuckDB Secret、运行态连接与列表），不包含数据源导航树；导航树、来源短码 P/G/GP 展示、分组/标签/缓存管理均属 database-nav 模块（见 `docs/architecture/database/database-nav-dev-plan.md`）。
> 遗留（2026-09-12 更新）：**已知缺口统一以 `connection-dialog-architecture.md` §14「已知问题与后续项」为准**（原 13 项，#1/#2/#3/#5/#6/#8 已关闭；USIT 第 1 轮又关闭 2 项本轮发现的缺陷，**剩 7 项**：🟡 #4 脏草稿下新增/打开项目走确认后继续、#7 分组管理在导航侧；⚪ #9 宿主消费分支无自动化测试、#10 原型 HTML 人工同步、#11 类型树不可折叠、#12 尺寸常量化仅本模块、#13 缺图像回归/性能基准/fuzz）。🔴 已无（驱动插件能力仍待平台排期，无驱动类型已置灰不可选）。工程侧：C3 传输/协议层已全部下沉（`url.rs` / `url_params.rs` / `chain.rs`），剩余编排层因依赖 engine 保留在 workbench `connection_service.rs`；C4（模板导入导出）仅 UI 入口暂缓；数据库导航侧 Phase B/C（分组/标签视图、缓存管理、预热增量）见 `docs/architecture/database/database-nav-dev-plan.md`。
> 类型 × 驱动去噪（2026-09-11，真机 UI 反馈）：㉙ **左侧选类型、右侧只选驱动实现**——① 左侧栏保持数据库类型选择（类型树行首加类型 `icon` emoji）；暂存条目最左侧由状态点改为**缩小的数据库类型 UI**（类型 emoji 徽标，无类型/旧草稿时回退状态点），右侧 Header 不再复述类型。② Header 驱动下拉改为**只列当前选中类型的启用驱动**并显示**实现短名**（`MySQL (sqlx)` → `sqlx`、`MySQL (Official)` → `Official`、`SQLite (rusqlite)` → `rusqlite`）；短名提取为纯函数 `driver_short_name`（括号内文本，无括号时原值；单测覆盖中英文括号与空括号）。③ 未选类型时驱动下拉为空且 Header 显示提示「请先在左侧选择数据库类型」（`warning` 描边徽标）；信息条（info-banner）文案改为「类型 · 驱动实现 · 特性」。④ 解析统一走类型作用域优先：`find_driver_by_value`（驱动 id → 完整名 → 短名三路匹配）+ `resolve_driver` / `set_driver_by_value`（回读 / 草稿恢复时先定类型再定驱动，跨类型同名短名不歧义），`ClonedDialogState` 新增 `selected_type` 字段供落库反查。⑤ 草稿新增 `type_id` / `driver_id` 字段（迁移 `global/021`），条目类型徽标与恢复精确定位均基于它们；旧行（仅完整名）可回退解析。⑥ 测试：`helpers.rs` 新增 4 项单测，新增窗口测试 `crates/workbench/tests/connection_type_driver.rs`（选类型过滤 + 短名 + 按驱动 id 回读 + 快照携带两层标识）。
> 布局与组织元数据（2026-09-11，真机 UI 反馈）：㉚ **切 Tab 布局稳定 + Header 去拥挤 + 标签/分组入口 + 文档体系**——① **布局稳定**：Tab 内容区固定高度（`h(20.5rem)`）+ `overflow_y_scrollbar`（`ScrollableElement`），切换 Tab 不再改变对话框高度，侧栏与 Header 不跳动；② **性能修复**：新增 `meta_refreshed` 标记（dialog builder 每次渲染都会执行，原先每次重渲染都建 tokio runtime + 查库 → hover 即卡），改为每次「打开对话框」拉取一次，入口 `EditorPanel::request_*` 重置标记；③ **Header 去拥挤**：作用域由下拉改为三态**分段按钮**（仅全局 / 仅项目 / 全局+项目，底层仍写 `scope` Select）；项目路径在已打开项目时仅显示**项目名**（`project_hover` + 绝对定位气泡悬停显示完整路径；未打开项目保留可编辑输入）；④ **标签 / 分组入口**：常规 Tab 新增「组织（标签 / 分组）」卡片（`Checkbox` 多选分组；标签逗号分隔），服务层新增 `list_groups` / `groups_of` / `set_connection_groups`（替换语义，项目级能力），`ConnectionOrgStore::set_connection_groups` 落地写库；标签走 `SaveInput.tags` → `connection_tags`（既有同步），分组在保存 / 更新成功后同步；⑤ **草稿扩展**：新增 `tags` / `groups_json` 字段（迁移 `global/022`），条目快照与恢复一致；⑥ **文档体系**：新增 `connection-user-guide.md`（入口 / 导览 / 流程 / 快捷键 / FAQ / USIT 验收清单），架构文档新增 §10 数据字典与迁移、§11 降级矩阵、§12 性能·可观测·安全、§13 成熟度评估（工业级对照），并在 README 索引登记；⑦ **测试**：`connection_staging.rs` 新增 `draft_carries_tags_and_groups`、`connection_multi_save.rs` 新增 `groups_sync_through_service`（真实项目库替换语义 + 未打开项目降级）、`helpers.rs` 新增标签文本 ⇄ JSON 单测、`connection_org_store` 新增 `set_connection_groups_replaces_membership`；⑧ 修复 `connection_draft_store::replace_all` 占位符缺一（新增列后 `?25` → `?26`，此类静默写入失败已由 roundtrip 单测拦住）。
> 模板与 UI 修复（2026-09-11，真机反馈）：㉛ **C4 模板导入导出 + UI 缺陷修复**——① **模板导入导出**：`ConnectionTemplate` / `TemplateItem`（`kind = rds.connection.template`、`version = 1`），`templates_export` 只导出**未保存且非空**草稿（已保存条目仅有占位字段），`templates_import` 校验标识/版本/非空后追加为草稿并选中（驱动短名由目录解析，密码留空）；UI 在暂存列表标题行提供「导出 / 导入」（剪贴板 JSON，无文件对话框依赖），结果行提示条数与「密码请补填」；② **作用域分段控件改自绘**（真机反馈“文字看不到但可点击”）：原是 `Button` 的 `.primary()` / `.secondary()` 变体，而 `button_secondary_foreground` 未被 RDS 主题覆盖 → 文字与背景近乎同色；改为自绘三态（选中 `primary` 底 + `primary_foreground` 字，未选 `muted_foreground` + hover 底色）；③ **类型 / 驱动自行变动修复**：`SelectState::set_items` 不清理 selection → 可能留下“下拉显示旧驱动但列表里没有”；`refresh_driver_items` 新增选中校正（值不在新选项则清空）；render 的类型回推改为**仅在未选类型时补全**（不再把用户显式选择的类型弹回驱动所属类型）；④ **Header 微调**：备注固定宽度（28rem，不再 `flex_1 + max_w`），URI 保持弹性；⑤ **来源提示**：切换条目 / 恢复草稿 / 新建草稿在结果行提示来源，解释字段为何变化；⑥ 测试：新增 `crates/workbench/tests/connection_template.rs`（导出脱敏 + 导入还原 + 非法输入拒绝）。
> 布局再收敛（2026-09-11，真机反馈）：㉜ **区域固定 + Header 元素定宽**——① **暂存区滚动**：条目多了会拉长对话框 → 暂存列表改「固定高度 7.5rem + 内部滚动」（标题行「+ 添加」常驻可见）；② **类型树滚动**：类型树改「占满侧栏剩余高度 + 内部滚动」，侧栏 `h_full() + min_h_0()` 高度恒定；③ **项目栏移位**：从 URI 行移到**备注行右侧**（固定宽 17rem、固定位置），备注改弹性；「仅全局」作用域时项目不参与落库 → 置灰不可编辑（文案「仅全局不需要」），避免布局跳动；④ **Header 定宽**：类型徽标固定 8.25rem + 文本省略（MySQL / PostgreSQL / SQL Server 切换时驱动下拉不再移动）；⑤ **模板 UI 入口撤下**：`templates_*` 能力与测试保留（标注待启用），暂存标题行只留「+ 添加」（按用户决策避免当前阶段 UI 变动）。
> Header 再设计（2026-09-11，真机反馈）：㉝ **Header 收敛为 3 行 + 统一标签列**——① 删除 Header 中独立的「类型徽标」与独立「作用域提示行」：类型不再重复展示（靠左侧类型树高亮 + 常规 Tab 信息条），**无法辨识时在驱动位置以提示形式说明**（`请先在左侧选择数据库类型`，不再渲染空下拉）；② 新布局：行① 名称（10.5rem）+ 驱动（11rem）+ 作用域三态分段（短标签 全局 / 项目 / 全局+项目，写库仍用全称标签）+ 右侧落点短提示（`→ 全局库` / `→ 项目库` / `→ 全局库 + 项目快照`）；行② URI（弹性）+ 项目（固定 17rem，仅全局灰显）；行③ 备注（弹性全宽）；③ 新增 `header_label`（标签列统一 2.75rem，三行左对齐）与 `scope_short_hint`；④ 测试：`connection_dialog_ui` 3 项 + `connection_type_driver` 2 项通过（渲染与类型联动无回归）。
> Header 行序调整（2026-09-11，用户建议布局）：㉞ **行序改为「类型徽标 + 名称 + 作用域 / 备注 + 项目 / 驱动 + URI」**——① 类型徽标回到 Header 行首（定宽 6rem、emoji + 类型名、超宽省略；未辨识时显示「未选类型」），不再依赖左栏回顾；② 作用域分段移入行 ① 尾部（短标签），删除行内落点短提示（`scope_short_hint` 随之移除，落点信息由分段选中态与常规 Tab 信息条表达）；③ 行 ② 备注（弹性）+ 项目（定宽 17rem）；行 ③ 驱动（定宽 11rem）+ URI（弹性），URI 获得整行剩余宽度；④ 未选类型时驱动位置提示「先选类型」；⑤ 验证：`connection_dialog_ui` 3 项 + `connection_type_driver` 2 项通过。
> UI 规范与项目栏（2026-09-11，真机反馈）：㉟ **类型徽标仅图标 + 项目栏下拉 + UI 约束规范化**——① 类型徽标改为**仅图标**（定宽 1.75×1.5rem，只显示 emoji；未辨识显示 `?`），类型名不再占用 Header 宽度；② 项目栏改为**下拉框**：选项 = 当前项目 + 最近项目（`project::service::list_recent(8)`，按路径去重）+「手动输入路径…」；默认下拉模式由 render 把选中项写回 `project_path`（保存与作用域预检仍读路径输入，单一数据源），手动模式显示路径输入框 + 「列表」按钮切回；**删除 hover 路径气泡**（此前气泡被后续兄弟元素遮挡）；③ **UI 约束规范化**：新增 `docs/architecture/theme/ui-constraints.md`（字体三档 / 图标三档 / 间距五档 / 控件与区域尺寸 / 自绘控件规则 / 交互态 / 提交检查清单 + CI 建议 + 三阶段迁移计划），`connection_dialog/helpers.rs` 抽出常量（`GAP_XS/SM/MD/LG`、`LABEL_W`、`BADGE_W/H`、`SEG_ITEM_H`、`DRIVER_W`、`PROJECT_W`、`TAB_BODY_H`、`STAGING_H`、`ROW_H`），Header / 侧栏 / 暂存区 / Tab 内容区的尺寸字面量改为引用常量（`cargo check` 零警告）；④ **发现的待办**：`DataSourceService::get` 仅查全局库，编辑 P_/GP_ 连接时 `load_for_edit` 取不到记录（项目库回读缺失），需补「项目连接 → DataSource」映射与 `get_with_project`，已记入后续任务。
> Header 细节收敛（2026-09-11，真机反馈）：㊱ **标签间距 / 控件占位**——① 标签列宽 2.75rem → **1.75rem**（`LABEL_W`，刚好容纳两字标签），标签与控件间距由约 2rem 降到约 1rem（名称 / 备注 / 驱动 / URI 四处一致）；② **未选类型**时驱动位不再显示裸文本，改为**禁用下拉占位**（placeholder「先选类型」）；③ **仅全局**作用域下项目栏同样保留**禁用下拉占位**（placeholder「仅全局不需要」），不再换成纯文本；④ 规则写进 `theme/ui-constraints.md`（“禁用占位规则”：无数据可选/不适用时保留控件形态并置灰，避免行高与布局跳变）；⑤ 项目下拉只显示项目名（label = 项目名，路径不出现在选项文本，已被 ㊲ 升级为“名称 + 路径”双列）。
> 项目栏单下拉 + 新增项目（2026-09-12，真机反馈）：㊲ **项目下拉重构与「＋ 新增项目」链路**——① **删除「编辑 / 列表」两态切换**（含 ✎ 与「列表」按钮）与手动路径模式：项目栏只留**一个下拉**，选项行为**项目名（左）+ 路径（右）**双列（路径 `opacity(0.6)` 弱化 + `text_ellipsis_start` 头部省略，保留尾部目录名），搜索同时匹配名称与路径（`ProjectItem::matches`）；未选 / 仅全局时保留下拉形态占位（「选择项目…」/「仅全局不需要」）；② **末项固定为 `＋ 新增项目`**（`PROJECT_NEW_LABEL`，无路径）：选中 → 置位 `Shared::project_new_request` + 清空本下拉选中 → `WorkbenchView::render` 消费标记 → `project::ui::open_create_dialog` 开「新建项目」（编辑区有未保存草稿时先走 `request_close` 的未保存确认，不静默丢弃）；③ **确认逻辑抽成可测落点** `ConnectionDialogState::handle_project_confirm`（新增项目 → 置位请求；普通项目 → 项目根写回 `project_path`；空确认无副作用），下拉订阅只负责转发事件 + `notify_host`；④ **项目会话变更改为每帧检测**：原先写在 `meta_refreshed` 一次性守卫内的会话快照改为每帧比较 `Shared::project`（仅一次 `Option<(String,String)>` 借用），变更即重建选项并选中新项目（新建项目后自动跟随）；编辑回读的项目路径仍优先于会话默认值，且不会因此重跑建 runtime + 查库的元数据拉取；⑤ **测试**：新增 `crates/workbench/tests/connection_project_picker.rs`（4 项：会话项目置顶与默认选中 + 项目根写回 / 末项「＋ 新增项目」在选项中（以“选中回读”证明）/ 确认新增项目置位且清空选中 / 确认普通项目写回路径 + 空确认无副作用 / 下拉项数据契约），连接模块 8 个测试文件共 20 项全绿；⑥ 文档同步：原型设计 §2 / §4、用户指南 §3 / §4.1 / §8 / §9、架构文档 §4.3 / §5.1 / §6 决策（#26 · #28 · #29）/ §7 / §8、`theme/ui-constraints.md` 下拉项双列约定。
> 项目侧回读闭环（2026-09-12）：㊳ **P_/GP_ 编辑回读补齐（封闭 ㉟ ④ 待办）**——① 新增 `DataSourceService::get_with_project(conn_id, project_path)`：按 ID 前缀路由（`P_`/`GP_` 开项目库 `ProjectConnectionStore::get_connection` → `map_project_connection_to_data_source`，其余走全局 `get`）；项目库不存 URL（只有 host/port/database），URL 仍由对话框 `reconstruct_url` 重拼；② 作用域按 ID 前缀回推（`GP_` → 全局+项目快照、`P_` → 仅项目），编辑时三态分段与落点提示不再错位；③ `load_for_edit` 增加 `project_root` 参数：`open(Some(id))` 传会话项目根（在回读前先取快照，与项目栏预填共用）、暂存列表切到已保存条目时 `apply_draft` 传条目 `project_path`（空则回退会话根）；分组勾选回显改用同一个项目根（此前读尚未预填的路径输入 → 项目侧分组永不回显）；④ 降级：无项目根时项目侧返回 `None`（不报错），表单保持空而非报错；⑤ 测试：`data_source_lifecycle.rs` 新增 `project_scope_readback_requires_project_path`（P_ 回读含作用域 / 标签；无项目根与全局 `get` 均取不到），`global_and_project_scope_writes_both_sides` 补 `get_with_project` 断言（GP_ 回读字段 + 作用域 + 降级路径）；⑥ 文档同步：架构 §8 / §11、用户指南 §8。
> 订阅位置修正（2026-09-12，回归修复）：㊴ **面板内重入 `update` 致宿主管道 panic**——① 现象：`dialog_host_layer` 3 项全挂（`cannot update rds_workbench::panels::EditorPanel while it is already being updated`），三个测试都走生产入口（按钮 / 侧栏 ✎ / 面板 notify）→ 只要从 `EditorPanel::request_*` 打开对话框就必现；② 根因：上一轮把“项目下拉确认订阅”建立在 `ConnectionDialogState::open` 内部，而 `open` 正是由 `EditorPanel::request_*` 在面板的 `update` 上下文里调用的，里面再 `entity.update(cx, …)`（同一实体）即重入 panic；③ 修复：新增 `ConnectionDialogState::subscribe_project_confirm(self: &Rc<Self>, …, cx: &mut Context<EditorPanel>)`，由面板入口 `ensure_dialog_subscription` 用自己的 `cx.subscribe_in` 直接建立（无需也不允许再 `update` 面板），`_dialog_sub` 句柄由面板持有；`open()` 不再管订阅；新增 `EditorPanel::dialog_state()`（只读访问对话框状态，供宿主 / 测试）；④ 测试：`connection_dialog_ui` / `connection_project_picker` 两个宿主从“直接 `dialog.open(…)` + 自建状态”改为**走生产入口 `request_*` + 从面板取状态**（这样订阅、宿主通知等副作用与真机一致）；⑤ 教训：对话框“打开”链路上的副作用（订阅 / 通知）一律放面板入口，`open()` 只做状态与渲染准备。
> 原型与文档全量同步（2026-09-12）：㊵ **原型重写 + 导航入口同源修复 + 权威待办清单**——① 排查项目侧连接解析时发现**第二个同根因缺陷**：`nav_runtime::connect_entry` 解析连接只用 `service.get(conn_id)`（只查全局库），而导航树在项目打开时会展示 P_/GP_ 条目 → 点「连接」必报「数据源不存在」，与 C18 的对话框回读同一根因；修复：新增 `nav_runtime::load_entry(_with)` 改走 `get_with_project`（`connect_entry` 复用），新增测试 `nav_runtime_resolves_project_connection_with_project_path`；② **原型 HTML 按实现重写**：Header 三行（类型徽标仅图标 + 名称 + 三态作用域 / 备注 + 项目单下拉 / 驱动 + URI）、类型树改为**平面分类 + 真实种子目录**（关系型 5 / 文件型 1 / 分析型 2 / NoSQL 2，与 `data_source_types` 一致，emoji 与种子一致）、暂存条目改为**类型徽标 + `●` / `P`·`G`·`GP` 短码 + ✕（仅草稿）**、常规 Tab 补第 4 张「组织（标签 / 分组）」卡片、footer 补「保存并关闭」、项目下拉可展开（项目名 + 路径双列、末项「＋ 新增项目」）并联动作用域三态；删除已废弃的「分类折叠」「URI 编辑 / 完成两态」「落点提示行」交互；③ **新增架构 §14「已知问题与后续项」**：13 项按 🔴 阻断 / 🟡 语义与体验 / ⚪ 工程与文档债 分级（含 4 个内置驱动之外的 6 个类型无驱动、类型树未按 `enabled` 过滤、项目下拉缺「打开现有目录」、脏草稿下新增项目的绕行、GP_ 快照无同步策略、`get` 仍公开易踩坑等），作为后续排期的**权威清单**；④ 同步文档：原型设计 §2 / §4 / §6 / §7（待确认项 → 开放问题并关闭 6 项历史问题）、用户指南 §3（类型树与驱动局限提示）/ §4.2（项目侧回读）/ §9（新增 I. 已知局限清单）、架构 §12/§13、dev-plan 遗留行。
> 已知缺口收口（2026-09-12）：㊶ **§14 #1/#2/#3/#5 关闭 + 一个新缺陷修复**——① 排查中又发现一个**必现的凭据丢失缺陷**：`ProjectConnectionStore::update_connection` 无条件写 `password_encrypted`，而 `project_connection_from_input` 在输入无密码时给 `None` → **编辑项目侧连接（密码框留空）保存一次就把库中密码清成 NULL**（全局侧有保留逻辑，项目侧没有；C18 打通项目侧编辑后才可达），修复为 `COALESCE(?9, password_encrypted)` + 回归测试 `project_update_keeps_password_when_blank`；② **#1/#2 类型可用性**：类型目录查询加 `WHERE enabled = 1`；新增纯函数 `type_has_driver`（驱动目录可选≠类型可用）；类型树对无可用驱动的类型**置灰 + 行尾「暂无驱动」**，点击不切换、`select_type` 在结果行给原因；旧草稿指向无驱动类型时驱动位为**禁用占位**「该类型暂无可用驱动」；`collect` 的全量回退也要求驱动启用（避免拿禁用驱动落库）；③ **#3 目录入口**：项目下拉动作项改为「打开现有目录…」+ 末项「＋ 新增项目」，`ProjectItem` 改用 `ProjectItemKind` 枚举（Project / NewProject / OpenFolder），宿主新增 `project_open_request` → `project::ui::open_folder_dialog`（脏草稿仍先走未保存确认）；④ **#5 快照同步**：新增 `DataSourceService::sync_snapshot_from_global(snapshot_id, project_path)`（复制配置 + 凭据密文，保留 ID / 创建时间 / 分组；错误路径：非 GP_ / 缺项目路径 / 全局定义已删），对话框 footer 在**编辑 GP_ 时**显示「从全局定义同步」，成功后重载表单并在结果行提示；⑤ 测试：`data_source_lifecycle` 15 项（+2）、`connection_type_driver` 3 项（+1）、`connection_project_picker` 5 项（+1）、`helpers` 单测（+1）；⑥ 文档：架构 §3.2（GP_ 独立副本 + 同步入口）/§6 决策 #32–#35/§7 测试表/§8 进度 #25–#27/§12 安全（空密码保留）/§14（已关闭段），原型设计 §2/§4，用户指南 §3/§4.2/§8/§9，`ui-constraints.md`（不可用数据项规则），原型 HTML（类型置灰 + footer 同步按钮 + 目录入口）。
> 数据来源审计（2026-09-12，用户提问“有没有前端自己造的数据”）：㊷ **零 UI 造数据 + 项目栏去无效态**——① **审计结论**：本模块是进程内直连本地库（无独立后端服务，也就不存在 HTTP/IPC 的“前后端联通”环节），“联通”＝ UI 只经 `DataSourceService` / engine 商店读写 SQLite/DuckDB；逐项来源表见架构文档 **§15**（新增章节，含允许的三类非库数据：domain 枚举 / 标签字典 / 用户输入与派生值）。② **修复的三处“UI 造数据”**：(a) 能力矩阵原为硬编码 6 项布尔常量（与面板文案“能力由驱动声明”矛盾）→ 改读 `drivers.capabilities`（新增 `driver_capabilities` / `capability_rows`，字典外键原样展示）；(b) 高级 Tab 策略覆盖原为硬编码 6 项（read_only/no_ddl… 与库里 5 类策略无映射）→ 改读 `environment_policies`（选中环境的启用策略 + 库里配置摘要；覆盖键改存 `policy_type`；新增 `refresh_env_policies` / `toggle_policy_override` / `DataSourceService::list_environment_policies_by_name`，环境切换每帧重查）；(c) 环境管理器策略标签把 `policy_type` 当旧键下标匹配 → `position()` 永远 None，**5 条策略全部错标「只读连接」**，且新建会把 `read_only` 这类假类型写进库 → 改为字典往返映射，新建只写空模板并明确提示“配置项待编辑器实现”。③ **其它收口**：`DataSourceService::get` → `get_global`（命名带前提，避免再踩“以为查全部”）；项目下拉补 **「不需要项目（仅全局）」**（切作用域而非留无效空态，§14 #6 关闭）；草稿 `sec_overrides` 改存策略类型（旧布尔数组忽略）。④ 测试：`helpers` +2 单测（能力解析/字典、策略类型↔标签往返与摘要）、`data_source_lifecycle` +1（按环境名读库）、`connection_project_picker` +1（不需要项目切作用域）；⑤ 文档：架构 §6 决策 #36–#39、§7/§8、§14（已关闭扩到 #6/#8 + 新增 3 处修复）、**新增 §15 数据来源审计**；原型设计 §3.3/§3.5/§4；用户指南 §3/§5/§9（新增 K. 数据来源验收）；原型 HTML（能力 chips 换成 MySQL 真实声明值 + 项目动作项第三项 + 能力提示）。
> 脏数据防护与接口闭环（2026-09-12，USIT 前置）：㊸ **五类脏数据/接口问题一次性收口**——① **项目根预检（读写分离）**：`ProjectDatabaseManager::open` 与 `ConnectionOrgStore::open_project` 都会 `create_dir_all(.RSmeta)`，因此“把连接写到随便一个目录”会在磁盘上凭空造出项目骨架。新增 `workspace_loader::is_project_root`（忽略大小写，含前导点），写路径（save/update/delete/快照同步）预检失败即报错，读路径（项目侧回读/列表加载/分组读写）降级为空并告警，**读操作不再有建目录副作用**（实现过程中该判定自己的大小写 bug 被 `connection_multi_save` 的分组用例当场抓住，已补 4 断言单测）。② **项目侧时间戳兜底**：全局侧一直用 SQL `CURRENT_TIMESTAMP`，项目侧却把调用方传入的空串写进库 → 项目连接的 `created_at` / `updated_at` 长期为空；存储层改用 `COALESCE(NULLIF(?,''), CURRENT_TIMESTAMP)`。③ **项目元数据目录拼写统一**：代码库同时存在 `.RSmeta`（project 模块与连接服务，45 处）与 `.RSMETA`（engine project_db/org_store、insight、nav_store），Windows 下同目录、大小写敏感系统会分叉 → 统一为 `.RSmeta`（`connection_org_store::RS_META_DIR_NAME` 提为 `pub` 供复用）。④ **「认证方法」接口闭环**：此前 UI 无该字段、`collect` 硬编码 `None`，导致“引用已保存认证配置”在连接时因缺 `auth_method` 被静默跳过（引用了档案也不注入凭据）；现补 UI 下拉（选项来自 `drivers.supported_auth_types`，驱动未声明则禁用占位）、保存/回读、草稿持久化（迁移 `023` + 存储列 + 快照字段）、并让连接链路在 `auth_method` 缺失时回退到档案声明的 `auth_type`。⑤ **暂存列表与导航一致**：合并改用 `load_connections_for_scope`（全局 + P_/GP_，与导航同一加载器），并清理 `saved_id` 已不可见的幻影条目（未保存草稿永不清理，光标越界保护）；全局库未初始化时跳过合并，避免测试/降级启动碰用户真实目录。⑥ 测试：`data_source_lifecycle`（+1 非项目根拒绝、时间戳断言）、`connection_scope_and_state`（+1 加载器降级不建目录）、`connection_multi_save`（+1 幻影清理）、`connection_type_driver`（+1 认证方法随驱动）、`connection_drafts_persist`（+1 认证方法跨会话）、`workspace_loader`（+1 内嵌单测）；⑦ 文档：架构 §6 决策 #40–#46、§7/§8、§10.3（迁移 023）、**§15.4 脏数据防护（写入侧守卫）**，用户指南 §3/§4.2/§5/§8/§9（新增 K 段验收），原型设计 §3.1 与原型 HTML（认证方法行 + 驱动联动）。
> 文档补充（2026-09-13）：**真机测试环境 + 类型树折叠三选项** —— ① 用户提供四条实际连接（MySQL / PG 同主机 `192.168.3.138`、SQLite `D:\FossilT\T.fossil`、DuckDB `D:\data\123`）→ 记入 **用户指南 §9.0 测试环境**（含明文凭据警示、文件型路径要点、内网默认关 TLS 的说明、**建议矩阵**“哪条连接盖哪些清单段”、本机 LAN 诊断用例的用法），README §5 加指引；② 类型树折叠：**决策 #91 仍为不做**，但把「三选项 + 实现预案」补全到 **§14 #11**（① 维持现状 / ② 上折叠＋三条硬规则与测试清单 / ③ 分类头吸顶需先核实 0.6.1 滚动容器是否支持 pinned header），原型 §4 同步标注；口径：实测种子目录 relational 5 / file-based 1 / analytics 2（nosql 2 条 `enabled=0` 不显示）＝ 3 分类 + 8 项。
> 连接 ID 采用 B 案（2026-09-13，#32 关闭）：**界面与提示词只出现名称，ID 内部化**——`saved_result`（保存结果行：摘要只有名称，ID + 落点进「详情」，顺带给 #28 的 `detail` 字段补上第一个真实生产者）、状态栏提示去 ID、快照同步提示改用重载后的名称、同名拦截消息去 ID；改名语义成文：**编辑改名不换 ID**（`update` 按 ID 定位）、新建重名被拦（不静默覆盖）；生成规则与 `id_prefix` 路由**不动**（仍 `G_`/`P_`/`GP_`）。**原 C 案（连接主键改 ULID + 存量迁移）列入 beta2**（架构 §3.2 + dev-plan 后续表 G 行，触发条件：名称变更频繁 / 需要外部稳定引用 / 与缓存索引表同轮）。测试：`mod.rs` 内嵌 +1（`saved_result` 摘要不得含 ID / 详情含 ID），lib 52 项；文档：架构 §3.2（ID 规则与改名语义表）、§5.2 时序图、§6 决策 #82/#93、§7、§8 #58、§14（#32 关闭）、原型 §2.1/§4、用户指南 §7.1 + §8 FAQ + §9 V/R 段。
> 组件化·尺寸契约·标识·标签·引导·项目动作（2026-09-13，#15/#14/#17/#18/#29/#31/#4 七项关闭）：**本模块可做的项一次做完**——① **#15 组件化**：Tab 条 → `TabBar::underline()`（Small 档）、作用域三态 → `TabBar::segmented()`（Small = 24px，与原型等高）、DuckDB 加速与策略覆盖开关 → `Switch`（**默认 36×20**，与原型 HTML 的 `.switch` 一致，接 `form_disabled`）；策略开关回调改 `set_policy_override(policy_type, want)`（组件传的是请求值而非取反）；可见下标映射提为纯函数 `helpers::{dialog_tab_defs, visible_tab_index}`（文件型隐藏「网络」后可见下标与内部索引错开一位，写错就是“点能力显示网络内容”），`mod.rs` 的「自绘是待迁移技术债」注释同步改写；视觉变化：Tab 为下划线指示条（2px `primary` + spring 动画）、作用域为浅底分段 + 白色胶囊（不再是主色填充）；② **#14 尺寸契约**：对话框模块 `px(...)` 清零（`min_w(px(0.))`→`min_w(rems(0.))` 16 处、`.px(rems(…))`→`px_1/2/3()` 5 处（值相等，仅短码徽标内距 3→4px）、图标→`rems(ui::ICON_SIZE_SM)`、色条→`ui::TREE_ACTIVE_BAR`，新增 `DIALOG_STATUS_DOT_SIZE` / `DIALOG_CHIP_RADIUS`），`ui_contract` 尺寸契约扫描扩到对话框 6 文件——**顺带发现用户侧 `view.rs` / `panels/` 也已清零，`ui_contract` 5 项全绿**；③ **#17 标识**：`prop-del-{属性 key}` / `draft-{saved_id \| new-i}` / `policy-{policy_type}`（与分组标题的 `sec-` 分家；未保存草稿保留位 id 的理由写进注释）；④ **#18**：`project_path` 明确为数据载体（不渲染输入框、由项目下拉写入），删掉无意义的 `set_placeholder` 与「手动输入路径…」残留注释；⑤ **#29 首次引导**：常规 Tab 顶部引导条（五步流程 + 快捷键 + 草稿不丢），仅“未选类型 + 名称/地址都空”时出现；⑥ **#31 标签单一权威**：`overlay_authoritative_tags`（`connection_tags` 为准，含“已清空”；表里无记录时回退行内 JSON 以兼容旧库）接入 `list` / `get_with_project`（决策 #89）；⑦ **#4 项目动作**：`PendingAction::{CreateProject,OpenFolder}`（携带可克隆的 `ProjectInputs`）+ `request_create_project` / `request_open_folder`，脏草稿确认后**直接**打开目标对话框（不再丢请求让用户回选择器再点一次）；⑧ 测试与回归：`helpers` +1（Tab 可见下标映射）、`data_source_lifecycle` +1（标签权威 4 段，**28 项**）、`project/src/ui/tests.rs` +1（确认后推进：脏 → 确认 → 直接开窗 + 表单重置）、`ui_contract` 扩扫描后 5 项全绿；workbench lib 51 + 14 个连接套件全绿，project 26 项全绿，`cargo check --workspace --all-targets` 零警告；⑨ 文档：架构 §6 决策 #84–#90、§7 测试表（+4 行）、§8 #53–#56、§11 降级矩阵、§14（七项已关闭段 + 行标 ✅ + #10 漂移注记）、§15.2（标签权威）；原型 §4（项目动作 / 组件化控件 / 首次引导）；用户指南 §8 FAQ（+4 条）+ §9 新增 V 段；⑩ **原型 HTML 同步（#10）**：Tab 改下划线、作用域改浅底分段、开关保持 36×20（组件取默认尺寸对齐原型）、结果行改成“分级文本 + 详情 / 复制”（可点开看完整原文）、新增首次引导条（点左上类型徽标可切换演示空态）、**删掉早就撤下的内联协议链**（#72 漏同步），并在页头加上“示意稿（非权威）+ 同步戳（决策号）”；⑪ **#11 判定：不做**（类型树可折叠）——4 类共 ≤10 项、侧栏已内部滚动，折叠的交互与状态成本不抵收益；重评触发条件（目录 > 15 项或分类 > 6）写进架构 §6 决策 #91 / §14 #11；⑫ 遗留：本模块只剩 🟡 #7（分组管理在导航侧）与 ⚪ #9 / #10（宿主分支测试 / 原型 HTML 手工性）/ #12 / #13（其他模块尺寸迁移 / 图像回归与基准），另 #19/#21/#22/#23（M4 与宿主侧）、#32/#1（待拍板 / 平台）；收尾补充（同日）：#9 的宿主消费分支抽成 `Shared::take_project_action_request`（同帧两标记 → 新建优先且都清）并补单测（`connection_project_picker` 7 项），决策 #92。
> 结果行分级（2026-09-13，#28 关闭）：**把「成功 / 失败一行字」升级为「级别 + 摘要 + 可展开详情 + 复制」**——① 数据模型：新增 `ResultLevel { Info, Success, Warning, Error }` 与 `ResultLine { level, summary, detail }`（`Display` 只输出摘要）；对话框状态 `result` 由 `Rc<RefCell<Option<String>>>` 改为 `Rc<RefCell<Option<ResultLine>>>`，布尔字段 `result_ok` 删除，换成 `result_expanded`；唯一写入口 `set_result_ok(...)` / `set_result(...)` 取代“写文本 + 设布尔”两步（漏设布尔就是旧实现的隐性缺陷面），保存 / 测试 / 同步 / 驱动属性 / 暂存来源 / 未选类型拒绝全部改走它们；② 渲染：级别决定配色（`success` / `warning` / `danger` / `muted_foreground`），「测试中…」为 Info 级；摘要 **> 80 字（按 char 计，非字节）或含换行** 时行尾出现 `详情 / 收起` 与 `复制`（`App::write_to_clipboard`，内容 = 详情原文），展开正文 `max_h(6rem)` + 纵向滚动；测试接缝 `result_level()` / `result_summary()`；③ **踩到的坑（值得记）**：窗口测试最初用 `.id("conn-result-toggle")` + `debug_bounds` 断言——短消息断言恒真（假绿）、长错误断言恒假；读 gpui 源码（`gpui-0.2.2/src/elements/div.rs` 的 paint 分支）后确认 `debug_bounds` **只登记 `debug_selector`**（`.id` 只登记元素状态与交互，不登记坐标），补 `.debug_selector(...)` 后断言立即转绿（非测试构建自动 no-op；gpui-component 的 `dialog-layer` 同款做法）；④ 测试：`helpers` 内嵌 +1（阈值：80 字不折叠 / 81 字折叠 / 换行必折叠）、`connection_dialog_ui::result_line_levels_and_detail_entry`（短消息无入口 / 长错误双入口 / 未展开无正文 / 展开有正文）、`connection_type_driver` 改用级别 + 摘要断言；⑤ 回归：工作台 lib 50 项 + 13 个连接套件全绿（`data_source_lifecycle` **27**（+1 分组降级可见）/ `connection_dialog_ui` 4 / `dialog_host_layer` 4 / `real_connections` 5 等），`cargo check -p rds-workbench --lib` 零警告；⑥ **服务侧配套（让 Warning 有真实生产者）**：`set_connection_groups` 由“只打日志”改为返回 `Result`，保存路径据此把结果行降为 Warning 级（「已保存：…（分组未同步：原因）」）——否则 Warning 级就是没有生产者的装饰；测试 `data_source_lifecycle::group_sync_failure_is_reported_to_caller`（项目库文件损坏 → 返回 Err 带原因）；⑦ 文档：架构 §6 决策 #82/#83、§7 测试表（含 `debug_selector` 约定）、§8 #52、§11 降级矩阵、§13、§14（#28 已关闭段 + 行标 ✅）、§15.2；原型设计 §4（新增结果行一行 + 测试连接行补充）；用户指南 §8 FAQ + §9 新增 U 段。
> 跨项目引用与存量迁移（2026-09-12，#33 关闭 + #34 残留消除）：**两件事共用同一份「已知项目」来源（项目名册）**——① **#33 引用计数覆盖未打开的项目**：`count_references_batch` 在全局库之外不再只看当前项目，而是遍历 `GlobalDatabaseManager::get_all_projects()` 名册里的每个项目根（路径过 `is_project_root`，不建目录），逐库读 `connections` 统计；`ReferenceCount` 增加 `other: Vec<String>`（记**项目名**、每项目只计一次，避免同一项目的多条连接重复列出），`total()` 相应更新；`ensure_no_references` 的消息从「未打开项目中的引用不在统计内」改为「统计范围：全局库 + 已登记项目库」，并列出「其它项目 N 条（项目名…）」；② **#34 残留：项目库存量明文迁移**：engine 新增 `network_store::reencrypt_project_network_configs(root)`（`{root}/.RSmeta/project.db`，**库文件不存 / 无 `network_configs` 表即跳过，不建目录不建表**——与全库读路径无副作用约定一致），`initialize_global_system` 在全局库迁移之后遍历名册逐库迁移（单库失败仅告警，不阻断启动）；③ 测试：engine 内嵌 +1（`project_migration_skips_missing_db_and_table`：不存在/无表/明文迁移/幂等）、`data_source_lifecycle::manager_reference_count_and_delete_guard` 扩展跨项目段（登记第二项目 → `other == ["另一项目"]`；未打开项目时同样可见；拦截消息列出项目名）；④ 验证：engine 247 项、workbench lib + 13 个连接套件全绿，`check --workspace --all-targets` 零警告；⑤ 文档：架构 §6 决策 #81、§7 测试表、§8 #51、§14（#33 已关闭段 + #33/#34 行标 ✅ + 残留说明去除）、§15.4；用户指南 §8 FAQ + §9 R 段。
> 网络档案凭据加密与暂存区收敛（2026-09-12，真机反馈 + §14 #34 关闭）：**两件事**——① **#34：网络档案 `config` 内的密码不再明文入库**。用户指出“不应该明文入库，我记得有加盐加密的设计”——确认：`auth_configs.auth_data` 一直走 `shared::crypto`（AES-256-GCM + 随机 nonce，`AES:` 前缀），但 `network_configs.config` 没走。实现：`network_store::{encrypt_network_config, decrypt_network_config}`（写路径加密 / 读路径解密，通吃任意层级的 `password` / `passphrase`，覆盖 SSH 扁平字段、代理 `auth.password`、`chain` 数组内每跳；`AES:` 前缀幂等、非 JSON 原样返回）+ `reencrypt_all_network_configs`（存量明文一次性迁移，`initialize_global_system` 调用，幂等、失败仅告警）；服务层 `list_network_configs` 真脱敏（`config` 置空）+ 新增 `network_config_detail_by_name`（编辑回填，managers.rs 改走它）；**集成测试拖出一个真缺陷**：项目库的 `project_query_network_config_with_auth` 是直查 SQL，绕过 `network_store` 读路径 → 加密后隧道会拿 `AES:…` 当密码用，已在函数内补解密（测试第 4 步专门盯这条链路）；② **暂存区只放未保存草稿**（用户决策）：`staging_merge_saved` → `staging_prune_saved`（不再从库并入已保存连接，只清历史残留的 `saved_id` 条目）；`staging_after_save(window, cx)`（签名去掉 conn_id/name）改为**移出草稿 + 补空草稿**；`staging_persist` / `staging_restore` 都过滤 `saved_id`（表里不留已保存条目）；保存成功文案改「已保存：…（编辑请从导航栏进入；暂存区只保留未保存草稿）」；上一轮的分区标题随之撤下（两类不再共存）；③ 测试：engine 内嵌 +4（加密往返 / 幂等 / 容错 / 存量迁移）、`data_source_lifecycle` +1（密文落库 + 列表脱敏 + 回填明文 + 解析链路明文）、`connection_staging` 改写 2 项、`connection_multi_save` 改写 2 项，全绿；④ 文档：架构 §6 决策 #79/#80、§7 测试表、§8 #49/#50、§14（#34 关闭段 + #34 行标 ✅ + 残留说明）、§15.2/§15.4；原型 §2.2（只草稿）/§3.6（网络档案加密）/§4/§6；用户指南 §8 FAQ + §9 C/T 段。
> 文件选择语义与暂存分区（2026-09-12，真机反馈）：**两处“用户看不懂”的收口**——① **「新建文件…」与「打开文件…」职责互斥**：上一版（㊻ ④）为“避免误损数据”让新建选到已存在文件时直接引用，真机表现却是「点新建却引用了旧库」（用户原话：“duckdb 的新建为什么还是打开功能”；代码路径实际上 sqlite / duckdb 完全相同，差异只在“选到已存在文件时直接引用”这个语义）→ 现在选到已存在文件**不创建、不引用、不碰原文件**，结果行提示换名或用「打开文件…」；同时建议文件名改按**驱动 id** 判族（`new_db_file_suggested_name`：duckdb → `.duckdb`，其余 → `.db`），消除“类型 id / 驱动 id”两套事实来源；两段逻辑提为纯函数（`create_new_db_file`）并单测（创建**空**文件 / 已存在不采用且不清空）；② **暂存列表分区展示**：条目按类别插入小标题（「草稿（未保存）」/「已保存连接（点条目可编辑）」）——已保存连接并入暂存区是为“一个对话框连续编辑多条连接”（决策 #26/#45），但此前两类条目混排，用户会问「暂存的连接为什么可以读到数据库实际的连接」；分区**不改变数据来源**（草稿仍只存 `connection_drafts`，已保存条目每次打开实时读取，不写入草稿表）；③ 测试与校验：`helpers` 内嵌 +2（lib 44 项全绿）、`connection_dialog_ui` / `connection_staging` / `connection_type_driver` / `dialog_host_layer` / `connection_drafts_persist` / `connection_multi_save` / `data_source_lifecycle` 全绿；④ 文档：架构 §6 决策 #77/#78、§7 测试表、§8 #47/#48；原型 §2.2（分区 + 加载器更正）/§3.1（文件选择器 + 连接设置）/§4/§6；用户指南 §8 FAQ + §9 C/O 段；⑤ **待用户拍板**：若希望暂存区**只放未保存草稿**（已保存连接完全回到导航栏编辑），删除 `staging_merge_saved` 的合并段即可（约 40 行），已登记为观察项。
> 认证档案字段化与列表脱敏（2026-09-12，审计 A5，§14 #30 关闭）：**认证管理器不再要手写 JSON**——① 新增纯函数层 `helpers::{auth_field_specs, build_auth_config_json, auth_config_values}`（与网络档案同款）：字段键**严格对齐后端读取端**——`password`/`ldap` → `username`/`password`；`ssh_key` → `username`/`keyPath`/`passphrase`/`password`；`proxy_pwd` → `username`/`password`；`pg_class` → `certPath`/`certKeyPath`；`kerberos` → `principal`/`keytabPath`（注入函数是驼峰，写错就等于没配）；② 管理器按类型展开真实字段（`AUTH_TYPES` 3→6：`password`/`ldap`/`pg_class`/`kerberos`/`ssh_key`/`proxy_pwd`，别名 `ssh`/`proxy` 兼容旧档案）；校验必填 / SSH 凭据二选一 / 代理凭据成对，口令只在私钥路径存在时写入（与注入函数读取分支一致）；③ 编辑回填走新增的 `DataSourceService::auth_config_detail_by_name`（按名称查 ID → `get_auth_config` → 解密）；④ **列表真脱敏**：存储层 `list_auth_configs` 会解密后返回明文（内部凭据注入用），服务层列表接口现在把 `auth_data` 置空——此前注释写「脱敏」但实际返回解密明文（注释与实现不一致的安全缺口）；⑤ 管理器字段机制统一：认证（kind 0）与网络（kind 1）共用输入实体与渲染分支，声明缓存键带 kind 前缀（`meta_field_specs_now`），编辑回填后立即按新类型重算声明（旧实现直接写缓存键会跳过重算）；⑥ 测试：`helpers` 内嵌 +4（类型覆盖 / 后端键一致 / 校验与回填 / **组装结果喂给 `inject_auth_into_url` 的闭环**）、`data_source_lifecycle` +1（列表脱敏 + 单条解密回填）；⑦ **新发现登记**：网络档案 `config` 里的 SSH / 代理密码仍是明文入库（`auth_store` 只加密 `auth_configs.auth_data`）→ 登记为 §14 #34（🔴）；⑧ 文档：架构 §6 决策 #76、§7 测试表、§8 #46、§14（A5 已关闭段 + #30 关闭 + 新增 #34）、§15.2；原型设计 §3.6 + §4 管理器表单行；用户指南 §8 FAQ + §9 S 段。
> 档案引用完整性与严格模式（2026-09-12，审计 A2，§14 #27 部分关闭）：**不再“删了档案就静默直连”**——① 管理器列表显示**被引用计数**（`DataSourceService::count_references_batch`：全局库一次拉取 + 当前项目库一次拉取，按 `auth_config_id` / `network_config_id` / `environment_id` 统计，`ReferenceCount{global,project}`）；② 删除被引用的配置被拦下（`ensure_no_references`，消息含引用数与范围，如「认证配置「X」正被 全局 1 条、当前项目 1 条 连接引用，不能删除」——原型 §3.6「被引用的配置不可删除」）；③ **严格模式**：`inject_auth_config_credentials` 返回 `Result`——引用的认证档案不存在 / 读不出 / 注入失败 → 连接与测试连接都报错（不再回退“直连 + 无凭据”）；引用了网络档案但解析不出连接方式（被删 / 类型未知 / 内容非法）→ `connect` 在 `apply_network_method` 之前拦下，`build_probe_config` 同样失败（测试与真实连接结论一致）；④ 计数范围 = 全局库 + 当前打开项目（`mgr.project_root` 由会话 `Shared::project` 写入），未打开项目的引用不可见——拦截是“尽力而为”，连接时报错是兼底（残留登记 §14 #33）；⑤ 测试：`manager_reference_count_and_delete_guard`（无引用 0 / 空 id 不误报 / 全局与项目分别可见 / 消息含范围）、`referenced_network_profile_without_method_is_rejected`（connect 守卫）、`probe_config_applies_referenced_auth_profile`（缺失 → Err）与 `probe_config_applies_referenced_network_profile`（不可达 SSH + 未知类型 → Err）；⑥ 文档：架构 §6 决策 #75、§7 测试表、§8 #45、§14（A2 已关闭段 + #27 部分关闭 + 新增 #33）、§15.2/§15.4；原型设计 §3.6 实现状态 + §4 测试连接行；用户指南 §8 FAQ + §9 R 段。
> 测试连接与真实连接同源（2026-09-12，审计 A1，§14 #26 关闭）：**测试不再“只看表单”**——① 缺陷：`DataSourceService::test` 只吃 `url/username/password/driver_properties`，而引用认证 / 网络档案时 UI 把用户名密码换成只读说明 → **引用了档案的测试必然缺凭据**（假失败；宽松库还会假成功），引用 SSH / 代理档案也不走隧道（给出与真实连接相反的结论）；② 修复（单一来源）：`ConnectionService` 抽出 `inject_auth_config_credentials`（`auth_method` 优先、缺失回退档案 `auth_type`；档案缺失 / 读取 / 注入失败返回**可见说明**；`connect` 与测试共用），新增 `build_probe_config`（与 `connect` 同规则：认证凭据 → **真实建立网络档案隧道**（`apply_network_method`，探测结束 drop 守卫关闭本地端口）→ 驱动属性 / 高级选项；凭据从最终 URL 回填 `username`/`password`，覆盖 `url_override` 之外的字段路径；隧道建不起来直接判失败，不拿未改写地址探测）；`DataSourceService::test(input, project_path)` 走该构建器并把说明拼进结果消息；对话框测试动作从项目栏读项目根传入（P_/GP_ 档案才能解析）；③ 凭据编码：新增 `url_params::merge_credentials`（RFC 3986 userinfo 转义：`p@ss:w/rd` 不再截断 host），`build_effective_url` 改调它，保存 / 更新 / 测试三处写法统一；④ 接线：`load_auth_data_from_db` 增加 `global_db` 入参（`None` 回退进程单例——连接入口行为不变；`DataSourceService` 传自身库，测试可注入临时库）；⑤ 测试：`data_source_lifecycle` +2（档案 → URL + 字段凭据 + 说明；不可达 SSH 档案 → 直接失败，证明真的建隧道）、`rds-connection` +1（`merge_credentials` 特殊字符 / 仅密码 / 已有 userinfo / 非 URL）；⑥ 影响面：`test` 签名新增 `project_path`（调用点 3 处）；工作区 check 零警告；⑦ 文档：架构 §6 决策 #74、§7 测试表、§8 #44、§14（A1 已关闭段 + #26 关闭 + 新增 #27–#32）、§15.2/§15.4；用户指南 §8/§9。
> 驱动注册与文件型取址（2026-09-12，USIT 第 5 轮）：㊽ **两个🔴主链路缺陷 + 一个条目显示不一致**——① **内置驱动从未注册**：真机「测试连接」报 `CONN_DRIVER_NOT_FOUND: Driver 'sqlite' not found in registry`；全仓搜索发现 `AutoDriverRegistrar::auto_register()` **只在测试里被调过**，应用启动路径（`crates/app` 与 `engine::migration::initialize_global_system`）从未注册 `DriverRegistry`——不只是测试连接，**导航树连接 / 重连也全都不通**（同一张注册表）；修复：`initialize_global_system()` 首行调 `auto_register()`（幂等）；② **文件型工厂忽略 `url_override`**：网络型工厂一直用 `config.to_url()`，而 sqlite / duckdb 工厂直接读 `config.database` → 服务层传的地址（url_override）被忽略，修完①后下一次点击就会报「Database path is required for SQLite」；现改为优先 `to_url()`（去 `sqlite://` 前缀与 driver_properties 追加的查询串，不造怪文件名），回退 `database → file_path`；③ **暂存条目与表单不一致**（真机：表单已是 SQLite、条目还显示 mysql 图标）：草稿只在“切条目 / 保存 / 关闭”时写回，显示不能等写回 → 新增 `staging_display_type_id(草稿类型, live 类型)`，**光标位条目用 live 表单快照**（名称与脏标记同源），删除重复计算快照的 `draft_dirty`；④ 测试：engine `auto_register` 补内置 id 断言（6 个种子 id 必须可解析）、`data_source_lifecycle::catalog_drivers_resolve_and_sqlite_probe_succeeds`（**真实文件探测**：驱动目录 id 均可在注册表解析 + SQLite 测试连接成功且磁盘出现空库文件）、`helpers::staging_type_badge_prefers_live_form_for_current_entry`；⑤ 校验：engine 228 项、工作台 107 项全绿；⑥ 文档：架构 §6 决策 #60–#62、§7/§8 #36、§14（已关闭段 +3），用户指南 §8 FAQ +3 条。
> 暂存列表热路径（2026-09-12，§14 #16 后半关闭）：**只取展示字段，不再每帧构造整份草稿**——① 旧实现：`render` 里 `drafts_list.borrow().clone()` 克隆**整张草稿表**（N × 约 40 字段）+ `snapshot_form(cx)` 每帧构造一份完整 `ConnectionDraft`（约 40 次 `to_string` + 两个 `Vec` 克隆）只为比较脏标记；② 新实现：`LiveEntryView { name, type_id, dirty }`（`staging.rs::live_entry_view`）+ `form_matches_draft`（逐字段比较，`InputState::value()` 返回 `SharedString`（引用计数），因此比较无分配；`props` / `sec_overrides` 直接比 `RefCell` 切片）；`render` 循环改成索引 + 每行短借用（只取 `type_id` / `display_name` / `saved_id`），光标位只看 live 视图；③ 代价与防护：“表单字段集合”现有两处定义（`snapshot_form` 与 `form_matches_draft`），由等价性测试 `connection_staging::form_matches_draft_agrees_with_snapshot`（不脏 / 改名脏 / 写回不脏 / 切 Tab 脏 / 越界 None）锁定不漂移；④ 验证：`connection_staging` 6 项、各连接套件与 workspace check 全绿；⑤ 文档：架构 §6 决策 #73、§8 #43、§14 #16 关闭（保留项：目录每帧克隆，已记录数据量依据与后续路径）。
> 内联协议链撤下（2026-09-12，审计 #25 关闭）：**删除“看起来能配、实际不生效”的占位链**——① 判定依据：`Hop { kind, label, enabled }` 没有任何主机 / 凭据字段，**无法构造可执行的 `ChainHop`**；且初始状态里写死了两条假跳（`跳板机·prod-gw` / `公司代理·http`，属 §15 禁的 UI 造数据）；而同样的能力（含**多跳**）已由类型 `chain` 的网络档案完整提供（`parse_network_config_json` → `ConnectionMethod::Chain` → `apply_network_method` 逐跳执行），所以选择**删除而非补齐**；② 改动：删除 `Hop` 模型与 impl、链列表 UI（上移 / 下移 / 启用 / 删除）、`+ SSH 跳` / `+ Proxy 跳`、拓扑预览、`hops_valid` 与 `collect` 里的 `advanced_options.network_chain` 写入、`load_for_edit` 的链回读；网络 Tab 改为「引用下拉 + 管理入口 + 诚实提示 + 数据路径预览（本机 → 档案/直连 → 目标数据库）」；③ 持久化：`connection_drafts.hops_json` 列保留（**不迁移 schema**）固定写 `[]`，旧草稿的占位链直接忽略；④ 测试：工作台 lib 33 项 + `connection_dialog_ui` / `connection_staging` / `connection_drafts_persist` / `connection_type_driver` / `data_source_lifecycle` / `dialog_host_layer` 全绿；⑤ 文档：架构 §2.2 / §3.5 / §4.2 / §5.2 / §5.3 / §6 决策 #72 / §8 #42 / §14 #25 关闭；用户指南 §5 网络行 + §8 FAQ；原型设计 §3.4；**注**：原型 HTML（手工维护）未同步，归 §14 #10。
> 网络配置结构化表单（2026-09-12，审计 #24 表单部分收口）：**管理器不再要求手写 JSON**——① 新增字段声明与纯函数层（`helpers::{network_field_specs, build_network_config_json, network_config_values}`，`NetFieldSpec { key, label, path, kind, required, placeholder, array }`）：按类型给出字段（`ssh` / `proxy` / `socks` / `ssl`，与 `connection::config` 的 serde 模型一一对应）、组装并校验（必填 / 端口 / 布尔 / SSH 密码与私钥二选一，私钥优先）、从 config JSON 反向回填；② 管理器按类型展开真实字段（字段输入一次性创建于 `ManagerWorkspace.net_inputs`，字段声明按类型缓存在 `net_specs`；`chain` 仍用「数据(JSON)」文本框）；③ **修一个潜在数据丢失**：编辑档案此前只回填名称（`new_data` 不动，保存会把已有 config 覆盖成空）→ 现改为回填类型 + 真实字段（并同步类型下拉）；保存成功 / 取消编辑时清空字段；④ 「网络」Tab 对内联占位链加显式提示（不再静默无效）；⑤ 测试：`helpers` 内嵌 +4（字段覆盖 / 组装 JSON 直接反序列化为 `ProxyConfig`·`SshConfig`·`SslConfig` / 校验分支 / 编辑回填往返）；⑥ 文档：架构 §6 决策 #71、§7 测试表、§8 #41、§14（#24 关闭 + 新增 #25 内联链）；用户指南 §5 网络行 + §8 FAQ。**残留（#25）**：内联协议链 UI 仍在（仅提示，未实现）。
> 网络配置真正生效（2026-09-12，审计 #20 收口）：**三层根因一次性收口**——① 入口：`nav_runtime::connect_entry` 现在会解析引用的网络档案（`resolve_network_method_with_project`）并随请求传出，新增可测纯函数 `build_connect_request`（作用域路由 + 字段透传，网络方式由调用方传入）；此前所有生产 connect 路径恒传 `network_method: None`——配了跳板机 / 代理也静默直连。② 解析：`parse_network_config_json` 的类型键改大小写 / 别名归一（`SSH`/`Proxy`/`TLS`/`socks5` 等均可），未知类型告警返回 `None` 不 panic——此前只认小写，而 UI 写入的是大写标签，档案再好也不生效。③ 生命周期：隧道注册表从 `ConnectionService` 实例字段改为**挂在 `ConnectionManager`**（`ConnectionManager::tunnels()`；`ConnectionService::new` 从 manager 取），同一管理器下的服务实例共用一张表，题断开时另一个实例也能正确释放；传独立管理器的测试仍天然隔离。④ 管理器表单：新增 `NETWORK_TYPES = [ssh, proxy, ssl, chain]`（规范键）——打开网络管理器时按类填充类型下拉（此前借用认证选项 `password`/`ssh_key`/`proxy_pwd`，会把错值写进 `network_type`），写库前白名单校验（未知 → `ssh`）。⑤ 测试：`data_source_lifecycle::referenced_network_profile_reaches_connect_request`（大写类型档案 → 可解析 → 进入请求 + `P_` 项目路由）、`connection_service` 内嵌 2 项（注册表共享 / 类型键归一）、`connection_tunnel_cleanup` 回归通过；⑥ 文档：架构 §6 决策 #69/#70、§7 测试表、§8 #40、§14（已关闭段 + #20 关闭 + 新增 #24）、§16 运行时连接行；用户指南 §5 网络行 + §8 FAQ 两条。**残留（#24）**：管理器 `config` 仍是裸 JSON 文本框、内联协议链仍是占位（不参与执行）。
> M3↔M4 契约审计与修复（2026-09-12）：**本模块侧四处收口 + 三项 M4 侧待办登记**——① 审计方法：逐契约点比对“生产方 → 消费方”（列表可见性 / 来源短码 / 运行时连接 / 断开 / 删除 / 编辑 / 标签 / 分组 / L2 缓存 / 状态点 / 项目会话 / 前缀契约），每条带 `file:line` 证据，结论入架构文档 **§16**；② 本模块已修：**(a)** `sync_snapshot_from_global` 补标签权威表同步（此前只复苙 JSON `tags`，`connection_tags` 留旧值 → `tag:x` 检索读到同步前标签）；**(b)** 删除全局连接时一并清理项目侧分组成员（「全局连接加入项目分组」是合法配置，否则 B3 分组视图出现幻影成员；非项目根跳过）；**(c)** 作用域判定收归 `id_prefix::{is_global_connection, uses_project_storage}`（`G_` 与遗留 `conn-` → 全局库；`P_`/`GP_` → 项目库）——服务层 3 处 + `nav_runtime` 4 处全部改调，消除“服务层当全局、导航侧当项目”的分裂；**(d)** `nav_runtime::rename_group` 适配 engine `update_group` 新增的 `sort_order`（保留库中现值）；③ 登记待办（不在本模块）：**#20 🔴 协议链 / SSH 隧道未生效**（所有生产 connect 路径 `network_method: None`，且 `nav_runtime` 每次新建 `ConnectionService` → 隧道守卫随实例释放）、#21 启动/关闭项目不刷新列表（宿主侧）、#22 M4 导航行无删除入口、#23 M4 标签/分组视图未接线 + `NavSource::from_conn_id` 建议改依赖 `id_prefix`；④ 验证：engine 单测新1项、`data_source_lifecycle` 20 项（含新1项 + 扩1项）全绿；⑤ 文档：架构 §6 决策 #68、§7 测试表、§8 #39、§14（已关闭段 + #20–#23）、**新增 §16**。
> 元数据缓存身份指纹（2026-09-12，规则冻结）：㊾ **缓存复用键先落规则与纯函数（未接线）**——① 动机：L2 元数据缓存按**连接 ID** 分文件（`conn_{id}.sqlite`），改名 / 改密 / 换驱动实现 / 调连接参数都会整份重建；② 身份 = 数据库**族**（`type_id`，非驱动实现 id）+ **规范化地址**（网络型：主机小写、IPv6 括号归一、端口空 → 驱动默认端口、库名与 schema 不折叠；文件型：去 scheme 与查询串、`\`→`/`、折叠重复分隔符、去 Windows 三斜杠残留、Windows 折叠大小写）+ **principal**（用户名 → 认证档案 id → 认证类型 → `-`）+ **缓存格式版本**；**不进身份**：连接 ID / 显示名 / 驱动实现 / 密码与密钥 / SSL·代理·超时等连接参数；③ 指纹 = `sha256(规范化串)` 前 16 hex，文件名约定 `meta_{fp}.sqlite`；主机或路径为空、`:memory:` / `file::memory:` 一律**不共享**；④ 实现 `engine::persistence::metadata_identity`（纯函数、无 I/O，13 项单测：默认端口归一 / 主机折叠而库名与 principal 不折叠 / IPv6 / 类型族与格式版本 / 空地址与内存库 / 文件路径跨 scheme·分隔符·查询串 / UNC / 指纹形状）；⑤ **未接线**：缓存路径仍按连接 ID，切换与 database-nav 接入 L2 同轮（同时落 `metadata_cache_index` 引用计数与孤儿、per-fingerprint 互斥、旧 `conn_*.sqlite` 按 legacy 保留），避免两套键并存；⑥ 文档：架构 §3.6（规则与配套）+ §6 决策 #63–#66 + §7 测试表 + §8 #37 + §14 #19；导航原型设计 §4.1（缓存键）、导航开发方案 Phase C **C8**。
> 渲染热路径收敛第一批（2026-09-12）：㊿ **驱动派生数据缓存 + 地址占位守卫（§14 #16 前半关闭）**——① 背景：真机侧反复出现过“hover 就卡”，上轮已用 `meta_refreshed` 挡掉了每帧建 runtime + 查库，但 `render` 里仍有**每帧 JSON 反序列化**（`config_schema.fields` / `capabilities` / `supported_auth_types` 三处，共 4 个调用点）与**每帧 `set_placeholder`**；后者经源码核实：gpui-base 0.6.1 `input/base/state.rs:847` 的 `set_placeholder` 是**无条件赋值 + `cx.notify()`**（上游自己在 `command/state.rs:132` 也写了“变化才写”的规避注释）；② 修复：新增 `helpers::DriverDerived`（键 = 驱动 id + 三份声明原文；`resolve()` 解析、`cache_key()` 只拼字符串）+ `ConnectionDialogState::driver_derived()`（命中缓存则只克隆已解析结果，声明变了自动重算）——刷新点仍在 `render`（权威同步点，与 `fields_synced_for` 同一约定）；地址占位复用已声明但未使用的 `url_placeholder_for` 字段做守卫，只在真正变化时写；③ 测试：`helpers::driver_derived_parses_declarations_and_keys_on_them`（解析结果 + 声明改动换键 + 未选驱动不造字段）、`connection_type_driver::address_placeholder_is_cached_and_follows_driver`（占位与输入框一致 / 再画一帧不重写 / 切驱动后同步；以 `presentation().placeholder()` 读实际值）；④ 验证：工作台全量测试通过 + `check --workspace --all-targets` 零警告；⑤ 文档：架构 §6 决策 #67、§7 测试表、§8 #38、§14 #16（改为“已收敛第一批 + 剩余两项”）；**遗留**：类型 / 驱动目录每帧深拷贝、每帧构造整份 `ConnectionDraft` 比脏（#16 后半）。
> 连接设置可编辑与字段同步（2026-09-12，USIT 第 4 轮）：㊼ **“连接设置无法输入”根因是字段被做成只读摘要**——① 现象：用户反馈连接设置不能输入，并问“每个表单是否都有后端接口、能写进库”；② 设计错位：上一轮把主机/端口/数据库做成**只读数据框**（“编辑在 Header URI”），而工业级连接管理器（DataGrip / TablePlus）都允许直接改这三个字段；③ 修复：三字段改 `Input`，新增**字段 ⇄ URI 双向同步**——新增 `connection::url_params::rewrite_url_authority(url, host, port, database)`（**scheme 无关**；保留用户名密码与查询串；端口空→驱动默认端口；数据库空→去路径段；主机空→不重建）+ `data_source_service::rebuild_url_from_fields`（幂等：与 URI 解析结果一致就原样返回）；渲染层每帧只走一个方向，靠 `fields_synced_for: (驱动 id, URI)` 防循环；**保存仍以 URI 为权威**（引擎解析后写库列）；④ 四字段入口验证：打开对话框→设 URI → 字段回填；改字段 → URI 回写（含凭据保留），再画一帧不变（幂等）；窗口测试 `connection_fields_and_uri_stay_in_sync`；⑤ **对齐用户侧新全局 UI 规范**（`rds-ui-spec` skill / `ui-design-spec.md` / `ui_contract`）：对话框结构尺寸改登记在 `crates/workbench_shell/src/ui.rs`（`DIALOG_*`：标签列 4.25rem / 行高 1.75rem / Tab 区 20.5rem / 暂存区 7.5rem / 驱动与项目定宽 / 徽标与分段项）并从 `ui.rs` 别名引用；间距改用统一 `GAP_SM/MD/LG`（对话框原 `GAP_SM` 0.375rem 对齐为全局 0.25rem），面板圆角改 `theme.radius`，图标改 `rems(ui::ICON_SIZE_SM)`；⑥ **遗留**（已记入本文档）：对话框仍有存量裸 `px(...)`（图标 / 半径 / 描边等）待逐步迁移到 `ui.rs` 或 Tailwind 尺度；⑦ 测试与校验：`rds-connection` 42 项（含新 URL 改写 1 项）、工作台 104 项（含新双向同步 1 项）全绿，`check --workspace --all-targets` 零警告。
> 大纲视觉收敛与文件选择修复（2026-09-12，USIT 第 3 轮）：㊻ **四项真机反馈一次性收口**——① **“标签和输入框距离太远”**：表单行标签列 92px → **68px**（`LABEL_COL_W`）、标签与控件间距 12px → **6px**（`GAP_SM`），`hint_line` 缩进同步改为 `68+6px`；仍是定宽列（控件左边缘对齐），但不再把短标签推远；② **“连接设置没有数据框”**：上一轮把只读值改成纯文本属于**过度收敛** → 改为 `value_box`（白底 + `input` 边框 + 固定行高，与可编辑输入同形），靠**面板浅底**建立对比（两个需求同时满足）；③ **“区域分辨不清”**：分组从“标题行 + 若干行”改为**整幅面板**（`group_box` 浅底 #F5F5F5 / #171717 + 8px 圆角 + 内边距）+ 标题栏底部分隔线 + 组间距 8–10px；④ **“新建没实现 / 无法验证打开”**（根因）：Windows 的 `prompt_for_paths` 带 `FOS_FILEMUSTEXIST`（只能选已存在文件），上一轮拿它当“新建”用 → 用户输入新文件名被系统拒掉；且三种结局（成功/取消/失败）都被静默吞掉 → 看着像按钮没反应。修复：`新建文件…` 改用真正的**保存对话框** `App::prompt_for_new_path(start_dir, suggested)`（不存在则 `File::create`，已存在则直接引用且**不清空**），打开仍用 `prompt_for_paths`；**取消 / 失败 / 无响应都写结果行**（“已取消新建（地址保持原值）”等）；默认目录取当前地址的父目录，建议名按类型给 `new_database.db` / `.duckdb`；⑤ **未选类型 / 驱动的无效输入**：新增 `form_disabled`（`current_driver.is_none()`）→ 表单**正常渲染但整体置灰**（Input / Select / Checkbox / 按钮统一 `.disabled(true)`；SSL 分组也以禁用形态出现），避免“能打字但存不了”；⑥ 工程细节：`.disabled` 对 Button/Checkbox 来自 `gpui_base::Disableable` trait（需显式导入，Input 为本体方法）；⑦ 测试与校验：`cargo check --workspace --all-targets` 零警告、工作台 98 项全绿；⑧ 文档：原型设计 §3.1（面板/数据框/标签宽/未选置灰/文件选择器）、架构 §6 决策 #55–#57、§8 #33–#34、用户指南 §3/§5/§9（O 段扩充）、`ui-constraints.md` §8.1/§8.2、原型 HTML（面板底 + `.v-box` 数据框 + 68px 标签列 + group-box token）。
> 单列分组大纲与可见性（2026-09-12，USIT 第 2 轮）：㊺ **弃用卡片 → 单列分组大纲 + 输入框可见性根因 + schema 驱动字段**——① **用户反馈两条**：常规 Tab 的卡片布局不舒适（应避免遮挡类问题）、建议改“类似树结构”；排查发现真正的阻塞是 **输入框几乎看不见**；② **可见性根因**（不是遮挡是**对比度**）：`Input` 的边框色就是 `theme.colors.input`（JSON 的 `input.border`），而 Light 下输入框底 = `background` = 卡片底 = `#FFFFFF` → 只剩一条极浅的 `#D4D4D4` 细线；`val_readonly` 白底白框同理。修复：主题 `input.border` Light `#B0B0B0` / Dark `#4F4F4F`；只读值改为**纯文本行**（不再用只读输入框壳子）；③ **布局语言改为「单列分组大纲」**（常规 + 高级统一）：`outline_section`（标题行 = chevron + 图标 + 标题，点击折叠；默认全展开，折叠态存 `collapsed_sections`，不落库）+ `form_row`（标签定宽 5.75rem + 控件弹性）+ `text_row` + `hint_line`；卡片（`sec_card` / `val_readonly`）删除。回归了原卡片布局的问题：`flex_wrap + min_w` 窄宽下换行（4 卡排成 3+1）、卡高不齐、卡内长值被挤；④ **字段集合改为「驱动 schema × 连接方式」的函数**：新增 `helpers::driver_form_fields`（解析 `drivers.config_schema.fields[]`：key/label/type/required/placeholder）+ `field_spec` / `address_field` / `address_row_label`；行的存在性（如驱动未声明 `database` 就不出该行）与标签取声明，schema 为空才回退内置字段（界面不变空）；地址行取 `type=file` 或键名 `file_path`/`path`/`file`；用户名/密码标签也优先取 schema；⑤ 测试：`helpers` +1 内嵌单测（真实种子 schema / 缺 key / 非法 JSON / 回退），`connection_type_driver` 补折叠态断言（98 项全绿）；⑥ 文档：原型设计 §3.1（重写为大纲 + schema 契约）/§3.5/§6/§7、架构 §6 决策 #51–#54、§7/§8 #33、§15.2/§15.4，用户指南 §3/§5/§9（新增 O 段）、`ui-constraints.md` §8.1/§8.2、`theme-design.md`（input.border 取值与理由）、原型 HTML（常规/高级改为可折叠大纲 + 纯文本只读值）、skill（动态渲染 / 单输入源 / `cx.theme()` 借用顺序）；⑦ **遗留**（原型 §7 + 架构 §14）：未映射的 schema 字段类型（多主机 / 服务地址 / token）与「字段表单 ⇄ 连接 URL」双向组装尚未打通，留给驱动插件阶段——这是服务型 / HTTP / NoSQL 驱动完全靠库里声明驱动 UI 的最后一块。
> 常规 Tab 动态渲染与文件型地址链（2026-09-12，USIT 第 1 轮）：㊹ **文件型地址丢失（🔴 真实缺陷）+ 常规 Tab 随驱动变形**——① **现象**：选 SQLite 后地址框仍显示 mysql 的连接串（实为写死的 `url_placeholder`），且常规 Tab 仍出现用户名 / 密码 / SSL 卡片；② **排查又拓出一个真缺陷**：`parse_url_host_port_db` 对 `sqlite`/`duckdb` 直接返回 `(None, None, None)` → **项目侧（P_/GP_）文件连接的路径根本没写进 `database` 列**，`connection::url::build_connection_url` 会报「文件型连接缺少数据库路径」，编辑回读的地址也是空的（全局侧因引擎自解析而看起来正常，掩盖了半年）；修复：文件型统一存**裸路径**（`normalize_file_db_path` 去 scheme + 修 Windows `sqlite:///C:/x.db` 的多余前导 `/`），`build_effective_url` 对文件型不注入凭据；③ **地址列随驱动变形**：网络型 Header ③ = `驱动 + URI`（占位取 `drivers.url_template` + `default_port`，不再写死 mysql）；文件型 Header ③ = `驱动 + 地址`（只读路径 / 引导文案），**可编辑地址 + `打开文件…` / `新建文件…` 移入「常规 → 连接设置」**（同一地址只留一个输入框，`App::prompt_for_paths` 选文件，新建时 `File::create` 空库文件，已存在则直接引用）；④ **常规 Tab 卡片集合按驱动渲染**：文件型仅「连接设置（地址 + 打开/新建）+ 组织」；网络型四张卡，且 SSL 卡片仅当驱动声明 `ssl` 时出现；⑤ **落库清洗**：新增纯函数 `strip_file_db_noise`（清 `username` / `password` / `auth_method` / `auth_config_id` / `network_config_id` 与 `advanced_options.ssl`/`network_chain`，策略覆盖保留），SSL 覆盖也仅当驱动声明 `ssl` 时写入；⑥ 测试：`data_source_lifecycle` +1（`file_db_path_survives_save_and_readback`：全局/项目两侧路径 + 更新 + `build_connection_url` 还原）、`helpers` 内嵌 +2 单测（占位随驱动 / 文件型清洗）、`connection_type_driver` +1（文件型↔网络型互切渲染不 panic）；⑦ 文档：架构 §6 决策 #47–#50、§7/§8 #31–#32、§14「已关闭（USIT 第 1 轮）」、§15.1/§15.2/§15.4；原型设计 §2/§3.1/§4；用户指南 §3/§4.1/§5/§8/§9（新增 N 段验收）。
> 全量回归轮（2026-09-17，测试体系补强）：**新增两个套件 + 拖出并修掉三个真缺陷**——① `connection_render_matrix`（状态 × 渲染矩阵：空态引导条「出现 → 填名称消失 → 清空复现」、五个 Tab 在全局库未初始化时逐一渲染、作用域三态、结果行四级）；② `connection_edit_backfill`（编辑入口端到端：真实服务落库 → `request_edit_connection` → 名称 / 地址 / 备注 / 标签 / 类型 / 驱动 / 作用域逐项回填 + 五 Tab 渲染 + 已保存连接不进暂存区）；③ **缺陷 1（🔴，由 ② 拖出）**：编辑既有连接时类型 / 驱动不回填——`load_for_edit` 在驱动目录加载（首帧 `refresh_meta`）之前执行，按 `db_type` / `driver_id` 反查必失败 → 左侧类型树无选中、驱动下拉为空、类型徽标不显示；修复：`pending_driver_value` + `replay_pending_driver_locator`（`refresh_meta` 之后重放一次定位，命中即清空）；④ **缺陷 2（🔴，由 ① 拖出）**：暂存区 / Tab 内容区看似固定高度，实测仍被内容撑开——`.h(rems(…)).min_h_0()` 夹不住 flex 子项的自动最小尺寸（13 条草稿时暂存区 120px → **412px**，长内容 Tab 同样会把对话框拉长，正是真机反馈过的“切 Tab / 加草稿时布局变大”）；修复：三向显式约束 `h + min_h + max_h`；⑤ **缺陷 3（🔴，由 ① 拖出，决策 #94）**：两列不等高且侧栏撑高对话框——侧栏自然高 522px vs Tab 内容区 328px（右列下方 194px 空白；类型树条目数反向决定对话框高度）；修复：新增 `DIALOG_BODY_HEIGHT = 32.5rem` 先定行高（三向夹住），侧栏与内容区各自同高填满（`DIALOG_TAB_BODY_HEIGHT` 降为最小高参考值）；测试断言：`conn-staging-scroll` 高度恒定、`conn-tab-body` 五个 Tab 高度一致、`conn-side-panel == conn-tab-body`（目录就绪与降级两条路径）；⑥ 测试锚点补缺：`conn-general-guide` / `conn-staging-scroll` / `conn-tab-body` / `conn-side-panel`（不改布局，仅测试构建登记坐标）；⑦ 验证：18 个目标 / 148 用例全绿 + 四条真机连接（MySQL·PG·SQLite·DuckDB）测试连接与真实连接双链路通过；⑧ **测试写法坑（值得记）**：宿主设了 `host_redraw` 桥时，面板入口必须**从宿主外部**触发（在 `harness.update(cx, …)` 内调用会因 `notify_host` → 重入 `Harness` update 而 panic）。
> 标签权威表回填迁移（2026-09-17，兼容回退收尾，§14 H / 决策 #89）：**标签只剩一个来源**——① 新增 `connection_org_store::{backfill_all_connection_tags(conn), backfill_project_connection_tags(root)}` 与 `ConnectionOrgStore::backfill_tags_from_json()`：把连接行内 `tags` JSON 灌进权威表 `connection_tags`；② 规则：只迁移**表里没有任何记录**的连接（表里有记录 = 新路径已生效或用户改过 → 不覆盖）；空 JSON / 非数组 / 元素全空或重复 → 按无标签处理；`connection_tags` 或连接表不存在 → `Ok(0)` 且**不建表不建目录**（老库零副作用）；返回写入连接数供日志 / 测试；③ 接入 `initialize_global_system`：与网络档案加密迁移同模式（全局库 + 项目名册，失败仅告警，不阻断启动）；④ **读取侧删掉兼容回退**：`overlay_authoritative_tags` 现在“表里无记录 = 无标签”（不再回退行内 JSON），行内 `tags` 降为纯写入侧投影（v1 形态 / 导出）；仅当权威表根本打不开时才降级读投影并告警；⑤ 测试：engine 内嵌 +2（仅迁移“表无记录 + JSON 非空”、不覆盖已有记录、二次运行为 0；缺文件 / 缺表 → 0 且不建表）与 `data_source_lifecycle::tag_reads_follow_the_authority_table` 改写（表无记录 → 无标签；回填后由表提供且幂等）；⑥ 窗口：首次纳入项目名册的项目在下一次启动完成回填（与网络档案迁移同一已知窗口，已写进架构 §11 降级矩阵）。
> 标签权威表回填迁移收尾（2026-09-17，决策 #95）：**读取只认权威表，存量数据两处幂等迁入**——① **启动迁移抽成可测函数** `migration::global_init::migrate_legacy_data(&manager) -> (加密条数, 回填条数)`（原先是 `initialize_global_system` 内的内联块，而该函数路径不可注入 → “回填是否真的被启动调用”只能靠人读代码）；新增接线回归 `startup_migration_backfills_tags_for_global_and_project`（全局 1 + 项目 1 + 幂等）。② **补齐时序缺口**：项目库的 `connection_tags` 由项目迁移创建，而启动迁移早于项目被打开——旧项目库那时无表，回填按“不建表”规则返回 0，表现为“升级后第一次打开项目看不到旧标签，重启一次才回来”；现在 `ProjectDatabaseManager::open` 在跑完项目迁移后立即回填（失败仅告警），新增 `opening_project_backfills_legacy_connection_tags`（真实时间线：建表 → 手写旧行 → 再打开 → 标签已在权威表）。③ 验证：engine lib **338 项全绿**；workbench 连接模块 **18 目标 / 150 用例全绿**（lib 66）。④ 文档：架构 §6 决策 #95、§14 已关闭段、README §5 基准与标签条目、本行。
> **数字口径注（2026-09-19）**：上方两条 09-17 记录里的用例数是**当日快照**；**当前权威口径**见 [`../module-status.md`](../module-status.md) §2（逐包：连接 crate **48** · 工作台连接套件 **15 目标 / 75 项** · 引擎 lib **443**）。台账 §7 约定：数字变了就重跑复现命令，不要手改。
> 驱动侧 TLS 能力补齐（2026-09-19，`driver-capability-matrix.md` §7 #7/#8）：**URL 表达不了的 TLS 配置改走结构化请求**——① 新增 `connection::config::TlsRequest { mode, ssl }`（含 `verifies_chain` / `verifies_hostname` / 三个证书路径访问器），随 `DriverConnectionConfig.tls`（`with_tls`；`#[serde(skip)] #[specta(skip)]`——进程内派生参数，不落库不进绑定）进驱动；**唯一派生点**是 `connection_service::tls_request_of`（网络档案 SSL 跳优先 → 「连接安全」内联覆盖），与 URL 注入同一套规则、成对使用。② **MySQL Official**：新增 `native::mysql_native::mysql_async_ssl_opts`——CA 进 `SslOpts::with_root_certs`、verify 两档映射到两个 `danger_*` 开关；注意 `PathOrBuf` **未在 crate 根导出**（`mod opts` 私有），但 `vec![PathBuf::from(ca).into()]` 靠类型推断可编译（已实测）；**客户端证书只接受 PKCS#12**（native-tls 后端），PEM 两件套/只给私钥/只给证书三种畸形输入都**报可见错误**并引导用 sqlx。③ **PostgreSQL Official**：新增 `native::postgres_native::{pg_tls_connector, pg_tls_policy}`——把原来的无条件 `danger_accept_invalid_certs(true)` 改为按请求决定：无请求保持历史行为（不校验），`verify-ca` 校链、`verify-full` 链+主机名；CA 文件读到 PEM/DER 都认，客户端证书走 `Identity::from_pkcs8`（PEM 即可）；文件读不到 / 两件套不齐 → 报错。④ **URL 侧同步**：`append_ssl_params` 的 native 分支不再报错（能力已具备），改为写全它能表达的——`mysql_native` 显式写 `require_ssl=true&verify_ca=false`（只给 `require_ssl` 时 mysql_async 默认**是校验证书链+主机名**，自签服务端连不上）、verify 档写 `verify_ca/verify_identity`；`postgres_native` 的 verify 档在 URL 上落 `sslmode=require`（校验靠连接器），并保证**证书路径永不进 URL**（有单测钉住）。⑤ 验证：`cargo check --workspace --all-targets` 零警告；connection 48+4、engine lib **446** passed / 24 ignored、workbench lib **114**、`data_source_lifecycle` 28 / `connection_type_driver` 7 / `connection_render_matrix` 2 / `connection_edit_backfill` 1 / `connection_dialog_ui` 4 全绿。⑥ **真机待验**：Official 驱动 + 真实 MySQL/PG 的 TLS 握手与证书校验（本机无端点；§7 #7 已记）。
> **真机验收（2026-09-19 当晚，端点 `192.168.3.138`）**：新增 `crates/workbench/tests/official_driver_real.rs`（4 项，未设 `RDS_TEST_*` 时自动跳过；单开文件的原因：既有真机套件把 URL 原样当 `url_override`，**测不到**应用自己的连接串构造路径）。① **§7 #10（驱动 id 当 scheme）真机通过**：`mysql_native` / `postgres_native` 双驱动用 `{驱动 id}://…` 连上并执行查询——修复前必失败。② **TLS**：`mysql_native` + `require` 加密连接成功且可查询，`verify-full` **如预期失败**（schannel 报“证书链在不受信任的根证书中终止”）——证明校验真的在做（修前既不加密也不报）；`postgres_native` 的 require/verify 在现有端点**未覆盖**：诊断（sqlx + `?sslmode=require`）显示服务端 `server does not support TLS`，是端点能力而非缺陷，已写成诊断用例钉住。③ **Secret 族解析**：真实驱动目录下 `mysql_native → MYSQL` / `postgres_native → POSTGRES`。④ **回归**：`editor_exec_real`（四库执行链 + 本地加速通道 20 项）与 `federation_sources`（`mysql_native` 联邦源）改后仍全绿。⑤ 参上：`mysql_async` 的池绑定创建它的 runtime——新测试第一版把连接与查询拆到两个 runtime，得到 `Pool was disconnected`（已修，注释留在测试里）。
> 能力字典与按驱动能力位（2026-09-19，`driver-capability-matrix.md` §3.3）：**同一个库的不同驱动能力本来就不同（这正是「能力 Tab」存在的理由），但实现里三个面各说各话**——界面读 `drivers.capabilities` 的键（**没有任何门控**，标签表是第二份字典）、运行时 `DataSourceMeta` 有 6 位但 5 位无读者、且按**数据库族**给（`mysql` 与 `mysql_native` 共用一份），编辑器的事务闸门反而硬编码 `true`。本轮收敛：① 新增 `engine::driver::capability`（**唯一字典**：键 → 中文标签 + 可选运行时能力位 + 真机验收状态；`META_BITS_WITHOUT_UI_KEY` 显式登记无界面键的位，“新增一个位不表态就红”）；② `DataSourceMeta` 拆出 `mysql_native()` / `postgres_native()`（目前同值，但差异从此可表达，各驱动 `meta()` 只用自己的那份）；③ `EngineQueryRunner::supports_transactions()` 改**读连接实际能力位**（取不到才回退 `true`，不误伤 UI）；④ 能力 Tab 行尾 `✓` = 已真机验收（只对已声明的键标注，未验收不给 ✓，D10 口径）；对话框里的 12 键标签表删除，改由字典驱动；⑤ 测试：engine 新增 6 项（字典唯一性 / 位映射穷尽性 / **种子声明 ↔ 运行时位双向一致** / 未知键回退 / 验收文案），workbench `capabilities_come_from_driver_json_with_label_dictionary` 改写（含运行时位与验收断言）。⑥ 待办（已登记能力矩阵 §7 #12）：`driver_properties` 的种子键对两个客户端库都不生效（sqlx 静默忽略、mysql_async 报未知参数）→ 建议按“属性规格化 + 工厂翻译”两步做；以及 `index_analysis` / `table_editor` / `sql_autocomplete` / `export` 等键仍是纯展示（无门控）。
> 驱动类型审计轮（2026-09-19）：**三个静默失效 + 两份重复声明**——背景是「`db_type` 字段名承载驱动 id」这一个语义债（详见 `data-layer-wiring-matrix.md` §7 第 9 条）。① **驱动 id 被当 scheme**：`connection::url::build_connection_url` 拿 `db_type` 拼 `{driver}://`，而 `mysql_async 0.37` 只认 `mysql://`、`tokio-postgres 0.7` 只认 `postgres://`（依赖源码：`opts/mod.rs:1749`、`config.rs:716`）→ 选 Official 驱动保存后，导航点「连接」拿 `mysql_native://…` 建连必失败；新增 `url_params::normalize_url_scheme`，两个 native 工厂在建连前归一（`factory::{mysql_native_url, postgres_native_url}` 提为可测纯函数）。② **SSL 参数不能按数据库族写**：两种客户端库的 TLS 词汇完全不同，且 mysql_async / tokio-postgres **未知参数会直接报错**（sqlx 是静默忽略）——新增 `url_params::{SslMode, append_ssl_params}` 按**驱动**分派，做不到的组合（Official + 证书 / verify）**报错不降级**；同步把「连接安全」内联 SSL（`advanced_options.ssl`）真正接入 `connect_with_type` 与 `build_probe_config`（新增 `apply_inline_ssl_override` + 纯函数 `inline_ssl_override` / `method_has_ssl_hop`，**档案优先**）——此前它只在对话框里读写。③ **DuckDB Secret 类型按族给**：新增 `secret_integration::secret_type_of`（族 id 快路径 → 未命中查 `driver_catalog::type_id_of` → `driver_store::get_type_id`），补 `mariadb → MYSQL`；此前 `mysql_native` 直接返回 `None` → Secret 静默不注册。④ **两份重复驱动声明删除**：`driver/metadata.rs`（`DriverType` + `DriverMetadata`，528 行零引用）与 `driver/driver_config.rs`（未在 `mod.rs` 声明，**不参与编译**，含第二个同名 `BuiltinDriverDiscovery`）；声明唯一真相源定为 `drivers` 表（新发现：`DriverDescriptor` 实际只有 `id` 被读，其余字段无消费者）。⑤ 验证：`cargo check --workspace --all-targets` 零警告；connection 48+4、engine lib 443 passed / 24 ignored、workbench lib 113、`connection_type_driver` 7 / `data_source_lifecycle` 28 / `connection_render_matrix` 2 / `connection_edit_backfill` 1 全绿（三个集成目标需**逐个**跑：并发链接多个重型测试二进制会撞 MSVC `LNK1318` PDB 上限）。⑥ 台账：`driver-capability-matrix.md` 新增 **§2.1 连接串形态矩阵**（含依赖源码行号证据）与 §7 缺口 6–11；`data-layer-wiring-matrix.md` §7 新增 8–10。⑦ **真机待验**：Official 驱动 + 真实 MySQL/PG 的连接与 SSL 注入（本机无端点）。⑧ **同日更正（决策 ② 落地）**：声明权威改为**代码**（`registry/descriptors.rs`）+ 启动幂等 upsert（`driver/declaration.rs`），`drivers` 表降为读模型、迁移种子降为首装兜底——本条目 ④ 里「声明唯一真相源定为 `drivers` 表」以该更正为准（见能力矩阵 §2 / §7 #6、#12）。
> 缺口补齐记录（2026-09-10）：① 环境策略 CRUD（环境管理器内嵌策略面板）与高级 Tab 策略覆盖落库（`advanced_options.policy_overrides`）；② 连接编辑回读（侧边栏「编辑」→ 全 Tab 预填 → `update` 按 ID 前缀路由 G_/P_/GP_）；③ SSL/TLS 配置字段编辑与落库（`advanced_options.ssl`，常规→连接安全）；④ 双作用域 UI（仅全局/仅项目/全局+项目）与落库（P_ 走 `ProjectConnectionStore`、GP_ 走 `generate_gpid` 快照，项目路径来自对话框输入，未打开项目时提示）；⑤ DuckDB 缓存路径落库（`metadata_path`，模型/服务/对话框全链路打通）。
> 前置：v1 后端/前端实现为行为蓝本（`v1/backend/src/core/{services,persistence}`、`v1/frontend/extensions/builtin/connection/ui/components/AddDataSourceDialog.vue`）；v2 engine 持久化层与 connection crate 传输层已完成迁移（见 §6 对齐表）

## 1. 现状结论（盘点摘要）

| 层 | 状态 |
| --- | --- |
| 持久化层（engine：global_db CRUD+update、auth/network/env/driver store、id_prefix、snapshot_service、migrations 008-014） | ✅ 已实现 |
| 传输层（connection crate：ConnectionConfig 协议链、5 连接器、TunnelGuard、factory、stream、known_hosts、DuckDB SecretManager） | ✅ 已实现 |
| 服务编排层（workbench `services/data_source_service.rs`；应用级 test_connection；模型 `connection/src/model.rs`） | ✅ 已实现（落点自 connection crate 调整至 workbench，见 A2 注） |
| UI 层（GPUI 对话框 `workbench/components/connection_dialog.rs`；列表/导航走 `workspace_loader`） | ✅ 已实现 |
| 启动装配（全局系统库单例初始化 + 常驻运行时） | ✅ 已补齐（`crates/app/src/main.rs`） |
| v1 蓝本（stores/services/AddDataSourceDialog.vue 53KB） | ✅ 已实现，可参照 |

**当前缺口**：Phase C（C1–C4）+ 运行时连接服务接入（ConnectionService → UI 连接动作/状态）。

## 2. 阶段划分

### Phase A — 核心闭环（目标：替换 5 字段手填表单）

| # | 任务 | 落点 | 验收 |
| --- | --- | --- | --- |
| A1 | 模型：`DataSource` / `SaveInput` / `TestResult{success,message,latency_ms,version}`，字段对齐 v1 全局扩展 + `G_` 前缀 | `crates/connection/src/model.rs` | 编译通过；字段与 global_connections 列一一对应 |
| A2 | 服务：`DataSourceService`——类型/驱动查询（driver_store）、认证/网络/环境配置读取（stores）、`test_connection`（独立会话，复用 ConnectionFactory + 探测版本）、`save/update/delete`（global_db + AES 凭据 + Secret 联动） | `crates/connection/src/service.rs`（实际：`crates/workbench/src/services/data_source_service.rs`——connection 不得反向依赖 engine） | 集成测试：真实 PostgreSQL/DuckDB 新增→测试→保存→回读→更新→删除 |
| A3 | 对话框骨架：标题栏/左侧栏（搜索+暂存+类型树）/Header（名称+驱动类型+作用域+备注+URI 编辑）/常规 Tab（动态表单+认证+**连接安全 SSL**）/测试/保存/取消 | `crates/connection/src/connection_dialog.rs`（实际：`crates/workbench/src/components/connection_dialog.rs`） | UI 手动走通全流程；配置按 config_schema 渲染 |
| A4 | panels/ 5 字段表单退役，改弹对话框；列表刷新走新服务 | `crates/workbench/src/panels/`、`workspace_loader.rs` | 空态/列表入口可弹框；保存后列表即时刷新 |
| A5 | `update_global_connection` 已有，补齐 `test_connection` 探测 SQL（按 driver） | `engine`（复用现有管道） | 返回 version/latency 正确 |
| A6 | 文档验收：`cargo check --workspace` 零告警；无 unwrap/expect；架构红线复核 | 全仓 | 全绿 |

### Phase B — 完整 Tab

| # | 任务 | 落点 | 验收 |
| --- | --- | --- | --- |
| B1 | 网络 Tab：**引用网络配置档案**（协议链 / SSH / 代理 / SSL / 多跳 `chain`）+ 拓扑预览（DB 带 TLS 徽标）+ 数据路径预览｜✅ 已实现；**内联链已于 2026-09-12 撤下**（占位模型无法执行，见架构 §14 #25 / 决策 #72）——多跳改走 `chain` 类型档案 | 对话框 + `network_store` + `config.rs` 校验 | 档案引用与回读一致；连接入口解析并应用（协议链 / 隧道） |
| B2 | 高级 Tab：环境选择 + 策略摘要/覆盖 + DuckDB 加速卡片（仅网络库）｜✅ 已实现 | 对话框 + `env_store` | 环境引用正确落库；加速开关仅网络库可见 |
| B3 | 能力/驱动属性 Tab：capabilities 矩阵 + driver_properties key-value｜✅ 已实现 | 对话框 | 只读展示正确 |
| B4 | 认证配置管理器覆盖层（CRUD + 脱敏列表 + 类型分类 + 引用联动）｜✅ 已实现 | 对话框 + `auth_store` | 新建/编辑/删除闭环；列表无明文密码；选中引用后字段只读 |
| B5 | 网络配置管理器覆盖层（NetworkConfigManager：档案 CRUD + 引用联动）｜✅ 已实现 | 对话框 + `network_store` | 引用已保存协议链回读一致 |
| B6 | 环境管理器覆盖层（EnvironmentManager：环境 + 策略 CRUD）｜✅ 已实现（策略面板：环境行「策略」→ 类型/启用开关/删除，落库 `env_store` policy） | 对话框 + `env_store` | 环境/策略引用正确 |

### Phase C — 生态收尾

| # | 任务 | 落点 | 验收 |
| --- | --- | --- | --- |
| C1 | introspection 触发 → 数据库导航树衔接 | `database` crate + `global_metadata/<id>` 缓存 | ✅ 2026-09-11 由 database-nav Phase A 承接（实时内省 + 懒加载 + 展开态持久化）；connection 侧无需重复建设 |
| C2 | 双作用域连接：仅全局（G_）/仅项目（P_）/全局+项目（G_ 定义 + GP_ 共享快照）｜✅ 2026-09-11 已接入当前项目会话（对话框项目根自动预填、删除路由携带项目路径、列表按作用域合并显示 P_/GP_）；来源短码展示归 database-nav（B8） | 对话框 + `project_connection_store` + `snapshot_service` | 三种组合落库与回读正确；可见性测试已覆盖（`connection_scope_and_state.rs`） |
| C3 | 遗留 workbench `connection_service.rs` 收敛：调用点逐个迁入 connection crate｜✅ 2026-09-11 传输/协议层全部下沉——第一批（URL 处理族 → `url.rs`）、第二批（URL 参数注入/改写 → `url_params.rs`）、第三批（协议链执行 + 隧道生命周期 → `chain.rs`）；workbench 仅保留依赖 engine 的会话生命周期编排（连接信息 / 持久化 / 元数据缓存 / 连接池），符合依赖方向 | `workbench` + `connection` | 遗留服务仅剩编排层；传输层单测全绿（`cargo test -p rds-connection --lib`） |
| C4 | 连接模板导入导出（无密码）｜⏸ 按决策暂缓（「暂存撤销 / 重做」语义已拆入 C5） | 对话框 | 模板不含明文凭据 |
| C5 | **多连接暂存列表（连续编辑）**：草稿条目新增 / 切换 / 删除（删至最后一条自动补位）、保存后草稿转正式并自动补空草稿、关闭对话框不丢失；参考 v1 v5 §8（`saved-section`）与原型设计 §2.2｜✅ 2026-09-11 已实现（`ConnectionDraft` 快照 + 侧栏暂存列表 + 保存后保持打开连续编辑；窗口测试 4 项） | 对话框侧栏 + `ConnectionDialogState` 草稿快照 | 一个对话框内可连续编辑并保存多个连接；切换不丢字段；关闭对话框草稿保留 |
| C6 | **草稿跨会话持久化（不存密码）**：迁移 `global/020` + `connection_draft_store`（`list` / `replace_all`）；`staging_restore` 首次打开恢复、`staging_persist` 变更与关闭时落库｜✅ 2026-09-11 已实现（`connection_drafts_persist.rs` 跨会话恢复 + 密码不落库） | `crates/engine/migrations/global/020_add_connection_drafts.sql`、`engine::persistence::connection_draft_store`、`connection_dialog/staging.rs` | 重启后草稿与表单恢复；表无 password 列；未初始化全局单例的进程不写库 |
| C7 | **对话框 UX 收尾**：「保存并关闭」/ 条目脏标记`●` / 来源短码`P/G/GP` / 键盘导航（Ctrl+Enter / Ctrl+T / ↑↓）｜✅ 2026-09-11 已实现（Action + `key_context("connection-dialog")`） | `connection_dialog/{render,staging}.rs`、`commands.rs`、`app/main.rs` | 按钮与快捷键行为一致；输入框内 Ctrl+Enter 可保存 |
| C8 | **结构拆分**：`connection_dialog.rs`（3777 行）→ `connection_dialog/{mod,state,staging,render,managers,helpers}.rs`，并删废弃副本｜✅ 2026-09-11 已实现（零警告、测试全绿） | `crates/workbench/src/components/connection_dialog/` | 公开路径不变；工作台测试无回归 |
| C9 | **真机集成用例**：单例临时库连续保存两条连接（暂存转正式 + 补空草稿 + 库可读回）｜✅ 2026-09-11 已实现；导航栏「在对话框中编辑」入口（✎）同步补齐 | `crates/workbench/tests/connection_multi_save.rs`、`database/src/nav_view.rs::render_connection_row` | 两条记录落库；暂存状态机与需求一致 |
| C10 | **类型 × 驱动两层选择去噪**：左侧只选类型（类型树行首 + 暂存条目显示缩小的类型 UI = 类型 emoji），右侧驱动下拉只列**当前类型启用驱动**并显示实现短名（`MySQL (sqlx)` → `sqlx`）；未选类型时 Header 提示「请先在左侧选择数据库类型」；草稿/持久化新增 `type_id` + `driver_id`（迁移 021）｜✅ 2026-09-11 已实现 | `connection_dialog/{helpers,state,staging,render}.rs`、`engine/migrations/global/021_add_draft_type_driver.sql`、`connection_draft_store.rs` | 同类型下实现名唯一；跨类型同名短名不歧义（按类型过滤解析）；旧草稿（完整名）可回退解析 |
| C11 | **布局稳定 / Header 去拥挤 / 标签·分组入口 / 文档体系**：Tab 内容区固定高度 + 内部滚动（切 Tab 不改变对话框高度）；作用域三态分段按钮 + 项目名（悬停显示完整路径）；常规 Tab 新增「组织」卡片（标签 + 项目分组勾选，替换语义同步，迁移 022）；`meta_refreshed` 一次性元数据拉取修复重渲染查库；新增用户指南（含 USIT 清单）与架构文档 §10–§13（字典 / 降级 / 性能·可观测·安全 / 成熟度）｜✅ 2026-09-11 已实现 | `connection_dialog/{render,staging,state,helpers}.rs`、`data_source_service.rs`、`connection_org_store.rs`、`engine/migrations/global/022_add_draft_tags_groups.sql`、`docs/architecture/connection/connection-user-guide.md` | 切 Tab 时侧栏 / Header 不跳动；标签回显与分组勾选可往返（`connection_staging.rs` / `connection_multi_save.rs`） |
| C12 | **C4 模板导入导出（不含密码）**：`ConnectionTemplate` / `TemplateItem` + `templates_{export,import}`（剪贴板 JSON；`kind`/`version` 校验；仅未保存且非空草稿）；**UI 入口按决策暂缓**（避免当前阶段界面变动）｜✅ 2026-09-11 能力与测试就绪（`connection_template.rs` 2 项） | `connection_dialog/staging.rs`、`tests/connection_template.rs` | 导出 JSON 无密码；非法输入被拒且列表不变 |
| C13 | **UI 缺陷修复（真机反馈）**：作用域分段控件改自绘（组件变体默认 token 致文字不可见）；`refresh_driver_items` 校正失效选中；类型同步改为“仅补空”；备注宽度固定 / URI 弹性；暂存操作结果行提示来源｜✅ 2026-09-11 已实现 | `render.rs`、`state.rs`、`staging.rs` | 作用域按钮三态文字可见；类型 / 驱动不再自行变动 |
| C14 | **布局再收敛（真机反馈）**：① 暂存区固定高度 7.5rem + 内部滚动（条目再多不拉长对话框）；② 类型树占满侧栏剩余高度 + 内部滚动；侧栏 `h_full` 高度恒定；③ 项目栏移至**备注行右侧**（固定宽 17rem）且「仅全局」时置灰不可编辑；④ 类型徽标固定宽 8.25rem + 文本省略（切换类型时驱动下拉位置不移动）；⑤ 模板导入导出 UI 入口撤下（待后期）｜✅ 2026-09-11 已实现 | `render.rs` | 暂存 / 类型 / Tab 三区均不随内容拉长；Header 元素位置不随类型变化 |
| C15 | **Header 再设计（建议布局）**：3 行——① 类型徽标（定宽 6rem）+ 名称（弹性）+ 作用域三态分段（短标签 全局 / 项目 / 全局+项目）；② 备注（弹性）+ 项目（定宽 17rem，仅全局灰显）；③ 驱动（定宽 11rem）+ URI（弹性）；标签列统一 2.75rem；类型未辨识时徽标显示「未选类型」+ 驱动位置提示「先选类型」｜✅ 2026-09-11 已实现 | `connection_dialog/{render,helpers}.rs` | Header 高度降低、标签左对齐；切换类型 / 作用域时元素位置不移动 |
| C16 | **类型徽标仅图标 + 项目下拉 + UI 约束规范化（真机反馈）**：类型徽标只显示 emoji（定宽 1.75×1.5rem，未辨识显示 `?`）；项目栏改下拉（当前项目 + 最近项目 + 「手动输入路径…」，非手动模式 render 同步路径，删除 hover 气泡以免遮挡）；新增 `docs/architecture/theme/ui-constraints.md` 并在 `helpers.rs` 抽出尺寸常量（Header/侧栏/暂存区改用常量）｜✅ 2026-09-11 已实现 | `connection_dialog/{render,state,helpers}.rs`、`docs/architecture/theme/ui-constraints.md` | 徽标定宽不移动后续元素；项目可下拉选择或手输；对话框新代码尺寸全部取自常量 |
| C17 | **项目栏单下拉 + 新增项目入口（真机反馈）**：删除「编辑 / 列表」两态切换；项目栏只保留一个下拉，选项为**项目名 + 路径**左右结构（路径弱化且从头部省略、可按名称或路径搜索）；**末项 `＋ 新增项目`** → 置位 `Shared::project_new_request` → 宿主开「新建项目」（脏草稿先走未保存确认）；确认逻辑抽为 `handle_project_confirm`（可测）；项目会话变更**每帧检测**并自动跟随（不再占用元数据一次性守卫）｜✅ 2026-09-12 已实现 | `connection_dialog/project_picker.rs`（新增）、`{state,render,mod}.rs`、`panels/`（`project_new_request`）、`view.rs`、`tests/connection_project_picker.rs` | 下拉无模式切换；选项名 + 路径双列展示；选项目写回路径；`＋ 新增项目` 可拉起新建项目；4 项窗口/契约测试全绿 |
| C18 | **项目侧连接编辑回读（㉟ ④ 待办闭环）**：新增 `DataSourceService::get_with_project(id, project_path)`（按 ID 前缀路由：P_/GP_ 开项目库取行并映射为 `DataSource`，其余走全局 `get`），`load_for_edit` 增加 `project_root` 参数（`open` 传会话项目根、`apply_draft` 传条目路径，缺路径时回退会话根），分组勾选回显改用同一项目根｜✅ 2026-09-12 已实现 | `services/data_source_service.rs`（`get_with_project` + `map_project_connection_to_data_source`）、`connection_dialog/{state,staging,render}.rs`、`tests/data_source_lifecycle.rs` | P_/GP_ 编辑回读字段完整（含标签 / 分组 / SSL / 协议链）；无项目根时不报错仅降级为空表单 |
| C19 | **导航入口同源修复 + 原型/文档全量同步**：① `nav_runtime::load_entry(_with)` 改走 `get_with_project`——此前导航树点 P_/GP_ 连接的「连接」会报「数据源不存在」（与 C18 同一根因）；② `connection-dialog-prototype.html` 按实现重写（Header 3 行 / 类型徽标仅图标 / 三态作用域 / 项目单下拉 / 扁平类型树 / 真实类型与驱动目录 / 暂存条目徽标 / 四张卡片 / 保存并关闭）；③ 原型文档 §2/§4/§6/§7、用户指南 §3/§4.2/§5/§9、架构 §12/§13 同步；④ **新增架构 §14「已知问题与后续项」**（13 项，🔴/🟡/⚪ 分级）作为权威待办清单｜✅ 2026-09-12 已实现 | `services/nav_runtime.rs`、`tests/data_source_lifecycle.rs`、`connection-dialog-prototype.html`、`connection-prototype-design.md`、`connection-user-guide.md`、`connection-dialog-architecture.md` | 导航树可连接项目侧连接；原型与实现一致；待办清单可追踪（新增 1 项导航侧测试） |
| C20 | **已知缺口收口（§14 #1/#2/#3/#5 + 新缺陷）**：① 类型目录按 `enabled` 过滤（`WHERE enabled = 1`）；② 无可用驱动的类型**置灰 + 「暂无驱动」+ 不可选**（`select_type` 拒绝并说明原因；驱动位保留禁用占位），新增 `type_has_driver`；③ 项目下拉补 **「打开现有目录…」**（动作项在前、末项仍为「＋ 新增项目」；宿主 `project_open_request` → `open_folder_dialog`，脏草稿同走未保存确认）；④ **GP_ 快照同步**：`DataSourceService::sync_snapshot_from_global`（配置 + 凭据密文，保留 ID / 创建时间 / 分组）+ footer 「从全局定义同步」（仅 GP_ 编辑时显示）；⑤ **修复项目侧更新清空密码**（`ProjectConnectionStore::update_connection` 改 `COALESCE`，与全局侧语义对齐）｜✅ 2026-09-12 已实现 | `engine/persistence/{driver_store,project_connection_store}.rs`、`connection_dialog/{render,state,helpers,project_picker,mod}.rs`、`services/{data_source_service,nav_runtime}.rs`、`panels/`、`view.rs`、`tests/{connection_type_driver,connection_project_picker,data_source_lifecycle}.rs` | 无驱动类型不可选且能解释；目录项目可选；快照可显式同步；编辑不再清空密码（全工作区 check 零警告） |
| C21 | **数据来源审计（零 UI 造数据，§15）+ 项目栏去无效态**：① 能力矩阵改读 `drivers.capabilities`（字典外键原样展示）；② 高级 Tab 策略覆盖清单/摘要改读 `environment_policies`（切环境每帧重查；覆盖键 = `policy_type`；未选环境不造默认值），`refresh_env_policies` / `toggle_policy_override` / `list_environment_policies_by_name`；③ 环境管理器策略标签按 `policy_type` 字典映射（修复“5 条策略全错标只读连接”与新建时写假 `policy_type`）；④ `DataSourceService::get` → `get_global`；⑤ 项目下拉补「不需要项目（仅全局）」（切作用域，非清除空态）；⑥ 草稿字段 `sec_overrides` 改存策略类型（旧布尔数组解析失败即忽略）｜✅ 2026-09-12 已实现 | `connection_dialog/{helpers,state,render,managers,project_picker,staging,mod}.rs`、`services/data_source_service.rs`、`tests/{helpers 单测,data_source_lifecycle,connection_project_picker}` | UI 无自造业务数据（§15 逐项表）；环境策略与能力矩阵与库一致；全工作区零警告 + 85 项工作台测试全绿 |
| C22 | **脏数据防护 + 接口一致性（USIT 前置）**：① **项目根预检读写分离**（`workspace_loader::is_project_root`，忽略大小写；写路径报错、读路径降级为空，均不建目录）；② 项目侧连接时间戳由存储层 `COALESCE(NULLIF(?,''), CURRENT_TIMESTAMP)` 兜底；③ **项目元数据目录拼写统一 `.RSmeta`**（engine project_db / org_store、insight、workbench nav_store）；④ **「认证方法」闭环**：常规 Tab 新增下拉（选项 = `drivers.supported_auth_types`）+ 保存 / 回读 + 草稿新列（迁移 `023`）+ 连接链路按档案 `auth_type` 兜底（修“引用档案不注入凭据”）；⑤ **暂存列表按作用域合并**（`load_connections_for_scope`，P_/GP_ 也在列）+ 幻影条目清理；⑥ 全局库未初始化时跳过合并（不碰用户真实目录）｜✅ 2026-09-12 已实现 | `services/{data_source_service,workspace_loader,connection_service,nav_store}.rs`、`engine/persistence/{project_db,connection_org_store,project_connection_store,connection_draft_store}.rs`、`engine/migrations/global/023_add_draft_auth_method.sql`、`insight/src/rule_registry.rs`、`connection_dialog/{helpers,mod,state,render,staging}.rs`、`tests/{data_source_lifecycle,connection_scope_and_state,connection_multi_save,connection_type_driver,connection_drafts_persist}.rs` | 无效项目路径不再造骨架；项目连接时间戳非空；引用认证档案可真正注入凭据；暂存列表与导航一致且无幻影条目；工作台 91 项测试 + engine 228 全绿、`check --workspace --all-targets -j 2` 零警告 |
| C27 | **驱动注册 / 文件型取址 / 条目显示（USIT 第 5 轮）**：① `initialize_global_system` 首行注册内置驱动（修 `CONN_DRIVER_NOT_FOUND`，导航连接/重连一并恢复）；② sqlite/duckdb 工厂改从 `to_url()`（`url_override`）取地址并去前缀/查询串（修「Database path is required」）；③ 暂存条目当前项显示 live 表单（类型徽标/名称/脏标记同源，修“表单 SQLite、条目还显示 mysql”）｜✅ 2026-09-12 已实现 | `engine/{migration/global_init.rs,driver/factory.rs,driver/auto_register.rs}`、`connection_dialog/{helpers,render,staging}.rs`、`tests/data_source_lifecycle.rs` | 测试连接/真实连接可通；SQLite 探测建库成功；条目与表单一致；engine 228 + 工作台 107 项全绿 |
| C26 | **连接设置可编辑 + 字段⇄URI 双向同步（USIT 第 4 轮）**：① 主机/端口/数据库 改**可编辑输入**（修“连接设置无法输入”）；② 新增 `connection::url_params::rewrite_url_authority`（scheme 无关，保留凭据/查询串；端口空→默认端口；数据库空→去路径；主机空→不重建）与 `data_source_service::rebuild_url_from_fields`（幂等）；③ 渲染层每帧单方向同步（`fields_synced_for` 防循环），保存仍以 URI 为权威（引擎写 `host`/`port`/`database` 列）；④ 结构尺寸登记到 `ui.rs`（`DIALOG_*`）并对齐全局间距（`GAP_SM` 0.25rem）与 `theme.radius`；⑤ 文档补“UI 字段 → 落库列”映射表（用户指南 §7.1）｜✅ 2026-09-12 已实现 | `connection/src/url_params.rs`、`services/data_source_service.rs`、`connection_dialog/{helpers,state,render,mod}.rs`、`ui.rs`、`docs/architecture/connection/connection-user-guide.md` | 连接设置可直接输入且与 URI 双向一致；字段全部有落库列；`rds-connection` 42 + 工作台 104 项测试全绿 |
| C25 | **大纲视觉收敛 + 文件选择修复（USIT 第 3 轮）**：① 标签列 92px→68px + 间距 12px→6px（修“标签离输入框太远”）；② 只读值改回**数据框**（`value_box`，白底 + `input` 边框）；③ 分组改**整幅面板**（`group_box` + 圆角 + 标题分隔线）区分区域；④ `新建文件…` 改用系统**保存**对话框（`prompt_for_new_path`，可输新名；已存在则直接引用不清空），打开/取消/失败均写结果行；⑤ 未选类型/驱动时表单**显示但置灰不可输入**（Input/Select/Checkbox/按钮统一 `disabled`）｜✅ 2026-09-12 已实现 | `connection_dialog/{helpers,state,render,mod}.rs`、`docs/architecture/connection/*`、原型 HTML | 标签贴近控件；只读值有数据框且可见；分组可辨；新建/打开可验证（取消也有提示）；未选类型不可输入；98 项测试全绿 |
| C24 | **单列分组大纲 + 输入可见性 + schema 驱动字段（USIT 第 2 轮）**：① 主题 `input.border` Light `#B0B0B0` / Dark `#4F4F4F`（修“输入框几乎看不见”）；② 常规 / 高级 Tab 改为单列可折叠分组（`outline_section` / `form_row` / `text_row` / `hint_line`），删除卡片（`sec_card` / `val_readonly`）；折叠态存 `collapsed_sections`（默认全展开，不落库）；③ 字段集合改由 `drivers.config_schema.fields[]` 推导（存在性 / 标签 / 占位；地址行取 `type=file`），schema 为空回退内置字段；④ 用户名/密码标签优先取 schema；⑤ 组集合仍随驱动类型（文件型 = 连接设置 + 组织；SSL 组按声明）｜✅ 2026-09-12 已实现 | `connection_dialog/{helpers,state,render,mod}.rs`、`assets/themes/rds-theme.json`、`tests/connection_type_driver.rs` | 亮色下输入框清晰可见；分组可折叠且不改变对话框高度；行集合与标签跟 `config_schema` 一致；工作台 98 项测试全绿 |
| C23 | **常规 Tab 动态渲染 + 文件型地址链修复（USIT 第 1 轮）**：① 地址标签 / 占位随驱动（网络型取 `drivers.url_template` + `default_port`；文件型给文件提示）；② 文件型 Header 不再放地址框，地址输入 + `打开文件…` / `新建文件…` 移入「常规 → 连接设置」（`App::prompt_for_paths`；新建时创建空库文件）；③ 常规 Tab 卡片集合按驱动：文件型 = 连接设置 + 组织（无认证 / SSL），网络型四张卡且 SSL 仅驱动声明 `ssl` 时显示；④ **修文件型地址丢失**：`parse_url_host_port_db` 文件型返回裸路径（`normalize_file_db_path` 去 scheme / 修三斜杠），`build_effective_url` 不注入凭据，`reconstruct_url` 回裸路径；⑤ `strip_file_db_noise` 清洗文件型连接的凭据 / 网络 / TLS；⑥ `delete` 之外的文件型路径也同步落到 `database`（项目侧 `build_connection_url` 不再报缺路径）｜✅ 2026-09-12 已实现 | `connection_dialog/{helpers,state,render}.rs`、`services/data_source_service.rs`、`tests/{data_source_lifecycle,connection_type_driver}.rs` | 文件型地址可填可选可回读且两侧落库；常规 Tab 随驱动变形；工作台 94 项测试全绿 |
| C13 | **UI 缺陷修复（真机反馈）**：作用域分段控件改自绘（组件变体默认 token 致文字不可见）；`refresh_driver_items` 校正失效选中（避免列表与显示不一致）；类型同步改为“仅补空”（不再将用户选择的类型弹回驱动所属类型）；备注宽度固定 / URI 弹性；暂存操作结果行提示载入 / 恢复 / 新建来源｜✅ 2026-09-11 已实现 | `render.rs`、`state.rs`、`staging.rs` | 作用域按钮三态文字可见；类型 / 驱动不再自行变动 |
| C28 | **驱动声明单源（决策 ②/③）**：① 声明权威在代码——`DriverDescriptor` 新增 `driver_properties`（**只写各客户端库真认的键**：`mysql` 不写、`postgres`/`postgres_native` 写 `application_name`、`mysql_native` 写 `max_allowed_packet`）与 `capability_keys()`（显式能力键 + 由 `supports_*` 布尔位**派生** `ssh_tunnel`/`ssl_tls`/`proxy`）；② 新增 `driver/declaration.rs`：descriptor → `drivers` 行列值（`config_schema` 按视图实际读的键组装），启动 `sync_startup_state` 幂等 upsert，**不覆盖** `enabled`/`download_*`/`driver_files`（列归属写进头注）；③ 迁移 008/013/016/017 的种子降为**首装兜底**；④ 能力字典补三个网络键（15 键）；⑤ 修 `postgres*` 的族 id（`postgres` → `postgresql`）并补齐声明侧漏掉的能力键（DuckDB 的 `transactions`/`schema_browser`、SQLite 的 `index_analysis`）；⑥ 显示名与既有种子对齐（`MySQL (sqlx)` / `MySQL (Official)` 这种「库 + 实现」写法不能丢——下拉按类型过滤后靠括号内的短名区分同族实现）｜✅ 2026-09-19 已实现 | `engine/src/driver/{registry/descriptors.rs,declaration.rs,capability.rs,mod.rs}`、`engine/src/migration/global_init.rs`、`engine/src/persistence/{driver_store,driver_catalog}.rs`、`engine/migrations/global/{008,013,016,017}_*.sql`（仅注释） | 启动 upsert 与声明逐列一致（`startup_sync_writes_driver_declarations_and_is_idempotent`）；用户关掉的驱动不被启动打开；能力键 ↔ 运行时位双向一致（`declared_capabilities_and_runtime_bits_agree`）；属性键全在各库接受清单内（`declared_property_keys_are_accepted_by_the_client_library`） |
| C29 | **属性规格化（能力矩阵 §7 #9 阶段 2）**：① 新增 `engine/src/driver/property_spec.rs`——每驱动一份「客户端库认的键清单 + 中文标签 + 副作用提醒 + 未知键的效果」，判决入口 `verdict(driver, key)`（四种：会下发 / 当前实现会忽略 / 当前实现会报错 / 当前实现不下发），另给 `known_keys`（常用键）与 `accepts`（声明自检用）；② 属性页逐行标「去向」（三色）并列出该驱动常用键，添加时立即分级反馈；③ `declaration` 的接受键测试改为读这份规格（不再自己拷一份清单）；④ **顺手修掉真机拖出来的根因 bug**：`DriverConnectionConfig::append_query_params` 硬拼 `?`，URL 已有查询串时会把已有参数并进上一个键的值（`?ssl-mode=DISABLED` + `?ssl_mode=prefer` → sqlx 报 `unknown value`）——改为按有无查询串选 `?` / `&`，补单测；⑤ 真机对照用例：同一对 UI 旧初值交给 native 驱动报错、交给同族 sqlx 驱动则静默忽略（一矩阵四种命运的真机证据）｜✅ 2026-09-19 已实现 | `engine/src/driver/property_spec.rs`、`engine/src/driver/registry/config.rs`、`workbench/src/components/connection_dialog/{helpers,render}.rs`、`workbench/tests/{connection_type_driver,official_driver_real}.rs` | 属性页行行有去向、添加即有反馈；`property_spec` 六个单测（驱动覆盖 / 标签键必须在接受清单内 / 四种命运 / 空键与未收录驱动不装作知道）+ `helpers` 翻译层单测 + 窗口测试（`conn-prop-note-*` 锚点）；真机 5/5 |
| C30 | **文件型属性真下发（§7 #9 阶段 3）**：`driver_properties` 对 `sqlite` / `duckdb` 从“写下就存在”改成“写下就生效”——SQLite：`journal_mode` / `synchronous` / `busy_timeout` / `foreign_keys` / `cache_size` / `temp_store` 转 PRAGMA，`mode`（ro/rw/rwc）转开库标志；DuckDB：`access_mode`（automatic/read_only/read_write）转开库配置，`threads` / `memory_limit` / `temp_directory` / `max_temp_directory_size` / `preserve_insertion_order` 转 `SET`（排在 `configure_connection` 之后 → 用户值胜过应用默认）。取值全部**白名单校验**后才拼 SQL（用户输入不能直接进语句），应用后**读回比对**（`journal_mode` 用 PRAGMA 的回值，其余读一次；不符/非法 → 报错带键与值）；旧驼峰名作为别名仍能生效；清单外的键不执行（记 warn + 属性页标“不认这个键”）；两个文件型工厂改传 `config.driver_properties`（含接线回归测试）｜✅ 2026-09-19 已实现 | `engine/src/driver/native/{sqlite,duckdb}.rs`、`engine/src/driver/registry/descriptors.rs`（注释）、`engine/src/driver/factory.rs`、`workbench/src/components/connection_dialog/helpers.rs` | 引擎新增 8 项：规划白名单/注入拒绝（两边）、真库读回（`wal` / `busy_timeout` / `foreign_keys` / `synchronous` / `temp_store` / `cache_size` / `temp_directory` / `threads` / 保序）、`:memory:` 不能 WAL 必报错、`mode=rw` 不建文件、`access_mode=read_only` 拒写、工厂接线；真机 `editor_exec_real`（四库）与 `official_driver_real` 全绿 |
| C31 | **能力键分归属 + L1 联邦再验（§7 #9 阶段 4）**：① `CapabilitySpec::scope`（`Driver` / `App`）：`export` / `mock` / `resource` 是应用级功能（无驱动声明、不随驱动变），从能力矩阵里拿出、改由一句说明带出；两个新单测盯住「应用级键不得被任何驱动声明」与「驱动声明的键必须在能力字典里」；② 同轮清点门控现状（台账记录）：只有 `federation` 与 `transactions` 两个键有真消费者，其余 10 个键只是展示——要门控得先定“哪个键管哪个入口”（属产品拍板，不是接线活）；③ L1 跨源真机复验（`RDS_TEST_MYSQL_URL` + `RDS_TEST_SQLITE_PATH` 都要设，否则用例静默跳过）｜✅ 2026-09-20 已实现 | `engine/src/driver/{capability.rs,mod.rs}`、`workbench/src/components/connection_dialog/{helpers,render}.rs`、`docs/architecture/driver-capability-matrix.md`（§3.3 / §4）、对话框架构 §15.2 + 决策 #99 | 能力矩阵不再出现应用级行（`capability_rows_exclude_app_level_features`）；联邦四条真机全绿（跨源 join / 换主源 / 重挂 354 表 / 单源如实拒绍）|

## 3. 测试场景清单（参照 v1 §10.4，Phase A 覆盖前 6 项）

1. 新建连接（网络库）：类型选择 → 表单 → 测试成功（版本+延迟）→ 保存 → 列表出现 → 回读字段一致
2. 新建连接（文件库：SQLite/DuckDB）：文件选择、网络 Tab 隐藏、保存成功
3. 测试失败：错误主机/密码 → 红色错误信息，不落库
4. 更新连接：改端口/密码 → 保存 → 回读更新值；凭据仍加密
5. 删除连接：列表移除 + Secret 清理（若开启联邦）
6. 作用域三态：仅全局→G_；仅项目→P_；全局+项目→G_ 定义 + GP_ 共享快照；Header 提示行实时更新（Phase A 支持选择与提示，Phase C 全量落库）
7. 认证配置：引用已保存配置 → 字段只读；管理覆盖层新建/编辑/删除走 AES 加密（Phase B）
8. 网络：引用「网络配置」档案 → 保存回读一致；多跳用 `chain` 类型档案（`ChainHop` JSON 数组）；连接入口解析为 `ConnectionMethod` 并逐跳执行（内联链已撤下）
9. 环境：切换环境 → 策略摘要变化；覆盖标记"已覆盖"；环境管理器 CRUD（Phase B）
10. DuckDB 加速：开关开启 → Secret 注册；删除连接 → Secret 移除
11. URI 预览：字段变更实时更新
12. 暂存：关闭对话框 → 重开恢复未保存配置
13. 明暗主题切换：对话框各区域 token 正确（Light/Dark 手动核对）

## 4. 风险与对策

| 风险 | 对策 |
| --- | --- |
| workbench `connection_service.rs`（85KB）与 engine 连接管道职责重叠 | Phase A 并行、Phase C 收敛，避免一次性大搬迁 |
| 测试连接污染正式连接状态 | 独立测试会话（新建连接对象，不注册进连接池/管理器） |
| 凭据泄露（对话框/导出/日志） | 全程 auth_store AES 加密；列表脱敏；模板导出无密码 |
| 动态表单与 config_schema 格式漂移 | 复用 v1 `parseConfigSchema` 兼容逻辑（自定义 fields 与 JSON Schema 双格式） |

## 5. 与 v1 实现对齐表

| v1 设计/实现 | v2 现状 | 本方案 |
| --- | --- | --- |
| `DATA-SOURCE-MODULE.md` 表设计/ID 前缀/快照 | engine 已迁移（migrations 008-014） | 直接对接，不重建 |
| `auth_configs` AES-256-GCM | `auth_store` 已实现 | 认证 Tab 对接 |
| `network_configs` 协议链 + CONNECTION-METHOD-DESIGN | `network_store` + connection crate 传输层 | 网络 Tab 对接 + 校验器 |
| `environments` + 5 类策略 | `env_store` 已实现 | 高级 Tab 对接 |
| `AddDataSourceDialog.vue`（V3 五 Tab） | GPUI 占位 | 按 v5 布局重写 |
| 40 个 data_source_commands | 已退役/迁移（Tauri→GPUI 命令） | 对话框直接调 store/service |

## 6. 验证方式

- 每阶段：`cargo check -p rds-workbench -p rds-connection --all-targets` 零告警
- 连接模块测试清单（不影响布局调试）：
  - `cargo test -p rds-connection --lib`（协议链执行 + 隧道注册表 / URL 处理族 / url_params / Secret / known_hosts / 模型）
  - `cargo test -p rds-engine --lib`（持久化与迁移层回归）
  - `cargo test -p rds-workbench --test data_source_lifecycle`（服务层单测 + 全局联动）
  - `cargo test -p rds-workbench --test connection_dialog_ui`（窗口级 headless 测试）
  - `cargo test -p rds-workbench --test real_connections`（加载器契约）
  - `cargo test -p rds-workbench --test global_service_singleton`（单例生产路径：`global()` / 列表单例分支 / 重复注入拒绝）
  - `cargo test -p rds-workbench --test connection_scope_and_state`（作用域可见性 P_/GP_ 合并 + 运行态 connected 填充）
  - `cargo test -p rds-connection --test tunnel_roundtrip`（隧道数据面：SOCKS5 / HTTP CONNECT / 两跳链真实转发 + 守卫释放关闭）
  - `cargo test -p rds-workbench --test connection_tunnel_cleanup`（连接失败路径隧道回收）
  - `cargo test -p rds-workbench --test dialog_host_layer`（对话框宿主层渲染：入口调起 / 关闭移除层 / 面板 notify 级联）
  - `cargo test -p rds-workbench --test connection_staging`（暂存列表：切换 / 补位 / 转正式 / 已保存不删除）
  - `cargo test -p rds-workbench --test connection_drafts_persist`（跨会话恢复：变更落库 → 新状态恢复；密码不落库）
  - `cargo test -p rds-workbench --test connection_multi_save`（单例临时库连续保存两条 → 暂存转正式 + 补位）
  - `cargo test -p rds-workbench --test connection_type_driver`（类型 × 驱动：选类型过滤下拉 + 短名显示 + 按驱动 id 回读 + 快照携带 type_id/driver_id）
  - `cargo test -p rds-workbench --test connection_staging`（暂存：切换 / 补位 / 转正式 / 标签与分组快照）
  - `cargo test -p rds-engine --lib org`（组织元数据：标签检索 / 分组多对多 / 替换语义 / 全局库降级）
  - `cargo test -p rds-engine --lib driver`（驱动声明单源：能力键 ↔ 运行时位、属性键接受清单、upsert 幂等 / 不越权）+ `--lib startup_sync`（启动接线）
  - `cargo test -p rds-workbench --test connection_template`（模板：导出无密码 / 导入还原 / 非法输入拒绝）
- **编译/测试统一加 `-j 2`**（或用 alias `cargo test-all`）：并发链接重型 crate 会耗尽内存（DuckDB 已改动态链接），触发 rustc `STATUS_STACK_BUFFER_OVERRUN` 崩溃并拖慢宿主（Zed 卡顿）；`.cargo/config.toml` 已含 `RUST_MIN_STACK` 补偿与 alias 说明
- UI：`cargo run -p rds-app -j 2` 手动走通 §3 场景清单（实例日志可重定向：`target/rds-app.log` / `target/rds-app.err.log`；若运行期报主线程栈溢出，参记录 ㉑）
- 主题：明暗切换核对 token（theme-preview.html 色卡为基准）

## 7. 实现位置映射（设计决策 → 代码文件）

| 设计决策 | 代码文件 |
| --- | --- |
| 启动初始化全局系统库（单例 + 常驻运行时） | `crates/app/src/main.rs` |
| 全局库单例注入（测试 / 嵌入） | `crates/engine/src/migration/global_init.rs`（`install_global_db_manager`） |
| 全局库 / 分析库路径定义 | `crates/engine/src/migration/global_init.rs` |
| 连接加载（单例优先，路径注入降级） | `crates/workbench/src/services/workspace_loader.rs` |
| DuckDB 迁移执行（复用已打开连接，global / project 共用） | `crates/engine/src/migration/duckdb.rs` |
| 连接 CRUD / 测试 / 同名检查 / 项目预检 | `crates/workbench/src/services/data_source_service.rs` |
| DuckDB Secret 注册 / 注销 / URL 解码 | `crates/workbench/src/services/secret_integration.rs`、`crates/connection/src/secret.rs` |
| URL 组装 / 脱敏 / 凭据提取（C3 收敛） | `crates/connection/src/url.rs` |
| URL 参数注入 / 改写（认证 / SSL / Kerberos / host:port / no_proxy） | `crates/connection/src/url_params.rs` |
| 协议链执行 / 隧道生命周期（`TunnelRegistry` / `apply_network_method` / `process_chain`） | `crates/connection/src/chain.rs` |
| 隧道数据面集成测试（SOCKS5 / HTTP CONNECT / 两跳链） | `crates/connection/tests/tunnel_roundtrip.rs` |
| 失败路径隧道回收测试（`tunnel_count` 断言） | `crates/workbench/tests/connection_tunnel_cleanup.rs` |
| 对话框宿主重绘桥（`Shared::host_redraw` / `notify_host`）与可测入口 | `crates/workbench/src/panels/`（`EditorPanel::{request_new_connection, request_edit_connection}`）、`crates/workbench/src/view.rs` |
| 对话框宿主层渲染测试（`debug_bounds("dialog-layer")`） | `crates/workbench/tests/dialog_host_layer.rs` |
| 暂存列表行为测试（多连接连续编辑，C5） | `crates/workbench/tests/connection_staging.rs` |
| 暂存草稿跨会话持久化（C6） | `crates/engine/src/persistence/connection_draft_store.rs`、`crates/engine/migrations/global/020_add_connection_drafts.sql`、`crates/workbench/tests/connection_drafts_persist.rs` |
| 草稿类型/驱动列（C10） | `crates/engine/migrations/global/021_add_draft_type_driver.sql` |
| 类型 × 驱动两层选择（C10） | `connection_dialog/helpers.rs`（`driver_short_name` / `find_driver_by_value` / `type_badge`）、`state.rs`（`select_type` / `set_driver_by_value` / `resolve_driver`）、`render.rs`（类型徽标 + 驱动短名下拉 + 条目 emoji） |
| 类型 × 驱动测试（C10） | `crates/workbench/tests/connection_type_driver.rs` |
| 标签 / 分组服务接口（C11） | `crates/workbench/src/services/data_source_service.rs`（`list_groups` / `groups_of` / `set_connection_groups`） |
| 分组替换写库（C11） | `crates/engine/src/persistence/connection_org_store.rs::set_connection_groups` |
| 草稿标签 / 分组列（C11） | `crates/engine/migrations/global/022_add_draft_tags_groups.sql` |
| 用户使用指南（C11） | `docs/architecture/connection/connection-user-guide.md` |
| 模板导入导出（C12） | `connection_dialog/staging.rs`（`ConnectionTemplate` / `TemplateItem`）+ `render.rs`（标题行按钮）+ `tests/connection_template.rs` |
| 连续保存集成用例（C9） | `crates/workbench/tests/connection_multi_save.rs` |
| 连接组织元数据（标签 / 分组，M3 与 M4 共用） | `crates/engine/src/persistence/connection_org_store.rs` |
| 作用域可见性 / 运行态状态填充 | `crates/workbench/src/services/workspace_loader.rs` |
| 连接对话框（五 Tab / 作用域 / SSL / 编辑回读） | `crates/workbench/src/components/connection_dialog/`（`mod` / `state` / `staging` / `render` / `managers` / `helpers`） |
| 对话框原型对齐辅助（`sec_card` / `grid_row` / `val_readonly` / `reuse_note` / `lucide`） | `crates/workbench/src/components/connection_dialog/helpers.rs` |
| 对话框快捷键 Action（Ctrl+Enter / Ctrl+T / ↑↓） | `crates/workbench/src/commands.rs`、`crates/app/src/main.rs` |
| 领域模型（DataSource / Scope / TestResult） | `crates/connection/src/model.rs` |
| 传输层（协议链配置 / 连接器 / 流 / known_hosts） | `crates/connection/src/{config,connector,factory,stream,known_hosts}.rs` |
