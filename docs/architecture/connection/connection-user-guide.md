# 数据源连接模块 · 使用指南（M3）

> 读者：产品 / 测试（USIT）/ 新加入的开发者。设计原理见 `connection-dialog-architecture.md`；
> 视觉与交互原型见 `connection-prototype-design.md`；任务与轮次记录见 `connection-dev-plan.md`。

---

## 1. 这个模块能做什么

管理**数据源连接**本身：新建 / 编辑 / 测试 / 删除连接，配置作用域（全局 / 项目）、标签、分组、
SSL 与协议链（SSH / 代理）、认证与环境引用、DuckDB 本地加速。

不在本模块内：数据库导航树、表数据浏览、分组/标签的**视图消费**（那是 database-nav 模块）。
本模块只负责"连接的定义与组织元数据的写入"，导航模块负责"按分组/标签展示与检索"。

---

## 2. 入口

| 入口 | 位置 | 行为 |
| --- | --- | --- |
| 新建数据源 | 编辑区数据库面板「新建数据源」按钮（空态 / 列表态均有） | 打开新建对话框 |
| 编辑连接 | 数据库导航栏连接行 ✎ | 打开对话框并回读该连接全部字段 |
| 连续新建 | 对话框内「暂存列表 → + 添加」 | 在同一对话框内编辑下一条连接 |

> 打开 / 关闭对话框后宿主会显式重绘（`Shared::notify_host`）；若发现点入口无反应，
> 见 §8 排查。

---

## 3. 界面导览

```
┌─ 标题栏：新建/编辑数据源连接 ─────────────────────────────────────┐
│ 左栏（200px，高度恒定）        │ 主区（Header 3 行，建议布局）        │
│  搜索类型                     │  ① 类型徽标(仅图标) · 名称(弹性) · 作用域分段
│  暂存列表（+ 添加）            │  ② 备注(弹性) + 项目(定宽，仅全局灰显；  │
│    [固定高度，超出滚动]         │       单下拉：项目名 + 路径，末项新增项目)  │
│    类型徽标 + 名称 ●/P/G/GP    │  ③ 驱动(定宽) · 地址(弹性)            │
│  数据库类型树                  │  Tab 条：常规/网络/能力/驱动属性/高级 │
│    [占满剩余，超出滚动]         │  Tab 内容（固定高度，超出滚动）       │
│                               │  结果提示行                         │
├──────────────────────────────────────────────────────────────────┤
│ 底栏： [测试连接]  [取消]  [保存]  [保存并关闭]                    │
└──────────────────────────────────────────────────────────────────┘
```

> Header 只保留**需要操作的三类信息**：身份（类型徽标 + 名称）、落点（作用域 + 项目）、连接串（驱动 + 地址）与备注；
> 类型徽标常驻行首便于一眼识别当前数据库类型，类型未辨识时徽标与驱动位置同步给出提示。

> **地址列随驱动变形**：网络型（MySQL / PostgreSQL…）标签为 `URI`，Header 内直接编辑，占位会按当前驱动的连接模板变化（如 `mysql://user:password@localhost:3306/db`，不再是写死的 mysql 示例）；
> 文件型（SQLite / DuckDB）标签为 `地址`，Header 只显示当前文件路径（截断），**实际的地址输入与文件选择在「常规 → 连接设置」卡片里**（只保留一个地址输入框，避免两处分不清权威值）。

> 左右两侧均为**区域固定 + 内部滚动**：条目 / 类型 / Tab 内容再多也不会拉长对话框（保证操作时视线不跑位）。

> **常规 / 高级 Tab 用「单列分组大纲」**：一节一个可折叠分组（标题行 = chevron + 图标 + 标题），组内每行是「标签（定宽）+ 控件（占满剩余宽）」；只读值以纯文本展示。
> 不再用并排卡片：卡片在窗口变窄时会换行、高度参差，且输入框底色与卡底同色时几乎看不见。

**关键分工：类型在左，驱动在右。**

- 左栏选中**数据库类型**（MySQL / PostgreSQL / SQLite / DuckDB…），类型行首是类型图标（emoji）。
- 右栏「驱动」只列**当前类型的驱动实现**，显示实现短名：`sqlx`、`Official`、`rusqlite`、`duckdb-rs`。
- 未选（或无法辨识）类型时，驱动位置保留**禁用下拉占位**（placeholder「先选类型」）而不是换行裸文本。

> ⚠️ **当前只内置 4 个驱动**：MySQL（`sqlx`）、PostgreSQL（`sqlx`）、SQLite（`rusqlite`）、DuckDB（`duckdb-rs`）。
> 其余类型（MariaDB / Oracle / SQL Server / ClickHouse 等）仍然列在类型树里，但**置灰并标注「暂无驱动」**：
> 点它们不会切换类型，只会在结果行提示原因（避免“选了类型却存不了”）。驱动安装（插件）能力开放后这些类型自动可用。
> MongoDB / Redis 在类型目录里本身就是禁用状态（`enabled=0`），不会出现在类型树。

**认证方法**（常规 Tab「数据库认证」卡片）：下拉选项来自**驱动声明**（`drivers.supported_auth_types`）；
引用认证档案时该行只读并显示档案声明的方法（连接时也是用它的类型注入凭据）。

**类型树**：左侧下半区，按分类平铺（关系型 / 文件型 / 分析型 / NoSQL），分类行显示 `名称（数量）`，条目行 = 选中指示条 + 类型 emoji + 类型名（不可选的类型右侧额外显示「暂无驱动」）；**不可折叠**（超出时区域内部滚动）；上方搜索框同时过滤类型名 / 类型 id / 驱动名。

**项目栏（落点）**：

- 作用域为 `仅全局` 时项目栏**禁用**（placeholder「仅全局不需要」），三态切换不会引起布局跳动。
- 其余作用域下为一个**下拉框**：每一行是「**项目名**（左）+ **路径**（右，弱化、超长从头部省略）」；点开可搜项目名或路径。
- 尾部三个动作项（顺序固定）：
  - **`不需要项目（仅全局）`**：把作用域切为「仅全局」并清空项目（项目作用域必须有项目，不会停在无效态）；
  - **`打开现有目录…`**：选择一个不在“最近项目”里的本机项目（走项目管理的目录选择）；
  - **`＋ 新增项目`**（末项）：弹出「新建项目」；创建成功后新项目成为当前会话，本项目栏自动跟随。
- 有未保存草稿时，后两个动作会先弹未保存确认（不静默丢弃）。
- 选中项目的**项目根会写回路径输入**（保存 / 作用域预检读的就是它）；项目栏不再提供手输路径。

**暂存列表条目**（`saved-section`）：

| 元素 | 含义 |
| --- | --- |
| 最左类型图标 | 该条目所属数据库类型（缩小的类型 UI）；无类型信息时显示状态点 |
| 名称后 `●` | 当前条目有未写回的修改（关闭 / 切换会自动写回） |
| 名称后 `P` / `G` / `GP` | 已保存条目的作用域来源（仅项目 / 仅全局 / 全局+项目快照） |
| `✕` | 删除草稿（已保存条目不可在此删除，删除入口在导航栏） |

> 暂存区**固定高度**（约 4 条可见），条目更多时在该区域内滚动；标题行「+ 添加」始终可见。
> 模板导入导出（C4）能力已实现、**UI 入口按决策暂缓**（避免当前阶段界面变动）。

---

## 4. 典型流程

### 4.1 新建连接（4 步）

1. **选类型**：左栏点数据库类型（如 MySQL / SQLite）→ 右侧驱动自动选中该类型第一个实现。
2. **选驱动 + 填名称 / 地址**：驱动可换（如 `sqlx` ⇄ `Official`）；名称为连接显示名。
   - **网络型**：Header 第三行 `URI` 直接填写（占位会随驱动显示模板，如 `mysql://user:password@localhost:3306/db`）。
   - **文件型（SQLite / DuckDB）**：切到常规 Tab，在「连接设置」卡片的 `地址` 里填路径，或点 `打开文件…` / `新建文件…`（新建会在所选位置创建空库文件，已存在则直接引用；DuckDB 可输 `:memory:`）。
3. **选作用域与项目**：分段按钮
   - `仅全局`：写入 `global.db`，所有项目可见（ID `G_…`）；
   - `仅项目`：写入当前项目 `.RSmeta/project.db`（ID `P_…`），需已打开项目；
   - `全局+项目`：全局定义 + 项目快照引用（ID `GP_…`），需已打开项目。
   - 需项目侧时，用**项目下拉**选项目（默认已选中当前打开的项目；末项 `＋ 新增项目` 可新建）。
4. **测试 → 保存**：`测试连接` 独立会话验证（不改动库）；`保存` 落库且**不关闭**对话框
   （草稿转为已保存 + 自动补一条空草稿）；`保存并关闭` 成功后关闭。

### 4.2 编辑已有连接

导航栏连接行 ✎ → 对话框打开并回读全部字段（含协议链 / SSL / 策略覆盖 / 标签 / 分组）。
保存走 `update`（按 ID 前缀 G_/P_/GP_ 路由）。
项目侧连接（P_/GP_）的回读走 `DataSourceService::get_with_project`（带项目根）——无项目根时会得到空表单而不是报错。

**编辑时不重填密码 = 保留原密码**：密码框留空保存不会清空库中凭据（全局侧与项目侧一致）。

**认证方法与引用配置**：`认证方法` 决定连接时如何从「引用的认证档案」注入凭据。
档案未选方法时以档案自身声明的类型为准（服务层兜底），因此**引用档案不必再手选方法**。

**「全局+项目」快照的同步**：GP_ 是**独立副本**，全局定义（G_）后续修改不会自动跟随。编辑 GP_ 连接时 footer 会出现
`从全局定义同步`：点它会把全局侧的最新配置与凭据复制到本项目快照（保留快照 ID、创建时间与分组关系），
结果行提示同步完成，表单同时重载——无需再点保存。

### 4.3 连续配置多个连接

1. 「+ 添加」追加空草稿并选中；
2. 填完保存 → 当前条目变"已保存"，自动补新空草稿；
3. 重复即可；关闭对话框不丢草稿，**重启应用后再次打开对话框仍会恢复**（密码不落盘）。

### 4.4 标签与分组（常规 Tab「组织」卡片）

- **标签**：逗号分隔（如 `prod, core`），保存时写入连接 `tags` 并同步 `connection_tags` 检索表。
- **分组**：项目级勾选（一个连接可属多个分组），保存时以**当前勾选为准**替换成员关系；
  未打开项目时该区域提示「打开项目后可用」。
- 分组/标签的**新建与管理**在数据库导航模块（本模块只做连接侧的绑定）。

### 4.5 模板导入导出（C4，能力已就绪，UI 入口暂缓）

> 当前界面**未展示**导入 / 导出按钮（避免 UI 变动）；能力与测试已就绪（`ConnectionTemplate` /
> `templates_export` / `templates_import` + `connection_template.rs` 2 项），后期只需在暂存列表
> 标题行接上两个按钮（走剪贴板 JSON）。以下为届时行为：

暂存列表标题行的 `导出` / `导入` 通过**剪贴板**交换 JSON（无需文件对话框）：

| 操作 | 行为 |
| --- | --- |
| 导出 | 把暂存列表中**未保存且非空**的草稿序列化为模板 JSON 并写入剪贴板；结果行提示条数 |
| 导入 | 读取剪贴板 JSON，解析后**追加**到暂存列表（选中第一条导入项，光标落位）并提示「密码请补填」 |
| 导入校验 | `kind` 必须为 `rds.connection.template`；`version` 不高于当前（1）；列表非空 |

模板内容：名称 / 数据库类型 / 驱动 id / URL / 用户名 / 备注 / 作用域与项目路径 / SSL 模式 / 标签 / 联邦开关。
**不含密码**（也不含分组与协议链内联值）—— 导入后需人工补填凭据，写入前可用「测试连接」验证。

---

## 5. Tab 内容速览

| Tab | 内容 | 备注 |
| --- | --- | --- |
| 常规 | **单列分组大纲**（可折叠；组集合随驱动）：信息条（类型 · 驱动短名 · 认证方法）+ 分组 | 分组标题行可点击折叠；折叠态不落库（重新打开对话框恢复全展开） |
| 常规（网络型） | 连接设置（主机 / 端口 / 数据库，从 URI 解析的文本行）/ 数据库认证（认证方法 + 引用档案 + 用户名密码）/ 连接安全（SSL，仅驱动声明 `ssl` 时出现）/ 组织（标签·分组） | 地址编辑在 Header URI；摘要行标签取驱动 schema 声明（未声明不出该行）；地址行标签固定为 `地址` / `URI` |
| 常规（文件型） | 连接设置（**地址输入 + `打开文件…` / `新建文件…`**）+ 组织（标签·分组）—— 没有认证 / SSL 分组 | `新建文件…` 在所选位置创建空库文件（已存在则直接引用）；DuckDB 可直接输 `:memory:` |
| 网络 | **引用网络档案**（下拉）+ **管理**入口 + 诚实提示 + **数据路径预览**（本机 → 档案/直连 → 目标数据库） | 选档案即生效（连接时解析为协议链 / SSH / 代理并执行）；**内联协议链已撤下**（占位模型无法执行，多跳请用 `chain` 类型档案）。点「管理」建档案：类型 `ssh` / `proxy` / `socks5` / `ssl` 用**结构化字段表单**（必填 / 端口 / SSH 凭据会拦下），`chain` 填 `ChainHop` JSON 数组 |
| 能力 | 驱动 capabilities 只读矩阵（**来自 `drivers.capabilities`**；未声明项置灰） | 由 `drivers.capabilities` 提供；UI 只做键→中文标签映射 |
| 驱动属性 | key-value 动态增删 | 落库 `driver_properties` |
| 高级 | 同一套单列分组大纲：**环境**（下拉 + 管理入口 + 策略摘要） / **安全策略**（**勾选项来自 `environment_policies`**，切环境自动重查） / **DuckDB 本地加速**（仅网络型库） | 加速开启后注册 DuckDB PERSISTENT SECRET（缓存路径行随开启展开） |

---

## 6. 快捷键

| 按键 | 行为 | 生效范围 |
| --- | --- | --- |
| `Ctrl+Enter` | 保存 | 对话框内任意位置（焦点在输入框时由 Input 冒泡兜底） |
| `Ctrl+T` | 测试连接 | 焦点不在输入框时 |
| `↑` / `↓` | 切换暂存条目 | 焦点不在输入框时（输入框内为光标移动） |
| `Esc` | 关闭对话框（等同取消，会写回草稿） | 对话框内 |

---

## 7. 草稿与数据安全

### 7.1 每个字段落到哪里（写库映射）

> 回答“表单是否都有后端接口、能否写进库”：**是**。本模块无独立后端服务，所有字段经 `DataSourceService`
> → engine 持久化层写入本地库（全局 `global.db` / 项目 `.RSmeta/project.db`，凭据经 AES-256-GCM 加密）。

| UI 字段 | 落库位置（表.列） |
| --- | --- |
| 名称 / 备注 / 作用域 | `global_connections.name` / `.description` / `.is_active`（项目侧 `connections` 同名列） |
| 主机 / 端口 / 数据库 | `.host` / `.port` / `.database`（保存时由 URI 解析写入；字段编辑会同步回 URI） |
| 文件型地址（SQLite/DuckDB） | `.database`（裸路径；不写 host） |
| 用户名 / 密码 | `.username` / `.password_encrypted`（密文，空密码保留原值） |
| 认证方法 / 引用认证档案 | `.auth_method` / `.auth_config_id` |
| 引用网络配置 / 环境 | `.network_config_id` / `.environment_id` |
| SSL（模式 / CA / 证书 / 私钥） | `advanced_options.ssl`（JSON，仅驱动声明 `ssl` 时写入） |
| 策略覆盖 | `advanced_options.policy_overrides`（值为 `environment_policies.policy_type`） |
| 驱动属性 | `.driver_properties`（JSON key-value，覆盖驱动默认） |
| DuckDB 加速 / 缓存路径 | `.use_duckdb_fed` / `.metadata_path` |
| 标签 | `.tags` + `connection_tags`（检索表） |
| 分组（项目级多选） | 项目库 `connection_groups` / `connection_group_members`（替换语义） |
| 草稿（跨会话） | `connection_drafts`（**无密码列**） |
| 作用域 | ID 前缀 `G_` / `P_` / `GP_`（全局定义 / 项目独占 / 项目快照） |

校验与守卫：同名拦截、项目根预检（读写分离）、文件型输入清洗、空密码保留、时间戳兜底——详见 `connection-dialog-architecture.md` §15.4。

### 7.2 草稿不落密码

| 项 | 说明 |
| --- | --- |
| 草稿归属 | 挂在对话框状态上（同一进程内关闭重开不丢）；同时写入 `global.db` 的 `connection_drafts` |
| 恢复时机 | 每次**打开对话框**时：仅当列表仍是初始单空草稿才用库中内容替换（不覆盖本次会话已编辑内容） |
| 密码 | **不写入草稿表**（无 password 列），恢复后密码框为空 |
| 模板（导出） | JSON **不含密码**（仅名称 / 类型 / 驱动 / URL / 用户名 / 备注 / 作用域 / SSL / 标签） |
| 落库凭据 | 正式保存时由 `auth_store`（AES-256-GCM）加密；DuckDB Secret 目录为应用数据目录（不写用户主目录） |
| 写失败 | 草稿持久化 / 标签 / 分组同步失败只记日志，不阻断连接保存 |

---

## 8. 常见问题与排查

| 现象 | 原因 / 处理 |
| --- | --- |
| 测试连接报 `CONN_DRIVER_NOT_FOUND` / `Driver 'xxx' not found in registry` | **已修**：应用启动漏注册内置驱动（`DriverRegistry` 为空）→ 现已在外层初始化时注册；导航树连接 / 重连的同类失败也一并修复。若仍复现，请附 `target/rds-app.err.log`（应能看到驱动注册日志） |
| 测试连接报 `Database path is required for SQLite` | **已修**：文件型工厂原先只读 `config.database`、忽略服务层传的地址（`url_override`）→ 现与网络型驱动一致从 `to_url()` 取地址 |
| 暂存条目图例与表单不一致（表单已 SQLite、条目还显示 mysql） | **已修**：当前条目（光标位）的类型徽标 / 名称改为取**正在编辑的表单**；其他条目仍显示各自快照 |
| **未选数据库类型时表单是灰的、打不了字** | 预期行为：未选类型/驱动时字段**正常显示但禁用**（避免“能打字却存不了”）。先在左侧栏选一个类型即可正常输入 |
| **连接设置（主机 / 端口 / 数据库）能改，URI 会跟着变吗** | 会：三个字段与 Header URI **双向同步**（改字段重建 URI，保留用户名密码与 `?参数`；改 URI 回填字段）。**保存以 URI 为准**（引擎从 URI 解析后写入 `host`/`port`/`database` 列） |
| 驱动下拉为空 | 未选数据库类型：先在左栏点类型；或该类型下驱动被禁用（`drivers.enabled=0`） |
| 类型行灰显 + 右标「暂无驱动」 | 该类型没有可用驱动（当前只内置 4 个驱动）：点它不会切换类型——待驱动插件能力开放 |
| 提示「未选择数据库类型」 | 同上；旧草稿只有驱动完整名时可自动回退解析，解析不到需手动选类型 |
| `仅项目` / `全局+项目` 保存报错 | 未打开项目：先打开项目，或把作用域改为 `仅全局`；也检查项目下拉是否选中了项目（项目根会写回路径输入） |
| 测试连接失败 | 看结果行错误信息；确认主机 / 端口 / 凭据 / 协议链（网络 Tab）与 SSL 模式 |
| 测试连接会带上我引用的认证档案 / 网络档案吗 | 会（A1 起）：测试按真实连接同一套规则组装——读取并解密**认证档案**凭据、**真实建立网络档案隧道**（SSH / 代理 / chain）、应用驱动属性与高级选项。结果行会附说明：「已应用认证档案凭据」/「已建立网络档案隧道」/「未找到引用的认证档案…」。测试不注册连接池、不写库，隧道在测试结束即关闭 |
| 点 `测试连接` 后要等几秒 | 引用 SSH / 代理档案时会真的发起一次隧道握手（与连接时同一条路径），耗时属正常；只想做纯直连测试时可先清空网络引用 |
| 测试连接报「网络档案应用失败：…」 | 隧道 / 代理建不起来，**真实连接同样会失败**。按错误内容检查跳板机地址 / 端口 / 凭据（可在网络管理器核对），不必去改数据库地址 |
| 测试连接报「未找到引用的认证档案…」 | 已升级为严格模式（A2）：不再静默直连，而是报「引用的认证配置不存在（ID）」。去认证管理器确认档案是否被删，或在连接里重新选择后保存 |
| 想删一个认证 / 网络 / 环境配置，但删不掉 | 该配置正被连接引用（列表里已显示「被引用 N」）：先在引用它的连接里换掉或删掉那个连接，再删除配置。拦截消息会列出范围与项目名（如「全局 1 条、当前项目 1 条、其它项目 1 条（另一项目）」）——统计已覆盖**全局库 + 已登记项目库**（不再漏掉未打开的项目） |
| 点「连接」报「引用的网络配置无法解析」 | 引用的网络档案已被删除、类型无法识别或内容非法：编辑该连接，在「网络」Tab 重新选择档案（或修正档案类型 / 内容）后保存。这是 A2 严格模式——不再静默直连 |
| **配了 SSH 跳板机 / 代理，导航里点「连接」却直连或失败** | **已修（审计 #20）**：此前连接入口根本没传网络方式，且隧道会随临时服务实例释放。现在会解析你引用的「网络配置」档案并应用；请确认「网络」Tab 真的**引用了档案**（内联协议链已撤下，多跳请用 `chain` 档案），且档案类型是规范键 `ssh` / `proxy` / `ssl` / `chain`（旧数据里的 `SSH` / `Proxy` 大写也能识别） |
| **网络配置档案怎么建（`config` 要填什么）** | 管理器（连接对话框「网络」Tab → 管理）已改为**结构化字段**：类型选 `ssh` / `proxy` / `socks5` / `ssl` 后按类型展开真实字段（必填未填 / 端口非法 / SSH 缺凭据都会拦下并提示）；只有类型 `chain` 需要手填 JSON 数组（每项形如 `{"type":"ssh","host":…}`，与 `ChainHop` 枚举一致）——**多跳只能用 `chain` 档案**（内联链已撤下）。档案建好后在「网络」Tab 引用它，**保存连接后点连接即生效** |
| **认证配置档案还用手写 JSON 吗** | 不用（A5 起）：认证管理器选类型后按类型展开字段——`password` / `ldap`（用户名 + 密码）、`ssh_key`（用户名 + 私钥路径 + 口令，或密码）、`proxy_pwd`（用户名 + 密码）、`pg_class`（客户端证书 / 私钥）、`kerberos`（Principal / Keytab）；必填未填、SSH 缺凭据、代理缺用户名或密码都会拦下。旧数据里的 JSON 仍可正常回填到字段（键为 `keyPath` / `certPath` 这类驼峰形式） |
| 认证管理器列表里看得到我的密码吗 | 看不到：列表只显示名称与类型，凭据在服务层就被置空（真脱敏）；只有点「编辑」时才会解密回填到字段里 |
| 项目下拉里没有我要的项目 | 下拉列当前项目 + 最近项目；未列出的项目删过 / 未在本机打开过——用末项 `＋ 新增项目` 新建，或在标题栏项目菜单切换后再打开本对话框 |
| 点 `＋ 新增项目` 后弹出未保存确认 | 当前项目编辑区有未保存草稿：先完成确认（不静默丢弃），随后从项目选择器新建项目 |
| 分组区域显示「打开项目后可用」 | 分组是项目级数据（`project.db`），需打开项目 |
| 分组列表为空 | 该项目下尚无分组：分组新建在数据库导航模块 |
| 导入提示「模板解析失败」/「kind 不匹配」 | 剪贴板内容不是本应用导出的模板 JSON：先用 `导出` 复制一份模板对照格式 |
| 导入提示「模板版本过新」 | 模板由更新版本的应用导出，需升级应用 |
| 导出的 JSON 里没有我保存的连接 | 导出仅包含**未保存草稿**（已保存连接请从导航栏用 ✎ 打开后另存为草稿再导出） |
| 保存后列表没变化 | 列表刷新由服务侧触发；若界面未更新，请复现并附 `target/rds-app.err.log` |
| 选了 SQLite 之后地址框还显示 mysql 的连接串 | 已修：那只是**占位提示**，旧版写死为 mysql 示例；现占位随驱动变化，且文件型在常规 Tab 给文件路径提示。若框里真有 `mysql://…` 内容，请确认是不是勾选了旧草稿（切换条目会把该条目的地址载回来） |
| 文件型连接（SQLite/DuckDB）的地址在哪里填 | 常规 Tab →「连接设置」卡片：`地址` 输入框 + `打开文件…`（选已有文件）/ `新建文件…`（创建空库文件）。Header 第三行只显示当前路径，不能编辑 |
| **用「新建文件…」选了一个已有文件** | 预期行为（本轮修正）：「新建」只产出新文件——已存在的文件**不会被引用、也不会被改动**，结果行提示你换名或用「打开文件…」。要打开旧库请用左边的「打开文件…」 |
| 新建时的默认文件名 | 随驱动：SQLite 给 `new_database.db`，DuckDB 给 `new_database.duckdb`（与文件型判定同一来源） |
| 暂存列表里会出现已保存的连接吗 | 不会（用户决策）：暂存区**只放未保存草稿**；已保存的连接从导航栏进入编辑（✎）与删除。保存成功后该草稿会移出暂存区并自动补一条空草稿，结果行提示「编辑请从导航栏进入」 |
| **网络档案里填的 SSH / 代理密码会明文入库吗** | 不会（#34 已修）：密码 / 口令在写库前加密（AES-256-GCM，`AES:` 前缀），读取时自动解密；列表与下拉不回显 config。升级前建的老档案会在下次启动时自动加密一次（幂等，失败不影响启动） |
| 文件型连接保存后、编辑时地址为空 | 已修：以前项目侧（P_/GP_）文件连接不会把路径写进 `database` 列（全局侧看不出来）。旧连接若已落空值，重新选一次文件并保存即可 |
| SQLite 下看不到用户名/密码/SSL 卡片 | 预期行为：文件型没有凭据与 TLS 语义，常规 Tab 只保留「连接设置 + 组织」；从 MySQL 切过来时残留的凭据/网络/TLS 也不会写进库 |
| 复制进来的 `sqlite:///C:/data/app.db` 能存吗 | 能：地址写法兼容裸路径 / `sqlite://路径` / 三斜杠，入库统一存裸路径（`C:/data/app.db`） |
| 编辑项目连接（P_/GP_）后表单为空 | 已修：编辑回读改走 `get_with_project`（带项目根）。若仍为空，确认该连接属于当前打开的项目；反之确认项目库文件未被外部删除 |
| 全局定义改了，项目里还是旧配置 | 预期行为：「全局+项目」的 GP_ 是**独立副本**；编辑该快照连接 → footer `从全局定义同步` 手动拉取最新配置 |
| 引用了认证档案但连接仍失败 | 已修：连接链路在 `auth_method` 缺失时会回退到档案声明的 `auth_type` 注入凭据；若仍失败，确认档案里的用户名/密码本身可用（可在认证管理器里核对） |
| 保存到项目时提示「项目路径无效」 | 该项目根缺少 `.RSmeta`（或目录已不在）：用下拉里的 `打开现有目录…` 重新定位项目；本应用不会为无效路径创建项目骨架 |
| 保存后连接连不上（提示密码错） | 已修：以前编辑项目侧连接（密码框留空）会把库中密码清空；现改为保留原密文。若仍复现请查看项目库该行 `password_encrypted` 是否为空 |
| 结果行的颜色分别代表什么 | 绿色 = 成功；黄色 = 警告（当前唯一情形：保存成功但分组没写进项目库，会显示「已保存：…（分组未同步：原因）」）；红色 = 错误；弱灰 = 进行中 / 普通提示（「测试中…」）。颜色只是级别，具体原因仍看文本 |
| 测试 / 保存失败时只看到一行提示 | 结果行会分级：内容超过 80 字或带换行的消息，行尾会多出 `详情`（展开看完整原文，可滚动）与 `复制`（整段复制到剪贴板，方便贴给同事 / 提 issue）。短消息本身已经完整，不会出现这两个入口 |
| 复制到剪贴板的到底是哪段文本 | 与展开后的「详情」正文完全一致（就是服务层返回的原文，未截断 / 未改写）；只有摘要的消息则复制摘要本身 |
| 对话框点入口无反应 | 历史问题：宿主重绘缺失（参 dev-plan ㉑）；请确认使用最新构建 |
| 第一次用，不知道从哪开始 | 打开对话框时若“未选类型 + 名称与地址都空”，常规 Tab 顶部会出现引导条（五步流程 + 快捷键）；选了类型后自动消失。也可照用户指南 §4.1 的四步走 |
| Tab 条 / 作用域按钮 / 开关的样式变了 | #15 组件化：Tab 条改为**下划线指示条**，作用域三态改为**浅底分段 + 白色胶囊**，开关改为库组件（尺寸统一、未选驱动时置灰不可点）；功能与写库字段未变 |
| 编辑区有未保存草稿时点「＋ 新增项目」还要点两次吗 | 不用（#4）：确认框里选「放弃更改并继续」或「保存并继续」后，**直接**出现「新建项目」对话框 |
| 标签到底以哪儿为准 | **`connection_tags` 表**（#31）：直接改表，列表里的标签跟着变；连接行里的 `tags` JSON 只在表里没有该连接记录时（旧库 / 历史同步失败）作兼容回退 |

---

## 9. USIT 验收清单（可勾选）

**A. 基础闭环**

- [ ] 新建 SQLite 连接：选类型 → 选驱动 → 在常规 Tab 用 `打开文件…` / `新建文件…` 选/建数据库文件 → 测试 → 保存 → 列表出现该连接
- [ ] 导航栏 ✎ 编辑该连接：全部字段正确回读（类型 / 驱动短名 / **地址（文件路径）** / 作用域）
- [ ] 修改驱动（`rusqlite` → 其它）并保存：库中 `db_type` / `driver_id` 为新驱动 id

**B. 类型 × 驱动**

- [ ] 左栏点 MySQL：驱动下拉只有 `sqlx`（无 `MySQL` 前缀）；点 PostgreSQL：只有 `sqlx`
- [ ] 未选类型时：驱动位为**禁用下拉占位**（placeholder「先选类型」）
- [ ] 暂存条目最左侧显示类型图标；无类型草稿显示状态点
- [ ] 类型树搜索框输入 `sqlx` / `My`：均可命中 MySQL（同时匹配类型名 / 类型 id / 驱动名）

**C. 暂存与持久化**

- [ ] 「+ 添加」后编辑第二条：切换条目字段互不串
- [ ] 保存第一条 → 条目变已保存（带 `G`/`P`/`GP` 短码）+ 自动补空草稿
- [ ] 关闭应用后重开 → 打开对话框草稿恢复，且密码框为空
- [ ] 当前条目有改动时名称后出现 `●`
- [ ] 保存一条连接后：该条目**从暂存区消失**（只留一条空草稿），结果行提示「已保存：…（编辑请从导航栏进入；暂存区只保留未保存草稿）」；导航栏能看到该连接
- [ ] 暂存区里**不出现**库中已保存的连接（关闭再打开对话框、重启应用后仍只有草稿）
- [ ] 连加 8 条草稿：暂存区**固定高度内部滚动**，对话框高度不变

**D. 作用域与项目**

- [ ] 未打开项目时 `仅项目` 保存被拦截并提示
- [ ] 打开项目后保存 `仅项目`：导航栏出现该连接（P_ 前缀），且**导航栏点「连接」能连上**（项目侧解析）
- [ ] Header 作用域分段按钮三态切换，项目栏启用 / 禁用态随之变化
- [ ] 已打开项目时项目下拉默认选中当前项目，右侧列显示项目路径（超长从头部省略）
- [ ] 项目下拉末项 `＋ 新增项目` → 弹出「新建项目」；创建后本栏自动选中新项目
- [ ] 编辑项目连接（P_/GP_）：字段全量回读（含标签与分组勾选），作用域仍是「仅项目 / 全局+项目」

**E. 标签与分组**

- [ ] 常规 Tab「组织」卡片填写 `prod, core` → 保存 → 重新编辑该连接可见标签回显
- [ ] 打开项目并勾选分组 → 保存 → 重新编辑勾选保持
- [ ] 取消全部勾选 → 保存 → 重新编辑无勾选

**F. 布局与交互**

- [ ] 逐个切换五个 Tab：对话框高度不变化，侧栏与 Header 不跳动（内容多的 Tab 内部滚动）
- [ ] `Ctrl+Enter` 保存、`Ctrl+T` 测试、`↑/↓` 切换条目、`Esc` 关闭
- [ ] 明暗主题切换：对话框各区域颜色正确（无裸色值残留）

**G. 网络与安全（需要环境）**

- [ ] SSH / HTTP 代理协议链：测试连接成功且断开后无残留端口（可用 `tunnel_count` 诊断）
- [ ] 开启 DuckDB 加速：保存后 Secret 注册；关闭 / 删除连接后清理
- [ ] 连接失败路径：隧道回滚，不残留后台任务

**H. 模板导入导出（能力就绪，UI 入口暂缓）**

> 当前界面**没有** `导出` / `导入` 按钮（按决策暂缓）；以下清单待入口接上后执行；
> 现阶段只能用测试保障：`cargo test -p rds-workbench --test connection_template`。

- [ ] 填一条草稿 → `导出` → 粘贴剪贴板：JSON 含 `rds.connection.template`，**不含密码**
- [ ] 清空剪贴板并粘贴一段非法文本 → `导入` → 结果行报「模板解析失败」（列表不变）
- [ ] 用导出的 JSON → `导入` → 暂存列表追加条目、表单载入该条、密码框为空

**I. 已知局限与预期行为**

- [ ] 类型树里 MariaDB / Oracle / SQL Server / ClickHouse 等**灰显 + 「暂无驱动」**：点击不切换类型，结果行给出原因（只内置 4 个驱动）
- [ ] MongoDB / Redis 不出现在类型树（目录里 `enabled=0`）
- [ ] 项目下拉里找不到未在“最近项目”中的本机项目 → 用下拉里的 `打开现有目录…`，或标题栏项目菜单
- [ ] 编辑区有未保存草稿时点项目下拉的两个动作项：先弹未保存确认，确认后回到项目选择器（需再操作一次）

**J. 认证方法与引用配置（接口闭环）**

- [ ] 常规 Tab「数据库认证」卡片出现 `认证方法` 下拉，选项与 `drivers.supported_auth_types` 一致（如 MySQL(sqlx) → `password`）
- [ ] 换驱动后方法选项随之变化（如 Official 声明含 `ssl`）
- [ ] 引用一个认证档案：方法行变为只读并显示档案声明的方法；已选档案时用户名/密码只读
- [ ] 保存后重新编辑：`认证方法` 正确回显
- [ ] 切换暂存条目 / 重启应用后再看：认证方法未被重置（草稿持久化 `auth_method`）

**K. 数据来源（零 UI 造数据）**

- [ ] 能力 Tab 的 chip 与 `drivers` 表 `capabilities` 一致（可用 `SELECT capabilities FROM drivers WHERE id='mysql'` 对照）
- [ ] 高级 Tab 切换环境后，策略勾选项/摘要随 `environment_policies` 变化；未选环境时提示“无可覆盖策略”
- [ ] 环境管理器里策略名与库里 `policy_type` 对应（安全策略/模式加载/性能策略/审计策略/界面策略），不再全部显示「只读连接」

**L. 项目快照同步与凭据保留**

- [ ] 编辑一个 GP_ 连接：footer 出现 `从全局定义同步`（编辑 P_/G_ 时不出现）
- [ ] 改全局定义（名称 / 端口 / 密码）→ 项目侧仍是旧值（快照独立）→ 点 `从全局定义同步` → 表单与导航栏均变为新值
- [ ] 同步后快照 ID、创建时间、已勾选分组保持不变
- [ ] 编辑任一连接（不改密码框）→ 保存 → 重新编辑并测试连接仍能成功（密码未被清空）
- [ ] 项目下拉三项动作：「不需要项目（仅全局）」切作用域；「打开现有目录…」能选到不在最近列表的项目；末项仍为「＋ 新增项目」

**M. 脏数据防护（写入侧）**

- [ ] 手改项目路径为一个普通目录（无 `.RSmeta`）→ 保存 `仅项目`：报「项目路径无效」，且该目录内**不会**出现 `.RSmeta`
- [ ] 项目连接保存 / 编辑后 `created_at` / `updated_at` 均不为空（可在项目库查 `connections` 表）
- [ ] 在导航栏删除一条已保存连接 → 重新打开对话框：暂存列表不再残留该条目的“已保存”空壳
- [ ] 打开项目后打开对话框：暂存列表里能看到项目侧连接（`P`/`GP` 短码），与导航栏一致

**N. 常规 Tab 随驱动动态渲染（USIT 第 1 轮新增）**

- [ ] 选 SQLite：Header 第三行标签为 `地址`（不是 `URI`），**不出现** mysql 示例占位；常规 Tab 只有「连接设置」与「组织」两组（无认证 / 无 SSL）
- [ ] 文件型「连接设置」：`地址` 可手输；`打开文件…` 选已有 `.db`；`新建文件…` 在所选位置创建空文件并回填路径；地址框悬浮可确认完整路径
- [ ] 切回 MySQL：Header 第三行恢复 `URI` 输入框，占位为 `mysql://user:password@localhost:3306/db`（随驱动模板）；常规 Tab 恢复四个分组
- [ ] 驱动声明了 `ssl`（`sqlx` / `Official`）时「连接安全」分组可见；未声明 `ssl` 的驱动不显示该组
- [ ] 文件型连接保存后查库：`database` = 裸路径（无 `sqlite://` 前缀、Windows 路径无多余前导 `/`），`host` / `username` / `password_encrypted` / `advanced_options.ssl` 为空
- [ ] 文件型连接用 `sqlite:///C:/data/app.db` 写法保存后，重新编辑时地址框显示 `C:/data/app.db`（规范化）
- [ ] 从 MySQL 切到 SQLite（同一草稿）：保存后库中不出现刚才输的 mysql 用户名/密码/SSL

**O. 单列分组大纲与可见性（USIT 第 2/3 轮）**

- [ ] 常规 / 高级 Tab 均为单列分组：每组是**整幅面板**（浅底 + 圆角），标题行有 chevron（▾ / ▸）与下方分隔线，点标题行可折叠 / 展开
- [ ] 标签与输入框紧靠（标签列 68px，间距 6px），各行控件左边缘对齐
- [ ] 输入框在亮色主题下**清晰可见**（边框不再是几乎看不见的浅灰）
- [ ] 连接设置的 主机 / 端口 / 数据库 显示为**数据框**（白底 + 边框，内容只读）
- [ ] **未选数据库类型时**：表单字段全部正常显示但整体置灰、点不进、打不了字（含地址 / 用户名 / 密码 / SSL / 标签 / 分组勾选）
- [ ] 文件型：`打开文件…` 弹系统**打开**对话框（只能选已存在文件）；`新建文件…` 弹系统**保存**对话框，输入新文件名 → 地址框回填且磁盘上出现空库文件；**取消**时结果行提示“已取消（地址保持原值）”
- [ ] **连接设置可直接输入**：改主机 / 端口 / 数据库任一字段 → Header URI 同步变化（用户名密码与 `?参数` 保留）；反向改 URI → 三个字段同步回填；两方向都不会互相循环改写
- [ ] 数据库字段留空 → 保存后 URI 无路径段；端口留空 → 用驱动声明默认端口；主机清空 → URI 不变（不产生非法 URL）
- [ ] 已存在文件用「新建文件…」选一次：**不引用、不覆盖**（结果行提示「该文件已存在，未创建…；请换一个文件名，或用「打开文件…」引用它」），文件内容一字不变
- [ ] 新建时的默认名随驱动：SQLite = `new_database.db`，DuckDB = `new_database.duckdb`
- [ ] 拖动窗口变窄：Tab 内容不换行错位、不出现卡高参差；内容超高时在 Tab 区域内滚动

**P. 测试连接与真实连接同源（A1 新增）**

- [ ] 引用一个认证档案（此时用户名 / 密码为只读说明）→ 点 `测试连接`：结果行出现「已应用认证档案凭据」；把引用改成不存在的 ID（或删除档案后编辑旧连接）再测 → 提示「未找到引用的认证档案，测试未包含档案凭据」（不再静默）
- [ ] 引用一个**不可达**的 SSH 网络档案（如端口写 `1`）→ `测试连接`：直接失败并提示「网络档案应用失败：…」（不再拿远端地址直连试探）
- [ ] 引用可用的 SSH / 代理档案并测试：结果行出现「已建立网络档案隧道」；测试结束后本机不残留监听端口（可对照 `connection_tunnel_cleanup` 的诊断思路）
- [ ] 同一连接：`测试连接` 通过后，导航栏点「连接」也应成功（两者共用同一套组装规则）；反之测试失败时真实连接也不应成功

**Q. 凭据特殊字符（A1 新增）**

- [ ] 密码含 `@` / `:` / `/` 的连接（如 `p@ss:w/rd`）保存后查库：URL 中密码为转义形式（`p%40ss%3Aw%2Frd`），主机 / 端口不被截断
- [ ] 此类连接测试连接与真实连接均可成功（转义后解析回原密码）

**R. 档案引用完整性与严格模式（A2 新增）**

- [ ] 建一个认证档案并让某连接引用它 → 管理器列表显示「被引用 1」→ 点「删除」：被拒绝并提示「…正被 全局 1 条 连接引用，不能删除…」
- [ ] 在引用它的连接里换掉档案（或删掉该连接）后再删：删除成功
- [ ] 项目侧引用：打开项目后再看管理器计数（当前项目的 P_/GP_ 引用也算进去）；未打开项目时计数不含项目侧
- [ ] **跨项目引用（#33）**：在 A 项目里建连接引用某档案 → 切到 B 项目（或关闭项目）再看该档案的「被引用」：计入了 A 项目的引用；点删除 → 消息里列出「其它项目 N 条（项目名）」
- [ ] 库里删掉一个仍被引用的认证档案 → 该连接点「测试连接」：报「引用的认证配置不存在…」（不再静默无凭据直连），点「连接」同样被拦下
- [ ] 把网络档案类型改成无法识别的值（或删掉档案）→ 测试连接与真实连接都失败并说明「网络配置无法解析」

**S. 认证档案字段化与脱敏（A5 新增）**

- [ ] 打开认证管理器 → 类型下拉有 6 项（`password` / `ldap` / `pg_class` / `kerberos` / `ssh_key` / `proxy_pwd`）；选 `password` 后出现「用户名 / 密码」字段（不再是 JSON 文本框）
- [ ] `ssh_key`：只填用户名不填凭据 → 保存被拦下并提示；填私钥路径 → 保存成功；编辑该档案 → 私钥路径 / 口令正确回填
- [ ] `kerberos` / `pg_class`：保存后查库 `auth_configs.auth_data`，键为 `principal` / `keytabPath` / `certPath`（驼峰，与后端读取端一致）
- [ ] 新建一个带密码的认证档案后，在不点「编辑」的情况下查看管理器列表与连接下拉：**不出现密码明文**
- [ ] 用字段化建出的档案引用到连接上 → 保存 → 测试连接：能带上凭据（结果行有「已应用认证档案凭据」）

**T. 网络档案凭据加密（#34 新增）**

- [ ] 建一个 SSH 网络档案并填密码 → 保存后用 SQLite 工具查 `network_configs.config`：密码为 `AES:…` 密文（不是明文）
- [ ] 重新打开网络管理器 → 点该档案「编辑」：字段回填出明文（可继续修改）
- [ ] 网络管理器列表 / 连接对话框的引用下拉：不显示 `config` 内容（列表脱敏）
- [ ] 用该档案的连接：测试连接与真实连接都能通过隧道（解析侧拿到的是明文密码）
- [ ] 旧库（升级前建的档案）：启动一次后查库，明文密码已变为密文（一次性迁移；失败不影响启动）

**U. 结果行分级与详情（#28 新增）**

- [ ] 保存一条连接：结果行为**绿色摘要**；若勾了分组但项目库不可写（罕见），结果为**黄色**且带出原因（「已保存：…（分组未同步：…）」）
- [ ] 制造一个长错误（如引用一个不存在的认证档案后点 `测试连接`）：结果行行尾出现 `详情` 与 `复制`
- [ ] 点 `详情`：展开完整原文（多行 / 超长时区域内部滚动），按钮变 `收起`；再点收起 → 正文消失
- [ ] 点 `复制` → 粘贴到文本编辑器：内容与详情正文一致
- [ ] 短消息（如「已保存：xxx」）行尾**不出现** `详情` / `复制`
- [ ] 点 `测试连接` 的过程中：结果行先显示弱灰色的「测试中…」，结束后变为成功 / 失败色（不是一直不变）

**V. 组件化 · 引导 · 标签权威（#15/#14/#29/#31/#4 新增）**

- [ ] Tab 条为**下划线**样式（选中项下方 2px 主色线）；逐一点击各个 Tab，内容随之切换（文件型只看得到 常规 / 能力 / 驱动属性 / 高级）
- [ ] 作用域三态为**浅底分段控件**（选中项白色胶囊 + 阴影）；点击切换作用域，写库仍是全称标签
- [ ] 高级 Tab：DuckDB 加速开关与策略覆盖开关尺寸一致；**未选类型 / 驱动时置灰不可点**
- [ ] 首次打开对话框（未选类型、名称与地址留空）：常规 Tab 顶部出现引导条；选完类型后引导条消失
- [ ] 编辑区留一条未保存草稿 → 项目栏选「＋ 新增项目」：先出未保存确认 → 选「放弃更改并继续」→ **直接**出现「新建项目」对话框（不再回到选择器重新点）
- [ ] 标签以库表为准：用 SQLite 工具把某连接的 `connection_tags` 改成另一个标签 → 重新打开对话框 / 列表，标签已跟着变
- [ ] 切 Tab / 拖动窗口宽度：控件位置与间距稳定，不出现尺寸闪动（#14 已把本模块纳入尺寸契约）

---

## 10. 相关文档

| 文档 | 用途 |
| --- | --- |
| `connection-prototype-design.md` | 原型与视觉规格（布局 / 交互 / 主题 token） |
| `connection-dialog-prototype.html` | 可交互原型稿 |
| `connection-dialog-architecture.md` | 架构、数据流、数据字典、错误处理与降级、性能与限制 |
| `connection-dev-plan.md` | 任务分解与逐轮实现记录（含已知坑） |
| `docs/architecture/database/database-nav-dev-plan.md` | 导航侧：分组 / 标签视图与检索（消费方） |
