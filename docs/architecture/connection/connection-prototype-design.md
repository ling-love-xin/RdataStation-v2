# 数据源连接模块 · 原型设计（新增连接对话框）

> 状态：**待二次确认** · 关联文件：`connection-dialog-prototype.html`（可交互原型）、`connection-dev-plan.md`（开发方案）
> 参考基准：布局对齐 v1 原型 `v1/prototype/add-datasource-v5.html`（+ `v5-analysis.md`）；配色服从 `docs/architecture/theme/theme-design.md`（RDS Light/Dark，rds-theme.json）；机制参考 DataGrip「数据源和驱动程序」方案
> 技术栈：GPUI（gpui-kit 0.6），组件消费 `cx.theme()` 语义 token，代码零裸 hex

## 1. 设计基准

| 维度 | 基准 | 说明 |
| --- | --- | --- |
| 布局 | v1 v5 原型 | 模态对话框：标题栏 + 左侧栏（搜索/暂存/类型树）+ 右侧主面板（Header 三行 / 五 Tab / 内容区）+ 底部操作栏 |
| 配色 | theme-design.md | 对话框=popover token；侧栏=sidebar token；选中项=coral 左边条；主按钮=primary coral |
| 机制 | DataGrip + v1 后端设计 | 数据源=连接配置（driver/auth/network/env 引用），测试→保存→内省闭环 |
| 后端契约 | v1 DATA-SOURCE-MODULE.md / DATASOURCE-DRIVER-ARCHITECTURE.md | drivers.config_schema 驱动表单；auth_configs AES-256-GCM；network_configs 协议链；environments+policies |

## 2. 布局结构（对齐 v5 原型）

```
┌─ dialog（模态，popover 底 + border 边框，圆角 8）────────────────────┐
│ 标题栏（Dialog title）：『新建数据源连接』/『编辑数据源连接』  ─ □ ✕ （模态层）│
├───────────────────────────────────────────────────────────────┤
│ sidebar（200px，sidebar 底，高度恒定）│ main-panel（flex:1，Header 3 行）
│  ├ 搜索框（input，placeholder 搜索类型…）│  ├ Header：
│  ├ 暂存列表（标题行 + 添加）        │  │  ① 🐬 类型徽标(仅图标,1.75rem) · 名称(弹性) · 作用域三态分段
│  │   ▸ 固定高度 7.5rem，超出滚动    │  │  ② 备注(弹性) + 项目(定宽 17rem，仅全局禁用占位)
│  │   ├ 🐬 MySQL 演示 ●            │  │     项目下拉 = 项目名 + 路径，末项「＋ 新增项目」
│  │   ├ 🐘 生产 PG [G]             │  │  ③ 驱动(定宽 11rem，实现短名) · 地址/URI(弹性)
│  ├ 数据库类型（平面分类，不折叠）    │  ├ tabs-bar（tab_bar 底）
│  │   ▸ 占满剩余高度，超出滚动       │  │  常规 | 网络 | 能力 | 驱动属性 | 高级
│                                   │  └ 结果提示行（载入 / 保存 / 校验消息）
└───────────────────────────────────────────────────────────────┘
   底部操作栏： [🔌 测试连接] [⟳ 从全局定义同步*]  ✓ 成功·版本·延迟   [取消] [保存] [保存并关闭]
   （* 仅编辑「全局+项目」快照连接（GP_）时显示）
```

- **布局恒定**：左侧两区（暂存列表 / 类型树）与 Tab 内容区均为**固定高度 + 内部滚动**——条目 / 类型 / Tab 内容再多也不会拉长对话框（真机反馈）。
- **Header 只 3 行（建议布局）**：① 类型徽标（定宽 1.75rem，仅图标）+ 名称（弹性）+ 作用域三态分段；② 备注（弹性）+ 项目（定宽 17rem）；③ 驱动（定宽 11rem）+ 地址（弹性，网络型标签为 `URI` / 文件型为 `地址`）；标签列统一 1.75rem 左对齐。
- **地址列随驱动变形（真机反馈）**：
  - **网络型**：标签 `URI`，Header 内为**可编辑输入框**（唯一地址输入）；占位取当前驱动声明的 `url_template`（`{username}`/`{port}` 等换成示例值，端口用驱动声明的默认端口），无模板时退回 `{type_id}://主机:端口/数据库`。
  - **文件型（SQLite / DuckDB）**：标签 `地址`，Header 内**不再放输入框**（避免与卡片重复），只显示当前路径（截断）或引导文案；真正的地址输入在「常规 → 连接设置」卡片内，带 `打开文件… / 新建文件…`。占位按类型给文件提示（`.db/.sqlite`、`.duckdb` 或 `:memory:`）。
  - **单地址输入框约定**：同一个地址（InputState）只在一处渲染——网络型在 Header、文件型在常规卡片；不两处重复渲染（两处会让人分不清哪一个是权威值）。
- **数据库类型以徽标形式常驻行首**（**仅图标**：emoji，定宽 1.75rem，不显示类型名）；**未辨识时**徽标显示 `?`（warning 边框），驱动位置保留**禁用下拉占位**（placeholder「先选类型」）。
- **类型可用性**：类型目录按 `enabled` 过滤（禁用的类型不出现）；**无可用驱动的类型**（当前只内置 mysql / postgres / sqlite / duckdb 四个驱动）置灰 + 行尾小字「暂无驱动」，点击**不切换**并在结果行给原因。
- **项目栏单下拉（无模式切换）**：下拉项为**项目名（左）+ 路径（右，弱化、超长从头部省略）**左右结构；末项固定为 **`＋ 新增项目`**（选中后由宿主打开「新建项目」对话框；创建成功后新项目成为当前会话，本栏下一次渲染自动跟随）。作用域「仅全局」时保留**禁用下拉占位**（placeholder「仅全局不需要」）。
- **项目栏固定位置**：位于备注行右侧（固定宽 17rem）；作用域为「仅全局」时不参与落库，置灰不编辑；选中项目的项目根会写回**路径输入**（保存与作用域预检统一读路径，单一数据源）。
- **Header 元素定宽**：类型徽标 1.75rem / 驱动 11rem / 项目 17rem / 作用域分段短标签——切换数据库类型时后续元素位置不移动。

### 2.1 作用域（全局 / 项目 / 全局+项目，三态单选 —— 对齐后端双轨设计）

| 选择 | 落库语义 |
| --- | --- |
| 仅 全局 | 保存至 `global.db` 的 `global_connections`（`G_xxx`） |
| 仅 项目 | 保存至当前项目 `project.db` 的 `connections`（`P_xxx`） |
| 全局 + 项目 | 保存为全局（`G_xxx`）**并共享至当前项目**（`GP_xxx` 快照引用）——连接同属两作用域 |

- 依据：v1 双轨制（`convert_to_global_connection` / `convert_to_project_connection`，v2 `workbench/services/connection_service.rs:1426/1516`）+ ID 前缀 `G_/P_/GP_`（engine `id_prefix.rs`）；规则：项目可引用全局（快照 GP_），全局不可引用项目私有配置
- UI 为**三态单选**（全局 / 项目 / 全局+项目），选中项即落库目标；**不再有行内落点提示行**（信息冗余；落点由选中态与常规 Tab 信息条表达）

### 2.2 暂存列表与多连接连续编辑（参考 v1 v5 §8）

侧栏上半区为「暂存列表」（`saved-section`），用于在**同一个对话框内连续编辑多个连接**（DataGrip「数据源和驱动程序」式交互）：

| 条目类型 | 来源 | 行为 |
| --- | --- | --- |
| **未保存草稿（Draft）** | 新建时自动生成（默认名「新建数据源」） | 可切换续编、删除；保存成功后转为已保存条目 |
| **已保存连接（Saved）** | `DataSourceService::list()`（按当前作用域可见性合并） | 点击进入编辑模式（等价于现状侧边栏「编辑」入口） |

- 条目结构（v1 v5 `saved-item`）：类型徽标（缩小的数据库类型 UI：类型 emoji，如 🐬；无类型时回退状态点）+ `item-name`（名称）+ 徽标列；选中项高亮（`sidebar.accent` + coral 左边条）；**未保存草稿右侧有条目 `✕`**（删除草稿；已保存条目不在此删除）。
- 本轮增强：已保存条目名称后附**来源短码徽标**（`P` / `G` / `GP`，按 `saved_id` 最长前缀优先解析）；当前条目有未写回修改时名称后显示**脏标记 `●`**（warning 色）；最左侧为缩小的数据库类型 UI（类型 emoji；无类型时回退状态点）。
- 标题行按钮：当前仅 `+ 添加`（追加空草稿）；C4 的 `导出` / `导入` 能力已就绪但**UI 入口按决策暂缓**（避免当前阶段界面变动）。
- 快照字段（与表单一致）：类型 / 驱动（`type_id` + `driver_id` + 实现短名）、URL、凭据、作用域与项目路径、SSL、协议链、驱动属性、策略覆盖、三类引用，以及**标签文本与已勾选分组 id**（保存时同步 `connection_tags` / `connection_group_members`）。
- 交互规则：
  1. 点击条目 → 先把当前表单状态写回该条目，再载入目标条目（各 Tab 字段 / 协议链 / SSL / 引用 / 作用域 / 驱动属性**全部按条目隔离**）；
  2. 标题「暂存列表 [+ 添加]」→ 追加空草稿并选中；
  3. 删除草稿；**删除到最后一条自动补一个空草稿**（v1 v5 行为，保证列表恒非空）；
  4. 保存成功 → 该草稿转为已保存条目（刷新列表）并自动追加一个空草稿，保持可连续新建；
  5. 关闭对话框不丢失草稿；**草稿跨会话持久化**（global.db `connection_drafts` 表，**无密码列**），应用重启后首次打开自动恢复；
  6. 快捷键（焦点在对话框内）：`Ctrl+Enter` 保存、`Ctrl+T` 测试连接、`↑/↓` 切换条目（焦点在输入框内时 ↑↓ 仍为光标移动）；
  7. footer 同时提供「保存」与「保存并关闭」：前者保持对话框开启（连续编辑），后者保存成功后关闭。
- 与作用域的关系：草稿仅在保存时落库（G_/P_/GP_ 语义不变）；已保存条目的来源短码在对话框内仅供辨识（导航栏展示归 database-nav）。
- 实现约束：草稿为**状态快照**（新增 / 编辑共用同一套字段；与 `load_for_edit` 的回读语义对齐），列表状态存 `ConnectionDialogState`（对话框层关闭不销毁，故草稿天然保留）。

## 3. 五 Tab 内容（对照 v5 原型）

### 3.1 常规（tab-general）—— 卡片式布局（**按驱动动态渲染**）
- 顶部信息条（info banner）：`类型 · 驱动实现 · 特性`（如「MySQL · sqlx · 原生连接」；文件型为「… · 文件型数据库 · 无网络与 SSL 配置」；未选类型时为选型提示）
- **卡片集合随驱动类型变化**（真机反馈：SQLite 下不得出现认证 / SSL 卡片）：

| 驱动类型 | 卡片 | 说明 |
| --- | --- | --- |
| **网络型**（MySQL / PostgreSQL…） | 连接设置 · 数据库认证 · 连接安全 · 组织 | 主机 / 端口 / 数据库为从 URI **解析出的只读摘要**（编辑在 Header URI）；SSL 卡片另按驱动声明（`supported_auth_types` 含 `ssl`）显示 |
| **文件型**（SQLite / DuckDB） | 连接设置 · 组织 | 连接设置 = **地址输入 + `打开文件…` / `新建文件…`**（无认证 / SSL / 网络语义） |

- **连接设置（网络型）**：主机 / 端口 / 数据库（解析摘要，落库同源）；**编辑在 Header URI**
- **连接设置（文件型）**：`地址` 行 = 本地文件路径输入 + 系统文件选择器（`打开文件…`）/ `新建文件…`（在所选位置创建空库文件；已存在则直接引用，不覆盖）；提示行说明「首次连接自动初始化结构」
- **数据库认证**（仅网络型；含**管理**入口，打开 AuthConfigManager 覆盖层）：
  - **认证方法** select：选项来自当前驱动的 `supported_auth_types`（数据源：`drivers` 表）；
    驱动未声明 → 禁用占位「驱动未声明认证方法」；引用档案时只读并显示档案声明的方法
  - 用户名 / 密码（未引用档案时可编辑）
  - **引用已保存认证配置**下拉（选中后字段只读 disabled + 降透明）
- **连接安全（SSL/TLS）**（仅网络型且驱动声明 `ssl`）：模式（disable/prefer/require/verify-ca/verify-full）、CA 证书、客户端证书/私钥
- **组织（标签 / 分组）**（所有类型都有）：
  - 标签：逗号分隔输入（`prod, core`）→ 保存时写 `tags` + 同步 `connection_tags`；
  - 分组：项目分组多选（`Checkbox`，多对多）；未打开项目时提示「打开项目后可用」，本项目无分组时提示「暂无分组」
- 驱动特有字段（form-driver-specific）追加在卡片下方
- **布局稳定**：Tab 内容区固定高度（20.5rem）+ 超出滚动 —— 切换 Tab 不改变对话框高度，侧栏与 Header 不抽动
- **落库卫生**：文件型连接不写凭据 / 认证引用 / 网络链 / SSL（表单从上个类型残留的值一律清洗，见 `strip_file_db_noise`）。

### 3.2 网络（tab-network）
- 提示条：协议链（SSH / HTTP(S) 代理）、最大 4 跳、拖拽排序；**SSL/TLS 见「常规 → 连接安全」**
- **引用网络配置**：选择已保存网络配置（network_store，内联编辑或整体引用）；**管理网络配置**入口（NetworkConfigManager 覆盖层）
- 链列表：列头（拖拽 · # · 协议 · 配置 · 启用 · 操作），行：SSH / Proxy 节点
- 添加跳：类型菜单（SSH / Proxy）+ 新建表单（各协议字段，见 v5 §3.3）
- **拓扑预览**（topo-preview）：数据路径图（Client → SSH → Proxy → DB，DB 节点带 TLS 徽标）
- 约束校验：SSH/Proxy 合计 ≤4 跳、hop 命名合法；违反时 chain-warning 提示

### 3.3 能力（tab-capabilities）
- 从 `drivers.capabilities` JSON 解析（数据源：驱动表；UI 只做「能力键 → 中文标签」字典映射），声明与未声明均列 chip（未声明置灰）；字典外的键原样展示不丢信息；未选驱动时提示选型。

### 3.4 驱动属性（tab-properties）
- `driver_properties` key-value 行，支持动态增删（高级用户）

### 3.5 高级（tab-advanced）
- **环境选择**（紧凑下拉）：环境 + 策略摘要行（只读/导航过滤/超时/行数/DDL·DML 摘要）；**管理环境**入口（EnvironmentManager 覆盖层，v1 内联于 AdvancedTab）
- **DuckDB 本地加速卡片**（warning 色标题，提高权重）：开关 + 展开 2×2 参数网格（缓存路径/大小/自动刷新等）+ 存储说明；仅网络数据库可见
- **安全策略**（可折叠）：勾选项 = 当前环境的**启用策略**（数据源 `environment_policies`，切环境自动重查）；行内展示库里的配置摘要（如 `readonly=true · rowLimit=1000`）；勾选即“覆盖该策略”并标记「已覆盖」，落库 `advanced_options.policy_overrides`（值为 `policy_type`）；未选环境 → 无项可覆盖并给出提示（不造默认值）

### 3.6 凭证 / 网络 / 环境的管理与引用（复用机制）
| 类型 | 引用（连接内） | 管理（覆盖层，v1 弹出窗） | 复用语义 |
| --- | --- | --- | --- |
| 认证配置 | 常规→数据库认证、网络→SSH/Proxy 认证：下拉引用已保存配置，选中后字段只读 | AuthConfigManager：分类列表（password/ssh_key/proxy_pwd…）+ 新建/编辑/删除；列表脱敏；显示**被引用计数** | 数据库 A 直接引用数据库 B 的认证配置（共用同一份凭据），修改一处全量生效；被引用的配置不可删除 |
| 网络配置 | 网络 Tab：下拉引用已保存协议链（内联编辑或整体引用） | NetworkConfigManager：配置档案（SSH 跳板/代理…）+ CRUD；显示**被复用计数** | 建一次网络档案，多连接复用 |
| 环境 | 高级 Tab：下拉选择环境 + 策略摘要 | EnvironmentManager：环境 CRUD + 策略编辑；显示**被使用计数** | 环境（5 类策略）共享；单连接可覆盖策略并标记"已覆盖" |

## 4. 关键交互

| 交互 | 行为 |
| --- | --- |
| 类型树 | **不折叠**（实现为四个分类小节，均占满侧栏剩余高度并内部滚动）：分类行 = `名称（数量）`，条目行 = 选中指示条 + 类型 emoji + 名称；搜索框过滤类型名 / 类型 id / 驱动名（命中驱动也保留该类型）；选中类型 → 右侧驱动下拉仅列该类型**启用驱动**（实现短名，如 `sqlx` / `Official`）并默认选中第一个；未选类型时驱动位保留**禁用下拉占位**（placeholder「先选类型」）；条目类型徽标取类型 `icon`（emoji） |
| 作用域 | **三态单选**：仅全局→`G_xxx`；仅项目→`P_xxx`；全局+项目→保存全局并共享至当前项目（`GP_xxx` 快照）；Header 用**分段按钮**（短标签 全局 / 项目 / 全局+项目，写库仍用全称标签）；**不再有行内落点提示行**（落点由选中态与常规 Tab 信息条表达） |
| 项目 | **单下拉**（项目名 + 路径左右结构）；尾部三个动作：**「不需要项目（仅全局）」**（切作用域，不留无效空态）、**「打开现有目录…」**、**末项「＋ 新增项目」**；作用域「仅全局」时禁用占位；选中项目 → 项目根写回路径输入；新增/打开动作 → 宿主开对应入口（有未保存草稿时先走未保存确认）；对话框打开期间项目会话变更 → 本栏自动跟随 |
| URI / 地址 | **网络型**：Header 内自绘**可编辑输入框**（直接改文本；无「✎ 编辑 / 完成」两态）；占位随驱动 `url_template` 变化。**文件型**：Header 只显示当前路径（截断）或引导文案，可编辑输入框与文件选择按钮在「常规 → 连接设置」；系统选择器取消时保持原值 |
| 测试连接 | 独立会话（复用 ConnectionFactory，不污染正式连接），返回 `{success, message, latency_ms, version}`；成功 → 绿色 ✓ + 版本 + 延迟；失败 → 红色错误信息 |
| 保存 | 校验 → 落库（ID 前缀按作用域 G_/P_/GP_）→ `use_duckdb_fed=1` 时注册 DuckDB Secret → 刷新列表；**保存后不关闭对话框**（草稿转正式 + 自动补空草稿）；「保存并关闭」则在成功后关闭 |
| 暂存列表 | **多连接连续编辑**（详见 §2.2）：草稿条目切换 / 新增 / 删除（删至最后一条自动补位）；保存后草稿转正式并自动补空草稿；关闭不丢失且**跨会话持久化**（`connection_drafts`，无密码列）；条目展示来源短码 `P/G/GP` 与脏标记 `●` |
| 模板导入导出 | 暂存列表标题行 `导出 / 导入`：剪贴板 JSON（`kind`/`version` 校验），**不含密码**；导出仅未保存且非空草稿；导入追加并选中（提示补填密码）——**能力已就绪，UI 入口按决策暂缓**（原型图中不展示）|
| 项目快照同步 | 编辑「全局+项目」快照（GP_）时 footer 出现 `从全局定义同步`：拉取全局侧最新配置与凭据密文到本项目快照（保留快照 ID / 创建时间 / 分组关系）；其他作用域不显示该按钮 |
| 类型不可用 | 无可用驱动的类型：置灰 + 「暂无驱动」+ 点击不切换（结果行给原因）；未选类型时驱动位为禁用占位 |
| 认证配置选择 | 按 auth_type 过滤已保存配置；选中后动态字段只读；**管理**按钮打开 AuthConfigManager |
| 网络/环境引用 | 网络 Tab「引用网络配置」+「管理网络配置」；高级 Tab「管理环境」——均打开对应覆盖层 |
| 组织（标签 / 分组） | 标签逗号分隔（回显时 `, ` 连接）；分组项目级多选（`Checkbox`，多对多）；保存时以勾选为准替换成员关系；未打开项目提示「打开项目后可用」；分组与标签的**视图消费**在导航模块 |

## 5. 主题映射（token → 视觉，两套一致结构）

| 元素 | RDS Light | RDS Dark | Token |
| --- | --- | --- | --- |
| 对话框/输入框底 | `#FFFFFF` | `#252526` | background / popover |
| 边框/输入边 | `#D4D4D4` | `#3C3C3C` | border / input.border |
| 标题栏 | `#DDDDDD` | `#323233` | title_bar |
| 左侧栏 | `#F3F3F3` | `#252526` | sidebar |
| 选中项底 | `#E4E4E4` | `#37373D` | list.active / sidebar.accent |
| 选中项左边条 | `#C25B46` | `#E8846F` | list.active.border（品牌 coral） |
| 悬停 | `#F0F0F0` | `#2A2D2E` | list.hover |
| 正文/弱文字 | `#333333` / `#8E8E8E` | `#CCCCCC` / `#8A8A8A` | foreground / muted |
| 主按钮（保存） | `#C25B46` 白字 | `#E8846F` 黑字 | primary |
| Tab 栏/激活 | `#F3F3F3` / 白底+coral 顶条 | `#252526` / `#1E1E1E` | tab_bar / tab.active |
| 成功/警告/信息 | `#16A34A` / `#B45309` / `#2563EB` | `#89D185` / `#CCA700` / `#3794FF` | semantic |
| 状态栏（如用） | `#C25B46` 白字 | `#A84A38` 白字 | primary 派生 |

> 实现时色值只存在于 `assets/themes/rds-theme.json`，GPUI 组件经 `cx.theme()` 读取，禁止写裸 hex（theme-design.md §5 约束）。

## 6. GPUI 落点映射

| 原型元素 | GPUI 落点 |
| --- | --- |
| 对话框 | `crates/workbench/src/components/connection_dialog/`（`render.rs` 为元素树；模态层 `window.open_dialog` + 宿主 `Root::render_dialog_layer`） |
| 标题栏/窗口点 | gpui-kit `TitleBar` 或自绘；窗口点由宿主窗口控制 |
| 类型树/暂存列表 | 自绘行（`render.rs`）；草稿为状态快照（见 §2.2），已保存条目来自 `DataSourceService::list()`；持久化在 `staging.rs` + `engine::persistence::connection_draft_store` |
| 快捷键（Ctrl+Enter / Ctrl+T / ↑↓） | `crates/workbench/src/commands.rs`（Action）+ `crates/app/src/main.rs`（`bind_keys`，context `"connection-dialog"`）+ `render.rs` 容器 handler |
| 动态表单 | 按 config_schema 渲染 `Form`/`Input`/`Select`（driver_store 数据） |
| 认证/网络/环境配置 | 对接 engine `auth_store` / `network_store` / `env_store` |
| 测试连接 | `DataSourceService::test_connection`（独立会话） |
| 保存链路 | `DataSourceService::save` → global_db + Secret 联动 + introspection |
| 五 Tab | **自绘 Tab 条**（gpui-kit 0.6 无 Tabs 组件）；Tab 内容区固定高度 20.5rem + 内部滚动；文件型驱动隐藏「网络」Tab（索引不变） |
| 项目栏 | `project_picker.rs`（`ProjectItem` = 项目名 + 路径双列，末项「＋ 新增项目」）；`state.rs::handle_project_confirm` 为确认落点；宿主（`view.rs`）消费 `Shared::project_new_request` → `project::ui::open_create_dialog` |

## 7. 开放问题

本文件只描述**目标形态（作为实现基准）**；当前实现与目标之间的差距、以及已知缺陷，统一记录在
`connection-dialog-architecture.md` §14「已知问题与后续项」——那里是**权威待办清单**（分级：🔴 阻断 / 🟡 语义与体验 / ⚪ 工程与文档债）。

本轮（2026-09-12）已确认并关闭的原待确认项：

1. ~~作用域同属~~ → 作用域为**三态单选**（全局 / 项目 / 全局+项目），落库语义与前一致（G_ / P_ / GP_）。
2. **SSL 移常规**（网络 Tab 仅协议链，≤ 4 跳）—— 已按此实现。
3. **管理与引用**（认证 / 网络 / 环境三类：引用 + 管理覆盖层）—— 已按此实现。
4. 入口形态（活动栏 / 导航树 / 空态按钮 → 模态对话框）—— 已按此实现。
5. ~~项目栏两态~~ → 项目栏为**单下拉**（项目名 + 路径，末项「+ 新增项目」）。
6. ~~Header 落点提示行~~ → 已删除（落点由作用域选中态与常规 Tab 信息条表达）。
